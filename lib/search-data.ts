// AUTO-GENERATED by scripts/build-search-index.mjs — do not edit.
export interface DocHit {
  locale: "en" | "es";
  repo: string;
  title: string;
  label: string;
  path: string;
  slug: string[];
  text: string;
}

export const DOCS_INDEX: DocHit[] = [{"locale":"en","repo":"api","title":"xtop api","label":"api","path":"README.md","slug":["api"],"text":"xtop api shared api crates for the xtop ecosystem. kernel, plugins, widgets, effects and extensions all depend on these crates — they define the contracts between the core and every extension point. workspace crates crate package purpose --- --- --- plugin-api xtop-plugin-api plugin protocol: manifest, capabilities, errors, lifecycle events; the shared data model (system snapshots, alertthresholds , ...) widget-api xtop-widget-api widget renderer contract: read-only widgetstate , renderer registration, and the canonical glyph/style mappings (to color , border for , marker for ) effect-api xtop-effect-api effect contract: stateful frame effects over the rendered ratatui buffer (effect , effectmanifest ) extension-api xtop-extension-api extension protocol: hooks around config, layout, theme and render pipeline documentation detailed, code-grounded guides live in docs/ : - docs/architecture.md — workspace layout, crate dependency graph, dr-1..dr-5 ownership, ecosystem consumption (git deps, temporary path-dep pattern) - docs/data-model.md — every model:: struct, what the kernel provider populates, snapshot lifecycle, the docker removal, the alertthresholds serde contract - docs/plugin-contract.md — plugin , pluginmanifest , plugincontext capability enforcement, hoststate , pluginerror , systemdataprovider , step-by-step plugin authoring - docs/widget-contract.md — widgetstate , widgetrenderer /widgetregistration , glyph enums + canonical helpers, pack rules - docs/extension-contract.md — extension , extensionhost , extensioncontext , server-style model - docs/effect-contract.md — effect /effectmanifest and the host contract - docs/changes.md — decision log (dr-1..dr-7, d1..d8) and breaking changes for consumers consumption the crates are not yet published to crates.io. they are consumed by sibling repos as git dependencies (git = \"https://github.com/xtop-cli/api\" , currently floating) from: - xtop kernel (github.com/xtop-cli/xtop) - widgets repo (github.com/xtop-cli/widgets) — live widget packs consumed by the kernel - plugins repo (github.com/xtop-cli/plugins) - layouts repo (github.com/xtop-cli/layouts) - effects repo (github.com/xtop-cli/effects) — live, with planned xtop-effect- crates - extensions repo (github.com/xtop-cli/extensions) the publish plan (crates.io + tags, then pinning consumers to the tagged revision) is tracked in the workspace roadmap, §6. local ci run from the repo root: local only: this repo keeps no github workflows. license mit"},{"locale":"en","repo":"api","title":"api — architecture","label":"docs · architecture","path":"docs/architecture.md","slug":["api","docs","architecture"],"text":"api — architecture the api repo hosts the shared contract crates of the xtop ecosystem. every other repo (xtop kernel, widgets , plugins , extensions , and later effects ) depends on these crates; nothing redefines the types they own (workspace roadmap, dr-1..dr-5). the repo never depends on the kernel. this document covers the workspace layout, the dependency graph between the four crates, what each crate owns, and how sibling repos consume the crates today. the contract guides live in the sibling documents of this folder: document scope --- --- data-model.md xtop plugin api::model structs, providers, snapshot lifecycle, alertthresholds plugin-contract.md plugin , pluginmanifest , plugincontext , hoststate , capabilities, systemdataprovider widget-contract.md widgetstate , renderer registration, glyph helpers (glyph module) extension-contract.md extension , extensionhost , server-style model effect-contract.md effect , effectmanifest , host contract changes.md decision log (dr-1..dr-7, m1 decisions d1..d8) and breaking changes workspace layout workspace facts (cargo.toml ): - resolver = \"2\" ; members are the four crates above. - [workspace.package] : version = \"0.1.0\" , edition = \"2021\" , license = \"mit\" , rust-version = \"1.87\" . edition stays 2021 on purpose (the 2024 sweep is deferred, roadmap §7). - [workspace.dependencies] (dr-7): ratatui = \"0.30.2\" , serde = { version = \"1\", features = [\"derive\"] } , serde json = \"1\" . no tokio/clap/chrono anywhere in the repo. - every member declares rust-version.workspace = true (and inherits version/edition/license), so the 1.87 msrv is actually enforced per package (decision d6). per-crate dependencies (member manifests): crate dependencies dev-dependencies notes --- --- --- --- xtop-plugin-api ratatui (workspace), serde (workspace) serde json model + plugin protocol xtop-widget-api ratatui, serde, xtop-plugin-api = { path = \"../plugin-api\" } serde json path dep is the in-workspace form xtop-effect-api ratatui — only dependency is ratatui xtop-extension-api — — zero dependencies today all four packages declare repository = \"https://github.com/xtop-cli/api\" . crate dependency graph only one edge exists between the four crates: widget-api depends on plugin-api , because widgetstate names the shared data model types (xtop plugin api::model::{systemsnapshot, systeminfo, processinfo} and alertthresholds ) in its method signatures. the data model deliberately lives in plugin-api , not in a separate neutral crate; consumers of widget-api , effect-api or extension-api get the model through plugin-api (or not at all, when their contract does not need it). effect-api and extension-api are standalone: they only need ratatui's buffer /frame types (effect-api) or nothing (extension-api). none of the four crates depends on the kernel or on any sibling repo. what each crate owns (dr-1..dr-5) decision owned surface where it lives --- --- --- dr-1 — data model + plugin contract systemsnapshot and every model:: struct, plugin , hoststate , plugincontext , plugincapability , pluginerror , pluginmanifest , systemdataprovider , alertthresholds (serde), hex to rgb , runtimeconfig , pluginwidget crates/plugin-api/src/ (see the plugin contract) dr-2 — widget render contract widgetstate , widgetrenderer , widgetregistration , chartcharset , widgetborders , canonical glyph helpers (to color , marker for , border for , ascii border in glyph ) crates/widget-api/src/ (see the widget contract) dr-4 — extension host extension , extensionhost , extensioncontext , extensionmanifest , extensionerror crates/extension-api/src/ (see the extension contract) dr-5 — effects effect (over the rendered ratatui buffer + elapsed time), effectmanifest crates/effect-api/src/ (see the effect contract) dr-2 also states the naming rule: plugin-side widgets (rendered over &dyn hoststate ) are the distinct pluginwidget type exported by xtop-plugin-api , while xtop-widget-api owns the widget-pack registration type drawn over widgetstate . a workspace grep gate (roadmap m7.4) checks that no widgetregistration symbol exists outside widget-api . two drs are ecosystem-wide and do not create api surface: dr-3 keeps layout widget names as plain strings (xtop-layout lives in the layouts repo, which declares no api dependency), and dr-6 puts ecosystem constants (samurai plugin id, action names) at the producer — the plugins repo — instead of in api. how the ecosystem consumes the crates the four crates are not published to crates.io (the readme states this; the publish plan is tracked in roadmap §6). today every consumer declares the crates as floating git dependencies — git = \"https://github.com/xtop-cli/api\" with no rev /tag — exactly as the workspace roadmap §2 (dr-7) and §6 prescribe for this cycle: repo git dependency edges (from its cargo.toml) --- --- xtop (kernel) xtop-plugin-api , xtop-extension-api , xtop-widget-api (all git = .../api , floating); plus git deps on widgets/layouts/plugins/extensions repos widgets xtop-widget-api , xtop-plugin-api (workspace deps, git) plugins (samurai) xtop-plugin-api (workspace dep, git) extensions (mcp) xtop-extension-api (workspace dep, git) effects none yet — repo has no crates until m5 layouts none — layout files reference widgets by plain string names (dr-3); no contract types needed consequences of the floating form: - a consumer build resolves whatever the remote default branch of github.com/xtop-cli/api points at. changes made in a local working tree of api are not visible to sibling repos until the owner pushes. - cargo caches the fetched git revision, so local consumer builds are reproducible only up to the last fetched remote head. - nothing can break silently from a publish/tag change because nothing is published yet; the follow-up (publish the four crates to crates.io + tags, then pin every consumer to the tagged api revision — or move consumers to the published versions) is roadmap §6 step 7. temporary path-dependency pattern (local multi-repo development) sibling repos validate against the local api working tree through temporary path dependencies (roadmap §5, m8.1/m8.2; workspace agents.md). two forms are used, depending on the goal: 1. per-repo development : replace the floating git dep in the consumer's manifest with a path into this repo, or keep the git declaration and override it with a [patch] section. both point into api/crates/ : a direct path dep works the same way when the consumer has no git declaration to patch: absolute file:// urls into api/crates/ are equivalent. the repos sit side by side in the workspace, so ../api/... relative paths are the norm. 2. combined verification (m8.1) : a scratch workspace under the workspace tmp/verify-ws wires all repos together with path deps, so cargo test /clippy across the whole ecosystem proves one combined compile. both forms are temporary by rule: every manifest is restored to its floating git deps before the owner pushes (m8.2; final manifest state must be exactly what the owner pushes). a [patch] section is the least invasive form because restoring means deleting the section. repo hygiene and local ci - ci is local only: ./scripts/ci.sh [fmt clippy check test] from the repo root (fmt check, clippy -d warnings , workspace check/test). no github workflows exist or are added (workspace rule). - the workspace carries 16 tests (plugin-api 5, widget-api 9, effect-api 2; extension-api none yet) covering capability gating, top processes ordering, the alertthresholds serde contract, the glyph mapping tables and a fake-frame effect test. cargo doc --workspace --no-deps builds warning-free. - grep gates that must stay green (roadmap m7.4): no widgetregistration outside widget-api , no alertthresholds definition outside plugin-api , no pluginwidgetfn , no plugins dir tmp . no \"docker\" symbol exists anywhere in this repo (m1.4). publish plan (pointer) the push order and the publish/pin follow-up live in the workspace roadmap, §6: api is pushed first (contracts first — everything depends on it); crates.io publication of the four crates plus tags, and the pinning of consumer git deps, are step 7."},{"locale":"en","repo":"api","title":"decision log & breaking changes","label":"docs · changes","path":"docs/changes.md","slug":["api","docs","changes"],"text":"decision log & breaking changes this file records the decisions that shaped the api repo in milestone m1 and the breaking changes consumers must absorb. the authoritative ecosystem decisions (dr-1..dr-7) live in the workspace roadmap §2; m1's room-for- interpretation decisions (d1..d8) were recorded in the milestone report tmp/m1-api-report.md §8 and are restated here in repo context, anchored to code. dr summary — what the api repo implements decision what it means inside this repo --- --- dr-1 data model + plugin contract types exist once, in crates/plugin-api/src/ (model.rs , host.rs , plugin.rs , context.rs , capability.rs , error.rs , manifest.rs , provider.rs , widget.rs , color.rs ). grep gate: no alertthresholds definition outside plugin-api (m7.4). dr-2 widget render contract exists once, in crates/widget-api/src/ (state.rs , renderer.rs , glyph.rs ). plugin-side widgets are xtop-plugin-api 's pluginwidget , a distinct type — no shared name with widget-api 's widgetregistration . grep gate: no widgetregistration outside widget-api . dr-3 no layout surface in api: layout files address widgets by plain string names (xtop-layout in the layouts repo needs no api types). dr-4 extension types exist once, in crates/extension-api/src/ . dr-5 effect contract exists once, in crates/effect-api/src/ : effect over the rendered buffer + effectmanifest . dr-6 ecosystem constants live at the producer (samurai plugin id/action names in the plugins repo, m4), not in api. dr-7 versions: ratatui 0.30.2 , serde/serde json latest 1.x, rust-version = \"1.87\" in the workspace and every member manifest; no tokio/clap/chrono; consumer git deps stay floating this cycle. m1 decisions d1..d8 (repo context) - d1 — capability gating forces result reads. enforcing readsysteminfo through the same check capability mechanism the mutating methods use is only possible if reads can fail, so plugincontext::snapshot , system info and top processes return result instead of bare values (context.rs ). denials are pluginerror::recoverable(\"plugin does not have required capability: readsysteminfo\") . consumers break on purpose; see the table below. - d2 — top processes sorts, then truncates. implemented as sort by(cpu usage desc via total cmp) + truncate(n) (context.rs ): same semantics as the earlier draft (sort + take ), no extra allocation, ordering guaranteed by the contract, not the producer. stable sort: equal-cpu ties keep snapshot order (tested). - d3 — border mapping follows ratatui 0.30.2 semantics. the spec draft read ratatui's border::empty as \"standard box drawing\" and border::plain as \"ascii +\" — true for ratatui ≤ 0.28, wrong since 0.29: empty is now a blank-space set and plain is the standard single-line box-drawing frame; ratatui ships no ascii set at all. the intended semantics won: native → border::plain (classic look), rounded → border::rounded , double → border::double , plain → ascii border , ascii → ascii border (glyph.rs ), with ascii border a new exported canonical + - set. - d4 — no effectregistration glue type. the task defined a minimal contract of exactly effect + effectmanifest and no speculative surface; no consumer exists until the kernel wiring (m5), so a registration type would be speculative. omitted; trivial to add when m5 lands. - d5 — mapping helpers live in the existing glyph.rs , glyph-only. the three functions (to color , border for , marker for ) and the ascii border const extended the module that already owned the enums; one canonical import path, xtop widget api::glyph::{...} , avoids ambiguity. the helpers are not re-exported at the crate root (only the enums are). border signatures use ratatui::symbols::border::set (the bare set needs a lifetime in real code). - d6 — rust-version.workspace = true in every member. a [workspace.package] entry alone is not inherited; the per-member opt-in is what makes dr-7's \"declared in every package\" true. - d7 — rename rationale without the literal old name. the pluginwidget doc comment explains the distinction conceptually (\"xtop-widget-api owns the canonical widget-pack registration type, the one drawn over widgetstate ; the two are distinct contracts and must not share a name\") without printing the removed widgetregistration symbol — which also keeps the m7.4 grep gate green. - d8 — no default derive on alertthresholds . the task only asked for serialize/deserialize ; the kernel copy's default (90/90/90) and copy /partialeq are kernel-side semantics that m2 can request when it removes its own copy. unrequested derives on a contract type were avoided. breaking changes for consumers all changes below land in the api working tree during m1 and reach the sibling repos when the owner pushes; until then consumers resolve the old remote head through their floating git deps (see architecture.md for the local path-dep validation pattern). change code anchor consumer impact migration --- --- --- --- pluginwidget rename: plugin-api's registration type renamed from the duplicated widgetregistration to pluginwidget (same shape: name + render closure over &dyn hoststate ) plugin-api/src/widget.rs , lib.rs ; plugin::widget() returns option kernel xtop/src/plugins/manager.rs and state/app.rs import widgetregistration from xtop plugin api ; samurai's widget() (plugins/plugins/xtop-plugin-samurai/src/lib.rs ) names the old type kernel m2.3: import pluginwidget ; delete the pluginwidgetfn alias in ui/layout/engine.rs ; plugin map stays name → pluginwidget . samurai m4.1. grep gate m7.4 reads return result : plugincontext::snapshot() , system info() , top processes() now enforce readsysteminfo and return result plugin-api/src/context.rs samurai calls ctx.snapshot() at 5 sites (system summary, process search/top/info, analyze) and reads values directly; also process.kill /threshold.set /config.set already map err samurai m4.1: handle result at every ctx.snapshot() /read call site (report §9); kernel m2 exercises the same paths through samurai + engine docker surface removed: dockerinfo struct, systemsnapshot::dockers field, systemdataprovider::docker info method and root re-export deleted plugin-api/src/model.rs (struct + field), provider.rs ; api-wide grep for docker is empty kernel provider still assigns dockers: vec![] (xtop/src/providers/sysinfo/provider.rs ) and composite still overrides docker info() (xtop/src/providers/composite.rs ) — those references cannot compile against the new api revision; nothing else consumed the surface kernel m2.4: drop the always-empty dockers assignment and the composite docker info override glyph helper canonicalization: packs must import to color /border for /marker for /ascii border from xtop widget api::glyph instead of re-implementing widget-api/src/glyph.rs (module doc: packs must not re-implement) base pack widgets/src/util.rs hand-rolls all four helpers; the blocks pack (widgets/packs/xtop-widget-blocks/src/lib.rs ) hand-rolls to color /ascii border /border for (no marker for — its chart hardcodes marker::block ) and the two packs already diverge on plain widgets m3.3 (base pack deletes its copies; pack-private format bytes /format uptime /gauge gradient stay), m3.4 (blocks pack deletes its copies + const : chartcharset hack and honors state.charset() ). call-site detail: canonical to color takes [u8; 3] by value (pack copies took &[u8; 3] ); border for takes the enum, not (state, widget, native) ratatui 0.30.2 in api (workspace dep) while consumers pin 0.29 api/cargo.toml (ratatui = \"0.30.2\" ) consumer crates that also use ratatui directly compile two ratatui versions until aligned; border::plain semantics changed in 0.29 already (see d3) kernel m2.6 (layout::vertical/horizontal etc. + crossterm alignment), widgets m3.1, samurai m4.2. api is the reference: already on 0.30.2 (m1.1). mcp (extensions) uses no ratatui — unaffected rust-version = \"1.87\" + edition 2021 policy workspace + all member manifests (d6) consumers on older toolchains must bump (samurai already needs 1.87) kernel m2.8, widgets m3.1, samurai + mcp m4.8 alertthresholds becomes the serde contract type with plain keys cpu high /mem high /disk high ; no default derive (d8) plugin-api/src/host.rs + round-trip test kernel keeps a same-shaped local copy (xtop/src/config/schema.rs ) with default 90/90/90 and converts in plugins/host.rs + state/widget state.rs kernel m2.2: delete config::alertthresholds ; persist xtop plugin api::alertthresholds ; hand-marshalling in host.rs/widget state.rs goes away; json keys stay identical data dir() doc no longer over-specifies a concrete path plugin-api/src/context.rs doc-only change; behavior unchanged (path still host-provided) none glyph enums unchanged at the root (chartcharset , widgetborders re-exported; serde snake case keys half block , plain , ...) widget-api/src/lib.rs , glyph.rs none — config persistence format is stable none reference pointers for the migrations: kernel milestone m2 (m2.2–m2.6, m2.8), widgets m3 (m3.1–m3.4), plugins + extensions m4 (m4.1, m4.2, m4.8), kernel effect wiring m5.3, and the cross-repo grep gates in m7.4, all in the workspace roadmap."},{"locale":"en","repo":"api","title":"data model — xtop plugin api::model","label":"docs · data-model","path":"docs/data-model.md","slug":["api","docs","data-model"],"text":"data model — xtop plugin api::model the shared system data model lives in xtop plugin api::model (api/crates/plugin-api/src/model.rs ). dr-1 makes this module the single source of truth: every struct below is defined once, here, and imported by every other repo — never redefined. all model structs derive debug, clone ; only systeminfo derives default (so providers can hand out an \"empty\" system info without inventing values). none of the model types derive serde: they cross repo boundaries as plain rust values. serde derives are reserved for types that are persisted or configured: alertthresholds (kernel config, see below) and the glyph enums in xtop-widget-api . population facts in this document are grounded in the kernel's sysinfo provider (xtop/src/providers/sysinfo/provider.rs ) and the platform probes under xtop/src/providers/sysinfo/platform/ , which implement the contract's systemdataprovider . \"extras\" (batteries, gpus, per-interface ips, thread counts, mount options, governors) come from the platform trees; the sysinfo crate provides the rest. platform coverage: linux reads /sys and /proc ; macos implements batteries (pmset ), interface ips (getifaddrs ), mount options (mount(8) ), thread counts (proc pidinfo ) and directory services users (dscl ) under platform/macos/ ; windows implements batteries (getsystempowerstatus ), interface ips (getadaptersaddresses ), mount options (getlogicaldrives /getvolumeinformationw ), thread counts (toolhelp snapshots) and local-account users (get-localuser , numeric rids) under platform/windows/ . fallback platforms return empty values. struct inventory struct role --- --- systemsnapshot one sample of the whole machine, assembled once per tick. container for every metric group below. cpuinfo one logical cpu core. memoryinfo physical memory totals/usage, with a percent. swapinfo swap totals/usage, with a percent. diskinfo one mounted filesystem. diskioinfo per-mount cumulative bytes and current throughput. networkinfo one network interface: cumulative bytes, current throughput, ips. processinfo one process, with the full forensic field set (p0/p1/p2 groups below). loadavg 1/5/15-minute load averages. batteryinfo one battery (platform probe). gpuinfo one gpu (nvidia-smi probe, sysfs fallback). systeminfo slowly changing machine identity (hostname, os, kernel, desktop, shell, cpu model, package power). systemsnapshot fields: cpus , memory , swap , disks , networks , processes , load avg , uptime , cpu temp , disk io , batteries , gpus , sys info . what the kernel provider populates the kernel's sysinfoprovider populates the snapshot as follows (line references are to xtop/src/providers/sysinfo/provider.rs in the sibling kernel repo, read-only reference): struct / field source --- --- cpuinfo { name, usage, cpu id, frequency, governor, temp c } sysinfo cpus() per core; cpu id is the enumeration index; governor from the platform probe read cpu governor(i) ; temp c per-core °c from the platform probe read core temps — linux coretemp when the sensors map onto the logical cores, none everywhere else (macos and windows have no per-core sensor source — windows thermal zones are aggregate, read via wmi by sysinfo components for cpu temp — or linux hosts without readable per-core sensors) memoryinfo { total, used, available, free, percent } sysinfo memory getters; percent = used/total 100 , 0.0 when total is 0 swapinfo { total, used, free, percent } sysinfo swap getters; same percent rule diskinfo { mount point, total space, available space, used space, percent, file system, mount options } sysinfo disks ; used = total - available ; mount options looked up from the platform probe read mount options() diskioinfo { name, read bytes, write bytes, read speed, write speed } sysinfo diskusage ; speeds are byte deltas since the previous refresh over the elapsed time networkinfo { name, received, transmitted, rx speed, tx speed, ip } sysinfo networks ; speeds computed from deltas since the previous refresh; ip from read interface ips() processinfo sysinfo processes() (see the p0/p1/p2 groups below) loadavg { one, five, fifteen } system::load average() batteryinfo platform read batteries() — linux /sys/class/power supply , macos pmset , windows getsystempowerstatus (single aggregate battery); fallback platforms empty gpuinfo read gpu info() : shared nvidia-smi probe first, then the platform sysfs fallback when the list is empty systeminfo { hostname, os version, kernel, desktop env, shell, cpu model, package power w } host/os/kernel/desktop/shell captured once at provider construction : system::host name() , system::long os version() , system::kernel version() , xdg current desktop /desktop session , shell /comspec ; cached on the provider. cpu model = the sysinfo cpu brand string of the first logical core at construction (system::cpus()[0].brand() , e.g. \"intel(r) core(tm) i7-14650hx\"); sysinfo fills it on every platform it supports — an empty brand yields none . package power w is sampled every refresh from the linux rapl probe (see below); none when no readable rapl source exists systeminfo::package power w (linux rapl probe) instantaneous package power in watts, computed from intel rapl energy-counter deltas at the refresh cadence. sources, in priority order: (1) /sys/class/powercap/intel-rapl: /energy uj for every domain whose name file reads package-0 (one per socket; the readings are summed), falling back to the lowest-index intel-rapl: domain when no name matches; (2) hwmon energy input under /sys/class/hwmon/hwmon /name == powercap , first sensor only. readings are in microjoules; wattage = delta energy uj / 1 000 000 / elapsed secs with wrap-around-safe deltas (counters wrap at max energy range uj ). the first sample after boot establishes a baseline and yields none (no previous counter); an unreadable source (absent driver, permission denied, transient read failure) also yields none and resets the baseline — the value is never fabricated. macos and windows keep none (no public rapl/package power source on either platform) and fallback platforms stub the probe to none systemsnapshot::cpu temp maximum temperature over sysinfo components systemsnapshot::uptime system::uptime() (seconds) processinfo — field groups the struct carries 22 fields in groups, all populated by the kernel (the p0/p1/p2 markers below follow the provider source): - base: pid , name , cpu usage , memory , user id , state , cmd (first argv element). - p0 (process identity): exe path , parent pid , cmd full (full argv). - p1 (forensics): start time (epoch seconds — the provider converts sysinfo's boot-relative start via boot epoch = now - uptime so consumers compare against one clock), run time (now - start time ), effective user id , group id , cwd , thread count (platform probe read thread count ). - p2 (resources): open files , open files limit , disk total read bytes , disk total write bytes , environ , session id . field count: 22 (7 base + 3 p0 + 6 p1 + 6 p2). the ux9.1 process row needs (program + command + user name) are fully covered by this set — no fields were added: cmd /cmd full /exe path carry the program and its command line, and user id (plus effective user id ) carries the numeric uid as a string. the uid → login name mapping is not part of the data model (it is a display mapping): widgets resolve it through widgetstate::uid to name(uid) (widget-api; the kernel reads /etc/passwd on unix, plus directory services users on macos and local accounts on windows, keyed by the numeric uid — windows exposes the sid's numeric rid as user id ) and fall back to the numeric uid when no name exists. the recent per-process cpu samples a braille spark draws also live on the widget view, not in the model: widgetstate::process cpu history(pid) returns the bounded per-pid series the kernel feeds each tick (see widget-contract.md). two ordering rules apply to snapshot().processes : 1. the provider pre-sorts by cpu usage descending and truncates to a cap (default max processes = 200 , overridable through xtop max processes ) so per-tick work stays bounded. 2. the contract's plugincontext::top processes(n) re-sorts and truncates itself, so the plugin-facing guarantee (\"top n by cpu\") never depends on producer ordering (see plugin-contract.md). snapshot lifecycle one systemsnapshot is produced per monitoring tick: 1. the kernel ticks at the configured interval (default update interval ms = 1000 in the kernel's config default; appstate::on tick in xtop/src/state/app.rs ). 2. provider.refresh all() refreshes sysinfo, disks, networks and components, and records the byte counters as baselines for the next rate computation (prev net , prev disk maps and last refresh ). 3. provider.snapshot() assembles the structs above into one systemsnapshot . 4. the kernel caches the sample on appstate (last snapshot ) and every widget/action in that frame reads the same cached sample (appstate::snapshot cache() ); the render path never resamples the system. history buffers (per-core cpu, memory percent, summed network rx/tx rates ) are pushed from the same sample. 5. plugin tick handlers then run against a plugincontext whose read methods return the same snapshot family. before the first tick there is no sample: the widget view (widgetstate::snapshot() ) returns none , and the plugin host view (hoststate::snapshot() ) falls back to forcing one fresh sample (appstate::snapshot() clones the cache or samples once). deliberately removed — docker surface m1.4 removed the dead docker model surface from the contract: - dockerinfo struct (was in model.rs ), - systemsnapshot::dockers field, - systemdataprovider::docker info default method and its root re-export. reason: nothing in the ecosystem consumed them. the kernel's sysinfo provider always assigned an empty dockers: vec![] , and no widget, plugin or extension read the field or the method. a repo-wide grep for \"docker\"/\"dockerinfo\" is now empty inside api . the kernel working tree still contains the stale references (dockers: vec![] in xtop/src/providers/sysinfo/provider.rs and the docker info() override in xtop/src/providers/composite.rs ); those are kernel-side removals tracked as m2.4 and will disappear when the kernel starts consuming this revision of the contract (see changes.md). alertthresholds — serde contract alertthresholds lives in xtop plugin api::host (not in model ) and is re-exported at the crate root. it derives debug, clone, serialize, deserialize (m1.2) — and deliberately nothing else: no default , copy or partialeq were added to the contract type (decision d8); the kernel keeps its own defaults until m2 adopts the api type. serde contract: plain field names, no rename attributes . the json keys are exactly cpu high , mem high , disk high — the same keys the kernel persists in its json config today (config.alerts in xtop/src/config/schema.rs uses an identical struct layout; m2.2 replaces that kernel copy with this type). the in-crate round-trip test pins the exact serialized form:"},{"locale":"en","repo":"api","title":"effect contract — xtop-effect-api","label":"docs · effect-contract","path":"docs/effect-contract.md","slug":["api","docs","effect-contract"],"text":"effect contract — xtop-effect-api xtop-effect-api (api/crates/effect-api/src/lib.rs ) defines the shared effect contract (dr-5). effects are stateful, full-buffer visual transitions that the kernel applies to every rendered frame — intro animations, fade-ins, wipes, post-processing over the whole terminal picture. the crate is minimal on purpose (decision d4): exactly two public items — effect and effectmanifest . no registration glue type exists yet; one is added when the kernel wiring (m5) needs it. the crate depends only on ratatui (for the buffer type) and never on the kernel. the contract effectmanifest is plain 'static data (ids/names are compile-time constants), so manifests compare equal across copies of the same effect — the in-crate tests assert partialeq on them. effect requires send + sync (the kernel may host it behind a shared handle) and gives the effect full ownership of its own state: everything an effect needs to remember lives on self . host contract (who calls what, when) the crate docs define the host side precisely, and the kernel (m5.3, feature gated) is expected to implement exactly this: 1. on frame runs on every rendered frame , after widgets and layout have been drawn into the ratatui buffer and before the terminal flush. the effect may rewrite any cell of buffer . 2. elapsed is the time since the effect started — not the frame delta. an effect that must converge after e.g. 800 ms keys off elapsed = 800ms , regardless of how many frames it took. 3. effects are stateful across frames : they keep whatever progress they need on self between calls; the host does not persist any state for them. 4. the host does not pace frames : it ticks at 1 s cadence plus ad-hoc repaints (key presses, layout changes). visual transitions must therefore advance by wall-clock time (elapsed ), never by frame count alone — a frame-count-based fade would crawl or jump depending on repaint luck. there is no renderer contract beyond the buffer: the effect sees the final composed picture (overlays included, once drawn) and rewrites it in place. implementing an effect an effect is a struct implementing the trait, holding its progress, and a test that drives fake frames. the in-crate test doubles (framecounter ) is the reference pattern: a struct with plain state, a const manifest, and a body that mutates self per call. drive it with synthetic frames in tests — hand the effect a buffer::empty(rect::new(0, 0, w, h)) and increasing duration s, then assert on buffer convergence or on your own counters (the in-crate framecounter test is the pattern). never depend on real frame pacing in tests or in the effect itself. status - contract : done in this crate (m1.6), with crate docs stating the host contract above. - implementations : none yet. the effects repo is a live repo with no crates; the first real effect (xtop-effect-fade ) and the kernel's feature-gated effecthost wiring are milestone m5. - consumers : none in the wild yet — the crate is validated by its own tests until m5. no effect may assume a pacing guarantee beyond the host contract above."},{"locale":"en","repo":"api","title":"extension contract — xtop-extension-api","label":"docs · extension-contract","path":"docs/extension-contract.md","slug":["api","docs","extension-contract"],"text":"extension contract — xtop-extension-api xtop-extension-api (api/crates/extension-api/ ) is the shared extension protocol (dr-4). extensions are optional behaviors the kernel can host — today that means servers : long-running integrations (the mcp server in xtop-extension-mcp ) that the kernel starts on demand and that drive the app through an extensionhost view. the crate is deliberately minimal and standalone: it declares zero dependencies and never depends on the kernel. its crate doc anticipates that extensions may also use xtop-plugin-api for the shared data model, but no extension needs it yet, so no such dependency exists in the manifest. public surface (all re-exported at the root): extension , extensionhost , extensioncontext , extensionmanifest , extensionerror . the model as it exists today — server-style only manifest is required; run server defaults to an unknown error, so an extension that declares a server must override it. the kernel wires concrete extensions: for mcp it dispatches the xtop mcp subcommand into run server(\"mcp\", ...) (kernel commands/mcp.rs ), which blocks for the lifetime of the server loop (in mcp's case: a json-rpc 2.0 read-eval-print loop over stdio). extensionmanifest : the servers vector is what a host would consult to discover which server id values the extension accepts; run server is the entry point for exactly those ids. what an extension may drive — extensionhost / extensioncontext the kernel implements extensionhost for its live application state (impl extensionhost for appstate in the kernel's xtop/src/plugins/extension host.rs ) and hands extensions an extensioncontext over it. extensions act through the context — never on kernel types: design intent, stated in the extensionhost docs: plugins remain the unit of domain behavior ; extensions act through them with execute plugin rather than re-implementing domain logic. the mcp extension follows this: every mcp tool call maps onto a (plugin id, action, params) tuple that is executed against the hosted samurai plugin, after an optional ctx.tick() to refresh data (extensions/extensions/xtop-extension-mcp/src/lib.rs ). the kernel's mapping of plugin errors to extension errors (map plugin error in xtop/src/plugins/extension host.rs ) is the reference for cross-contract error behavior: recoverable /fatal map one-to-one, unknownaction becomes a recoverable message. extensionerror display : verbatim message for recoverable , fatal: for fatal , unknown: for unknown . implements std::error::error . the mcp server maps its own failures onto these variants (fatal for stdio read/write failures, recoverable for malformed json-rpc). honest scope note — hooks are future work the crate doc describes two extension flavors: hooks that touch config, theme, layout or rendering (\"designed as they are needed\") and servers . only the server flavor exists today: there is no hook trait, no hook registration, and extensionmanifest carries no hook metadata. the hook-style surface will be added to this crate when a real consumer (kernel feature) needs it — until then it stays out of the contract on purpose (no speculative surface, same rule as decision d4 for effects). implementing an extension an extension implements extension and runs its server loop against the context. the pattern, mirroring mcpextension : the kernel side is already demonstrated by the mcp command: create the extension, create extensioncontext::new(&mut state) over the live app state, call run server(server id, &mut ctx) , and map the error up. extensions consume xtop-extension-api as a git dependency from the extensions repo (see architecture.md for the temporary path-dep pattern)."},{"locale":"en","repo":"api","title":"plugin contract — xtop-plugin-api","label":"docs · plugin-contract","path":"docs/plugin-contract.md","slug":["api","docs","plugin-contract"],"text":"plugin contract — xtop-plugin-api xtop-plugin-api (api/crates/plugin-api/ ) is the shared plugin protocol for xtop (dr-1). two parties implement against it: - the host (the xtop kernel) provides a hoststate and hands plugins a plugincontext over it; - plugins (the xtop-cli/plugins repo, e.g. xtop-plugin-samurai ) implement plugin and act through that context. the crate never depends on the kernel, so every repo can consume it standalone. it depends only on ratatui (for the frame type in the widget render closure) and serde (for alertthresholds ). public surface item module notes --- --- --- plugin trait plugin.rs core plugin trait pluginmanifest manifest.rs static metadata incl. declared capabilities plugincontext context.rs capability-checked access to host state + plugin data dir hoststate trait host.rs read-only-plus-actions view of the kernel, implemented by the host runtimeconfig host.rs theme/layout/interval/hostname view alertthresholds host.rs serde contract type, see data-model.md systemdataprovider trait provider.rs data source contract (kernel sysinfo provider, plugin extras) plugincapability capability.rs declared + enforced capabilities pluginerror error.rs recoverable / fatal / unknownaction pluginwidget widget.rs plugin widget registration (renders over &dyn hoststate ) hex to rgb color.rs rrggbb → [u8; 3] ; invalid/short inputs fall back to black per channel model module model.rs the shared data model, see data-model.md everything except model is also re-exported at the crate root (xtop plugin api::plugin , xtop plugin api::plugincapability , ...), which is how the example plugin in this file and xtop-plugin-samurai import it. pluginmanifest the kernel reads the manifest when the plugin is registered to build the context (capabilities) and to create the plugin's data directory under its plugin data base. consumers should take the version from env!(\"cargo pkg version\") rather than hardcoding it (m4.1 aligns samurai). plugin trait lifecycle semantics (from the method docs): - manifest — static metadata; the only method without a default. - on enable / on disable — called once each, on load/enable and on disable/shutdown. - on tick — called on every tick (the kernel ticks at the configured interval; default 1 s, and plugins such as samurai throttle their own work — every 5th tick — on top of that). - on key — key events as strings; return ok(true) when consumed. - data provider — optional extra system data; the kernel merges the returned provider into the main data stream via its composite provider (host-side only for plugins that declared readsysteminfo ). - widget — optional custom tui widget; renders against the hoststate view (see pluginwidget below). - execute — named commands with string parameters, used by external agents (ai/cli/ipc); returns a json-like string. the default answers unknownaction . debug + send is the whole supertrait burden: a plugin is boxed (box ) by the host manager, and the host logs failures without crashing the app (per-plugin error isolation in the kernel's pluginmanager ). plugincontext and capability enforcement plugincontext wraps a &'a mut dyn hoststate plus the plugin's data directory and declared capabilities. the live kernel state is only reachable through this context. every state-touching method checks the plugin's declared capabilities first; a missing capability yields pluginerror::recoverable with the message plugin does not have required capability: (so denied plugins can recover — the error is not fatal). method signature (real) capability required denied → --- --- --- --- snapshot fn snapshot(&self) - result readsysteminfo recoverable system info fn system info(&self) - result readsysteminfo recoverable top processes fn top processes(&self, n: usize) - result , pluginerror readsysteminfo recoverable kill process fn kill process(&mut self, pid: u32) - result killprocesses recoverable set alert thresholds fn set alert thresholds(&mut self, cpu: f64, mem: f64, disk: f64) - result modifyconfig recoverable set theme by name fn set theme by name(&mut self, name: &str) - result modifyconfig recoverable set layout by name fn set layout by name(&mut self, name: &str) - result modifyconfig recoverable set update interval fn set update interval(&mut self, ms: u64) - result modifyconfig recoverable alerts fn alerts(&self) - alertthresholds none — config fn config(&self) - runtimeconfig none — data dir fn data dir(&self) - &path none — contract details to rely on: - reads return result (decision d1). snapshot , system info and top processes enforce readsysteminfo exactly like the mutating methods enforce theirs, so callers must handle err . - top processes(n) sorts by cpu usage descending before taking n (sort by on cpu usage via total cmp , then truncate(n) — decision d2). it is a stable sort: equal-cpu processes keep snapshot order. the guarantee does not depend on the order produced by the data source. the in-crate tests pin this behavior. - data dir() is host-provided. the path is decided by the kernel and passed into plugincontext::new ; the contract deliberately does not prescribe a concrete location (m1.2 fixed an over-specified doc). plugins may persist per-plugin state inside it; the kernel creates it on registration (pluginmanager::register joins the base dir with the plugin id). - alerts , config and data dir are ungated reads available to every plugin. - plugincontext::new(host, plugin data dir: pathbuf, capabilities: vec ) is public; the kernel is its only caller today. plugincapability variants ( [non exhaustive] , derives clone, debug, partialeq ): readsysteminfo , killprocesses , modifyconfig , renderwidgets , and custom(string) for anything not covered. two enforcement points exist: the check capability calls above (in this crate) for context methods, and host-side filters in the kernel's pluginmanager , which only collects data provider() s from plugins that declared readsysteminfo and only collects widget() s from plugins that declared renderwidgets . custom(string) has no built-in check anywhere yet — it is the escape hatch for ecosystem-specific permissions the host may start honoring later. hoststate the kernel-side surface a plugin may touch; the kernel implements it for its live application state (impl hoststate for appstate in the kernel's xtop/src/plugins/host.rs ). plugins never depend on kernel types — only on this trait. the trait has 9 methods : method signature meaning --- --- --- snapshot fn snapshot(&self) - systemsnapshot full sample (host may serve the cached one) system info fn system info(&self) - systeminfo hostname/os/kernel/desktop/shell kill process fn kill process(&mut self, pid: u32) - bool send termination; true if the signal was sent set alert thresholds fn set alert thresholds(&mut self, cpu: f64, mem: f64, disk: f64) overwrite the three thresholds alerts fn alerts(&self) - alertthresholds current thresholds config fn config(&self) - runtimeconfig theme name, layout name, interval ms , hostname set theme by name fn set theme by name(&mut self, name: &str) - bool true if the theme exists set layout by name fn set layout by name(&mut self, name: &str) - bool true if the layout exists set update interval ms fn set update interval ms(&mut self, ms: u64) tick interval the in-crate tests implement a minimal fakehost with all 9 methods over a hand-built systemsnapshot — the pattern to copy when writing host doubles. pluginerror display : recoverable prints the message verbatim, fatal prints fatal: , unknownaction prints unknown action: . pluginerror implements std::error::error . the kernel's extension host maps it onto extensionerror when forwarding plugin actions to extensions (kernel xtop/src/plugins/extension host.rs ). systemdataprovider source of real-time system data (provider.rs ). implemented by the kernel's sysinfo provider and by plugins that contribute extra metrics; the kernel merges plugin providers into the main stream through its composite provider. method default --- --- refresh all(&mut self) required snapshot(&self) - systemsnapshot required disk io(&self) - vec vec![] batteries(&self) - vec vec![] gpu info(&self) - vec vec![] system info(&self) - systeminfo systeminfo::default() kill process(&self, pid: u32) - bool false as any(&self) - &dyn any / as any mut required (downcast for provider composition) add extras(&mut self, vec ) no-op (composite providers override it) pluginwidget a widget a plugin registers for tui rendering. its render closure draws against the plugin's hoststate view. this type is deliberately distinct from xtop-widget-api 's widgetregistration (which draws over widgetstate ); the two are different contracts and must not share a name (dr-2, m1.3 — this type was renamed from a duplicated widgetregistration ). the kernel's render engine gives plugin widgets precedence over every pack (see widget-contract.md). implementing a plugin — step by step the steps mirror what xtop-plugin-samurai (plugins/plugins/xtop-plugin-samurai/src/lib.rs ) does today and what the host expects. 1. declare the dependency. plugin crates live in the plugins repo and consume the crate as a git dependency (floating during this cycle; see architecture.md for the temporary path-dep pattern): 2. implement plugin . import from the crate root (the real import style used by samurai): note the capability flow: because manifest() declares only readsysteminfo , ctx.top processes(5) succeeds, while ctx.set alert thresholds(...) would come back as pluginerror::recoverable(\"plugin does not have required capability: modifyconfig\") . add plugincapability::modifyconfig to the declared capabilities to allow it. 3. optional: contribute a widget or data. return some(pluginwidget) from widget() — the kernel collects it only when the manifest declares renderwidgets — or some(box::new(my provider)) from data provider() — collected only with readsysteminfo declared — with the provider merged via the kernel's composite provider. 4. host wiring. the kernel registers the plugin as box through its pluginmanager (creates the data dir from the manifest id, builds plugincontext::new(state, dir, capabilities) , calls on enable ), then drives on tick per tick, on key per key, execute per named command, and on disable on shutdown. plugin authors do not touch hoststate ; they only ever see the context. writing a test double that implements hoststate is done by copying the fakehost pattern from the in-crate tests in context.rs ."},{"locale":"en","repo":"api","title":"widget render contract — xtop-widget-api","label":"docs · widget-contract","path":"docs/widget-contract.md","slug":["api","docs","widget-contract"],"text":"widget render contract — xtop-widget-api xtop-widget-api (api/crates/widget-api/ ) is the renderer contract for widget packs (dr-2). widgets are the visual parts drawn inside layout areas: the kernel ships a base pack, and any other pack (blocks pack, community, plugin) provides renderers for the same widget names or new ones. three pieces make up the crate: - state::widgetstate — the read-only view a renderer receives; - glyph — the glyph-style enums plus the canonical mappings to ratatui drawing primitives; - renderer::{widgetregistration, widgetrenderer} — how packs register renderers by widget name. the crate root re-exports widgetstate , widgetregistration , widgetrenderer and the glyph enums (chartcharset , widgetborders ); the glyph mapping helpers are reachable only under the glyph module (xtop widget api::glyph::to color etc. — decision d5). like every contract crate, it never depends on the kernel. it depends on ratatui (for frame , rect , marker , border sets), serde (for the glyph enums) and xtop-plugin-api (for the data model its widgetstate methods name). widgetstate — the renderer's view (28 methods) every renderer draws against this trait — never against kernel types. the kernel implements it over its live appstate (xtop/src/state/widget state.rs ) and the render engine hands it to the pack renderers. read-only by design: widgets never mutate app state. the trait's 28 methods, grouped as in the source: group method signature meaning --- --- --- --- sample snapshot fn snapshot(&self) - option current per-tick sample; none before the first tick theme theme name fn theme name(&self) - &str active theme name theme theme fg fn theme fg(&self) - &[u8; 3] theme foreground as an rgb triple theme theme bg fn theme bg(&self) - &[u8; 3] theme background as an rgb triple theme theme palette fn theme palette(&self) - &[[u8; 3]; 16] 16-entry palette theme alerts fn alerts(&self) - alertthresholds current alert thresholds glyph style charset fn charset(&self, widget: &str) - chartcharset resolved charset for a widget glyph style borders fn borders(&self, widget: &str) - widgetborders resolved border style for a widget history cpu history fn cpu history(&self) - &[vecdeque ] per-core history (x, y) history mem history fn mem history(&self) - &vecdeque memory percent history history net rx history fn net rx history(&self) - &vecdeque summed receive rate history (bytes/s) history net tx history fn net tx history(&self) - &vecdeque summed transmit rate history (bytes/s) history disk read history fn disk read history(&self) - &vecdeque summed disk read rate history (bytes/s, aggregate across disks); default empty history disk write history fn disk write history(&self) - &vecdeque summed disk write rate history (bytes/s, aggregate across disks); default empty history load history fn load history(&self) - &vecdeque 1-minute load-average history (load avg.one ); default empty view/control search query fn search query(&self) - &str active process search text view/control process selected pid fn process selected pid(&self) - option selected row, anchored by pid view/control process sort label fn process sort label(&self) - &str label of the active sort column view/control process sort desc fn process sort desc(&self) - bool whether the active process sorting is descending; default false (ascending) reproduces the pre-direction behavior for implementors that do not track a direction view/control layout name fn layout name(&self) - &str active layout name view/control is searching fn is searching(&self) - bool search overlay active view/control fullscreen label fn fullscreen label(&self) - option fullscreen widget label, none when not fullscreen view/control sys info fn sys info(&self) - systeminfo owned machine identity copy view/control process view fn process view(&self) - vec process rows the processes widget draws process mapping uid to name fn uid to name(&self, uid: u32) - option login name for a numeric uid (kernel resolves from /etc/passwd on unix, plus directory services users via dscl on macos and local accounts via get-localuser on windows, keyed by numeric uid); default none — renderers fall back to the numeric uid (ux9.1) process mapping process cpu history fn process cpu history(&self, pid: u32) - vec recent per-process cpu-usage samples (percent of one logical core), oldest → newest, 30 samples per pid; default empty — renderers draw nothing for an empty series (ux9.1) layout options logical core count fn logical core count(&self) - usize logical processors the host reports; default 1 reproduces the pre-dr-ux1 behavior for renderers that ignore it layout options widget options fn widget options(&self) - option options object of the widget being rendered (none when the layout node carries none); default none ; renderers must treat none as default behavior the five methods under \"layout options\" and \"process mapping\" (dr-ux1/ux3/ ux5/ux9.1 additions) come with default impls , so existing implementors compile unchanged and output without options is byte-identical to the pre-options behavior. logical core count lets a renderer normalize a per-process (or per-core) cpu usage — a fraction of one logical core — into a share of the whole machine's cpu (cpubasis::total display); the kernel implements it from available parallelism . widget options carries the per-instance display options from the layout node while that widget is drawn; the value is never null , is only valid during the render call, and unknown keys must be ignored. process sort desc feeds the processes widget's direction marker (▼ descending / ▲ ascending) on the sorted column header; the kernel returns its live cycle sort flag. the three history methods added for the ux8 data surface (disk read history , disk write history , load history ) also ship with default empty implementations: they are bounded histories the kernel feeds per tick (aggregate disk rates in bytes/s and the 1-minute load average), and implementors that do not track them yet keep compiling unchanged. the two ux9.1 process helpers default to none / an empty series: uid to name resolves a numeric uid to a login name (the uid→name mapping is a display concern and deliberately lives on the state view, not in the data model — renderers fall back to the numeric uid), and process cpu history returns the bounded per-process cpu samples (oldest → newest) for a small braille spark, empty for untracked pids. semantics worth noting: - glyph style is resolved : the kernel resolves per-widget overrides against the global style (uistyle::charset for/borders for in the kernel config), so the renderer must not resolve anything itself — the contract already returned the effective choice for the named widget. - history is (f64, f64) pairs — x (tick/elapsed coordinate) and y (value), the shape ratatui chart datasets consume. network history tracks rates, not cumulative counters, so charts show throughput; the same holds for the disk read/write rate histories (aggregate bytes/s across disks). load history tracks the 1-minute load average. all histories are bounded to the kernel's history points . - process view is the single per-tick sample filtered by the active search query and sorted by the user's chosen column; selection is anchored by pid so highlight and the kill action always agree on the same row. - snapshot returns a reference (option ) — borrow the sample; never clone it per frame. sys info is the one owned copy (cheap, slowly changing). widgetrenderer and widgetregistration a pack registers one renderer per widget name . widgetregistration is the canonical registration type (dr-2; nothing outside widget-api defines it — workspace grep gate m7.4). packs today return their registry directly as hashmap (registry() in the widgets repo); the engine resolves (pack, name) at render time. glyph enums and their serde values both enums live in glyph.rs , are re-exported at the crate root, and derive clone, copy, debug, partialeq, eq, serialize, deserialize with [serde(rename all = \"snake case\")] — these are the values the kernel persists in the user config (uistyle /widgetstyle in the kernel config schema). enum variants default json (serde) --- --- --- --- chartcharset braille , dot , block , halfblock , bar braille \"braille\" , \"dot\" , \"block\" , \"half block\" , \"bar\" widgetborders native , rounded , double , plain , ascii native \"native\" , \"rounded\" , \"double\" , \"plain\" , \"ascii\" canonical glyph helpers (xtop widget api::glyph ) packs must not re-implement these mappings — they import them here, so the same configuration draws identically in every pack. the kernel only stores the contract enums; turning them into glyphs is this module's job. in-crate tests pin every row of the mapping tables below. to color returns color::rgb(r, g, b) verbatim for the triple — palette entries are already 24-bit rgb, so no quantization is applied. note the by-value [u8; 3] parameter (the packs' private copies take &[u8; 3] ; call sites adapt by dropping the reference when they migrate, m3). border for and ascii border ratatui's symbols::border module no longer ships an ascii set (its plain is the single-line box-drawing frame since ratatui 0.29), so the contract provides the canonical one. both plain and ascii map to it; packs must not hand-roll their own copy. mapping decisions (one border look per ratatui set, no pack-specific divergence): widgetborders border for returns look --- --- --- native ratatui::symbols::border::plain standard single-line box drawing (┌─┐│└┘ ), ratatui's own default set — the classic look rounded border::rounded box drawing with rounded corners double border::double double-line box drawing plain ascii border pure ascii + - \\ frame ascii ascii border same frame (ascii is an explicit config spelling of the same intent) ascii border glyphs (coded exactly so, and asserted by test): glyph value --- --- top left \"+\" top right \"+\" bottom left \"+\" bottom right \"+\" vertical left \"\\ \" vertical right \"\\ \" horizontal top \"-\" horizontal bottom \"-\" marker for the mapping mirrors the ratatui marker of the same name: chartcharset marker for returns --- --- braille marker::braille dot marker::dot block marker::block halfblock marker::halfblock bar marker::bar a pack that needs a different glyph for the same config must not re-implement the table; it diverges deliberately and documents why. dr-2 rule in practice: packs import, never re-implement before this contract existed, every pack re-implemented to color , border for , marker for and an ascii set by hand — and the copies already disagreed (the base pack mapped plain to the box-drawing plain , the blocks pack mapped it to its own ascii frame). the migration (widgets repo, m3.3/m3.4): - the base pack's util.rs deletes its to color /border for /marker for / ascii border copies and imports the canonical ones; pack-private formatting helpers (format bytes , format uptime , gauge gradient ) stay pack-private where still used; - the blocks pack deletes its hand-rolled to color /ascii border / border for , drops its const : chartcharset lint hack, and honors state.charset(widget) instead of hardcoding marker::block . the plain /ascii divergence disappears by construction: both spellings are ascii here. plugin widgets vs pack registrations xtop-widget-api 's widgetregistration draws over &dyn widgetstate — the pack view. xtop-plugin-api 's pluginwidget draws over &dyn hoststate — the plugin view (see plugin-contract.md). the two are distinct contracts and deliberately do not share a name (m1.3). in the kernel render engine the resolution order is: plugin widgets first, then the user's chosen pack for the name, then the default pack (kernel ui/layout/engine.rs + ui/screen.rs ): plugin widgets can replace any name. implementing a pack a pack is a crate that returns a name → renderer registry. widget renderers are plain functions with the exact signature fn(&mut frame, &dyn widgetstate, rect) : rules of the road for pack authors: - render only from widgetstate ; never name kernel types. the base pack (xtop-widgets ) provides the classic names used by the default layouts — header , cpu , memory , storage , network , processes , disk io , battery , gpu — and alternate packs may replace any of them by name. - guard for snapshot() == none (first tick) and for small/empty areas; renderers must never panic on small/empty state. - use the canonical glyph helpers; any deliberate divergence must be documented (e.g. an alternate visual interpretation of a charset). - widget style choices come resolved from the contract (state.charset(name) , state.borders(name) ); do not resolve config yourself."},{"locale":"en","repo":"effects","title":"xtop effects","label":"effects","path":"README.md","slug":["effects"],"text":"xtop effects frame effects and transitions for xtop. each effect is a visual add-on that plugs into the kernel's render pipeline through the shared contract crate xtop-effect-api (in the api repo): it receives the fully rendered ratatui buffer of a frame plus the time since the effect started, and rewrites the buffer in place before the terminal flush. workspace layout each effect lives in its own xtop-effect- / crate. shared effect helpers would live in an effects-lib/ crate — that crate appears only once a second effect actually shares code with an existing one, so it is not created empty. effects crate effect manifest id behavior ------- -------- ------------- ---------- xtop-effect-fade fade fade fades the rendered frame in from black over 500 ms; deterministic, zero config documentation - docs/write-effect.md — how to implement an effect against xtop-effect-api (trait recap, host contract, testing pattern). - docs/effects.md — catalog of the built-in effects and the kernel integration note. development from this repo root: during active development all repos live side by side. the committed form of xtop-effect-api is a floating git dependency ({ git = \"https://github.com/xtop-cli/api\" } , shared [workspace.dependencies] ). to compile against the local api checkout instead — required until the api repo is pushed — temporarily point that dependency at ../api/crates/effect-api (a path relative to this repo), run the commands above, then restore the git form and delete the generated cargo.lock (the repo ignores it by design). local ci stages: fmt clippy check test . license mit"},{"locale":"en","repo":"effects","title":"effect catalog","label":"docs · effects","path":"docs/effects.md","slug":["effects","docs","effects"],"text":"effect catalog built-in frame effects shipped by this repository. all of them implement the xtop-effect-api contract (see docs/write-effect.md for the authoring guide and the host contract). the catalog is additive: one crate per effect, one row per effect. fade (xtop-effect-fade , manifest id fade ) the fade-in effect. type: xtop effect fade::fadeeffect — a stateless unit struct with no configuration of any kind. what it looks like fade takes the fully rendered frame of one tick and blends its rgb content in from black. during the window every color::rgb foreground and background is scaled toward black; colors that are not rgb (color::reset , indexed, grayscale) and all cell modifiers are never touched. - elapsed == 0 : all rgb channels are zero — the frame is black where the widgets painted rgb colors. - elapsed strictly between 0 and the window: every rgb channel sits at round(channel alpha) with alpha = elapsed / 500 ms . - elapsed = 500 ms : full intensity. on a terminal with a dark default background this reads as the whole frame fading in from black over half a second. on a light default background the color::reset backdrop (for example the empty space between text) is visible immediately, while rgb text and backgrounds still ramp up from black. exact window semantics - window: exactly fade duration = 500 ms (a const in the crate), measured from the moment the effect started, i.e. from the elapsed value the host passes on each frame. - the effect keeps no progress state: it is a pure function of the frame buffer and elapsed , so it never drifts with frame pacing or tick jitter. - determinism guarantee: once elapsed = fade duration , effect::on frame returns without writing a single cell. every later frame is byte-identical to the host-rendered buffer, for as long as the effect stays active — repeated calls are stable, and the buffer can be compared cell-for-cell (the test suite proves it). testing the suite in xtop-effect-fade covers: end-of-window byte-identity, zero-elapsed dimming (rgb cells dimmed, color::reset /indexed cells untouched), strict monotonicity at the mid-fade point, stable manifest metadata, repeated calls past the window, and empty buffers. integration note the kernel (xtop repo) wires built-in effects into its render pipeline behind an optional effects feature (ecosystem roadmap m5.3, delivered with the kernel milestone): the draw closure applies the active effect's on frame to the buffer after layout and before the terminal flush, passing the time since the effect started. until that wiring lands, this repository compiles and tests standalone against the contract crate xtop-effect-api ; nothing here depends on the kernel."},{"locale":"en","repo":"effects","title":"writing an effect for xtop","label":"docs · write-effect","path":"docs/write-effect.md","slug":["effects","docs","write-effect"],"text":"writing an effect for xtop how to implement a new effect against the shared contract crate xtop-effect-api , which lives in the api repository and is the single source of truth for the effect contract (dr-5 in the ecosystem roadmap). the reference implementation in this repository is xtop-effect-fade . the contract two public items, and deliberately nothing else: there is no registration or glue type: an effect is just a send + sync type implementing effect . the kernel holds concrete effects behind an optional feature and calls the trait. host invocation contract the kernel (xtop ) is the host. its obligations, as documented in the xtop-effect-api crate docs, are: - effect::on frame is called on every rendered frame , after widgets and layout have been drawn into the ratatui buffer and before the terminal flush. - elapsed is the time since the effect started — an absolute age, not a frame delta. - effects are stateful across frames : any progress an effect needs is kept on self between calls. - the host does not pace frames (it ticks at roughly one-second cadence plus ad-hoc repaints). a transition must therefore advance by wall-clock time (elapsed ), never by counting frames. an effect never depends on the kernel; it uses only xtop-effect-api and ratatui's buffer type. what the effect side must guarantee 1. manifest() returns stable, static metadata: a short id (used to select the effect), a human name , and a one-line description . 2. on frame may rewrite any cells of the fully rendered frame in place. 3. after an effect's window has elapsed it must become a no-op: later frames stay byte-identical to the host-rendered buffer. 4. the same (buffer, elapsed) pair must produce the same output. determinism is what makes effects testable without a time source. the pure-function pattern keep progress out of the effect when the semantics allow it. elapsed is absolute, so an effect can be a pure function of (buffer, elapsed) — the implementation below is exactly what xtop-effect-fade does: only reach for self state when the visual genuinely needs history that elapsed cannot express. stateful or not, the type must stay send + sync . testing pattern because the effect is a pure function of (buffer, elapsed) , tests need no terminal, no time source and no host: - build a buffer with ratatui::buffer::buffer::empty(rect::new(0, 0, w, h)) and paint a few cells of interest through the public content field. - call effect.on frame(&mut buffer, some duration) . - assert on buffer.content() — cell fields (fg , bg , modifier , symbol ) all implement partialeq , so whole buffers compare with assert eq!(a.content(), b.content()) . the xtop-effect-fade test suite covers the boundary cases every effect should test: elapsed == 0 (start state), a mid-window value, exactly the window, repeated calls past the window (stability), and an empty buffer. buffer: clone makes the byte-identical checks one line: landing a new effect in this repository 1. create xtop-effect- / with a manifest inheriting the workspace fields (version.workspace , edition.workspace , ...), depending on ratatui and xtop-effect-api through [workspace.dependencies] . 2. add the crate to members in the root cargo.toml . 3. implement effect , mirror the testing pattern above, and document the exact visual semantics in the crate docs. 4. add a catalog entry in docs/effects.md and a row in the readme table. shared helper code belongs in an effects-lib/ crate — but only once a second effect actually shares code with an existing one. do not create it empty."},{"locale":"en","repo":"extensions","title":"xtop extensions","label":"extensions","path":"README.md","slug":["extensions"],"text":"xtop extensions official repository for xtop extensions — optional behaviors the kernel can host. today this means server-style extensions : long-running integrations the kernel starts on demand, which drive the app through the host contract in xtop-extension-api . the contract crate provides exactly one shape today: an extension implements the extension trait (manifest() + run server(server id, ctx) ) and acts through an extensionhost view that offers tick() and execute plugin() — extensions reach kernel features through plugins, not through direct hooks. the kernel works fully without any extension. future work : hook-style extensions that touch config, theme, layout or rendering (pre/post-render hooks, config transforms, new commands) are designed as they become needed — nothing in xtop-extension-api implements them yet. workspace each extension lives in its own folder under extensions/ : current extension: xtop-extension-mcp - mcp (model context protocol) server over json-rpc 2.0 stdio, launched by the kernel as xtop mcp . - exposes the samurai plugin (plugins repo) as 12 mcp tools; the tool table is generated from the plugin's exported plugin id + actions:: constants, so the extension depends on the plugins repo at compile time. - crate readme · protocol and tool mapping in docs/mcp-protocol.md · extension architecture in docs/architecture.md. getting started (development) from this repo root: during active development all repos live side by side and use local path dependencies: license mit"},{"locale":"en","repo":"extensions","title":"xtop-extension-mcp architecture","label":"docs · architecture","path":"docs/architecture.md","slug":["extensions","docs","architecture"],"text":"xtop-extension-mcp architecture xtop-extension-mcp is a server-style extension built on the xtop-extension-api contract crate. it never talks to the kernel directly: the kernel hosts it behind its mcp-extension feature and launches it from the xtop mcp command. extension trait usage - mcpextension implements the [extension ] trait: - manifest() declares id mcp , name xtop mcp server , version from the crate version (env!(\"cargo pkg version\") ), and the single server id mcp . - run server(server id, ctx) rejects every server id other than \"mcp\" with extensionerror::unknown and otherwise runs the stdio server loop. - while running, the extension drives the kernel exclusively through the two methods of [extensioncontext ]: - tick() — advance the monitoring tick (also ticks hosted plugins); the server calls it before every tools/call execution. - execute plugin(plugin id, action, params) — run a plugin action; all tool calls funnel through it. structure - src/lib.rs : - run mcp server(ctx) — the stdio loop: reads one line at a time, skips blanks, serializes and writes the response, flushes. - handle line(line, ctx) — parses one json-rpc line and dispatches to the handlers; malformed json becomes extensionerror::recoverable (server stops), everything else returns the response object. extracted from the loop so the json-rpc layer is testable without a terminal. - handle initialize / handle tools list / handle tools call — the three protocol handlers. - tool actions , tool name() , action for tool() — the tool table derived from the samurai constants. - src/tests.rs — unit tests of the json-rpc layer against a mock [extensionhost ] that records execute plugin calls and returns canned results (tick() no-ops). why it depends on the plugins repo the tools this server advertises are the samurai plugin's actions. before dr-6 the plugin id \"samurai\" and the 12 action strings were duplicated by hand in this crate, so renaming an action (or the plugin) broke the mcp tools silently at runtime . depending on xtop-plugin-samurai makes the coupling compile-time consistent: - plugin id drives every execute plugin call (there is no other hardcoded plugin id in the crate); - actions:: drives both the tool list (tools/list , order and names) and the argument translation (tools/call ): each tool name is resolved back to its action constant before dispatch; - a rename on either side now fails the extension build instead of breaking an ai client later. trade-off: the extension build pulls in the plugin crate (and with it ratatui and xtop-plugin-api ). that is acceptable for a kernel-hosted server extension; in exchange both repos stay in lockstep. relationship to samurai docs - the wire protocol and the per-tool params mapping live in mcp-protocol.md. - the samurai actions, lifecycle and params mini-syntax are documented in the plugins repo (plugins/docs/architecture.md ); the heuristics behind process.alerts / alerts.status in plugins/docs/rules.md . [extension ]: https://docs.rs/xtop-extension-api/latest/xtop extension api/trait.extension.html [extensioncontext ]: https://docs.rs/xtop-extension-api/latest/xtop extension api/struct.extensioncontext.html [extensionhost ]: https://docs.rs/xtop-extension-api/latest/xtop extension api/trait.extensionhost.html"},{"locale":"en","repo":"extensions","title":"xtop mcp protocol","label":"docs · mcp-protocol","path":"docs/mcp-protocol.md","slug":["extensions","docs","mcp-protocol"],"text":"xtop mcp protocol xtop-extension-mcp implements a minimal model context protocol server over json-rpc 2.0 , line-delimited on stdin/stdout — one json object per line, one response per request, no http. the kernel launches it as xtop mcp (mcpextension::new().run server(\"mcp\", ctx) ). transport framing - every input line is trimmed; empty lines are skipped. - malformed json terminates the server with a recoverable error (the kernel reports it) — no json-rpc response is emitted, mirroring the stdio loop. - every well-formed request produces exactly one response object with \"jsonrpc\": \"2.0\" and the request's id echoed. handled methods method purpose --- --- initialize announces protocolversion 2024-11-05 , capabilities.tools , serverinfo (xtop , version = crate version). tools/list the 12 tools generated from the samurai plugin's actions:: constants. tools/call executes one tool against the hosted plugin. anything else json-rpc -32601 method not found: . notifications/initialized , ping , resources/ , logging , instructions and batching are not implemented — out of scope for this minimal server. tool table the tool table is generated from the samurai plugin's exported constants (dr-6): this crate depends on xtop-plugin-samurai at compile time and maps each of its 12 actions:: constants to an mcp tool whose name is the action with . replaced by (system.summary → system summary , process.alerts → process alerts , …). there is no second hand-maintained string list. tool samurai action mcp arguments → params string --- --- --- system summary system.summary — processes top processes.top count (default 10), filter → count[,filter= ] processes search processes.search pattern , fields → pattern[,fields= ] process info process.info pid → \" \" (required) process kill process.kill pid → \" \" (required) threshold set threshold.set cpu , mem , disk → \" , , \" (all required) threshold get threshold.get — config get config.get — config set config.set one of interval ms / theme / layout → \"interval ms= \" / \"theme= \" / \"layout= \" process alerts process.alerts — alerts status alerts.status — plugin status plugin.status — descriptions and json inputschema objects advertised by tools/list are maintained here per tool; the action strings, the plugin id and the tool-name mapping come from the plugin crate. tools/call behavior for every tool call the server: 1. resolves the tool name back to its action constant (unknown name → -32601 tool not found: ), 2. translates the typed mcp arguments object into the plugin's string params syntax (missing required arguments → -32602 with the argument name in the message), 3. calls ctx.tick() (refreshes the kernel state and ticks hosted plugins), 4. calls ctx.execute plugin(plugin id, , ) — always against the samurai plugin id imported from the plugin crate, 5. wraps a successful plugin json response as {\"content\": [{\"type\": \"text\", \"text\": \" \"}]} . error mapping json-rpc code meaning origin --- --- --- -32601 method not found / tool not found unknown method or unknown tool name -32602 invalid params missing required tool argument (pid, cpu/mem/disk, or config key) -32000 server/execution error extensionerror from execute plugin (plugin missing, capability denied, action failed) example session"},{"locale":"en","repo":"extensions","title":"xtop-extension-mcp","label":"xtop-extension-mcp","path":"xtop-extension-mcp/README.md","slug":["extensions","xtop-extension-mcp"],"text":"xtop-extension-mcp mcp (model context protocol) server extension for xtop. exposes xtop's samurai plugin (system monitoring, process queries and control, heuristic alerts) as mcp tools over a json-rpc 2.0 stdio server. any mcp-compatible ai — claude desktop, cline, etc. — can connect: protocol overview - json-rpc 2.0 over stdin/stdout, one json object per line. - handles initialize , tools/list and tools/call ; anything else is answered with -32601 . execution failures map to -32000 , missing arguments to -32602 . - malformed json stops the server with a recoverable error (no response is emitted). - wire details and an example session: docs/mcp-protocol.md. tools the server exposes the 12 samurai actions as 12 tools. the tool table is generated from the constants exported by xtop-plugin-samurai (dr-6) — tool name = action with . replaced by : tool samurai action arguments --- --- --- system summary system.summary — processes top processes.top count (default 10), filter (regex) processes search processes.search pattern (regex), fields (name,cmd,user,state,exe,cwd ) process info process.info pid process kill process.kill pid threshold set threshold.set cpu , mem , disk threshold get threshold.get — config get config.get — config set config.set interval ms / theme / layout process alerts process.alerts — alerts status alerts.status — plugin status plugin.status — typed arguments are translated to the plugin's string params syntax before every execute plugin call (see docs/mcp-protocol.md). samurai coupling this crate depends on xtop-plugin-samurai at compile time: - every tool call targets plugin id — there is no other plugin id; - actions:: drives the tool table and the argument translation. a rename of the plugin id or of an action now breaks this build instead of breaking mcp clients at runtime. architecture and rationale: docs/architecture.md. how the kernel launches it xtop mcp (kernel, mcp-extension feature) constructs mcpextension::new() and calls run server(\"mcp\", ctx) with a host context that resolves execute plugin against the kernel's hosted plugins. development license mit"},{"locale":"en","repo":"layouts","title":"xtop-layouts","label":"layouts","path":"README.md","slug":["layouts"],"text":"xtop-layouts data-driven layout definitions for the xtop tui system monitor. this crate (xtop-layout ) owns everything about layouts so the kernel stays thin: - model — layout tree (layoutdef → splits of areas → widget leaves), serializable from json/jsonc, no ui dependencies. - default/ — 10 embedded default layouts shipped with the crate: the seven mode-bound layouts (dashboard , vertical , horizontal , cpu focus , memory focus , network focus , process focus ) plus three preset extras — detail dashboard , detail network , detail processes (detail .jsonc ) — that sit after the modes in file order and showcase per-widget options (dr-ux6). preset table (ux8.5: dense full-monitor pages with zero orphan rows at common sizes — every split is percentage-exact or fill-absorbed, see docs/authoring.md \"density guidance\"): file \"name\" composition at 100x34 (31 body rows) widgets --- --- --- --- detail dashboard.jsonc detail dashboard header → 18-row monitor band (cpu left, 36% side column of summary/sensors) → 13-row full-width processes header, cpu, summary, sensors, processes detail network.jsonc detail network header → 22-row band (network per-iface box + summary/disk io/memory column) → 9-row processes header, network, disk io, summary, memory, processes detail processes.jsonc detail processes header → 8-row stat strip (summary/cpu/memory/storage/network) → 23-row full-height processes header, summary, cpu, memory, storage, network, processes summary (load/uptime/process-count panel) and sensors (per-core temperature heat view) are the ux8.4 widget-wave arrivals placed by the ux8.5 presets; names resolve at render time like every other widget id. on startup the kernel writes these as editable templates into the user config layouts dir when it initializes its assets (never overwriting files the user already edited). - custom/ — community layouts : installable extras, shared via prs. they never ship in the binary; the kernel's xtop layout install fetches this folder and copies the layout into the user config layouts dir (or copy the file there yourself, e.g. /.config/xtop/layouts/ on linux). - loader — loading/parsing of jsonc files and merging with user overrides. - mode — built-in layout modes and terminal-size degradation rules. layout structure use layout files are json/jsonc: size accepts a fixed number of rows/columns, a percentage (\"45%\" ) or \" \" /omitted for the remaining space. widgets are referenced by name; the kernel decides which renderer each name maps to. documentation - docs/layout-schema.md — formal jsonc layout schema (document structure, area grammar, constraint syntax, comments dialect, embedded defaults). - docs/authoring.md — step-by-step guide: writing, validating and installing layouts, widget ids, modes and terminal-size thresholds. - docs/decisions.md — design decision log (dr-3, unvalidated widget ids, name-based merging). personalization model 1. defaults are compiled into the binary and copied as templates to /.config/xtop/layouts/ on first run (seven mode-bound layouts first, then the detail preset extras — order is part of the mode-slot contract, see docs/authoring.md §6). 2. overrides : edit a file whose \"name\" matches a default → it replaces that default in place (same palette position, no duplicates). 3. new layouts : any extra file shows up as an additional layout. 4. community : share layouts via pr to layouts/custom/ ; users copy or install them into their config dir."},{"locale":"en","repo":"layouts","title":"community layouts (installable)","label":"custom","path":"custom/README.md","slug":["layouts","custom"],"text":"community layouts (installable) layouts in this folder are not built into the binary : they are extra layouts anyone can install or share. this folder is exactly what the kernel fetches for xtop layout install (it sparse-clones this repo's layouts/custom/ ), so every file here is an installable community layout. - drop a layout file here and open a pr to share it with everyone. - every file must follow the layout format (see the root readme.md and the formal schema in docs/layout-schema.md ) and use a unique \"name\" (do not collide with the default/ names or with other files here). - file name convention: my layout.jsonc (name inside the file is what the tui shows; xtop layout install accepts either as the lookup key). - validate before sharing: xtop layout check . using a community layout the kernel installs community layouts into the user config dir. either way the file lands in the user layouts folder and is loaded at the next startup of the tui (and becomes part of the l layout palette): xtop layout install matches a file by its file stem or by the layout \"name\" inside it (case-insensitive), keeps the original file name, and refuses to overwrite an existing file in the user layouts dir — edit that file in place instead. custom files override a built-in layout by reusing its \"name\" (same palette position) or appear as extra layouts with a new \"name\" ; see docs/authoring.md for the full flow."},{"locale":"en","repo":"layouts","title":"authoring guide: custom layouts","label":"docs · authoring","path":"docs/authoring.md","slug":["layouts","docs","authoring"],"text":"authoring guide: custom layouts step-by-step guide for writing a custom layout for xtop, validating it, and making it available — locally or as a community layout. the authoritative format description is docs/layout-schema.md ; this guide focuses on the workflow and on the ids, modes and install/validation flow as implemented in the kernel (xtop/src/commands/layout.rs , xtop/src/ui/layout/engine.rs , xtop/src/ui/screen.rs ). 1. where layouts live the crate embeds ten default layouts (layouts/default/ .jsonc ): the seven mode-bound defaults first (palette slots 0–6), then three preset extras (detail dashboard , detail network , detail processes — slots 7–9, dr-ux6). since ux8.5 the extras are dense, full-coverage pages: detail dashboard is the full-monitor page (tall cpu with summary /sensors side column + full-width processes ), detail network focuses the per-interface network box with a summary /disk io /memory side column and a bottom processes strip, and detail processes is a compact stat strip (summary + cpu /memory /storage /network ) over a full-height processes table. custom layouts are loaded from the user layouts directory at startup (xtop/src/commands/share/bootstrap.rs merges defaults with everything load layouts from dir finds there): platform user config dir user layouts dir --- --- --- linux $xdg config home/xtop , else /.config/xtop /layouts macos /library/application support/xtop /layouts windows %appdata%\\xtop \\layouts the xtop-layout crate itself never touches the filesystem layout dirs of users; the kernel owns the paths above (xtop/src/config/platform/ ). 2. writing a layout create a .jsonc file containing exactly one layout: rules of thumb (full grammar: docs/layout-schema.md ): - \"name\" must be unique. merging is by exact, case-sensitive name: a file whose name equals a default replaces that default in place (same palette slot); every other file is appended after the defaults. the embedded defaults are ordered so the first seven names are the mode-bound layouts and the three preset extras (detail dashboard , detail network , detail processes ) come right after them — never renumber the first seven, and new embedded extras must be appended at the end (see docs/layout-schema.md , \"embedded defaults\"). - use a 3-row header widget on top, like every default. - in a \"vertical\" split children stack top-to-bottom; in \"horizontal\" they sit side-by-side. percentages refer to the enclosing split. - comment freely with // or / / ; trailing commas are not accepted . - each widget instance can take an optional options object that tunes how that instance is drawn — e.g. { \"widget\": \"processes\", \"options\": { \"cpu\": \"total\" } } . options are passthrough: the layout crate only stores and forwards them, renderers interpret them (grammar and examples: docs/layout-schema.md , section \"widget options \"). no options means the widget's default behavior. - validate the file before shipping it (next section). density guidance: no orphan bands, no starved boxes ux8.5 presets are dense: every layout row is covered by a box and every box is tall enough for its content to grow (charts/lists fill their box — the ux8.4 widgets wave). the render engine (xtop/src/ui/layout/engine.rs ) feeds the constraints to ratatui, where percentages measure the whole split and fill takes the leftover, so write splits that tile by construction: - pure-percentage stacks : children sized \"p1%\" , \"p2%\" , ... with p1 + p2 + ... = 100 tile the split exactly at every terminal size (no fill needed). - fixed header + fill : put a fixed size: 3 header (or size: 8 strip) next to a \" \" /fill sibling, never next to a percentage — percentages would measure the whole split (including the header rows) and over-claim. this is why every default root is header (3) + body \" \" , with the tiling percentages living inside the body. - fill alongside percentages : fine when the percentages sum below 100 — the fill absorbs the remainder (e.g. a \" \" cpu column next to a \"36%\" side column). keep the percentage demand below 100 or the fill starves. - a split with neither a fill sibling nor percentages summing to 100 leaves an orphan band; the loader tests (test detail presets split coverage full tiling ) enforce the rule on every split of every detail preset. row budget at the common sizes (body = terminal rows − 3 header rows): widget content it absorbs rows with comfortable box (rows) keep under --- --- --- --- cpu core grid + per-core freq/temp + multi-row history 12–22 — processes process list 9–30 — summary load/uptime/proc-count lines + sparklines 6–9 10 sensors per-core temperature heat view (one-line fallback without temps) 7–12 14 memory ram/swp rows + sections + history chart 6–10 12 network per-iface rows + aggregate dual chart 6–26 — disk io per-disk rows + r/w chart 6–12 — storage per-disk bars 5–9 10 rough numbers at 100x34 (31 body rows): detail dashboard = 18-row monitor band (cpu 64 cols × 18 rows, summary 8 rows, sensors 10 rows) over 13 rows of processes; detail network = 22-row network box + 7/8/7 summary/disk io/memory side column over 9 rows of processes; detail processes = 8-row strip over a 23-row process table. the same trees give 80x24 (21 body rows) 12/9, 15/6 and 8/13 splits and 120x40 (37) 21/16, 26/11 and 8/29 — always full coverage, because every split is percentage-exact or fill-absorbed. 3. validating the kernel exposes xtop layout check (xtop/src/commands/layout.rs ), which runs the exact parser of this crate (parse layout err ): the underlying crate functions are also public: parse layout(source) - option and parse layout err(source) - result (the latter is what check evaluates). while developing, prefer a directory the kernel already reads, or call the crate from a scratch test — but remember how invalid files are handled: load layouts from dir skips any file that fails to parse and prints [xtop-layout] skipping invalid layout file: to stderr, so a broken file never kills the app — it simply does not appear in the palette. it is easy to miss, which is why xtop layout check exists. 4. making it active - personal layout : put the file directly into the user layouts dir (table above). the directory is read once at startup, so restart xtop (or start it) to pick changes up; cycle the palette with l , or select the layout by name from the command palette. the active layout name is persisted in the config and restored on the next run. - seeded defaults : on startup the kernel seeds copies of the ten default .jsonc templates into the user layouts dir (marker file .xtop initialized , version 4 since the preset extras were renamed to detail and the seeded set was refreshed; files that already exist are never overwritten — xtop/src/commands/share/assets.rs ). editing a seeded copy is the supported way to tweak a built-in default; because overrides match by \"name\" , the edited file replaces the default at its usual palette slot. seeded copies of renamed templates are never deleted either: after an upgrade that renames a template, the new files are seeded next to the older copies, and any older copy whose \"name\" no longer matches a built-in behaves like a user layout (an extra at the end of the palette). remove older copies yourself when you did not edit them. - overrides vs. new layouts : same name → in-place replacement (slot preserved, no duplicate). new name → appended as an extra layout. 5. widget ids layout files reference widgets by plain string ids; there is no compile-time allowlist, resolution happens at render time (xtop/src/ui/layout/engine.rs ). what the presets reference today: id where notes --- --- --- header , cpu , memory , storage , network , processes , disk io base pack (xtop-widgets registry) used by every default (mode layouts and detail presets); always available summary , sensors base pack registry (ux8.4 additions) referenced by the detail presets since ux8.5: summary is the load/uptime/process-count panel with sparklines, sensors the per-core temperature heat view (honest one-line fallback when no temp data). they resolve at render time like every other name — a layout file is valid before the pack lands, it just renders nothing for a name no renderer provides battery , gpu base pack registry registered renderers; selectable in full-screen mode, not used by any default. their data providers are platform-dependent, so expect empty values where no source exists samurai (plugin widgets) plugins with the renderwidgets capability (kernel plugins/manager.rs ) referencable when the kernel is built with the plugin enabled (e.g. plugin-samurai ). plugin renderers take precedence over pack renderers for the same name reference order at render time: plugin widgets first, then the pack chosen by the style config, falling back to the base pack. a typo'd or unavailable id renders nothing in that area (the area is left blank); the kernel prints a one-time stderr warning per unknown name (xtop: layout ' ' references unknown widget ' ' — xtop/src/ui/layout/engine.rs ), and the full-screen view shows no widget registered for ' ' instead. because of that, xtop layout check is structural validation only: it cannot know whether an id will resolve. 6. modes and thresholds the crate computes how the requested mode degrades with terminal size (src/mode.rs , detect effective layout ). exact thresholds: requested mode terminal size effective layout --- --- --- any width what the kernel does ( xtop/src/commands/layout.rs, cmd install): 1. runs git clone --depth 1 --filter=blob:none --sparse https://github.com/xtop-cli/layouts into a temp dir (git must be on path), then git sparse-checkout set layouts/custom. 2. scans layouts/custom/ for .json/ .jsonc files and picks the first whose file stem or parsed \"name\" equals (case-insensitive). 3. copies that file into the user layouts dir under its original file name ; it refuses to overwrite an existing target (edit in place instead) and errors when nothing matches or git fails. so to share a layout: put my layout.jsonc into layouts/custom/ of this repo (unique \"name\", validated with xtop layout check), open a pr, and after it lands anyone can xtop layout install `. you can equally skip the repo: copy the file into the user layouts dir yourself — the app loads it at the next startup either way. community files in this repo are not read by the kernel at runtime; they only become live after the install (or a manual copy)."},{"locale":"en","repo":"layouts","title":"design decisions","label":"docs · decisions","path":"docs/decisions.md","slug":["layouts","docs","decisions"],"text":"design decisions short log of the decisions that shape this crate and the layout file format. grounded in this repo's code and in the kernel sources referenced below. d-1 — layouts live in a standalone, ui-free crate (dr-3) decision (roadmap dr-3). the layout model, loader and modes live only in xtop-layout ; the kernel has no internal layout model or loader. the crate has no ratatui dependency and no dependency on any xtop- contract crate: it is pure data + serde (src/lib.rs module docs). the kernel consumes it as a git dependency and translates the tree to concrete ui constraints at render time (xtop/src/ui/layout/engine.rs maps layoutconstraint values to ratatui constraints). why. the kernel stays a thin host; layout authoring, defaults and the degradation rules are testable without a terminal or the widget pack ecosystem; the layout format is a stable interchange surface between repos that never needs the widget registry at compile time. consequences accepted: - the format schema is implicit in the serde model + manual visitor of src/model.rs — documented formally in docs/layout-schema.md instead of a generated .schema.json (nothing consumes a json schema file today). - widget ids are opaque strings (next entry), and nothing in this repo can verify them against the widgets or plugins repos. - the palette ordering contract (mode-bound defaults in slots 0–6, fixed order, preset/user extras appended after them) is enforced by default layout sources order + mode from layout index + merge layouts semantics and pinned by unit tests (see docs/authoring.md §6). d-2 — widget ids are unvalidated strings; resolution is runtime decision. layout files name widgets as plain strings (layoutnode::widget { name } in src/model.rs ). the crate does not carry an allowlist: it has no dependency on the widgets/plugins repos, so it cannot know which ids are valid for a given kernel build (which packs are compiled in, which plugins are enabled). consequence, as implemented in the current kernel. widget ids resolve at render time against, in order: plugin widgets (e.g. samurai ), then the style-chosen pack, then the base pack (xtop/src/ui/layout/engine.rs , render named ). if nothing matches, the id renders nothing — the layout area is left blank; the kernel reports unknown names once per process on stderr (warn unknown widgets , engine.rs ) and the full-screen path shows no widget registered for ' ' (xtop/src/ui/screen.rs ). this repo's own reporting is limited to skipping structurally invalid files on stderr (load layouts from dir ); a file with a valid shape but an unknown widget id parses fine. why it stays this way. compile-time validation would couple this repo to the widget registry and to every feature flag combination of the kernel; runtime resolution keeps layouts portable across kernel builds. the structural safety net that does exist is xtop layout check (kernel commands/layout.rs ), which validates files with this crate's parse layout err . d-3 — name-based merging, slot-preserving decision. merging is by the layout \"name\" field with exact, case-sensitive equality (merge layouts in src/loader.rs ): a user file replacing a default keeps the default's palette slot; new names are appended after the defaults. mode from layout index maps only slots 0–6 to modes; everything beyond them (the three embedded detail preset extras at slots 7–9 and user layouts) is addressed by name, and the kernel persists/restores layouts by name (config.layout name ). why. palette order and mode ↔ layout coupling must stay stable while users freely customize; see docs/authoring.md §6 for the contract this imposes on contributors (unique names, defaults order never renumbered). status - crate: xtop-layout v0.1.0, rust-version = \"1.87\" , edition 2021, mit, no third-party deps beyond serde/serde json. - decisions d-1..d-3 recorded 2026-09-04 as part of milestone m6 (documentation of the implicit schema)."},{"locale":"en","repo":"layouts","title":"layout file schema (jsonc)","label":"docs · layout-schema","path":"docs/layout-schema.md","slug":["layouts","docs","layout-schema"],"text":"layout file schema (jsonc) formal, code-grounded description of the layout format consumed by xtop-layout (crate xtop-layout v0.1.0). the schema is not enforced by a separate .schema.json : it is implemented by the serde model and the manual visitor in src/model.rs , plus the jsonc comment stripper in src/loader.rs . this document mirrors that code exactly, so it is authoritative for contributors and for the kernel's xtop layout check command (which runs the same parse layout err function from src/loader.rs ). file conventions - one layout per file. the layout's display name is the \"name\" field inside the file, not the file name. - files may use .jsonc or .json ; jsonc (comments allowed) is the convention everywhere in this repo. - built-in defaults live in layouts/default/ and are embedded into the binary through default layout sources in src/loader.rs (10 files: seven mode-bound defaults + three detail preset extras). - user layouts live in the platform config dir under layouts/ — see docs/authoring.md for the concrete paths per platform. - a directory is loaded by load layouts from dir : every .json / .jsonc entry is parsed; files that fail to parse are skipped and reported on stderr (see docs/authoring.md ). - layout names must be unique among defaults and user files: merge layouts matches names with exact, case-sensitive string equality. document structure a layout document is a single json object with exactly two keys: key type required meaning --- --- --- --- name string yes display name shown in the tui palette; used for mode and override matching. missing → error missing field \\ name\\ . root area object yes the layout tree. missing → error missing field \\ root\\ . any other top-level key is rejected (the custom deserializer only accepts name /root ; error unknown field ... ). inside root and inside every area object the parser is permissive: extra keys are silently ignored, only the keys in the table below are read. the root uses the same object grammar as every area (below). its size key is parsed and validated like any other area's, but the value is discarded : a layoutdef keeps only the root node, so the root always spans the whole terminal render area the kernel gives it. area objects every area in a split (and the root) is one json object: key type meaning --- --- --- size number, string, or omitted constraint this area gets from its parent split (see below). omitted ≡ \" \" (fill). widget string widget id; see docs/authoring.md for the ids that exist. options json object optional per-widget display options (dr-ux1); only meaningful together with widget . passthrough: this crate stores and serializes the object verbatim and never interprets it — the kernel hands it to the widget's renderer, which owns the semantics (see \"widget options \" below). direction string split direction: \"vertical\" or \"horizontal\" , case-insensitive (direction.to lowercase() is compared, so \"vertical\" , \"vertical\" etc. are accepted; anything else → error invalid direction: ). areas array of area objects children of a split. optional; missing/empty → an empty split that parses but renders nothing. exactly one of widget and direction must be present, otherwise the file is rejected with layout area must have 'widget' or 'direction' . if both are present the area parses, widget wins and the split keys (direction , areas ) are ignored. extra keys on an area object are ignored (the struct deserializer does not enable deny unknown fields ). widget options — per-widget display options every widget instance may carry an options json object that refines how that instance is drawn: semantics, exactly as implemented in src/model.rs (layoutarearaw ): - options is optional; an absent key and an explicit null both deserialize to none (widget renders with its default behavior). - the value must be a json object; it is a passthrough : the model stores it as serde json::value and never interprets its content. unknown keys inside the object are preserved verbatim through parse and re-serialization (key order inside options may be normalized to the serde json map ordering; the content is unchanged). - renderers own the semantics: each widget documents the keys it recognizes (see the widgets repo docs). options on split nodes, or on an area where direction wins, parse but are ignored. - layoutdef /layoutnode equality and the mirror serializer include options ; a widget without options serializes byte-identically to the pre-dr-ux1 format (the options key is only emitted when present), so existing layout files and their round trips are unchanged. - the root area follows the same grammar; options on a root widget leaf are kept on the node like any other area's (the root's size remains discarded). size — constraint syntax the raw value is deserialized as either a json number or a json string (sizeraw in src/model.rs ), then converted to a constraint: jsonc spelling constraint effect in the kernel --- --- --- (omitted) fill take the remaining space bare number 3 length(3) fixed 3 rows (vertical split) or columns (horizontal split) \"45%\" percentage(45) 45% of the parent container \" \" fill take the remaining space parsing details, exactly as implemented in tryfrom : - bare number : deserialized as u16 — must be an integer between 0 and 65535 . fractions (3.5 ), negatives and out-of-range values fail json deserialization of the untagged enum (data did not match any variant of untagged enum sizeraw ). - \" \" : the exact string, checked before the percentage branch. any other string ending in % goes to the percentage branch. - \"nn%\" : all trailing % characters are trimmed (trim end matches('%') , so \"45%%\" parses as 45 ), then the remainder is parsed as u16 ; failure produces invalid percentage: . the parsed number is not range-checked against 100 — anything 0..=65535 is accepted syntactically; the kernel hands the value to the tui constraint engine, so percentages above 100 are possible but meaningless there. \"%\" alone fails (invalid percentage: % ). - any other string (not \" \" , not ending in % , or empty) → error invalid size constraint: . fill areas are resolved by the kernel's renderer with equal weight (constraint::fill(1) in xtop/src/ui/layout/engine.rs ): when several siblings use \" \" (or omit size ), the leftover space is shared equally. direction semantics the format is ui-framework agnostic; the kernel's render engine maps each split to a tui layout of the same direction: - \"direction\": \"vertical\" — children are stacked top to bottom ; each area spans the full width and gets a height according to its size . - \"direction\": \"horizontal\" — children are placed left to right ; each area spans the full height and gets a width according to its size . constraint translation (kernel ui/layout/engine.rs ): length(n) → constraint::length(n) , percentage(p) → constraint::percentage(p) , fill → constraint::fill(1) . percentages are relative to the full enclosing split (fixed lengths are not subtracted first — a length(3) header next to a \"60%\" child makes the percentage measure 60% of the whole split, not 60% of the leftover). the constraint engine always tiles the parent completely: lengths are carved out exactly and fill areas absorb every remaining row/column (several fills share it equally). percentages are satisfied exactly when no fixed length in the same split over-claims the area (pure percentage stacks summing to 100); once lengths sit next to percentages, the demands exceed the area and the solver relaxes them in unspecified ways — which is why every built-in preset keeps lengths next to fills only, and expresses row shares with pure percentage stacks (\"tiling rules\" below). tiling rules: building splits without orphan bands because percentages measure the whole split and fills absorb the rest, a split leaves an orphan band (rows/columns no box covers) only when it has neither a fill sibling nor percentage children summing to 100. the built-in presets follow two rules, and xtop layout check users should too: 1. pure-percentage stacks : children sized \"p1%\" , \"p2%\" , ... with p1 + p2 + ... = 100 tile the split exactly at every terminal size — no fill needed, no fixed length mixed in. 2. length + fill stacks : a fixed header/strip (size: 3 , size: 8 ) sits next to a \" \" /fill sibling, which absorbs the leftover. percentages must not appear next to lengths in the same split (they would measure the full split and over-claim). 3. fill alongside percentages is fine when the percentages sum below 100 — the fill absorbs the remainder (e.g. \" \" + \"36%\" → the fill box gets the other 64%). keep the percentage demand below 100 or the fill starves to zero. ux8.5 applies these rules to every detail preset so no stack leaves an empty band at the bottom of the terminal at common sizes (100x34, 80x24, 120x40); the unit test test detail presets split coverage full tiling walks every split of every detail preset and enforces them, and docs/authoring.md (\"density guidance\") shows the worked rows. jsonc dialect comments are removed before json parsing by strip jsonc comments in src/loader.rs (the file is then parsed with serde json , which accepts strict json only): - // line comments — up to the next newline. - / ... / block comments — not nestable; an unterminated block comment silently consumes the rest of the file. - the stripper is string-aware: comment markers inside a string literal do not start a comment. both \" and ' are tracked as possible string delimiters (escapes are honoured), but only \"...\" is valid json, so use double quotes for every value. - trailing commas are not supported (the stripper does not remove them; serde json rejects them). keep json syntax otherwise strict. canonical (re-)serialized shape layoutdef implements a mirror serializer, so re-serialized layouts come out as the same grammar with these exact spellings: size numbers for fixed lengths, \"nn%\" strings for percentages, \" \" for fill (fill is always written explicitly), then either widget (followed by options when the node carries one) or direction + areas . embedded defaults default layout sources in src/loader.rs embeds ten files. the first seven are the mode-bound defaults in palette order (index 0–6); after them three preset extras (index 7–9) ship the detail variants (dr-ux6). extras are appended after the mode-bound defaults so the mode indices never move; they are ordinary layouts with a \"name\" that maps to no layoutmode (addressed by name from the palette and reachable from the kernel's layout cycling key after the modes). slot order and names are covered by unit tests (test default layouts count and order , test preset extras parse with widget options , test detail presets split coverage full tiling , test detail presets reference registry widget names ): slot file \"name\" root structure widget ids used --- --- --- --- --- 0 dashboard.jsonc dashboard vertical: header (3) → horizontal split (45%, cpu 50% + vertical split of memory/storage/network) → processes (52%) header, cpu, memory, storage, network, processes 1 vertical.jsonc vertical vertical: header (3), cpu (8), memory (8), storage (6), network (5), processes ( ) header, cpu, memory, storage, network, processes 2 horizontal.jsonc horizontal vertical: header (3) → horizontal split ( ) of cpu/memory/storage/network (25% each) header, cpu, memory, storage, network 3 cpu focus.jsonc cpu focus vertical: header (3), cpu (60%), processes ( ) header, cpu, processes 4 memory focus.jsonc memory focus vertical: header (3), memory (60%), processes ( ) header, memory, processes 5 network focus.jsonc network focus vertical: header (3) → horizontal split (50%) of network + disk io (50% each) → processes ( ) header, network, disk io, processes 6 process focus.jsonc process focus vertical: header (3) → horizontal split (8) of cpu/memory/storage/network (25% each) → processes ( ) header, cpu, memory, storage, network, processes 7 detail dashboard.jsonc detail dashboard vertical: header (3) → body ( ) split into a monitor band (58%) of a horizontal split (cpu with options {cores/show freq} + side column 36% of summary 42% / sensors 58%) and full-width processes (42%, options {cpu: total}) header, cpu, summary, sensors, processes 8 detail network.jsonc detail network vertical: header (3) → body ( ) split into a network band (70%) of a horizontal split (network 60% options {ifaces: all} + side column 40% of summary 30% / disk io 40% / memory 30%) and processes (30%, options {cpu: total}) header, network, disk io, summary, memory, processes 9 detail processes.jsonc detail processes vertical: header (3) → body ( ) split into a stat strip (horizontal split, 8 rows: summary 20% / cpu 28% / memory 18% / storage 17% / network 17%) and processes ( , options {cpu: both}) header, summary, cpu, memory, storage, network, processes nine distinct widget ids are referenced across the defaults: header , cpu , memory , storage , network , processes , disk io plus the ux8.4 arrivals summary and sensors (used by the detail presets; they are plain names to this crate — resolution happens at render time). none of the defaults puts a size on the root. the mode layouts share the same shape: a 3-row header , the focused widget (plus a companion column for network focus ) taking most of the middle, and the processes list filling the remainder (see docs/authoring.md ). the three preset extras showcase per-widget options (section \"widget options \") and follow the tiling rules above (section \"tiling rules\"): the processes widget exercises the cpu basis keys (\"total\" /\"both\" ), detail dashboard gives cpu the cores and show freq keys, detail network gives network the ifaces key, and the new dense widgets summary and sensors (ux8.4 registry additions) fill the content-heavy columns of the redesigned presets (ux8.5). those keys are only display refinements — the files are valid layouts without them, and renderers that do not recognize a key yet ignore it."},{"locale":"en","repo":"plugins","title":"xtop plugins","label":"plugins","path":"README.md","slug":["plugins"],"text":"xtop plugins official repository for native and community xtop plugins. workspace each plugin lives in its own folder under plugins/ : how plugins work - every plugin is published/installed independently and integrated into the kernel through xtop-plugin-api . - the kernel enables plugins optionally (feature flags for built-ins, runtime discovery for external ones). a plain xtop build never requires this repo. - install a plugin from the kernel with: xtop plugin install (default source: https://github.com/xtop-cli/plugins) getting started (development) from this repo root: during active development all repos live side by side and use local path dependencies: license mit"},{"locale":"en","repo":"plugins","title":"samurai plugin architecture","label":"docs · architecture","path":"docs/architecture.md","slug":["plugins","docs","architecture"],"text":"samurai plugin architecture xtop-plugin-samurai is the ecosystem's security-monitoring plugin: it implements the [plugin ] contract from xtop-plugin-api , runs ten heuristic rules over the process table, exposes everything through a json action api, and renders one tui widget. it never depends on the kernel — only on the contract crate. the canonical id and action names are exported constants (plugin id , actions:: ) and are the single source for xtop-extension-mcp 's tool table (dr-6). lifecycle the kernel hosts the plugin through xtop-plugin-api : hook behavior --- --- manifest() id samurai (= plugin id ), name samurai , version from the crate version (env!(\"cargo pkg version\") ), description, capabilities: readsysteminfo , killprocesses , modifyconfig , renderwidgets . on enable() marks the plugin enabled. on disable() marks the plugin disabled. on tick() counts ticks; every 5th tick it replaces the alert list with a fresh analysis run (tick count % 5 == 0 ). analysis errors propagate as pluginerror instead of being swallowed. execute(action, params) json action api (below); always records last action /last action result . the plugin starts enabled and reports itself through plugin.status . action api execute(ctx, action, params) returns a json string (or pluginerror::{recoverable, unknownaction} ). params use the mini-syntax below — plain strings, hand-parsed per action. action params notes --- --- --- system.summary — cpu average, memory gb/percent, process/disk/interface counts, uptime, hostname, current alert count. processes.top count or count,filter= count defaults to 10 when unparsable; 0 is an error. the optional filter regex is matched with is match against any of name, cmd, exe. processes.search pattern or pattern,fields=a,b pattern is one compiled regex; matched with is match against any listed field. fields: name , cmd , user , state , exe , cwd (default name ; unknown fields are ignored). sorted by cpu desc, capped at 100 results. process.info pid full json for one process; error when the pid is absent. process.kill pid requires killprocesses . process.alerts — the full alert array of the last analysis run. threshold.set cpu,mem,disk three comma-separated percentages; requires modifyconfig . threshold.get — current cpu high /mem high /disk high thresholds. config.get — theme, layout, interval ms, hostname. config.set interval ms= or theme= or layout= requires modifyconfig ; anything else is a parse error. alerts.status — total + per-severity counts and the top 5 alerts. plugin.status — enabled, tick count, last action/result, active + critical alert counts. process json entries carry: pid , name , cpu , mem bytes , state , user , cmd , exe , ppid , threads , run time , cwd . heuristic analysis - runs every 5th tick ( 1 s kernel ticks by default, so about every 5 seconds in wall time — the kernel decides the tick rate). - ten rules with the exact thresholds and pattern lists documented in rules.md; at most 50 alerts per run. - reads the process table through the capability-gated plugincontext::snapshot() (readsysteminfo ). - spawn-storm history (spawn history ) persists across runs inside the plugin instance. widget the plugin registers one plugin widget named samurai (the same plugin id ). it renders a bordered panel with the samurai title and two lines of agent status. for the widget to appear the plugin must be enabled (host feature + renderwidgets capability) and a layout must reference a widget named samurai ; the kernel renders plugin widgets ahead of the built-in pack widgets. relationship to xtop-extension-mcp the mcp server no longer lives in this repo — it is xtop-extension-mcp in the extensions repo, launched by the kernel as xtop mcp . the extension: - depends on this crate at compile time and uses plugin id for execute plugin(plugin id, …) ; - builds its 12-tool table from the actions:: constants (tool name = action with . → ); - translates typed mcp arguments into the params mini-syntax above and maps execution failures to json-rpc errors. see the extension's mcp-protocol.md for the wire-level mapping. [plugin ]: https://docs.rs/xtop-plugin-api/latest/xtop plugin api/trait.plugin.html"},{"locale":"en","repo":"plugins","title":"samurai heuristic rules","label":"docs · rules","path":"docs/rules.md","slug":["plugins","docs","rules"],"text":"samurai heuristic rules samurai's analyzer runs the ten rules below over the live process table and keeps up to 50 alerts per run (max alerts , truncated at the end of the pass). each rule returns at most one alert per process per run; rules are evaluated for every process in priority order, so a single process can produce several alerts. severities: critical , warning , info . alerts persist between runs: a new analysis replaces the whole alert list, so process.alerts / alerts.status reflect the last completed run (runs happen every 5th tick — see architecture.md). all pattern matching is plain substring / regex containment on the searched field — there is no slash-wrapping and no whole-string anchoring. the listed thresholds are the exact constants in src/lib.rs . --- rule 1 — suspicious executable path (suspicious exe path ) - trigger : exe path starts with one of /tmp/ , /dev/shm/ , /var/tmp/ , /proc/ , /private/tmp/ , /private/var/tmp/ . - severity : critical. - knobs : allow-list of prefixes suspicious path prefixes . rule 2 — orphan process (orphan process ) - trigger : parent pid == 1 and the lowercase process name does not contain any of the allowed daemon names: systemd , init , launchd , sshd , login , getty , nginx , apache2 , httpd , bash , sh , zsh , tmux , screen . - severity : critical when run time = 500 and the name is not in the allow-list: chrome , firefox , code , code , idea , java , dotnet , python , node , mysqld , postgres , xorg , dockerd . - severity : critical when thread count 1000 or cpu usage 200.0 , else warning. rule 9 — high file descriptor count (suspicious fd anomaly ) - trigger : open files = 1000 and the name is not in the allow-list: mysql , postgres , nginx , httpd , apache , chrome , firefox , code , java , dotnet , dockerd . - severity : info. rule 10 — spawn storm (recent spawn storm ) - trigger : more than 5 distinct fresh pids of the same process name within a rolling 120 second window. fresh means run time 5) and the freshness check are literals in rule spawn storm`; history lives per-plugin across runs."},{"locale":"en","repo":"plugins","title":"xtop-plugin-samurai","label":"xtop-plugin-samurai","path":"xtop-plugin-samurai/README.md","slug":["plugins","xtop-plugin-samurai"],"text":"xtop-plugin-samurai samurai is an ai-aware system monitoring and management plugin for xtop. it exposes system metrics, process information and runtime configuration through a json action api, runs ten heuristic threat-detection rules over the process table, and renders a small tui widget. the plugin is a library implementing the plugin trait from xtop-plugin-api — the kernel hosts it via a feature flag; it does not ship a binary of its own. features - json action api over a single execute() entry point: system summary, process queries (top by cpu, regex search, per-pid info), process kill, alert thresholds (get/set), runtime config (get/set), alert status and plugin status. - heuristic process analysis with 10 rules (suspicious paths, orphan processes, masquerading, euid/uid escalation, browser children, known miner/rootkit names, pipe/download command lines, high thread/fd counts, spawn storms) — runs every 5th tick, capped at 50 alerts per run. - alert and status data exposed as json for scripts, agents and the mcp extension. - one plugin widget named samurai for custom tui layouts. ecosystem constants this crate is the single source of truth (dr-6) for: - plugin id — the hosted plugin id (\"samurai\" ), used by the kernel and by the mcp extension for execute plugin . - actions:: — the 12 action names understood by execute() : system summary , processes top , processes search , process info , process kill , process alerts , threshold set , threshold get , config get , config set , alerts status , plugin status . action api every interaction goes through: the response is always a json string. action names, params and response shapes are documented in docs/architecture.md; quick reference: action params --- --- system.summary (none) processes.top count or count,filter= processes.search pattern or pattern,fields=name,cmd,user,state,exe,cwd process.info pid process.kill pid process.alerts (none) threshold.set cpu,mem,disk (percentages) threshold.get (none) config.get (none) config.set interval ms= , theme= or layout= alerts.status (none) plugin.status (none) regex semantics. search and filter patterns are passed straight to regex::regex::new — no /…/ wrapping. each pattern is one compiled regex, matched with is match() against any of the fields you select (fields= on processes.search ; name/cmd/exe on the processes.top filter). examples: heuristic analysis the analyzer runs every 5th tick (the kernel ticks about once per second, so roughly every 5 seconds) and keeps up to 50 alerts per run across 10 rules . the rules and their exact thresholds are documented in docs/rules.md; summary: 1. executable from a suspicious path (/tmp , /dev/shm , /var/tmp , …) 2. orphan process (ppid=1) that is not a known daemon 3. masquerading (system name off canonical path, or name/exe mismatch) 4. euid != uid privilege escalation 5. unknown child of a browser process 6. known miner/rootkit names or command patterns (xmrig , minerd , pool.monero , …) 7. pipe/download command lines (curl … sh , base64 -d , …) 8. thread count = 500 (not an allow-listed server/browser) 9. open file descriptors = 1000 (not an allow-listed server/browser) 10. spawn storm: more than 5 fresh instances of a name within 120 s tui widget the plugin registers a widget named samurai (equal to plugin id ). to see it, enable the plugin (it needs the renderwidgets capability) and add \"samurai\" to a layout: mcp integration samurai itself has no built-in mcp server . ai tool access is provided by the xtop-extension-mcp crate in the extensions repo, which the kernel launches with xtop mcp . it depends on this crate at compile time, drives the plugin with execute plugin(plugin id, action, params) and derives its 12 tools from the actions:: constants — see docs/mcp-protocol.md in that repo for the wire mapping. documentation - docs/architecture.md — lifecycle, action api and params syntax, capabilities, widget, mcp relationship. - docs/rules.md — the 10 heuristic rules with exact thresholds and trigger patterns. development license mit"},{"locale":"en","repo":"widgets","title":"xtop-widgets","label":"widgets","path":"README.md","slug":["widgets"],"text":"xtop-widgets base widget pack for the xtop tui. widgets are pure renderers: they draw inside a rect receiving only the read-only xtop-widget-api widgetstate contract — never kernel types. the kernel resolves (pack, name) at render time, so any pack can replace a built-in widget by name. layout (ux9.3: one crate per widget) widget names (default layouts) header , cpu , memory , storage , network , processes , disk io , summary , sensors , battery , gpu . layouts reference widgets by these names; the aggregator registers the widget crates under the same names. battery and gpu are not part of the default layouts — the kernel reaches them through fullscreen mode. summary (aggregate loads/gauges/process counts) and sensors (per-core temperatures; honest empty state without sensor data) are the ux8.4 additions for the dense layouts. selecting a pack in the kernel - global style.pack applies to every name without a per-widget override. - per-widget style.widgets. .pack wins over the global choice. - missing names in a chosen pack fall back to the base pack. - plugin widgets keep precedence over every pack. packs are integrated into the binary as cargo features (like plugins). the widget-blocks feature enables the ascii blocks pack (xtop-widget-blocks , a sibling of the per-widget crates so git consumers resolve its dependency on xtop-widget-core ). designing a widget 1. copy a widget crate (xtop-widget- / ) or use the kernel's widget scaffold — the crate is the designable/installable unit. 2. implement pub fn render(f, state, area) in src/lib.rs against xtop-widget-core (frame, roles, charts) and the contract. 3. register the crate under a widget name (the aggregator for built-ins; the pack table for community crates) — the kernel widget install command handles the wiring. 4. write tests next to the widget with the testkit double. glyph mapping (colors, borders, chart markers) comes from the canonical helpers in xtop widget api::glyph — see docs/authoring.md for the full contract walkthrough. documentation - docs/authoring.md — how a widget crate is built and registered: render , registry() , widgetrenderer /widgetregistration , the widgetstate view, the shared engine, the canonical glyph helpers, pack-selection semantics and the kernel install flow. - docs/widgets.md — reference of every registered widget name (base pack + fullscreen-only extras), the full per-widget layout-options schema (keys, defaults, examples, fallback rules), and how the blocks pack differs."},{"locale":"en","repo":"widgets","title":"community widget packs","label":"custom","path":"custom/README.md","slug":["widgets","custom"],"text":"community widget packs this directory is the reserved home for community packs. it is currently empty scaffolding: no pack has been contributed yet. community packs follow the same shape as the built-in ones (registry() over xtop-widget-api ), and the kernel engine integrates them at compile time — there is no runtime pack loading: 1. write the pack crate with a pub fn registry() - hashmap ; packs that live here are added as members of this workspace (under custom/ ). 2. the kernel depends on the crate behind an optional cargo feature and appends a pack { name, renderers } entry to its precedence list (xtop/src/ui/layout/engine.rs::packs() ). 3. users select the pack with style.pack (global) or style.widgets. .pack (per widget); missing names fall back to the base pack. see docs/authoring.md for the full authoring contract. runtime dynamic discovery of packs is deliberately deferred (workspace roadmap, §7)."},{"locale":"en","repo":"widgets","title":"authoring a widget crate","label":"docs · authoring","path":"docs/authoring.md","slug":["widgets","docs","authoring"],"text":"authoring a widget crate a widget is a crate : it renders against the read-only xtop-widget-api contract and exposes a single render entry point. the kernel shows a widget when a pack registers it by name; this repo ships two packs — the base pack xtop-widgets (this workspace root, an aggregator of the per-widget crates) and the ascii pack xtop-widget-blocks (a sibling of the per-widget crates). community packs live in custom/ (see its readme). repository layout the unit a user designs and installs: a crate the installable, designable unit of a widget is its crate folder xtop-widget- / . to design your own widget: 1. copy an existing widget crate (cp -r xtop-widget-processes xtop-widget-mycpu ) or scaffold one (the kernel's widget scaffold command emits the same shape). 2. rename the package in cargo.toml and keep the contract dependencies: xtop-widget-api + xtop-plugin-api (model types) + xtop-widget-core (shared engine) + ratatui ; version stays 0.1.0 (version policy: everything early, no bumps). 3. implement pub fn render(f: &mut frame, state: &dyn widgetstate, area: rect) in src/lib.rs — draw your view inside area , guarded for tiny rects, using xtop-widget-core for the frame, colors and charts. 4. register the crate under a widget name. built-in crates are registered by the aggregator (xtop-widgets::registry , src/lib.rs ); community crates are added as workspace members and wired into the kernel the same way packs are (see \"pack selection semantics\" below) — the kernel resolves (pack, name) at render time, so a designed crate replaces a built-in widget by name with no layout change. the widget's own tests live in its [cfg(test)] mod tests (next to the code) and use the shared test double from xtop-widget-core 's testkit feature — declared in the crate's [dev-dependencies] as xtop-widget-core = { features = [\"testkit\"] } . renderer contract a renderer is a plain function drawn on a ratatui [frame ] inside a [rect ], receiving the app state as &dyn widgetstate : widgetregistration is the canonical registration type and lives in xtop-widget-api only. renderers never see kernel types — every value they need comes through widgetstate . the registry() entry point a pack exposes one function returning its renderers by widget name (the names layouts and fullscreen mode use). the aggregator builds a hashmap from its widget crates: a pack registers only the names it draws. names it does not provide fall back to the base pack (see pack selection below), so xtop-widget-blocks registers cpu , memory , processes , network , storage , disk io , summary and sensors in its ascii look while every other name keeps the base rendering. what widgetstate offers widgetstate (xtop-widget-api, state.rs ) is the sampled, read-only view of the running app. its methods group as: - the sample — snapshot() - option : one snapshot per tick; none before the first tick, so renderers start with an early return. - theme — theme name() , theme fg() , theme bg() , theme palette() (16 rgb entries), alerts() (cpu/mem/disk thresholds). - glyph style, already resolved — charset(name) , borders(name) honor the global style plus any per-widget override; renderers never resolve config themselves. - history for charts — cpu history() (per-core vecdeque ), mem history() , net rx history() , net tx history() . each entry is (x, y) ; the x axis is the sample index/time. - process mapping (ux9.1) — uid to name(uid) resolves a numeric uid to the login name the kernel read from /etc/passwd (none = show the numeric uid) and process cpu history(pid) returns the recent per-process cpu samples (oldest → newest; empty = nothing drawn). - view/control state — search query() , process selected pid() , process sort label() , process sort desc() (direction for the sort marker: true = descending), layout name() , is searching() , fullscreen label() , sys info() (incl. the ux9.1 cpu model and package power w readouts), and process view() (the process rows, already filtered by the search query and sorted by the user's column; selection is pid-anchored). - display options (dr-ux1) — widget options() returns the options object of the layout node currently being rendered (none = default behavior) and logical core count() the host's logical processor count (used to normalize per-process cpu values to a whole-machine percentage). the recognized keys per widget are documented in docs/widgets.md (\"layout options per widget\"). the shared engine (xtop-widget-core ) widget crates do not re-implement palette roles, option parsing, glyph resolution, the chart engine or the temperature ramp — those live in xtop-widget-core : - util — formatting (bytes/rates/uptime/used-free), palette-role constants, gauge gradient , the temperature ramp (temp color ), resolved charset /resolved borders , draw frame (the standard widget frame prologue) and the painter direct-buffer canvas. - options — parse helpers for the layout options json plus the cpu chart/core-selection types. - chart — the per-cell colored chart engine (histories) and the one-row spark helpers (spark cells etc.) for per-row braille. - testkit (cargo feature, dev-only) — the widgetstate double + the offscreen terminal helpers for tests. canonical glyph mapping (colors, borders, chart markers) lives in the contract crate — packs must not re-implement it: (to color /border for /marker for are deliberately not re-exported at the crate root; the glyph import path above is the single canonical one.) - to color([u8; 3]) - color — converts a theme palette entry to a ratatui color (color::rgb verbatim). - border for(widgetborders) - set — the border frame for the resolved per-widget border choice. native → the standard single-line box-drawing frame (border::plain , ratatui's default look); rounded → border::rounded ; double → border::double ; plain and ascii → ascii border (the pure + - set). - marker for(chartcharset) - marker — the ratatui chart marker of the same name (braille , dot , block , halfblock , bar ). - ascii border — the canonical ascii frame, exported for cases that need the set itself. because the mapping is canonical, the same config draws identically in every pack. a pack that wants a genuinely different glyph for the same config diverges deliberately and documents why; it must not copy the mapping table. the blocks pack keeps its own look in the cpu gauge labels (an ascii fill) while chart markers honor state.charset(name) like the base pack. theme colors access pattern widgets paint from the theme arrays, converting entries through to color : gauge colors pick palette indices from state.alerts() thresholds via xtop widget core::util::gauge gradient . indices are semantic roles (dr-ux3): the role table lives in xtop-widget-core/src/util.rs (0 bg, 1 alert, 2 good, 3 warn, 4 read/download, 5 write/upload, 6 accent, 7 fg, 8 dim, 9–15 multi-series ramp) and docs/widgets.md restates it. packs may use the same numbers but must not invent undocumented slots. pack selection semantics (kernel contract) the kernel engine resolves (pack, name) at render time, as implemented in xtop/src/ui/layout/engine.rs : - packs are compiled into the kernel binary in a precedence-ordered list starting with the built-in default pack (xtop widgets::registry ); additional packs (e.g. blocks ) are compiled behind kernel cargo features. - for every widget name the engine asks the style configuration which pack provides it: a per-widget override (style.widgets. .pack ) wins over the global style.pack ; when no pack is chosen, the name resolves against the default pack. - if the chosen pack does not register that name (or the pack name is unknown), the engine falls back to the default pack. - plugin widgets (rendered over the plugin hoststate view) are checked first and take precedence over every pack. so a pack that wants to replace the built-in cpu registers a renderer under \"cpu\" , the user selects the pack, and any name the pack does not provide keeps its base rendering. installing a designed crate (ux9.2 kernel flow) the kernel's widget command mirrors the plugin commands: - widget scaffold emits a single-widget crate template (the shape of this repo's xtop-widget- crates: manifest with the four contract deps + src/lib.rs with render + a test using the testkit). - widget list shows the built-in crates and their registration names. - widget install adds the crate to the manifest and to the pack table, so xtop renders the widget under its registered name from the next launch — layouts select it exactly like a built-in widget."},{"locale":"en","repo":"widgets","title":"widget reference","label":"docs · widgets","path":"docs/widgets.md","slug":["widgets","docs","widgets"],"text":"widget reference every widget of the base pack is its own crate (xtop-widget- ), sharing the engine modules in xtop-widget-core (see readme.md for the layout). both packs draw inside a standard frame: title, borders::all , the border set resolved from the per-widget border configuration through the canonical xtop widget api::glyph::border for mapping, and the theme's fg/bg colors. with the default config (widgetborders::native ) every widget uses ratatui's standard single-line box-drawing frame (border::plain ) — the per-widget border look is a configuration choice, not a pack choice. glyph options (ux7.4) every layout node that names a widget may carry an options json object. two keys are recognized by every widget of both packs, with a fixed precedence — layout-node option style-config value contract default : key values effect --- --- --- charset \"braille\" \\ \"dot\" \\ \"block\" \\ \"half block\" \\ \"bar\" chart glyphs of the widget (contract enum serde names). when the node sets nothing, the style config (state.charset(widget) , global or per-widget style.widgets. ) wins; the config default is braille . borders \"native\" \\ \"rounded\" \\ \"double\" \\ \"plain\" \\ \"ascii\" the widget frame border set. when the node sets nothing, the style config (state.borders(widget) ) wins; the config default is native . the resolution runs once per widget render in the shared helper (xtop-widget-core/src/util.rs resolved charset /resolved borders , consumed by every widget crate and by the blocks pack). a malformed or unknown option value is ignored — the config value then applies. layout options per widget (dr-ux1 + ux7) the tables below are the complete list of recognized keys. rules: - unknown keys and malformed values are ignored (the documented default applies); a widget never breaks rendering on a bad option. - when an option filters items (cores, interfaces, mounts) and nothing matches, the widget falls back to all items so it never goes blank. - all colors come from the theme palette through the canonical xtop widget api::glyph::to color ; palette indices are semantic roles (dr-ux3). the role table lives at the top of xtop-widget-core/src/util.rs and mirrors the kernel's docs/customization.md role table: 0 bg, 1 alert, 2 good, 3 warn, 4 read/download (rx), 5 write/upload (tx), 6 accent, 7 fg, 8 dim/separators, 9–15 the multi-series ramp. cpu key values default effect --- --- --- --- chart \"average\" \\ \"per-core\" \"average\" history area: the machine-wide average, or the per-core view — each column takes the color of the shown core that peaks there, from the theme's series ramp (slots 9..15 cycling). cores \"all\" \\ subset spec \"all\" restrict the shown cores: \"0,2,4-7\" (ids/ranges, ascending). applies to the core rows and the per-core chart. show freq bool false append the per-core frequency (dim, right-aligned, 2.40ghz ) whenever a shown core reports one. the model carries cpuinfo.frequency (mhz); rows degrade gracefully when all frequencies are 0. show temp bool \\ \"auto\" \"auto\" per-core temperature cell (47° , rightmost, bold) and the per-core heat braille mark: \"auto\" shows them whenever the snapshot carries a per-core temperature (cpuinfo.temp c , linux); true forces them on, false hides them. temperatures are never fabricated : with no some temperature anywhere the cells stay hidden even under true . rendering (ux9.5): one normalized row per core — label cell (fixed width), percent cell (right-aligned, role-colored), optional frequency cell (dim), then the gradient bar filling the remainder of the column, and on the right the per-core heat mark (one braille/block glyph whose height is the temperature's share of the 80 °c anchor, colored by the heat ramp) plus the optional temperature cell (47° , ramp-colored, rightmost) when the row reports a temperature. two columns are used only when each column keeps its minimum row width (label + percent + bar ≥ 4 cells, plus the extras when enabled); narrower areas fall back to one column. when both extras would starve the bar, the temperature block yields first, then the frequency. rows that do not fit the height are clipped (the top cores win, column-major order). title (ux9.5): the cpu model name (sanitized, truncated) is appended in parentheses when the kernel reports one — cpu (amd ryzen 7 5800x …) — max 48°c — together with the existing max-temperature suffix; without a model the classic cpu (max: 47.5°c) stays byte-identical. long titles are cut to the area width with … . unified usage+temp+power row (ux9.5): when the grid leaves at least two rows, the first leftover row is a one-line composite gauge: usage 42% ▂▄▆▆ temp 47° ▅▆▅▆ power 38.4w ▂▃▄▅ — word tokens with real values interleaved with colored portions (the row is its own legend, so no separate key is needed). every portion is an honest share: usage of its 100% scale (gauge gradient), temp of the 80 °c hot anchor (heat ramp), power of a documented 200 w display ceiling (gauge gradient, warn at 50 %, alert at 90 % of the ceiling). segments appear only for data that is some : the temp segment needs at least one per-core temperature (and the show temp display preference), the power segment the kernel's package readout (systeminfo.package power w — rapl on linux). with only usage data the row is the classic average bar (avg: nn% + gradient bar). when no chartable history exists the numeric summary line trails the package power (avg: 42% pkg 38.4w ) so power is never hidden. the row never draws empty garbage: no segments are fabricated. history spec line (ux9.5): the anonymous bottom braille history row now self-describes — its divider carries the dim label history: cpu % when the width allows. below the rows the history area uses the chart engine (see \"chart engine\" below). when the engine cannot draw (area narrower than 12 columns, or fewer than two history samples) the leftover row shows a compact numeric summary instead of garbage: avg: nn% (with the shown/total core counts when a cores subset hides cores, and the pkg readout above). processes key values default effect --- --- --- --- cpu \"core\" \\ \"total\" \\ \"both\" \"core\" cpu column basis. core = fraction of one logical core, cells like 12.5% . total = cpu usage / logical core count() (the share of the whole machine's cpu), machine-share style: one decimal below 10 (0.7 ), an integer at/above 10 (34 ). both shows the two columns side by side (cpu per-core + cpu% total). the underlying per-process cpu usage never changes. columns.memory bool true show the mem column (the column also drops automatically when the area is too narrow). columns.user bool true show the user column (same auto-drop rule). columns.cmd bool true show the command column (same auto-drop rule). zebra bool true alternate dim backgrounds on odd rows. fixed layout (ux7.3 + ux9.4): pid (right-aligned, 7), name (left-aligned, flexible, truncated with … ), the cpu spark (see below), cpu% (right, 6; two columns under both ), mem (right, 10), user (left, 9) and command (left, flexible) — separated by dim │ . the name and command columns share the row's flexible width (name keeps at most 24 chars, command the rest); numeric columns are right-aligned; the header row is accent-bold; the sort marker (▼/▲ from process sort desc ) renders only in the sorted column header cell. columns drop right-to-left as the area narrows: command, then user, then mem, then the total-basis cpu column, then the spark; below the minimum only pid cpu% remains. rows are single logical lines — nothing wraps or collides. user names (ux9.4): the user column shows the resolved login name via state.uid to name(uid) (the kernel reads /etc/passwd ); when the kernel has no mapping (unknown or non-local account) the numeric uid is shown — names are a display mapping, never fabricated. command (ux9.4): each row shows the full command line — the kernel's argument vector (cmd full , joined) when populated, falling back to the single cmd , then the executable path, then ? — next to the short program name . cpu spark (ux9.4): a small per-row braille spark (4 cells, degrading to 2 on narrow rows) of the process's recent cpu samples (state.process cpu history(pid) , oldest → newest, one glyph per bucket, bucket peaks preserved). each cell is colored by usage through the heat ramp — idle processes paint small low glyphs, hammering ones full ⣿ cells in the alert color. braille charset paints braille glyphs (⣀⣰⣶⣿ ), the block charsets the 8-level block ramp (the charset option applies). an empty history (freshly seen pid) draws a dim · placeholder — samples are never fabricated. viewport scroll. the kernel's process view() returns the full sorted list (search-filtered); selection is anchored by pid. the widget derives the selected index from the pid's position in that list and renders a window around it — the selection is always visible, the window starts at row 0 while the selection is near the top, and jumps keep roughly half a screen of context. every process in the list is reachable with the existing up/down keys; a dim scrollbar (accent thumb) appears at the right edge when the list is longer than the area. no kernel change was needed (the kernel already moves the selection over the full sorted list). search (kernel pre-filter): the query substring in the name column is highlighted (accent background, bold). selection keeps the accent row style; kill-confirmation highlighting would need a \"pending kill\" flag on the widget contract, which does not exist — flagged as a kernel-side dependency. network key values default effect --- --- --- --- ifaces \"all\" \\ [\"eth0\", ...] \"all\" which interfaces the rows and the aggregate lines cover. rows are single logical lines (never wrapped). width tiers (inner width): = 60 per-interface rows with name, activity bar (scaled to the fastest visible interface, colored by its dominant direction), rx and tx rates in their direction roles, and dim cumulative totals; = 41 drops the totals; = 26 also drops the bar; below 26 the widget shows two aggregate lines (rx rate tot bytes / tx … ) over the selection, truncated with … . a dim +n more hint replaces the tail when the interface list overflows the box. the machine-wide rx/tx history chart (the contract only tracks one net history per direction, never per-interface) is drawn below the rows when the box is at least 16 columns wide and both histories carry ≥ 2 samples; it uses the chart engine with the fixed rx role (4) / tx role (5) coloring and consumes every leftover row. while the history is still empty the widget expands the interface rows to the full box and — when the list is short — appends aggregate live rx/tx lines so no dead rows sit between the content and the frame. storage key values default effect --- --- --- --- disks \"all\" \\ [\"/\", \"/boot\"] \"all\" which mounts to show (matched by exact mount point ). one row per mount, never wrapped. wide rows (ux9.6) are mount …bar… nn% used 50 gb · free 200 gb — the used and free amounts (free = total space − used space , honest: the kernel maps used space exactly that way); the free amount is colored by the used gauge ramp (plenty of free space reads good, a nearly full disk reads alert) and, on very wide rows, a free-share braille bar trails the detail (height = the current free share, color = the same headroom ramp — diskinfo carries no capacity history, so there is no time series to spark; the bar is an honest instantaneous braille readout). below the wide width the tiers degrade: mount nn% plus the bar (the label yields space to the bar), below 11 columns plain mount nn% text. when the box gives every mount at least two rows (height/n = 2, height = 4, width = 18 ) each mount renders as a meter block instead — mount line (label + right-aligned percent + used/free amounts), then the u used bar, plus the a available bar (diskinfo.available space , mirrored through the used ramp inverted) when a third row exists — so the per-disk bars scale with the box height. only capacity metrics live here — per-device i/o speeds are in disk io (which has no capacity fields in the model, so the free indicators belong to this widget only). memory key values default effect --- --- --- --- sections [\"memory\", \"available\", \"swap\"] all three which meter rows to draw (ram, then the available row, then swap). an empty/unknown list keeps all three. one meter row per section, never wrapped: label (bold), a right-aligned percent cell (role-colored), then the gradient bar. wide rows show the used and free amounts (ux9.6): ram 50% ██████ used 8.0 gb · free 7.0 gb ⣿⣶⣰ — the free amount (kernel memoryinfo.free for ram, swap.free for swap) is colored through the headroom ramp (good while there is plenty of headroom, warn/alert as the machine tightens), and the ram row trails a braille spark of the free share over time derived honestly from the kernel's used-percent history (free = 100 − used per sample; the kernel only tracks the used percent). each spark cell is colored by the scarcity of the free share, so a cramping machine paints low red glyphs. the available (avl ) row renders only when the snapshot can derive it (memoryinfo.available , nonzero total) — its bar fills with the available share, colored by the same gauge ramp inverted (gauge(100 − avail%) ), so it turns alert-red when the machine runs out of headroom; swap turns alert-red past the memory threshold. below 10 inner columns the whole widget collapses to one summary line (ram 50% avl 50% swp 13% ). the ram history chart is drawn below the rows when the area is at least 14 columns wide and history has ≥ 2 samples; the chart consumes every leftover row (see \"chart engine\") — the chart is the ram history, never per-section. disk io one single-line row per device, never wrapped: wide rows show name , read and write rates (direction roles 4/5) plus small rate bars scaled to the fastest device in the view; below the compact width rows fall back to name r rate w rate with space-less units on very narrow boxes, truncated with … as a last resort. no disk i/o data when the snapshot carries no device. when the box is at least 16 columns wide and the contract tracks the machine-wide aggregate disk histories (disk read history() / disk write history() , both ≥ 2 samples), the rows reserve two text rows and the leftover rows host the dual read/write chart — reads role 4, writes role 5, y-axis the visible peak of both series (same geometry as the network chart; rows are per-device while the histories are machine-wide aggregates, exactly like network rows vs its aggregate history). a dim +n more hint replaces the device tail that does not fit. with no histories the rows simply expand to the whole box (text-only fallback). diskioinfo exposes rates and byte counters only — no capacity — so the used/free indicators of ux9.6 live in storage (per-mount diskinfo ). summary compact always-filled panel of aggregate numbers; no widget-specific options (glyph keys only). content rows (each a single logical line, truncated with … , never wrapped): 1. load 2.81 2.30 2.42 — values colored by their share of the logical cores (logical core count() ), same gauge rule as the header. 2. cpu gauge row — machine-average usage: percent cell, role-colored gradient bar, dim core count on wide rows. 3. mem gauge row — used percent + bar; used/total detail on wide rows. 4. procs 264 run 2 sleep 211 … — the snapshot's process count plus per-state counts (buckets by case-insensitive substring of processinfo.state : run/sleep/zombie/idle/stop, anything else folds into \"other\"); when every state string is empty only the total shows. counts never fabricate a bucket. 5. uptime 0d 7h 27m 9s . boxes taller than the five content rows draw the load-average history chart (load history() ) into the leftover rows — auto-scaled to the visible window peak (a trend view, good-role colored, the same scale and color as the inline sparkline); with too little height for the chart (or no history) the load row trails an inline block-ramp sparkline when width allows. at height 4 the widget shows the top content rows and still fills the box. sensors per-core temperature panel; no widget-specific options (glyph keys only). when any core exposes cpuinfo.temp c (linux), the widget renders a column-major grid of cpu0 47° cells — every value colored by the temperature ramp (interpolated between the theme's good/warn/alert roles, see \"temperature ramp\") with a /gradient bar scaled to the 80 °c anchor on single-column rows; the title carries the max temperature (snapshot().cpu temp , falling back to the max per-core value). rows overflowing the height are clipped silently (like the cpu grid). when no temperature data exists anywhere (temp c is none on every core — macos, windows, sensor-less hosts), the box renders the honest line no temperature data plus the load averages (role-colored as in the header) — never empty, never fabricated. leftover rows below the grid (or the empty-state lines) host the load-average history chart when the kernel tracks it and the width allows. header , battery , gpu no widget-specific options are recognized (unknown keys are ignored; defaults apply). the glyph keys charset /borders from the table above apply to every widget, including these. chart engine (ux7.1 + ux8.4) xtop-widget-core/src/chart.rs implements the per-cell colored plotter used by the cpu/memory/network/disk io/summary/sensors history areas and the one-row spark helpers (spark cells /spark glyph /spark levels ) used by the per-row braille sparks (processes cpu sparks, memory free sparks, storage free braille bars). model: - a series is a list of (x, y) samples (x-ordered, evenly spaced — the contract histories are). columns sample a piecewise-linear interpolant at their center index; samples sharing a column also contribute their maximum (peak preserving). - columns fill from the zero baseline at vertical resolution: braille = 4 sub-rows per text row (dots ⣀ ⣰ ⣶ ⣿ for 1–4 lit sub-rows), block /half block = 8 sub-rows per text row via the block ramp ▁▂▃▄▅▆▇█ . a chart area of height h renders h text rows — the engine never collapses: the widgets hand it the whole leftover rect, so a 100×34-style memory/network/disk io box with five leftover rows draws a five-row braille plot (a 25% value lights the bottom row(s) fully plus a partial top cell). a height-1 plot is a sparkline and always uses the 8-level block ramp — one text row cannot host more than 4 braille levels, which is exactly the cramped one-line braille this engine replaces. - one-row mini sparks are different: discrete samples map to discrete cells, so braille charsets paint braille glyphs (⣀⣰⣶⣿ — one braille cell already carries 4 sub-levels) and block charsets the 8-level ramp. per-cell colors come from a caller-supplied role mapping (heat rules for usage, scarcity rules for free shares). series shorter than the cell count paint their samples only (the remaining cells stay empty). - dot /bar charsets keep the classic ratatui chart path (marker via the canonical marker for ), including its axis labels — the engine never draws them. color rule (deterministic, per cell): - fixed-role series (cpu per-core, network rx/tx, disk io r/w): the cell takes the role color of the series whose top-most lit sub-row is highest; ties resolve to the first listed series (network passes rx before tx, so ties read as rx; disk io passes reads before writes). - heat (single average/ram/load series): the top-most lit sub-row maps to gauge gradient(level/total subrows 100, alert at) on the same role slots the gauges use — cells under 50% of the axis are good , 50% to the alert threshold warn , at/over it alert . cpu uses alerts().cpu high , ram alerts().mem high ; summary/sensors load charts use the logical-core count as the axis and alerts().cpu high as the threshold (the same semantics as the header load coloring); network/disk charts are always role-colored. history areas show a dim ─ divider when at least three rows are available and the plot below is at least two rows tall (the cpu divider carries the dim history: cpu % label when the width allows — ux9.5). chart minimum widths: cpu 12, memory 14, network 16, disk io 16, summary/sensors load 12 columns — narrower areas fall back to the numeric summary described per widget. temperature ramp (ux8.4) temperature ui (the cpu show temp cell + per-core heat marks, the sensors widget grid, the cpu unified-bar temp segment) is colored by a ramp derived from the theme's own gauge roles — no new palette slot is invented , so the role table stays consistent and low-contrast role colors lifted by the kernel at theme load propagate into the ramp. endpoints (documented in xtop-widget-core/src/util.rs ): - at/under 45 °c the ramp is the good role color (slot 2), - at 60 °c it passes the warn role color (slot 3), - at/over 80 °c it is the alert role color (slot 1), with the intermediate colors channel-interpolated between the role colors (util::temp color ). base pack (xtop-widgets ) registry() registers 11 names: header , cpu , memory , storage , network , processes , disk io , battery , gpu , summary , sensors . header , cpu , memory , storage , network , processes , disk io , summary and sensors are the names default kernel layouts reference; battery and gpu are not part of any default layout and are reachable through the kernel's fullscreen mode (fullscreenwidget::battery/gpu map to the names \"battery\" /\"gpu\" in the kernel's ui/screen.rs ). all renderers return early when the snapshot is none (pre-first-tick), so every widget is safe on an empty state. name draws data from options --- --- --- --- header one summary line (area.width = 80 ) or two: color-coded segments — host (bold fg) \\ theme (accent) \\ layout (ramp slot 9) \\ uptime \\ load averages colored by their share of the logical cores; appends [full: …] and [/] search markers when active. block belongs to a paragraph . sys info().hostname , layout name() , snapshot().uptime /load avg , fullscreen label() , is searching() , logical core count() glyph keys only cpu one row per core (label/percent/freq/heat-mark/temp/bar, see above), 2 columns when wide enough; the unified usage+temp+power row and the labeled history chart below when the area allows. title: model + max temperature. snapshot().cpus (incl. temp c ), sys info().cpu model /package power w , cpu history() , alerts().cpu high chart , cores , show freq , show temp , glyph keys memory ram/avl/swp meter rows (used/free amounts + ram free-share spark on wide rows) plus the ram history chart when the area is wide and tall enough; title gains a ⚠ marker over the memory threshold. snapshot().memory /swap , mem history() , alerts().mem high sections , glyph keys storage one single-line row per mounted disk (label, percent, gradient bar; used/free amounts on wide rows + free braille bar when very wide); tall boxes render three-line meter blocks per disk (mount + u used bar + a available bar) so per-disk bars scale with the box height. snapshot().disks , alerts().disk high disks , glyph keys network per-interface single-line rate rows (or aggregate rx/tx lines when narrow); rx/tx history chart below when wide and tall enough (aggregate live lines fill the gap while the history is empty). snapshot().networks , net rx history() , net tx history() ifaces , glyph keys processes fixed-column table with pid / name / cpu-spark / cpu% / mem / user / command, dim │ separators, accent header with the sort marker on the sorted column only, zebra rows, selection row, viewport scroll window with a right-edge scrollbar; user names resolved through the kernel uid map, commands from the full argv, per-process cpu braille sparks. process view() , uid to name() , process cpu history() , process sort label() , process sort desc() , search query() cpu , columns , zebra , glyph keys disk io one single-line row per device: name, r/w rates in the direction roles (wide rows add rate bars); dual read/write history chart in the leftover rows when the contract tracks the aggregate disk histories; \"no disk i/o data\" when empty. snapshot().disk io , disk read history() , disk write history() glyph keys only battery one gauge per battery: name, %, state, minutes to full/empty when applicable; \"no battery data available\" when empty. snapshot().batteries glyph keys only gpu one gauge per gpu: name, %, memory used/total, temperature; \"no gpu data available\" when empty. snapshot().gpus glyph keys only summary load averages (role-colored by core share) + cpu/mem gauges + process counts + uptime; load-average history chart in leftover rows (inline sparkline on the load row at small heights). snapshot().load avg /uptime /processes /memory /cpus , load history() , logical core count() glyph keys only sensors per-core temperature grid colored by the temperature ramp (title carries the max); \"no temperature data\" + load averages when no temp c exists anywhere; load chart fills leftover rows. snapshot().cpus (temp c ) /load avg , cpu temp , load history() glyph keys only histories are drawn by the chart engine described above (braille/block/ half-block charsets, per-cell colors), with the classic ratatui chart path kept for the dot /bar charsets. blocks pack (xtop-widget-blocks ) registers cpu , memory , processes , network , storage , disk io , summary and sensors ; every other name falls back to the base pack (kernel contract, see docs/authoring.md ). enable with the kernel's widget-blocks feature and select the pack per widget or globally. the blocks pack consumes the same shared engine as the widget crates (xtop-widget-core ): palette roles, option parsers, glyph resolution, the chart engine and the spark helpers are canonical — the pack keeps its ascii identity ( fills) and its row/table composition in its own code. how it differs from the base pack: - ascii identity: bars and fills are characters instead of block glyphs; the frame and chart glyphs still follow the resolved charset and borders exactly like the base pack. per-row braille sparks and heat marks use the same glyph helpers as the base pack (charset-resolved). - cpu — titled \"cpu blocks\" (+ the model and max temp): one single-line row per core (label, percent, optional frequency, per-core heat mark, temperature and bar), honoring cores , show freq , show temp ; when the grid leaves a row, the unified usage+temp+power line ( portions, ux9.5 parity) is drawn below it. - memory — titled \"memory (blocks)\": ram/avl/swp meter rows with bars, used/free amounts on wide rows and the ram free-share braille spark, plus the ram history chart through the engine (braille default; block /half block glyph sets reachable per widget), honoring sections . - processes — titled \"processes (blocks)\": an ascii -separated table (pid / name / cpu-spark / cpu… / mem / user / command) with the base pack's column-drop policy, resolved user names, full command lines, per-process cpu braille sparks, sort marker on the sorted column, zebra rows and the same viewport scroll window; honoring cpu , columns , zebra . - network — titled \"network (blocks)\": per-interface -bar rows (or aggregate lines when narrow) with the same width tiers and the dual rx/tx engine chart in the leftover rows, honoring ifaces . - storage — titled \"storage (blocks)\": one -fill row per mount with used/free amounts on wide rows (+ the free-share braille bar when very wide), honoring disks and the disk alert threshold; tall boxes render the three-line per-disk meter blocks (mount + u + a bars). - disk io — titled \"disk i/o (blocks)\": per-device r/w lines with bars and compact units on narrow boxes plus the dual read/write engine chart in the leftover rows when the aggregate histories exist. - summary — titled \"summary (blocks)\": same content rows with gauge bars and the load-average engine chart in the leftover rows. - sensors — titled \"sensors (blocks)\": per-core temperature grid with heat bars (same ramp colors) and the same honest empty state. behavioral notes - all widgets are defensive about small areas: chart sections only render when the area is wide/tall enough, text rows are single logical lines (truncated with … where a tier cannot fit them), and list widgets stop adding rows when the area runs out. the smoke tests render every registered widget of both packs at 100x34, 100x30, 80x24, 60x20, 40x15 and 20x10 with empty and sampled state, assert every row stays inside the frame (no wrap detection) and that charts produce multi-row glyphs when the plot is at least two rows tall (see xtop-widgets src/lib.rs , mod tests , and xtop-widget-blocks/src/lib.rs , mod tests ; both test suites share the xtop-widget-core testkit — the widgetstate double behind the testkit cargo feature). - widgets never mutate state and never touch kernel types; the rendering inputs are the widgetstate view plus the per-tick systemsnapshot . - cpu history data (cpu history() ) is per-core and index-aligned with snapshot().cpus ; the per-core chart reads it through the shown cores' cpu id . network histories are machine-wide rates (bytes/s), so the network chart's y-axis is the visible maximum of both series. the same shape applies to the aggregate disk histories (disk read history() / disk write history() , bytes/s) and load history() (1-minute load average) — all three are the additive ux8.3 widgetstate surface with empty defaults, consumed only when non-empty. ux9.4 adds the uid→name map (uid to name , default none ) and the bounded per-process cpu history (process cpu history , default empty) to that additive surface. - every color an amount, segment or spark paints is theme-driven through the documented role/ramp rules; nothing new is invented when the data is none — widgets show the honest fallback (numeric uid, ? , · , hidden segment, classic bar) instead."},{"locale":"en","repo":"xtop","title":"changelog","label":"changelog","path":"CHANGELOG.md","slug":["xtop","CHANGELOG"],"text":"changelog [0.1.0] - 2026-09-04 version policy note: the ecosystem stays early — everything is 0.1.0. the entry below consolidates the post-refactor kernel work of this cycle (theme format v2 + contrast engine, preset extras, sort direction, role-colored chrome, ecosystem externalization, data surface, density and legibility) into one cumulative list. kernel data surface - theme format v2: the 12 shipped theme files carry explicit background /foreground keys from the canonical owner palettes (legacy files fall back to slot0/slot7); seed bumped 4 → 5. - contrast engine (wcag luminance): at load, text roles (fg, dim, accent, zebra, series marks) are normalized against the theme background with hue-preserving lifts when below floor; regression pinned (paris fg is never palette[7] when it equals the background). - data surface: cpuinfo.temp c (linux coretemp probe fills per-core temps where unambiguous; macos/windows/fallback stubs return none) and bounded widgetstate histories for aggregate disk read/write rates and load average. - cpuinfo.frequency , .governor , .name (provider.rs) and other collected metrics (state, run time, threads, open files, disk bytes, exe path, cmd, cmd full, parent pid) keep flowing through the snapshot for widgets and plugins. - dense widgets: boxes scale charts to full height; cpu per-core temp view (show temp ); memory available row; network/disk io multi-row dual braille charts; new summary and sensors widgets (both packs); detail presets rebuilt as full-monitor pages with zero orphan rows at common sizes (tiling rules documented). presets, chrome and layout extras - the three preset extras are named detail (file names detail dashboard , detail network , detail processes ; display names detail dashboard , detail network , detail processes ). help overlay, docs, tests and comments updated; seed version bumped 3 → 4 so existing installs re-seed the renamed layout templates (old seeded files are left untouched, per the never-clobber policy). - layout presets as extras (dr-ux6): the kernel embeds the three reference preset layouts from xtop-cli/layouts after the seven mode-bound layouts; the layout cycling key reaches them in file order and wraps around, the command palette lists them by name, and first-run asset seeding copies them as editable templates (seed version 2 → 3). - the ux-cycle design document is reworked into docs/design.md : a neutral statement of xtop's own design rules (theme roles, glyph sets, per-widget display options, minimum-width fallbacks, chart color rules, deferred features), with the recognized per-widget option keys cross-referenced to the widgets repo docs. all links updated. - kernel chrome polish (ux5 carry-over, gaps closed): the search overlay now draws its border with the configured style.borders glyph set like the help and palette popups; titles keep the accent role, separators the dim role, and no hardcoded double-line borders remain in kernel chrome. - sort-direction cycle and per-widget display options are carried over unchanged from the same cycle; widget-side rendering changes (visual language v2) land in the widgets repo changelog notes. process sort, command palette, role colors - process sort direction: the sort key (s , configurable) alternates between flipping the order on the current column (boot default: cpu% descending, unchanged) and advancing to the next column (cpu% → mem → pid → name), which starts descending — one press flips ▲ /▼ , the next press moves on. the order actually rendered honors the direction, and process sort desc() (new additive widgetstate method, default false ) lets the processes widget draw the column marker. - palette \"sort\" entry shows the current column with its direction marker. - role-colored kernel chrome (dr-ux3): new theme role accessors (accent() = slot 6, dim() = slot 8, documented in docs/colors.md ); help overlay and search/palette popups use accent titles/borders, dim separators and the configured style.borders glyph set (help no longer hardcodes double-line borders); minimal-view gauges keep their role colors with the roles documented. - help overlay text updated: sort-direction behavior and the preset layouts are listed; the \"layouts\" section reflects modes → presets → user layouts. ecosystem externalization (monocrate cycle complete) - the kernel is now a thin host over the externalized ecosystem. it consumes the contract crates from xtop-cli/api (xtop-plugin-api , xtop-widget-api , xtop-extension-api ), the widget packs from xtop-cli/widgets , layouts from xtop-cli/layouts , the samurai plugin from xtop-cli/plugins and the mcp extension from xtop-cli/extensions as floating git dependencies with optional feature flags. - deleted the dead uncompiled src/commands/plugins dir tmp/ leftover from the pre-monocrate layout. (path spelled in scripts/audit.sh as the absence guard.) - rust-version = \"1.87\" declared. local-only ci - the github actions ci workflow (.github/workflows/ci.yml ) has been removed: gating is local only (scripts/ci.sh , scripts/audit.sh ) and will not be re-enabled until the release pipeline is 100% ready. contract consolidation - config::alertthresholds removed: the persisted config now uses xtop plugin api::alertthresholds directly (identical json keys cpu high /mem high /disk high ; defaults 90/90/90 constructed at the config layer). hand-marshalling bridge code deleted from plugins/host.rs and state/widget state.rs . - plugin widget registrations use xtop plugin api::pluginwidget ; the duplicated pluginwidgetfn alias in ui/layout/engine.rs is gone. - docker leftovers removed (dockers: vec![] assignment, docker info composite forwarding) — the api model no longer carries docker data. - plugin context reads are capability-gated and result -typed upstream; consumers adapted. themes - miami is now embedded and seeded like the other 11 themes (12 jsonc theme files ship, all in default themes ); seed version bumped to \"2\" so existing installs receive the new template. - theme docs/counts corrected everywhere (12 themes; x compiled in as the startup fallback). - theme::themes dir() routes through the platform-aware config::config dir() , so themes live next to config.json and layouts on macos/windows too. dependency refresh - ratatui 0.29 - 0.30.2 (layout::vertical /layout::horizontal builder api); crossterm aligned with ratatui's backend (single crossterm 0.29 in the graph via ratatui-crossterm ). new features - unknown widget names in the active layout produce a one-time stderr warning per name (xtop: layout ' ' references unknown widget ' ' ). - optional effects feature (off by default) wires the built-in xtop-effect-fade through the effect config key (\"fade\" activates the 500 ms fade-in; absent/unknown disables). builds without the feature carry zero extra dependencies. docs and release plumbing - docs/ refreshed: configuration keys, plugin architecture (plugins as separate crates, plugin list install scaffold real behavior, mcp extension), multi-repo rfc status, feature/theme counts; colors.md moved under docs/ . - docs/colors.md palette role table corrected to actual code usage (slots 4/5 are the rx/tx read/write pair, not \"storage/tx\"); per-widget display options documented in docs/customization.md ; layout counts/cycles updated across the docs. - install.sh version synced to the crate version; openssl build-dep claims removed (the crate has no openssl dependency); install.ps1 placeholder comment removed. - kernel roadmap.md synced with the implemented state (phases 4-6 and the r2/r3 refactor items); scripts/audit.sh extended with dead-dir and theme-seed gates. - crate version pinned to 0.1.0 (installer version synced) per the ecosystem version policy: stay early, no more bumps. earlier history [0.0.1] - 2026-06-18 config: persistencia de layouts personalizados - config ahora tiene campo layout name que almacena el nombre del layout seleccionado (soporta layouts mas alla de los 7 built-in layoutmode ) - en configuraciones existentes, layout name es opcional ( [serde(default)] ) y se usa como respaldo layout mode - save config guarda el nombre del layout actual por su indice en layout defs , permitiendo restaurar layouts personalizados al reiniciar - appstate::new prioriza config.layout name si no es vacio, con fallback a layout index from mode - config se guarda automaticamente al seleccionar tema o layout desde la paleta de comandos paleta de comandos: soporte macos - agregada ruta directa para ctrl+p en el bucle de eventos, independiente del sistema de keybindings (soluciona problemas donde crossterm reporta la tecla con distinto casing o el keybinding no se resuelve) - key event to str aplica to ascii lowercase() al caracter cuando ctrl esta activo, normalizando \"ctrl+p\" a \"ctrl+p\" - agregado \"ctrl+p\" como variante alternativa en el keybinding por defecto - debug output de teclas en compilaciones debug (cargo build sin --release ) para diagnosticar problemas: [key] 'ctrl+p' plugins: seguridad y arquitectura - plugincontext ahora verifica capabilities antes de ejecutar acciones sensibles: kill process , set alert thresholds , set theme by name , set layout by name , set update interval requieren killprocesses o modifyconfig segun corresponda - plugincapability::custom migrado de &'static str a string para flexibilidad - pluginmanifest usa string en vez de &'static str , permitiendo plugins que generen metadata dinamicamente - [non exhaustive] agregado a plugincapability para evolucion segura del enum pluginmanager: robustez - with plugin manager mut() reemplaza el patron inseguro take() + some() que podia perder el manager si una ruta de error no lo restauraba. todos los callers migrados: on tick , manejo de teclas, shutdown, y mcp server - register() ahora devuelve result en vez de result , consistente con el resto del sistema - metodo build context() elimina la duplicacion de construccion de plugincontext en 5 metodos distintos - data dir del plugin ahora apunta al directorio del plugin (plugins/ / ), no a plugins/ /config.json capabilities: validacion real - collect widgets() solo acepta widgets si el plugin declara renderwidgets - collect data providers() solo acepta providers si el plugin declara readsysteminfo - plugincontext inyecta las capabilities declaradas y verifica en cada metodo sensible mcp server - migrado de take() /some() a with plugin manager mut() para seguridad - eliminado doble tick all() innecesario en el handler de herramientas sistema de providers - systemdataprovider ahora tiene metodo add extras() con default no-op, eliminando la necesidad de downcast mut:: () - kill process() verifica que el uid del proceso coincida con el usuario actual antes de enviar sigterm - limite de procesos configurable via sysinfoprovider::max processes (default 200) - eliminados noopbatteryprovider , noopgpuprovider , noopdockerprovider — codigo muerto no utilizado sentinel: calidad de datos - migracion completa de construccion manual de json (format! con strings escapados) a serde json::json!() — elimina riesgo de inyeccion json y corrupcion por caracteres especiales tui - layoutconstraint::fill mapeado correctamente a ratatui::constraint::fill(1) en vez de min(0) infraestructura - config dir() centralizada en xtop core::infrastructure::config::config dir() — eliminada duplicacion en 5 modulos - ci workflow creado en .github/workflows/ci.yml : cargo fmt --check , cargo clippy -- -d warnings , cargo test [0.2.0] - 2026-06-03 refactorizacion total del proyecto - migrado a workspace multi-crate: xtop-core , xtop-tui , xtop-cli - eliminado el monolito src/ — ahora cada capa vive en su propio crate - eliminadas 5 dependencias muertas: serde , serde json , clap , chrono , tokio (se redujo de 84 a 55 crates) - eliminado codigo muerto: inputmode , show help , swap history , process table state , graph colors() - eliminados todos los [allow(dead code)] nueva arquitectura hexagonal - capa de dominio (xtop-core/domain/ ): modelos de datos puros + trait systemdataprovider - capa de aplicacion (xtop-core/application/ ): appstate , metricshistory (con vecdeque ), layoutmode , effectivelayout - capa de infraestructura (xtop-core/infrastructure/ ): sysinfoprovider , theme loader , config , providers stub - capa de presentacion (xtop-tui/ ): terminal, render widgets separados, format helpers - binary (xtop-cli/ ): entry point con inyeccion de dependencias layout responsive - detect effective layout(width, height, mode) adapta el layout automaticamente: - dashboard ( 100x30): layout completo 2-columnas - compact ( 80x24): mas compacto - vertical ( 90% : color cambia a rojo - memoria 90% : color rojo + icono de advertencia en el titulo - thresholds configurables en alertthresholds (cpu high, mem high, disk high) mejoras de codigo - vec + remove(0) reemplazado por vecdeque con pop front() (o(1)) - helper format bytes() elimina repeticion de 1024.0 / 1024.0 / 1024.0 - helper format uptime() para formato legible de tiempo activo - metricshistory::set max points() para configurar puntos del historico configuracion persistente - /.config/xtop/config.json : guarda tema, layout, intervalo, history points, alerts - /.config/xtop/themes/ .json : temas personalizados por el usuario - guardado automatico al salir con q - temas built-in (13) se fusionan con temas personalizados tests - 39 tests unitarios (de 0): layout detection, history, themes, format helpers, config - ci workflow .github/workflows/ci.yml : check, fmt, clippy, test, build keybindings completos tecla accion ------- -------- q salir (guarda config) ? ayuda t / t siguiente/anterior tema l siguiente layout f / f toggle fullscreen / ciclar widget / buscar procesos esc cancelar busqueda / cerrar ayuda"},{"locale":"en","repo":"xtop","title":"contributing to xtop","label":"contributing","path":"CONTRIBUTING.md","slug":["xtop","CONTRIBUTING"],"text":"contributing to xtop first off, thank you for considering contributing to xtop ! it's people like you that make this tool better. how can i contribute? reporting bugs - ensure the bug was not already reported by searching on github under issues. - if you're unable to find an open issue addressing the problem, open a new one. be sure to include a title and clear description, as much relevant information as possible, and a code sample or an executable test case demonstrating the expected behavior that is not occurring. suggesting enhancements - open a new issue with the enhancement label. - provide a clear and detailed explanation of the feature you want and why it's important. pull requests 1. fork the repository and create your branch from main . 2. ensure you have installed the necessary dependencies (rust & cargo). 3. make your changes and test them locally using cargo run . 4. run cargo fmt and cargo clippy to ensure code quality and formatting. 5. create a pull request. make sure the pr description clearly describes the problem and solution. include the relevant issue number if applicable. development setup community if you have questions or need help, feel free to reach out via github issues or discussions (if enabled)."},{"locale":"en","repo":"xtop","title":"xtop","label":"xtop","path":"README.md","slug":["xtop"],"text":"xtop rust license platform ratatui a cross-platform tui system monitor written in rust. uses ratatui for the terminal interface and sysinfo for real-time system metrics. contents features previews quick install quick start documentation contributing license x features cpu usage per core with temperature sensing ram and swap monitoring with historical chart network rx/tx tracking per interface storage and disk i/o visualization process list with live search, cpu/memory/pid/name sorting and ▲/▼ direction toggling battery monitoring (real probes on linux, macos and windows) gpu monitoring (nvidia via nvidia-smi on any platform; amd/intel via linux sysfs probes) 12 color themes with custom theme support via jsonc 7 built-in layout modes + 3 detail preset layouts, with custom layout support via jsonc full-screen mode for any widget configurable alert thresholds persistent configuration see docs/features.md for a detailed feature breakdown. previews view more previews quick install linux curl -fssl https://raw.githubusercontent.com/xtop-cli/xtop/main/install.sh bash windows (powershell) irm https://raw.githubusercontent.com/xtop-cli/xtop/main/install.ps1 iex macos and other platforms install.sh targets linux package managers. on macos (or any other platform) install with cargo: cargo install --git https://github.com/xtop-cli/xtop --all-features or build from source (see below). build from source git clone https://github.com/xtop-cli/xtop.git cd xtop cargo run --release for detailed installation instructions, see docs/installation.md . quick start run xtop after installation. key controls: key action q quit (saves config) ? toggle help overlay t / t next / previous theme l next layout mode f / f toggle / cycle full-screen / search processes for full usage details, see docs/usage.md . documentation features -- detailed feature breakdown installation -- full install and uninstall guide usage -- keybindings, modules, help overlay configuration -- config file and settings reference customization -- custom themes and layouts colors -- palette reference for the 12 shipped themes plugins -- plugin architecture and authoring multi-repo architecture -- ecosystem rfc and layout roadmap changelog contributing license x dev & github profile & xscriptor"},{"locale":"en","repo":"xtop","title":"xtop roadmap","label":"roadmap","path":"ROADMAP.md","slug":["xtop","ROADMAP"],"text":"xtop roadmap this document outlines the development roadmap for xtop , a modern, cross-platform tui system monitor. it is synced automatically with github issues. phase 1: core features - [x] basic tui structure using ratatui and crossterm ( 1) - [x] system information collection using sysinfo ( 2) - [x] cpu usage per core and maximum temperature sensing ( 3) - [x] memory and swap monitoring with historical graphing ( 4) - [x] network upload and download tracking ( 5) - [x] storage usage visualization ( 6) - [x] top 50 process list sorted by cpu usage ( 7) - [x] cross-platform compatibility (macos, linux, windows) ( 8) phase 2: interface & theming - [x] dynamic layout manager supporting multiple modes (dashboard, vertical, process focus) ( 9) - [x] implement 13 built-in color schemes (x , madrid , tokio , etc.) ( 10) (12 theme files ship today, all embedded and seeded; the docs count 12. see docs/colors.md for the palette reference.) - [x] instant theme and layout cycling at runtime ( 11) - [x] responsive design for narrow terminals ( 12) phase 3: deployment & distribution - [x] automated install/uninstall scripts for linux and macos (install.sh , uninstall.sh ) ( 13) - [x] automated install/uninstall scripts for windows (install.ps1 , uninstall.ps1 ) ( 14) - [x] quick curl/wget installation commands ( 15) - [ ] ci/cd pipeline for automated multi-platform binary releases ( 16) - [ ] distribution packages (aur, homebrew, winget, apt) ( 17) phase 4: configuration & customization - [x] persistent configuration file support (save theme and layout preferences) ( 18) - [x] custom user theme creation via configuration ( 19) - [x] configurable update intervals for system metrics ( 20) - [x] customizable keybindings ( 21) phase 5: advanced monitoring features - [x] disk i/o read/write speed tracking ( 22) - [ ] granular network interface selection ( 23) — per-interface rx/tx data is ( 39) shown, but picking which interface the kernel reports/charts is not configurable yet. - [ ] gpu usage, temperature, and vram monitoring (nvidia/amd) ( 24) — ( 40) nvidia via nvidia-smi on any platform (linux/macos/windows); amd/intel stays linux-only partial (/sys/class/drm); macos/windows keep the honest empty state for amd/intel (no public utilization api). - [ ] battery status monitoring ( 25) — linux probes real (/sys/class/ ( 41) power supply); macos real (pmset); windows real aggregate battery (getsystempowerstatus; per-battery via setupapi pending); fallback platforms stay empty. - [ ] docker container resource usage integration ( 26) — docker support was ( 42) removed from the shared data model (api m1.4: nothing consumed it), so this item is parked until a real consumer appears. phase 6: interactive process management - [x] interactive process termination (send kill signals) ( 27) - [ ] search, filter, and highlight processes by name ( 28) — search and ( 43) filtering exist; highlighting matches inside the process list is pending. - [ ] tree view for process hierarchy ( 29) - [ ] sorting processes by memory, pid, or user ( 30) — cpu/memory/pid/name ( 44) sorting is implemented; no user column yet. phase 7: x integration - [ ] x integration ( 31) - [ ] create xp package ( 32) - [ ] add to x repositories ( 33) phase 8: kernel refactor baseline done: single-crate kernel (src/ by areas: config, theme, layout, state, plugins, providers, ui, commands); plugin and extension hosts over the api contracts; samurai and the mcp extension live in their own repos. only pending refactor work is listed here (git logistics are handled outside this roadmap). r2 - code quality pass - [x] module doc comments audit across src/ ( 40) — the seven top-level ( 45) modules declared in src/main.rs (commands, config, plugins, providers, state, theme, ui) all carry concise //! docs describing their area. - [x] cfg(target os) only inside platform/ trees ( 41) — enforced by ( 46) scripts/audit.sh (0 occurrences outside platform/). - [ ] wildcard re-export and pub hygiene review ( 42) — 22 wildcard ( 47) pub use ...:: re-exports remain (audit.sh threshold: 30); review still open. - [x] split commands/plugins.rs into list/install/scaffold modules ( 43) - [x] key event to str into a shared input module if reused elsewhere ( 44) — ( 48) single use site (commands/run.rs), so no shared module is needed. - [x] ui/share/error.rs only when real ui error handling appears (no empty ( 49) modules) ( 45) — intentionally not created; documented in the root roadmap deferred list. - [x] widgets subdivide internally when they outgrow one module ( 46) — ( 50) kernel widget renderers were externalized to the widgets repo (m3); the kernel no longer owns pack widgets. r3 - structural audit tooling - [x] scripts/audit.sh with failing thresholds ( 47) — the script gates: ( 51) cfg(target os) outside platform/ trees = 0, files over 600 lines = 0, todo/fixme/xxx/hack markers = 0, wildcard pub use ...:: <= 30, loc per top-level area <= 2400, dead pre-monocrate plugin tree absent (its path lives only inside the audit script as the guard itself), miami embedded in the theme seeds (12 themes total). module dependency graph/cycles and unused-pub detection are not implemented by the script (see the deferred note below). deferred follow-ups (tracked with the root roadmap §7 list): - audit.sh module dependency graph/cycles and unused-pub detection, plus per-file thresholds above 300/600 lines (the 200-line figure in the original issue predates the current 300/600 gates)."},{"locale":"en","repo":"xtop","title":"colors","label":"docs · colors","path":"docs/colors.md","slug":["xtop","docs","colors"],"text":"colors every theme (ux8.1 format v2) carries an explicit background / foreground pair plus a 16-entry palette of hex colors. the background is the screen/frame background every other role is measured against; the foreground is the primary text color. files without the explicit pair (legacy third-party themes) fall back to slot 0 / slot 7 at load. at load the kernel normalizes the theme (ux8.2): text and mark roles that fail their wcag contrast floor against the background are auto-lifted, deterministically and hue-preserving (see customization.md — contrast normalization ). the shipped files below stay canonical — they are the exact owner palettes; the in-memory theme is the guaranteed-legible runtime view. the slots are not arbitrary: each one is a fixed role (the widget packs read them through role constants and the kernel chrome through theme::bg()/fg()/accent()/dim() ). this legend is the summary; the full single-source table with per-role usage lives in customization.md — palette reference . all palettes below keep the same slot roles (legacy background alias at 0, accent at 6, legacy foreground alias at 7, dim at 8, bright series ramp from 9); only the hues differ per theme — light themes such as helsinki or madrid simply pick darker hues for the same roles so contrast works on a light background. slot role background explicit background — screen/frame background; theme::bg() foreground explicit foreground — primary text; theme::fg() 0 legacy background alias ( role bg ); matches background on every shipped theme except x , where the owner palette keeps a slightly lighter terminal black 1 alert (high fills, avg cpu line) 2 good (normal fills, ram line, battery) 3 warn (gradient mid stop) 4 read / rx (network rx, disk reads) 5 write / tx / gpu (network tx, disk writes) 6 accent (titles, headers, selection) 7 legacy foreground alias ( role fg ) — text renderers use the explicit foreground ; slot 7 is the near-white anchor of the base hue family and can legitimately equal the background (paris does) 8 dim (zebra rows, separators, dividers) 9 - 15 bright series ramp (multi-series charts) x madrid lahabana miami paris tokio oslo helsinki berlin london praha bogota"},{"locale":"en","repo":"xtop","title":"configuration","label":"docs · configuration","path":"docs/configuration.md","slug":["xtop","docs","configuration"],"text":"configuration xtop persists its configuration automatically on quit. the file is config.json in the platform config directory, next to the themes/ and layouts/ folders: /.config/xtop/config.json (linux) /library/application support/xtop/ (macos) %appdata%\\xtop\\ (windows) on linux you can override the base directory with $xdg config home . keys key type default description theme string \"x\" currently selected color theme name (one of the 12 shipped or a user theme). layout mode string \"dashboard\" built-in layout mode. one of dashboard , vertical , horizontal , cpufocus , memoryfocus , networkfocus , processfocus . ignored while layout name names a valid custom layout. layout name string \"\" name of the active layout when it is a custom (non-built-in) layout. when non-empty and found, it takes precedence over layout mode . update interval ms integer 1000 sampling interval in milliseconds. clamped to 100&ndash;3,600,000 on load. history points integer 100 data points retained for the historical charts. alerts object see below alert thresholds; see alert thresholds . keybindings object see below key bindings per action; see keybindings . style object see below widget glyph style (chart charset, borders, packs); see style . effect string (optional) absent frame effect applied to every rendered frame: \"fade\" activates the built-in fade-in (only in builds compiled with the effects feature). any other value disables effects. example { \"theme\": \"miami\", \"layout mode\": \"dashboard\", \"layout name\": \"\", \"update interval ms\": 1000, \"history points\": 100, \"alerts\": { \"cpu high\": 90.0, \"mem high\": 90.0, \"disk high\": 90.0 }, \"keybindings\": { \"quit\": [\"q\"], \"help\": [\"?\"], \"next theme\": [\"t\"], \"prev theme\": [\"t\"], \"next layout\": [\"l\"], \"toggle fullscreen\": [\"f\"], \"cycle fullscreen\": [\"f\"], \"search\": [\"/\"], \"command palette\": [\"ctrl+p\", \"ctrl+p\"], \"cancel\": [\"escape\"], \"kill process\": [\"k\"], \"process up\": [\"up\"], \"process down\": [\"down\"], \"cycle sort\": [\"s\"] }, \"style\": { \"charset\": \"braille\", \"borders\": \"native\", \"pack\": null, \"widgets\": {} } } unknown keys and missing optional keys are ignored; a file that cannot be parsed falls back to the defaults above. alert thresholds when a metric exceeds its configured threshold, the corresponding widget changes color to red and displays a warning indicator in its title. key description default cpu high cpu usage percentage that triggers a warning 90.0 mem high memory usage percentage that triggers a warning 90.0 disk high disk usage percentage that triggers a warning 90.0 keybindings every action accepts a list of key strings; the first matching key wins. keys are written as: single characters ( \"q\" , \"/\" , \"?\" ), shifted characters ( \"t\" , \"f\" ), modified keys ( \"ctrl+p\" , \"alt+x\" ) and named keys ( \"escape\" , \"enter\" , \"backspace\" , \"tab\" , \"up\" , \"down\" , \"left\" , \"right\" , \"delete\" , \"home\" , \"end\" , \"pageup\" , \"pagedown\" ). key (action) default binding action quit [\"q\"] save config and quit help [\"?\"] toggle the help overlay next theme [\"t\"] next theme prev theme [\"t\"] previous theme next layout [\"l\"] next layout toggle fullscreen [\"f\"] toggle full-screen view cycle fullscreen [\"f\"] cycle the full-screen widget search [\"/\"] start process search command palette [\"ctrl+p\", \"ctrl+p\"] open the command palette (ctrl+p also works as a hardcoded fallback) cancel [\"escape\"] cancel search / close overlays kill process [\"k\"] kill the selected process (same-user safety check) process up [\"up\"] move the process selection up process down [\"down\"] move the process selection down cycle sort [\"s\"] cycle the process sort column (cpu% → memory → pid → name) style the style object controls widget glyphs. values are the ecosystem-wide enums from xtop-widget-api : charset : braille (default), dot , block , half block , bar &mdash; chart markers used by history charts. borders : native (default, classic single-line frame), rounded , double , plain , ascii ( plain / ascii draw a pure ascii +- frame). pack : widget pack used for every widget without a per-widget override ( \"default\" or \"blocks\" when compiled with the widget-blocks feature). widgets : map of widget-name overrides, each accepting charset , borders and pack . see customization.md for an example and the full per-widget semantics. custom themes and layouts for custom themes and layouts, see the customization guide . back to readme"},{"locale":"en","repo":"xtop","title":"customization guide","label":"docs · customization","path":"docs/customization.md","slug":["xtop","docs","customization"],"text":"customization guide xtop supports runtime customization of color themes and layout modes via external jsonc files. this guide explains how to create and manage your own themes and layouts. table of contents themes location format palette reference starter themes loading order tips layouts location format size constraints available widgets examples starter layouts cycling order notes widget packs themes location theme files live in the themes/ subfolder of the platform config directory (the same tree as config.json and the layouts): /.config/xtop/themes/ .jsonc (linux) /library/application support/xtop/themes/ (macos) %appdata%\\xtop\\themes\\ (windows) the directory and the shipped theme files are created automatically on first run; no manual copy is needed. format each theme file defines a name , an explicit background / foreground pair and a 16-entry palette (format v2, ux8.1). colors are hex strings with an optional prefix. comments ( // and / / ) are supported in jsonc files. the background / foreground keys are optional for third-party files written against the old 16-slot format: absent keys fall back to palette[0] / palette[7] . the palette entries are not arbitrary colors: every slot has a fixed role (see the palette reference ), so themes stay interchangeable and every renderer — widget packs and kernel chrome alike — picks colors by role, never by taste. { // my-custom-theme -- dark background, warm accents \"name\": \"my-custom-theme\", \"background\": \" 1a1b1c\", // screen/frame background (role bg) \"foreground\": \" abb2bf\", // primary text (role fg) \"palette\": [ \" 1a1b1c\", // 0: legacy background alias (role bg) \" e06c75\", // 1: alert red (high fills, avg cpu line) \" 98c379\", // 2: good green (normal fills, ram line) \" e5c07b\", // 3: warn yellow (gradient mid stop) \" d19a66\", // 4: read / rx (network rx, disk reads) \" c678dd\", // 5: write / tx / gpu (network tx, disk writes) \" 56b6c2\", // 6: accent (titles, headers, selection) \" abb2bf\", // 7: legacy foreground alias (role fg) \" 3e4451\", // 8: dim (zebra rows, separators, dividers) \" e06c75\", // 9..15: bright series ramp (multi-series charts) \" 98c379\", \" e5c07b\", \" d19a66\", \" c678dd\", \" 56b6c2\", \" abb2bf\" ] } contrast normalization (ux8.2) every theme is normalized once, right after parsing, against its explicit background . the engine measures the wcag contrast ratio of each role and auto-lifts the colors that fail their floor, deterministically and hue-preserving (colors move toward white on dark backgrounds, toward black on light ones, in small steps until the floor clears): role floor source foreground text 4.5:1 explicit foreground (legacy files: slot 7) accent 3.0:1 slot 6 dim 3.0:1 slot 8 zebra-row text 3.0:1 foreground painted over the dim stripe (slots share 8) series ramp / accents 2.0:1 slots 1–5, 7, 9–15 (colored marks on the background) the dim floor and the zebra-row-text floor share slot 8, so they cannot both hold on palettes whose foreground sits close to the background: zebra-row text always wins, and dim keeps the highest value that still clears it (for the shipped helsinki / oslo palettes no lift is possible at all and dim keeps its canonical value). the lifted values replace the in-memory palette entries, so renderers keep reading theme palette() and the role accessors unchanged — the shipped/user files are never rewritten : normalization happens at load only. palette reference this table is the single truthful role reference: the kernel theme accessors ( bg() / fg() / accent() / dim() in src/theme/model.rs ) and the widget packs' role constants (widgets repo, src/util.rs ) map to exactly these slots, and the usage column reflects what the code really paints today. if a renderer needs a color, it takes it from here — no undocumented palette index. text is drawn with the explicit foreground / background pair ( theme fg() / theme bg() on the widget contract); the palette slots feed the colored marks. index role actual usage (kernel + widget packs) 0 legacy background alias ( role bg ) terminal/block background of the packs' frames; the screen itself paints the explicit background key ( theme::bg() ) 1 alert ( role alert ) red/high fills: cpu/mem/swap gauges past their alert threshold, the cpu average chart line, the minimal-view cpu gauge 2 good ( role good ) green/normal fills: low gradient stop, ram history line, battery fill, minimal-view memory gauge 3 warn ( role warn ) yellow fills: gradient mid stop for cpu/mem/storage/swap at or above 50% but below the alert threshold 4 read/rx ( role rx ) download/read metrics: network rx totals/lines, disk io read gauges 5 write/tx ( role tx ) upload/write metrics: network tx totals/lines, disk io write gauges, gpu fill 6 accent ( role accent ) accents: process table header/selection, help key spans, overlay titles/borders; theme::accent() 7 legacy foreground alias ( role fg ) near-white anchor of the base hue family; can legitimately equal the background (paris slot 7 == its background — renderers draw text with the explicit foreground , theme::fg() ) 8 dim ( role dim ) dim/secondary: zebra row backgrounds, column separators, chart dividers, muted notes; theme::dim() 9 – 15 bright series ramp ( role series start .. role series end ) seven bright variants for multi-series charts (per-core history lines, cycle by series index) the shipped themes repeat the base hue family in slots 9–15 (bright variants of slots 1–7 in the same order), which is what makes the series ramp look coherent. keep that convention when writing a theme: slots 9–15 should stay distinguishable from each other and from slots 1–8. starter themes xtop ships 12 themes . the x palette (almost-black background, purple-pink accents) is compiled into the binary as the startup fallback; all 12 definitions — including x and miami — are embedded in the binary as seeding templates. the first run writes them into the themes directory above, so every shipped theme is available without copying anything. if you want to restore them later, copy from the repository: cp -r assets/themes/ /.config/xtop/themes/ linux macos: /library/application support/xtop/themes/ available themes: x , berlin , bogota , helsinki , lahabana , london , madrid , miami , oslo , paris , praha , tokio . all theme palettes are documented in colors.md . loading order the compiled-in x palette (startup fallback, index 0). themes from the themes directory (seeded on first run) load on top; a file reusing the name x overrides the compiled palette in place. if a custom theme has the same name as a shipped one, it replaces it; new names are appended after the shipped set. tips try the grayscale themes ( london , berlin ) as a base and add your own accent colors. prefer writing the explicit background / foreground keys: the fallback (slot 0/slot 7) is only for legacy files, and a slot-7 foreground can equal the background (the shipped paris palette keeps that quirk on purpose — the explicit pair is what the roles are anchored on). do not fight the contrast normalizer: roles below their floor are lifted at load. write the palette you want; the engine guarantees the floors in-memory while the file stays canonical. slots 9–15 are the bright series ramp: make each entry a brighter sibling of slots 1–7 in order so multi-series charts stay distinct. never repurpose a slot: renderers pick colors by the role table above, so a theme that reorders roles breaks every widget that reads them. layouts location place layout files in the platform layouts directory (config dir + layouts ): /.config/xtop/layouts/ .jsonc (linux) /library/application support/xtop/layouts/ .jsonc (macos) %appdata%\\xtop\\layouts\\ .jsonc (windows) both .jsonc and .json extensions are accepted. format a layout is a recursive tree of splits and widgets : layoutdef ├── name: string └── root: area ├── direction: \"horizontal\" \"vertical\" ├── size: constraint (optional, defaults to \" \") └── areas: [area, ...] ├── area with \"widget\" → leaf node (renders a widget) └── area with \"direction\" → nested split size constraints syntax meaning \" \" or omitted fill remaining space 3 (number) fixed n rows/columns \"45%\" percentage of parent available widgets widget description header system info bar (uptime, load, keys) cpu per-core cpu usage gauges memory ram + swap gauges + ram history chart storage disk usage gauges per mount point network network rx/tx totals and speeds processes process table with search filter disk io disk read/write speeds battery battery charge gauges gpu gpu usage gauges examples simple custom layout a minimal three-row layout: header, cpu, and processes. { // \"monitor\" — cpu top-half, processes bottom-half \"name\": \"monitor\", \"root\": { \"direction\": \"vertical\", \"areas\": [ { \"widget\": \"header\", \"size\": 3 }, { \"widget\": \"cpu\", \"size\": \"55%\" }, { \"widget\": \"processes\", \"size\": \" \" } ] } } complex nested layout a full dashboard with a horizontal split in the middle section: { \"name\": \"my-dashboard\", \"root\": { \"direction\": \"vertical\", \"areas\": [ { \"widget\": \"header\", \"size\": 3 }, { \"direction\": \"horizontal\", \"size\": \"50%\", \"areas\": [ { \"widget\": \"cpu\", \"size\": \"60%\" }, { \"direction\": \"vertical\", \"size\": \"40%\", \"areas\": [ { \"widget\": \"network\", \"size\": \"50%\" }, { \"widget\": \"disk io\", \"size\": \"50%\" } ] } ] }, { \"widget\": \"processes\", \"size\": \" \" } ] } } starter layouts the 10 built-in layouts ship in the xtop-layout crate ( github.com/xtop-cli/layouts , folder layouts/default/ ) and are embedded in the binary. on startup their jsonc sources are copied to the platform layouts directory (see location ) as editable templates. community layouts live in layouts/custom/ of the same repo; install one with xtop layout install &lt;name&gt; (or copy the file into the layouts directory for your platform). validate a local file with xtop layout check &lt;file&gt; . a layout file whose name matches a built-in layout overrides it (e.g. edit dashboard.jsonc to customize the dashboard). files with new names show up as extra layouts. mode layouts: dashboard , vertical , horizontal , cpu focus , memory focus , network focus , process focus — these seven map to the layout modes. preset extras: detail dashboard , detail network , detail processes — detail-focused layouts appended after the modes (not modes themselves; they are selected by name). they exercise per-widget display options (cpu basis on processes , cores / show freq on cpu , ifaces on network ); see the per-widget options section below. cycling order mode layouts (dashboard → vertical → horizontal → cpu focus → memory focus → network focus → process focus) preset extras ( detail dashboard → detail network → detail processes ) any custom layout from the platform layouts directory with a new name (filesystem order) custom files that reuse a built-in name override that built-in in place (no duplicates) wraps back to dashboard press l to cycle forward through all available layouts. notes if a widget name in your layout doesn't match any available widget, that area is skipped and xtop prints a one-time warning to stderr ( xtop: layout '&lt;layout&gt;' references unknown widget '&lt;name&gt;' ). nested splits can be arbitrarily deep, but very deep nesting may overflow small terminals. the terminal must be at least 40×8 for any layout to render; smaller terminals show a warning. very small terminals (under 60×14) fall back to a minimal hardcoded layout (cpu + memory gauges + process list). widget glyph style charts (cpu/memory/network) and widget borders are drawn with glyph styles you can change in config.json under the style key (see configuration.md ): { \"theme\": \"x\", \"style\": { \"charset\": \"block\", \"borders\": \"ascii\", \"widgets\": { \"cpu\": { \"charset\": \"bar\" }, \"network\": { \"borders\": \"double\" } } } } charset : braille (default), dot , block , half block , bar . borders : native (default; the classic single-line box-drawing frame), rounded , double , plain and ascii (both plain and ascii draw a pure ascii +- frame). widgets : per-widget overrides. keys are the widget names layouts use: header , cpu , memory , storage , network , processes , disk io , battery , gpu . each entry accepts charset , borders and an optional pack (widget pack to render that name with, e.g. \"blocks\" ). a global style.pack sets the pack for every widget without a per-widget override. glyph styles only change the look: the data behind each widget is drawn by the widget packs (see plugin.md for how a plugin adds completely new renderers, which take precedence over packs). per-widget display options beyond glyph style, every widget instance in a layout file can carry an options json object that refines how that instance draws its data. the layout format accepts it on widget nodes as an opaque passthrough (see the layouts repo, docs/layout-schema.md , section \"widget options \"): { \"name\": \"my layout\", \"root\": { \"direction\": \"vertical\", \"areas\": [ { \"widget\": \"header\", \"size\": 3 }, { \"widget\": \"cpu\", \"size\": \"60%\", \"options\": { \"cores\": \"all\" } }, { \"widget\": \"processes\", \"size\": \" \" } ] } } the kernel forwards each node's options object to the widget's renderer while that instance is drawn (via widgetstate::widget options in the widget-api contract). multiple instances of the same widget in one layout can carry different options. no options key (or null ) means the widget renders exactly as before this feature — the defaults preserve the current behavior byte-for-byte. only documented keys refine a widget; unknown keys are ignored. recognized keys are documented per widget in the widgets repo ( docs/widgets.md ) as the ux milestones land them. the shipped detail presets showcase the first wave: the processes cpu basis ( cpu : \"total\" / \"both\" ), the cpu core/frequency keys ( cores , show freq ) and the network interface list ( ifaces ). until a widget documents a key, that key is inert (dr-ux2 defaults). fullscreen and minimal views look the options up by widget name in the current layout (first matching node); when the layout has no such node they use the defaults. plugin widget renderers see the plugin hoststate , not widgetstate : layout options are not forwarded to plugins. widget packs widget packs are the installable unit of widget code: each pack is a separate crate ( xtop-widget-&lt;name&gt; ) that registers renderers by widget name against the xtop-widget-api contract. the kernel ships two packs out of the box — the base pack ( default , always compiled in) and the blocks pack (gated behind the widget-blocks cargo feature) — and lists them in a single compile-time catalog ( src/ui/layout/pack table.rs , one (feature, label) row per pack), which the render engine and xtop widget list share. widget-pack management mirrors the plugin workflow: command description xtop widget list list the widget packs wired into the kernel (pack-table rows whose cargo feature is declared in the root cargo.toml ) xtop widget scaffold &lt;name&gt; create a compiling single-widget pack template in widgets-dev/xtop-widget-&lt;name&gt;/ (git-ignored) xtop widget install &lt;name&gt; install a pack by name from github.com/xtop-cli/widgets xtop widget install &lt;url path&gt; install a pack from any git url or a local crate directory xtop widget install is a self-modifying-source workflow (the same spirit as xtop plugin install ): it adds an optional dependency + a widget-&lt;name&gt; feature flag to the root cargo.toml , appends one (feature, label) row and its registry-linking arm to the pack catalog in src/ui/layout/pack table.rs , and runs cargo check . the pack is not enabled by default : add widget-&lt;name&gt; to the [features] default list (or build with --features widget-&lt;name&gt; ) and rebuild. once enabled, select the pack per widget with style.pack (all widgets) or style.widgets.&lt;name&gt;.pack (one widget), then place its widget names in a layout file. authoring guidance (the pack contract, how packs register renderers, the renderers' options) lives in the widgets repo docs ( docs/authoring.md , docs/widgets.md ). ← back to readme"},{"locale":"en","repo":"xtop","title":"xtop design language","label":"docs · design","path":"docs/design.md","slug":["xtop","docs","design"],"text":"xtop design language scope note for readers: this document states the visual design rules xtop renders by (the 0.1.0 kernel + the widget packs in their ux working state, 2026-09-04): theme roles, glyph sets, per-widget display options, minimum-width fallbacks and chart color rules. widget-side claims are cross-checked against the widgets repo working tree of the same date; the widgets agent's final report (tmp/ux2-4-widgets-report.md , if present) is authoritative for per-widget details and takes precedence over the summary claims here. the language is informed by btop-class resource monitors; the reference notes collected during the ux0 research live under tmp/ in the workspace root. rules rule where it is implemented --- --- chrome comes only from theme roles (dr-ux3): one role table in docs/customization.md / docs/colors.md ; kernel chrome (help/palette/search popups) uses accent titles/borders + dim separators and follows the configured border set (style.borders , incl. pure ascii +- ); widget frames draw accent/dim roles (widgets/src , role ) kernel src/ui/overlay/ , src/ui/screen.rs ; widget packs sort state is always visible: the process column that sorts carries a colored marker, one press flips the direction (▼ ↔ ▲ ), the next press advances cpu% → mem → pid → name (boot default = cpu% descending, classic order preserved) kernel appstate::cycle sort + process sort desc() on widgetstate ; the processes widget paints the marker from that state per-widget display discrimination is data-driven from the layout file (options on widget nodes, dr-ux1), not from global config only — two instances of the same widget in one screen can render differently layouts repo (docs/layout-schema.md , \"widget options \"); kernel widgetstate::widget options ; recognized keys per widget in the widgets repo docs row selection survives re-sorts: the selected process row is pid-anchored and highlighted (accent background/inverted row); zebra rows and column separators use the dim role processes widget (widgets/ ) + kernel selection state chart markers and borders honor the glyph set chosen per widget (charset: braille/dot/block/half block/bar; borders: native/rounded/double/plain/ascii), with global defaults and per-widget overrides in config.json under style ; per-layout-node options can refine them per instance chartcharset /border for in the widget-api contract; widget packs; kernel passes style.widgets. through data series get role-paired colors: per-core rows use a gradient fill, the alert/warn/good roles drive status coloring, and multi-series lines (rx/tx, read/write) use the documented slot pair (rx = slot 4, tx = slot 5) widget packs; role legend in docs/colors.md small terminals degrade cleanly instead of wrapping: layout modes fall back by size (below 60×14 → minimal gauges + process list; dashboard compacts below 100×28 and becomes vertical below 80 columns), with a hard floor of 40×8 xtop-layout mode.rs (detect effective layout ), kernel ui/screen.rs display options never fabricate data: every option refines how existing metrics are drawn (bases, columns, cores, interfaces, disks, units); underlying metrics are always real widget packs + data providers layout presets are named extras, not modes: seven mode-bound layouts keep the fixed mode slots; the detail dashboard , detail network and detail processes presets (detail .jsonc in the layouts repo) follow them in the cycling order, then user/community layouts (dr-ux6) layouts repo src/loader.rs ; kernel layout cycling + xtop layout install recognized per-widget option keys the layout options passthrough is interpreted by the widget renderers; each widget documents the keys it recognizes in the widgets repo (docs/widgets.md ). the shipped detail presets exercise the first wave on the processes , cpu and network widgets (see the layouts repo schema doc for the exact objects they carry). deferred (deliberately not modeled) these reference-monitor features have no xtop equivalent yet; each line states the data or architecture reason, so nothing here is stubbed: - per-process detail panel (graphs, status, io, parent, cmd, …): per-process cpu history exists since ux9.1 (widgetstate::process cpu history , a bounded per-pid ring fed from the visible process list), so a small cpu spark per row is drawable; no status/io/parent history buffers exist yet. process metadata (state, parent, cmd, thread count, open files, disk bytes) is already in the model and available to future views. - tree process view with expand/collapse prefixes: no tree semantics in the process view contract; pid anchoring assumes a flat, sortable list. needs a model decision before ui work. - follow mode + paused/following banners: no follow concept in the kernel state machine (inputmode is normal/searching/commandpalette). banners would need view-state additions. - per-core temperatures: since ux8.3 the provider collects per-core temperatures on linux (cpuinfo::temp c , coretemp sensors) when the sensors map onto the logical cores; machines without a readable mapping keep the aggregate max (cpu temp ) only, so no per-core temp row is drawn there. - 101-step gradient ramps per metric family: the 16-slot palette cannot host per-family 101-step ramps. xtop uses a documented 3-stop gradient (alert/warn/good roles) and the bright ramp for multi-series lines. - graph overlay captions (uptime on the cpu graph, net scale text): uptime/load live in the header widget; chart y-scales are drawn on the chart axis. overlay captions would need a graph-text style role the palette does not define. - transparent background and 16-color downconvert: the palette background role is always painted; there is no sgr 16-color path. both are kernel color-mode features, deferred. - mouse-driven chips / inline hotkey buttons: input is keyboard-first (help overlay via ? ); mouse support is limited to wheel scrolling of the process list. - runtime per-box show/hide toggles: layout switching covers box selection (l , palette, config), but runtime per-box show/hide toggles are not modeled in the layout engine. - fixed network chart ceilings: charts auto-scale to the sampled maximum; configurable fixed ceilings are not implemented. - kill confirmation dialog: k kills the pid-anchored selection directly; no dialog/banner exists (visual feedback is the row disappearing). where xtop goes further - widget packs are swappable per widget name and per layout instance (style.widgets. .pack ), so a single binary ships multiple visual languages for the same metric. - display discrimination is data-driven from the layout file (options on widget nodes), not from global config only — two instances of the same widget in one screen can render differently. - the kernel is provider-driven; data comes from the sysinfo provider and plugins, so detail views were only built where the data truly exists. related documents - docs/colors.md — theme palettes + role legend; docs/customization.md — the single role table, per-widget options , layout presets. - widgets repo — docs/widgets.md : recognized per-widget option keys. - layouts repo — docs/layout-schema.md : the detail preset layouts and the options passthrough grammar."},{"locale":"en","repo":"xtop","title":"features","label":"docs · features","path":"docs/features.md","slug":["xtop","docs","features"],"text":"features detailed breakdown of xtop's monitoring and interface capabilities. system monitoring cpu usage percentage per core and per thread, displayed as horizontal gauges. maximum cpu temperature reading when hardware sensors are available. color-coded bars that visually indicate load levels. memory ram usage gauge showing used, total, and percentage. swap usage gauge. historical line chart tracking ram usage over time. configurable number of history data points. network real-time upload (tx) and download (rx) tracking per network interface. total data transferred displayed alongside current transfer speeds. storage disk usage gauges per mount point showing used, available, and total space. visual percentage bars for each mounted filesystem. disk i/o read and write speed tracking per disk device. displayed in bytes per second with automatic unit scaling. processes scrolling list of running processes sorted by cpu usage. live search filtering by process name. displays process name, cpu usage, and memory usage. gpu gpu usage gauges: real data on linux (nvidia via nvidia-smi , amd/intel via /sys/class/drm ); nvidia is also detected via nvidia-smi on macos and windows. amd/intel gpus and apple gpus expose no public utilization api, so the widget stays empty for them (no fabricated readings). battery battery charge level gauges: real data on linux ( /sys/class/power supply ), macos ( pmset ) and windows ( getsystempowerstatus , aggregate battery). hosts without a battery show the honest empty state. theming 12 color schemes: 12 jsonc theme files ship in assets/themes/ and are embedded in the binary as first-run seeding templates. custom themes defined as jsonc files with an explicit background/foreground pair and a 16-entry hex color palette (legacy 16-slot-only files still load). always-legible themes: every theme is contrast-normalized at load (wcag floors for text and mark roles, ux8.2); shipped files stay canonical. instant theme cycling with t (next) and t (previous). palette reference in colors.md . layouts 7 built-in layout modes: dashboard, vertical, horizontal, cpu focus, memory focus, network focus, process focus, plus 3 detail preset layouts ( detail dashboard , detail network , detail processes ) that cycle after the modes and showcase per-widget display options. custom layouts defined as jsonc files with a recursive split/widget tree; every widget instance can carry an options object (cpu basis, cores, ifaces, ...) driven by the layout file. full-screen mode for any widget toggled with f . responsive design that adapts to terminal width and height automatically. minimal fallback layout for very small terminals. alert thresholds visual warnings when cpu, memory, or disk usage exceeds configurable limits. color changes to red and warning indicators in widget titles. persistence current theme, layout, update interval, history points, alert thresholds and glyph style are saved automatically on quit. configuration is stored as config.json in the platform config dir: /.config/xtop/ on linux, /library/application support/xtop/ on macos, %appdata%\\xtop on windows. back to readme"},{"locale":"en","repo":"xtop","title":"installation guide","label":"docs · installation","path":"docs/installation.md","slug":["xtop","docs","installation"],"text":"installation guide table of contents quick install (linux) quick install (windows) macos installer options build from source uninstall supported distributions quick install (linux) the installer script detects the distribution and its package manager, installs the build prerequisites (git and, when missing, the rust toolchain via rustup), clones the repository, builds it in release mode and installs the binary to /usr/local/bin . using curl curl -fssl https://raw.githubusercontent.com/xtop-cli/xtop/main/install.sh bash using wget wget -qo- https://raw.githubusercontent.com/xtop-cli/xtop/main/install.sh bash quick install (windows) requires rust (cargo) to be installed. run in powershell: irm https://raw.githubusercontent.com/xtop-cli/xtop/main/install.ps1 iex macos install.sh targets linux package managers and has no macos branch. install with cargo (needs the rust toolchain from rustup): cargo install --git https://github.com/xtop-cli/xtop --all-features the binary lands in /.cargo/bin ; make sure it is on your path. --all-features enables the samurai plugin, the mcp extension, the blocks widget pack and the effects module. installer options you can run the installer script with additional flags for more control: check dependencies without installing ./install.sh --check-deps install only dependencies (rust, build tools) ./install.sh --install-deps show help ./install.sh --help supported distributions arch linux and derivatives debian / ubuntu and derivatives fedora / rhel and derivatives opensuse and derivatives alpine linux and derivatives build from source clone the repository: git clone https://github.com/xtop-cli/xtop.git cd xtop build and run with release optimizations: cargo run --release uninstall the uninstallers remove the binary only; user configuration under the config directory is kept. linux curl -fssl https://raw.githubusercontent.com/xtop-cli/xtop/main/install.sh bash -s -- --uninstall windows irm https://raw.githubusercontent.com/xtop-cli/xtop/main/uninstall.ps1 iex back to readme"},{"locale":"en","repo":"xtop","title":"multi-repo architecture (xtop-cli org)","label":"docs · multi-repo","path":"docs/multi-repo.md","slug":["xtop","docs","multi-repo"],"text":"multi-repo architecture (xtop-cli org) status: live (2026-09-04). this document is the ecosystem architecture rfc location: it describes how the xtop-cli organization is split into repos, how the pieces depend on each other, and where the design is headed. detailed per-area docs live in each repo's docs/ folder; the push order and milestone state live in the root roadmap.md of the workspace. organization repo (xtop-cli) role content --- --- --- api contracts workspace with the four contract crates: xtop-plugin-api (data model, plugin/host traits, alertthresholds , pluginwidget ), xtop-widget-api (pack registration + glyph helpers), xtop-extension-api (extension host), xtop-effect-api (frame effects) xtop kernel the app: single-crate binary, src/ by areas (commands, config, plugins, providers, state, theme, ui). consumes every other repo widgets renderers packs of widget renderers against xtop-widget-api : base pack xtop-widgets + alternative pack xtop-widget-blocks (sibling crate of the per-widget crates so git consumers resolve its xtop-widget-core dependency) + custom/ community layouts arrangement xtop-layout crate: data-driven layout model + jsonc loader + layout modes, plus layouts/default/ (7 mode-bound layouts + 3 detail preset extras) and layouts/custom/ (community, installable) plugins functionality plugin implementations against xtop-plugin-api (first member: xtop-plugin-samurai ) extensions kernel hooks server-style extensions against xtop-extension-api (xtop-extension-mcp ) effects animation frame effects against xtop-effect-api (xtop-effect-fade : 500 ms fade-in from black) local development layout (sibling checkouts in one folder, as in this workspace): dependency principle: contracts in api , consumers point up each consumer repo depends only on the contract crates in api — never on the kernel — so every repo compiles standalone: - api : pure types + protocols (manifests, capabilities, errors, snapshot model, provider/widget/extension/effect contracts). depends only on ratatui /serde . not published to crates.io yet (see roadmap §6 follow-ups). - xtop kernel : hosts the ecosystem — implements hoststate / widgetstate / extensionhost for its live state, runs pluginmanager and the composite provider, renders layouts by resolving widget names into the packs, and drives effects (feature-gated). builds fine without any optional feature: cargo build --no-default-features is the pure core. - widgets / layouts / plugins / extensions / effects : consume only api types; each repo compiles standalone and never depends on the kernel. integration modes level mechanism use --- --- --- compile-time (today) cargo git dependencies + optional feature flags every integration: samurai plugin, mcp extension, blocks pack, fade effect dev-time xtop plugin install (clones, self-edits the kernel cargo.toml , runs cargo check ) first steps with a plugin repo runtime (future, rfc) binary/abi discovery of xtop-plugin- / xtop-effect- / xtop-extension- in config dirs + xtop dev dir third parties without recompiling — see the root roadmap §7 deferred list the kernel never requires any external repo at runtime: optional ecosystem pieces are cargo features (plugin-samurai , mcp-extension , widget-blocks , effects ); contract crates (the four xtop- -api crates plus xtop-widgets , xtop-layout ) are unconditional because the kernel's chrome and state views are written against them. development flow (temporary path deps) the ecosystem repos are edited before they are pushed. to compile a consumer against un-pushed sibling state, its cargo.toml temporarily replaces the git dependency with a path dependency (path = \"../api/crates/plugin-api\" , path = \"../widgets\" , ...), or — when the consumer's own manifest must stay untouched — a temporary [patch.\"https://github.com/xtop-cli/ \"] section redirects the git sources to the sibling checkouts. all temporary overrides are removed before the owner pushes; final manifests carry the floating git deps shown above. why the split exists the kernel was originally a workspace of kernel-owned crates (xtop-core /xtop-tui /xtop-cli ), then a monocrate with plugins inside. each customization axis grew into its own repo so that: - a contributor can ship a widget pack, layout, plugin, extension or effect without touching the kernel code base; - every repo compiles against the api contracts alone (testable in isolation, no kernel import); - the kernel stays a thin host: metrics model, layouts and widget renderers all come from the ecosystem crates. single-source rules that keep the split honest (see the root roadmap \"decisions\" section): - the metrics model and plugin protocol exist only in xtop-plugin-api ; - widget registration and glyph/style mapping exist only in xtop-widget-api (the plugin-side widget is xtop plugin api::pluginwidget ); - ecosystem constants live at the producer (xtop-plugin-samurai exports plugin id and its 12 action names; xtop-extension-mcp builds its tool table from them). phases 1. f0 (done) : org xtop-cli , repos created, local clones in one folder. 2. f1 (done) : api contract crates extracted from the kernel's domain model; kernel + siblings consume them. 3. f2 (done) : samurai moved to the plugins repo; mcp moved to extensions ; kernel features point at the sibling repos via git deps. 4. f3 (done) : effects workspace with the fade effect; kernel wires it behind the optional effects feature. 5. f4 (done) : extension host contract + xtop-extension-mcp as the first server-style extension. 6. f5 (pending) : publish the api crates to crates.io; tag and pin git deps; per-repo ci and kernel releases (see the root roadmap §6/§7). open rfc topics - runtime dynamic discovery (abi or directory-based loading) — explicitly deferred in the root roadmap §7 until the compile-time feature model stops being coherent. - publishing: versioning scheme for the contract crates and the pinning strategy of every consumer."},{"locale":"en","repo":"xtop","title":"plugin system","label":"docs · plugin","path":"docs/plugin.md","slug":["xtop","docs","plugin"],"text":"plugin system xtop hosts plugins : extra functionality shipped as separate crates that implement the contract in xtop-plugin-api (a crate of the xtop-cli/api repo). plugins are compile-time: the kernel wires them through cargo git dependencies and feature flags, never through runtime discovery. the built-in plugin is xtop-plugin-samurai (see multi-repo.md for the ecosystem layout). you can build xtop without any plugin or extension: cargo build --release --no-default-features or with the default set (samurai plugin + mcp extension): cargo build --release architecture the plugin system has four kernel components: component kernel location purpose plugin trait + contract types xtop plugin api (crate, external) interface every plugin implements; manifest, capabilities, errors, data model pluginmanager src/plugins/manager.rs registers, ticks and dispatches events to plugins hoststate impl src/plugins/host.rs kernel-side view of the live state plugins may touch compositeprovider src/providers/composite.rs merges the kernel provider with plugin data providers plugins never depend on the kernel: they see state only through plugincontext , which is built over the hoststate trait. plugin widgets render over &amp;dyn hoststate through xtop plugin api::pluginwidget (distinct from the widget-pack registration in xtop-widget-api , which draws over widgetstate ). the plugin trait pub trait plugin: debug + send { fn manifest(&amp;self) -&gt; pluginmanifest; fn on enable(&amp;mut self, ctx: &amp;mut plugincontext) -&gt; result&lt;(), pluginerror&gt;; fn on disable(&amp;mut self, ctx: &amp;mut plugincontext) -&gt; result&lt;(), pluginerror&gt;; fn on tick(&amp;mut self, ctx: &amp;mut plugincontext) -&gt; result&lt;(), pluginerror&gt;; fn on key(&amp;mut self, ctx: &amp;mut plugincontext, key: &amp;str) -&gt; result&lt;bool, pluginerror&gt;; fn data provider(&amp;self) -&gt; option&lt;box&lt;dyn systemdataprovider&gt;&gt;; fn widget(&amp;self) -&gt; option&lt;pluginwidget&gt;; fn execute(&amp;mut self, ctx: &amp;mut plugincontext, action: &amp;str, params: &amp;str) -&gt; result&lt;string, pluginerror&gt;; } all methods except manifest() have default implementations, so a minimal plugin only declares its manifest. method default called when manifest() required any time metadata is needed on enable() no-op plugin is registered at startup on disable() no-op xtop shuts down on tick() no-op every update cycle ( 1s by default) on key() false key press in normal mode (returns true to consume) data provider() none startup (merged into the compositeprovider) widget() none after every tick (refreshes the plugin widget map) execute() unknownaction external agent (ai, cli, mcp) invokes a command pluginmanifest each plugin declares its identity and needs in manifest().capabilities : readsysteminfo -- read system metrics killprocesses -- terminate processes modifyconfig -- change themes, layouts, thresholds, intervals renderwidgets -- register custom tui widgets custom(string) -- anything not covered above plugincontext safe, limited access to application state. capability-gated reads return result ; writes require the matching capability and fail with a pluginerror::recoverable otherwise: ctx.snapshot()? // full systemsnapshot (needs readsysteminfo) ctx.top processes(n)? // top n processes by cpu, sorted desc ctx.system info()? // hostname, os, kernel (needs readsysteminfo) ctx.kill process(pid)? // kill process by pid (needs killprocesses) ctx.set alert thresholds(cpu, mem, disk)? // (needs modifyconfig) ctx.set theme by name(\"tokio\")? // (needs modifyconfig) ctx.set layout by name(\"cpu focus\")? // (needs modifyconfig) ctx.set update interval(500)? // (needs modifyconfig) ctx.alerts() // current alert thresholds ctx.config() // theme, layout, interval, hostname ctx.data dir() // host-provided plugin data dir data providers the kernel samples the system with its own provider ( sysinfoprovider in src/providers/sysinfo/ ) and composes plugin providers through compositeprovider : refresh all() refreshes the primary provider and every extra snapshot() delegates to the primary disk io() , batteries() , gpu info() and system info() use the primary result when non-empty, otherwise the first non-empty extra provider result kill process() tries the primary first, then the extras plugin widgets a plugin can register one custom widget via widget() . the render closure receives the plugin view of the state ( &amp;dyn hoststate ) and draws with plain ratatui: fn widget(&amp;self) -&gt; option&lt;pluginwidget&gt; { some(pluginwidget { name: \"samurai\".to string(), render: arc::new( f, state, area { // draw using ratatui; state is &amp;dyn hoststate. }), }) } custom widgets are placed in layouts by name in jsonc layout files: { \"name\": \"my-layout\", \"root\": { \"direction\": \"vertical\", \"areas\": [ { \"widget\": \"header\", \"size\": 3 }, { \"widget\": \"samurai\", \"size\": \"30%\" }, { \"widget\": \"processes\", \"size\": \" \" } ] } } plugin widgets take precedence over widget-pack widgets with the same name (the kernel resolves plugin widgets first at render time). a layout referencing a name no pack and no plugin provides prints a one-time warning to stderr. cli commands command description xtop plugin list list plugins wired into the kernel cargo.toml (feature entries with dep:&lt;name&gt; ) xtop plugin install &lt;name&gt; install a plugin by name from github.com/xtop-cli/plugins xtop plugin install &lt;url&gt; install a plugin from any git url xtop plugin scaffold &lt;name&gt; create a new plugin crate template in plugins-dev/ (git-ignored) install flow (real behavior) xtop plugin install does not download a binary and does not register anything at runtime. it edits the kernel's own cargo.toml (a self-modifying-source workflow): resolves the source repo: github.com/xtop-cli/plugins for a name, or the given url clones it (shallow, sparse) into a temp dir locates the xtop-plugin-&lt;name&gt; crate inside the clone (repo root, or plugins/ / crates/ subfolders) adds an optional git dependency + a feature flag to the kernel's root cargo.toml (the same pattern the built-in xtop-plugin-samurai uses) runs cargo check to verify the manifest resolves, then cleans up the plugin is registered but not enabled by default . to enable it, add its feature to the default list in [features] in the kernel cargo.toml (or build with --features &lt;name&gt; ) and recompile. every installed plugin also needs a registration line in src/commands/share/bootstrap.rs under its feature flag. build xtop with samurai plugin enabled (default already includes it) cargo build --release --features plugin-samurai build xtop with samurai + another plugin cargo build --release --features \"plugin-samurai,plugin-mything\" extensions: the mcp server extensions are the kernel's server-style hooks (contract: xtop-extension-api ). the shipped extension is the mcp (model context protocol) server in the xtop-cli/extensions repo ( xtop-extension-mcp ), which exposes the hosted plugins' actions as mcp tools over stdio. it is compiled in when the mcp-extension feature is on (part of the default features). xtop mcp the mcp tools are executed against the samurai plugin, so the plugin-samurai feature is required too. compatible clients include claude desktop, cline, cursor, and continue.dev. claude desktop configuration { \"mcpservers\": { \"xtop\": { \"command\": \"xtop\", \"args\": [\"mcp\"] } } } interactive testing single command echo '{\"jsonrpc\":\"2.0\",\"id\":1,\"method\":\"tools/call\",\"params\":{\"name\":\"system summary\",\"arguments\":{}}}' xtop mcp interactive session xtop mcp the mcp extension depends on xtop-plugin-samurai at compile time: the plugin id and the 12 action names are single-sourced constants ( plugin id , actions:: ), so the tool table can never drift from the plugin implementation. adding a plugin manually scaffold the crate (or write it by hand in your own repo): xtop plugin scaffold mything creates plugins-dev/xtop-plugin-mything/ make it a git repo and push it (the crate must live at the repo root or under plugins/ / crates/ , e.g. github.com/you/xtop-plugin-mything ). install it into the kernel (adds an optional git dependency + feature flag in the root cargo.toml ): xtop plugin install https://github.com/you/xtop-plugin-mything equivalent manual edit: [dependencies] xtop-plugin-mything = { git = \"https://github.com/you/xtop-plugin-mything\", optional = true } [features] plugin-mything = [\"dep:xtop-plugin-mything\"] register it behind the feature flag in src/commands/share/bootstrap.rs : [cfg(feature = \"plugin-mything\")] use xtop plugin mything::mythingplugin; // in register plugins(): [cfg(feature = \"plugin-mything\")] { let plugin = box::new(mythingplugin::new()); if let err(e) = mgr.register(plugin, state) { eprintln!(\"[xtop] failed to load mything plugin: {e}\"); } } contract details for plugin authors (manifest, capabilities, error types, widgets) live in the api repo docs; the samurai plugin in xtop-cli/plugins is the reference implementation. back to readme"},{"locale":"en","repo":"xtop","title":"usage","label":"docs · usage","path":"docs/usage.md","slug":["xtop","docs","usage"],"text":"usage table of contents keybindings modules help overlay command palette full-screen mode responsive layouts command-line commands keybindings key action q quit application (saves configuration) ? toggle help overlay t next color theme t previous color theme l next layout (cycles through the layout modes, the detail presets, then custom layouts) f toggle full-screen mode for the current widget f cycle full-screen focus through all available widgets / open process search / filter enter confirm search filter backspace delete character in search input esc cancel search / close help overlay modules header -- shows system uptime, load average, current theme name, and active layout mode. cpu -- horizontal usage bars for each cpu core. if hardware sensors are available, displays the maximum cpu temperature. memory -- gauges for ram and swap usage, plus a line chart showing ram usage over a configurable history window. storage -- disk usage gauges per mount point showing capacity and used space. network -- total downloaded (rx) and uploaded (tx) data per interface, with current transfer rates. disk i/o -- read and write speeds per disk device in bytes per second. processes -- scrolling list of processes sorted by cpu usage (top 200 by default; override with xtop max processes ), with live search filtering by process name. gpu -- gpu usage gauges (available on supported hardware). battery -- battery charge level gauges (available on supported hardware). help overlay press ? at any time to display a full list of available keybindings on screen. press ? again or esc to close the overlay. process search search filters the process list in real time: press / to open the search bar at the top of the process list. type any query -- results filter instantly by process name. press enter to confirm the filter, esc to cancel, backspace to delete characters. a centered overlay with a /query indicator shows the current search input. command palette press ctrl+p to open the command palette, a searchable list of actions (also reachable with the command palette keybinding). the palette has three pages: main (go to themes/layouts, toggle or cycle full-screen, search, help, cycle the process sort, random theme, exit), themes (jump to any loaded theme) and layouts (jump to any layout). type to filter, use up / down to move, enter to run the selected action and esc to close. full-screen mode press f to toggle full-screen mode for the currently focused widget. the widget expands to fill the entire terminal area (minus the header bar). press f to cycle full-screen focus through widgets in sequence: cpu, memory, storage, network, processes, disk i/o, gpu, battery, then exit. responsive layouts the interface adapts automatically to the terminal size: terminal size behavior dashboard mode, 100+ cols and 28+ rows full dashboard layout with 2 columns dashboard mode, 80&ndash;99 cols or under 28 rows compact layout dashboard mode, narrower than 80 cols vertically stacked layout other modes the requested mode layout (vertical, horizontal, cpu/memory/network/process focus) narrower than 60 cols or shorter than 14 rows minimal layout: cpu, memory, and processes only smaller than 40 x 8 warning message displayed (terminal too small) command-line commands besides the tui, xtop ships management subcommands (run xtop --help for the same list): command description xtop mcp start the mcp server (stdio transport) for ai agents xtop plugin list list plugins wired into the kernel cargo.toml xtop plugin install &lt;name url&gt; install a plugin (self-edits the kernel manifest) xtop plugin scaffold &lt;name&gt; create a plugin crate template in plugins-dev/ xtop widget list list widget packs wired into the kernel (cargo features + pack table) xtop widget install &lt;name url path&gt; install a widget pack (self-edits the manifest + pack table) xtop widget scaffold &lt;name&gt; create a single-widget pack crate template in widgets-dev/ xtop layout check &lt;file&gt; validate a layout jsonc file xtop layout install &lt;name&gt; install a community layout from github.com/xtop-cli/layouts plugins and widget packs are compile-time: the commands register crates and feature flags in the kernel's sources; enable a feature in the [features] default list (or build with --features &lt;name&gt; ) and rebuild to include it. widget-pack details live in customization.md (&ldquo;widget packs&rdquo;) , plugin details in plugin.md . back to readme"},{"locale":"es","repo":"api","title":"xtop api","label":"api","path":"README.md","slug":["api"],"text":"xtop api crates de api compartidos para el ecosistema xtop. el kernel, los plugins, los widgets, los efectos y las extensiones dependen todos de estos crates — definen los contratos entre el nucleo y cada punto de extension. workspace crates crate package proposito --- --- --- plugin-api xtop-plugin-api protocolo de plugins: manifest, capacidades, errores, eventos del ciclo de vida; el modelo de datos compartido (instantaneas del sistema, alertthresholds , ...) widget-api xtop-widget-api contrato del renderizador de widgets: widgetstate de solo lectura, registro de renderizadores y los mapeos canonicos de glifos/estilos (to color , border for , marker for ) effect-api xtop-effect-api contrato de efectos: efectos de frame con estado sobre el buffer de ratatui ya renderizado (effect , effectmanifest ) extension-api xtop-extension-api protocolo de extensiones: hooks alrededor de la configuracion, el layout, el tema y el pipeline de renderizado documentacion guias detalladas y ancladas al codigo en docs/ : - docs/architecture.md — estructura del workspace, grafo de dependencias entre crates, propiedad de dr-1..dr-5, consumo del ecosistema (deps git, patron temporal de path-dep) - docs/data-model.md — cada struct de model:: , que rellena el provider del kernel, ciclo de vida de la instantanea, la eliminacion de docker, el contrato serde de alertthresholds - docs/plugin-contract.md — plugin , pluginmanifest , aplicacion de capacidades de plugincontext , hoststate , pluginerror , systemdataprovider , autoria de plugins paso a paso - docs/widget-contract.md — widgetstate , widgetrenderer /widgetregistration , enums de glifos + helpers canonicos, reglas de los packs - docs/extension-contract.md — extension , extensionhost , extensioncontext , modelo tipo servidor - docs/effect-contract.md — effect /effectmanifest y el contrato del host - docs/changes.md — registro de decisiones (dr-1..dr-7, d1..d8) y cambios incompatibles para consumidores consumo los crates aun no estan publicados en crates.io. los consumen los repos hermanos como dependencias git (git = \"https://github.com/xtop-cli/api\" , actualmente flotantes) desde: - kernel xtop (github.com/xtop-cli/xtop) - repo widgets (github.com/xtop-cli/widgets) — packs de widgets en vivo consumidos por el kernel - repo plugins (github.com/xtop-cli/plugins) - repo layouts (github.com/xtop-cli/layouts) - repo effects (github.com/xtop-cli/effects) — en vivo, con crates xtop-effect- planificados - repo extensions (github.com/xtop-cli/extensions) el plan de publicacion (crates.io + tags, y despues fijar a los consumidores a la revision etiquetada) se sigue en el roadmap del workspace, §6. ci local ejecutar desde la raiz del repo: solo local: este repo no conserva flujos de trabajo de github. licencia mit"},{"locale":"es","repo":"api","title":"api — arquitectura","label":"docs · architecture","path":"docs/architecture.md","slug":["api","docs","architecture"],"text":"api — arquitectura el repo api aloja los crates de contrato compartidos del ecosistema xtop. cualquier otro repo (kernel xtop , widgets , plugins , extensions y, mas adelante, effects ) depende de estos crates; nada redefine los tipos que poseen (roadmap del workspace, dr-1..dr-5). el repo nunca depende del kernel. este documento cubre la estructura del workspace, el grafo de dependencias entre los cuatro crates, que posee cada crate y como consumen hoy los crates los repos hermanos. las guias de contrato viven en los documentos hermanos de esta carpeta: documento alcance --- --- data-model.md structs de xtop plugin api::model , providers, ciclo de vida de la instantanea, alertthresholds plugin-contract.md plugin , pluginmanifest , plugincontext , hoststate , capacidades, systemdataprovider widget-contract.md widgetstate , registro de renderizadores, helpers de glifos (modulo glyph ) extension-contract.md extension , extensionhost , modelo tipo servidor effect-contract.md effect , effectmanifest , contrato del host changes.md registro de decisiones (dr-1..dr-7, decisiones m1 d1..d8) y cambios incompatibles estructura del workspace hechos del workspace (cargo.toml ): - resolver = \"2\" ; los miembros son los cuatro crates anteriores. - [workspace.package] : version = \"0.1.0\" , edition = \"2021\" , license = \"mit\" , rust-version = \"1.87\" . la edition se mantiene en 2021 a proposito (la migracion a 2024 esta diferida, roadmap §7). - [workspace.dependencies] (dr-7): ratatui = \"0.30.2\" , serde = { version = \"1\", features = [\"derive\"] } , serde json = \"1\" . sin tokio/clap/chrono en ningun lugar del repo. - cada miembro declara rust-version.workspace = true (y hereda version/edition/license), de modo que el msrv 1.87 se aplica de verdad por paquete (decision d6). dependencias por crate (manifests de los miembros): crate dependencias dev-dependencies notas --- --- --- --- xtop-plugin-api ratatui (workspace), serde (workspace) serde json model + plugin protocol xtop-widget-api ratatui, serde, xtop-plugin-api = { path = \"../plugin-api\" } serde json la path dep es la forma dentro del workspace xtop-effect-api ratatui — la unica dependencia es ratatui xtop-extension-api — — cero dependencias hoy los cuatro paquetes declaran repository = \"https://github.com/xtop-cli/api\" . grafo de dependencias entre crates solo existe una arista entre los cuatro crates: widget-api depende de plugin-api , porque widgetstate nombra los tipos del modelo de datos compartido (xtop plugin api::model::{systemsnapshot, systeminfo, processinfo} y alertthresholds ) en las firmas de sus metodos. el modelo de datos vive deliberadamente en plugin-api , y no en un crate neutro aparte; los consumidores de widget-api , effect-api o extension-api obtienen el modelo a traves de plugin-api (o no lo obtienen en absoluto, cuando su contrato no lo necesita). effect-api y extension-api son independientes: solo necesitan los tipos buffer /frame de ratatui (effect-api) o nada (extension-api). ninguno de los cuatro crates depende del kernel ni de ningun repo hermano. que posee cada crate (dr-1..dr-5) decision superficie propiedad donde vive --- --- --- dr-1 — modelo de datos + contrato de plugins systemsnapshot y cada struct de model:: , plugin , hoststate , plugincontext , plugincapability , pluginerror , pluginmanifest , systemdataprovider , alertthresholds (serde), hex to rgb , runtimeconfig , pluginwidget crates/plugin-api/src/ (ver el plugin contract) dr-2 — contrato de render de widgets widgetstate , widgetrenderer , widgetregistration , chartcharset , widgetborders , helpers canonicos de glifos (to color , marker for , border for , ascii border en glyph ) crates/widget-api/src/ (ver el widget contract) dr-4 — host de extensiones extension , extensionhost , extensioncontext , extensionmanifest , extensionerror crates/extension-api/src/ (ver el extension contract) dr-5 — efectos effect (sobre el buffer de ratatui ya renderizado + tiempo transcurrido), effectmanifest crates/effect-api/src/ (ver el effect contract) dr-2 tambien establece la regla de nombres: los widgets del lado del plugin (renderizados sobre &dyn hoststate ) son el tipo distinto pluginwidget exportado por xtop-plugin-api , mientras que xtop-widget-api posee el tipo de registro de packs de widgets dibujado sobre widgetstate . un gate de grep del workspace (roadmap m7.4) comprueba que no exista ningun simbolo widgetregistration fuera de widget-api . dos dr abarcan todo el ecosistema y no crean superficie de api: dr-3 mantiene los nombres de widgets de layout como cadenas simples (xtop-layout vive en el repo layouts , que no declara dependencia de api), y dr-6 situa las constantes del ecosistema (id del plugin samurai, nombres de acciones) en el productor — el repo plugins — en lugar de en api. como consume los crates el ecosistema los cuatro crates no estan publicados en crates.io (el readme lo afirma; el plan de publicacion se sigue en el roadmap §6). hoy cada consumidor declara los crates como dependencias git flotantes — git = \"https://github.com/xtop-cli/api\" sin rev /tag — exactamente como prescriben para este ciclo el roadmap §2 (dr-7) y §6 del workspace: repo aristas de dependencias git (desde su cargo.toml) --- --- xtop (kernel) xtop-plugin-api , xtop-extension-api , xtop-widget-api (todas git = .../api , flotantes); ademas deps git sobre los repos widgets/layouts/plugins/extensions widgets xtop-widget-api , xtop-plugin-api (deps del workspace, git) plugins (samurai) xtop-plugin-api (dep del workspace, git) extensions (mcp) xtop-extension-api (dep del workspace, git) effects ninguna aun — el repo no tiene crates hasta m5 layouts ninguna — los archivos de layout referencian widgets por nombres de cadena simples (dr-3); no se necesitan tipos de contrato consecuencias de la forma flotante: - una compilacion de consumidor resuelve lo que apunte la rama por defecto remota de github.com/xtop-cli/api . los cambios hechos en un arbol de trabajo local de api no son visibles para los repos hermanos hasta que el propietario hace push. - cargo cachea la revision git descargada, asi que las compilaciones locales de los consumidores solo son reproducibles hasta el ultimo head remoto descargado. - nada puede romperse en silencio por un cambio de publicacion/tag porque nada esta publicado todavia; el paso siguiente (publicar los cuatro crates en crates.io + tags, y despues fijar cada consumidor a la revision de api etiquetada — o mover los consumidores a las versiones publicadas) es el paso 7 del roadmap §6. patron temporal de path-dependency (desarrollo multi-repo local) los repos hermanos validan contra el arbol de trabajo local de api a traves de path dependencies temporales (roadmap §5, m8.1/m8.2; agents.md del workspace). se usan dos formas, segun el objetivo: 1. desarrollo por repo : sustituir la dep git flotante del manifest del consumidor por un path a este repo, o conservar la declaracion git y sobrescribirla con una seccion [patch] . ambas apuntan a api/crates/ : una path dep directa funciona igual cuando el consumidor no tiene una declaracion git que parchear: las urls absolutas file:// a api/crates/ son equivalentes. los repos estan lado a lado en el workspace, asi que las rutas relativas ../api/... son la norma. 2. verificacion combinada (m8.1) : un workspace de prueba bajo el tmp/verify-ws del workspace conecta todos los repos con path deps, de modo que cargo test /clippy en todo el ecosistema demuestran una unica compilacion combinada. ambas formas son temporales por regla: cada manifest se restaura a sus deps git flotantes antes de que el propietario haga push (m8.2; el estado final del manifest debe ser exactamente el que el propietario publica). una seccion [patch] es la forma menos invasiva porque restaurar consiste en borrar la seccion. higiene del repo y ci local - el ci es solo local: ./scripts/ci.sh [fmt clippy check test] desde la raiz del repo (check de fmt, clippy -d warnings , check/test del workspace). no existen ni se anaden flujos de trabajo de github (regla del workspace). - el workspace lleva 16 tests (plugin-api 5, widget-api 9, effect-api 2; extension-api ninguno todavia) que cubren el gating de capacidades, el orden de top processes , el contrato serde de alertthresholds , las tablas de mapeo de glifos y un test de efecto con frame falso. cargo doc --workspace --no-deps compila sin warnings. - gates de grep que deben permanecer verdes (roadmap m7.4): ningun widgetregistration fuera de widget-api , ninguna definicion de alertthresholds fuera de plugin-api , ningun pluginwidgetfn , ningun plugins dir tmp . no existe ningun simbolo \"docker\" en este repo (m1.4). plan de publicacion (referencia) el orden de push y el paso de publicar/fijar viven en el roadmap §6 del workspace: api se publica primero (los contratos primero — todo depende de ella); la publicacion en crates.io de los cuatro crates mas los tags, y la fijacion de las deps git de los consumidores, son el paso 7."},{"locale":"es","repo":"api","title":"registro de decisiones y cambios incompatibles","label":"docs · changes","path":"docs/changes.md","slug":["api","docs","changes"],"text":"registro de decisiones y cambios incompatibles este archivo registra las decisiones que dieron forma al repo api en el hito m1 y los cambios incompatibles que los consumidores deben absorber. las decisiones autoritativas del ecosistema (dr-1..dr-7) viven en el roadmap §2 del workspace; las decisiones m1 con margen de interpretacion (d1..d8) se registraron en el informe del hito tmp/m1-api-report.md §8 y se reafirman aqui en el contexto del repo, ancladas al codigo. resumen de dr — que implementa el repo api decision que significa dentro de este repo --- --- dr-1 los tipos del modelo de datos + contrato de plugins existen una sola vez, en crates/plugin-api/src/ (model.rs , host.rs , plugin.rs , context.rs , capability.rs , error.rs , manifest.rs , provider.rs , widget.rs , color.rs ). gate de grep: ninguna definicion de alertthresholds fuera de plugin-api (m7.4). dr-2 el contrato de render de widgets existe una sola vez, en crates/widget-api/src/ (state.rs , renderer.rs , glyph.rs ). los widgets del lado del plugin son pluginwidget de xtop-plugin-api , un tipo distinto — sin nombre compartido con widgetregistration de widget-api . gate de grep: ningun widgetregistration fuera de widget-api . dr-3 sin superficie de layout en api: los archivos de layout dirigen los widgets por nombres de cadena simples (xtop-layout en el repo layouts no necesita tipos de api). dr-4 los tipos de extensiones existen una sola vez, en crates/extension-api/src/ . dr-5 el contrato de efectos existe una sola vez, en crates/effect-api/src/ : effect sobre el buffer ya renderizado + effectmanifest . dr-6 las constantes del ecosistema viven en el productor (id del plugin samurai/nombres de acciones en el repo plugins , m4), no en api. dr-7 versiones: ratatui 0.30.2 , serde/serde json ultimos 1.x, rust-version = \"1.87\" en el workspace y en cada manifest de miembro; sin tokio/clap/chrono; las deps git de los consumidores permanecen flotantes este ciclo. decisiones m1 d1..d8 (contexto del repo) - d1 — el gating de capacidades fuerza lecturas result . aplicar readsysteminfo a traves del mismo mecanismo check capability que usan los metodos mutantes solo es posible si las lecturas pueden fallar, asi que plugincontext::snapshot , system info y top processes devuelven result en lugar de valores planos (context.rs ). las denegaciones son pluginerror::recoverable(\"plugin does not have required capability: readsysteminfo\") . los consumidores se rompen a proposito; ver la tabla siguiente. - d2 — top processes ordena y despues trunca. implementado como sort by(cpu usage desc via total cmp) + truncate(n) (context.rs ): las mismas semanticas que el borrador anterior (sort + take ), sin asignacion extra, orden garantizado por el contrato y no por el productor. orden estable: los empates de igual cpu conservan el orden de la instantanea (probado). - d3 — el mapeo de bordes sigue las semanticas de ratatui 0.30.2. el borrador de la especificacion leyo border::empty de ratatui como \"dibujo de caja estandar\" y border::plain como \"ascii +\" — cierto para ratatui ≤ 0.28, erroneo desde 0.29: empty es ahora un conjunto de espacios en blanco y plain es el marco estandar de dibujo de caja de una linea; ratatui no incluye ningun conjunto ascii. ganaron las semanticas previstas: native → border::plain (aspecto clasico), rounded → border::rounded , double → border::double , plain → ascii border , ascii → ascii border (glyph.rs ), siendo ascii border un nuevo conjunto canonico exportado + - . - d4 — sin tipo de pegamento effectregistration . la tarea definia un contrato minimo de exactamente effect + effectmanifest y ninguna superficie especulativa; no existe ningun consumidor hasta el cableado del kernel (m5), asi que un tipo de registro seria especulativo. omitido; trivial de anadir cuando llegue m5. - d5 — los helpers de mapeo viven en el glyph.rs existente, solo glifos. las tres funciones (to color , border for , marker for ) y la const ascii border ampliaron el modulo que ya poseia los enums; una unica ruta de import canonica, xtop widget api::glyph::{...} , evita ambiguedades. los helpers no se re-exportan en la raiz del crate (solo se re-exportan los enums). las firmas de bordes usan ratatui::symbols::border::set (el set pelado necesita un lifetime en codigo real). - d6 — rust-version.workspace = true en cada miembro. una entrada [workspace.package] por si sola no se hereda; el opt-in por miembro es lo que hace verdadero el \"declarado en cada paquete\" de dr-7. - d7 — racional del rename sin el nombre antiguo literal. el comentario de doc de pluginwidget explica la distincion conceptualmente (\"xtop-widget-api posee el tipo canonico de registro de packs de widgets, el que se dibuja sobre widgetstate ; los dos son contratos distintos y no deben compartir nombre\") sin imprimir el simbolo eliminado widgetregistration — lo que ademas mantiene verde el gate de grep m7.4. - d8 — sin derive default en alertthresholds . la tarea solo pedia serialize/deserialize ; el default (90/90/90) y copy /partialeq de la copia del kernel son semanticas del lado del kernel que m2 puede solicitar cuando elimine su propia copia. se evitaron derives no solicitados en un tipo de contrato. cambios incompatibles para los consumidores todos los cambios siguientes aterrizan en el arbol de trabajo de api durante m1 y llegan a los repos hermanos cuando el propietario hace push; hasta entonces los consumidores resuelven el antiguo head remoto a traves de sus deps git flotantes (ver architecture.md para el patron local de validacion con path-dep). cambio ancla de codigo impacto en el consumidor migracion --- --- --- --- rename de pluginwidget : el tipo de registro de plugin-api se renombra del duplicado widgetregistration a pluginwidget (misma forma: name + closure de render sobre &dyn hoststate ) plugin-api/src/widget.rs , lib.rs ; plugin::widget() devuelve option el kernel xtop/src/plugins/manager.rs y state/app.rs importan widgetregistration de xtop plugin api ; el widget() de samurai (plugins/plugins/xtop-plugin-samurai/src/lib.rs ) nombra el tipo antiguo kernel m2.3: importar pluginwidget ; borrar el alias pluginwidgetfn en ui/layout/engine.rs ; el mapa de plugins sigue siendo nombre → pluginwidget . samurai m4.1. gate de grep m7.4 las lecturas devuelven result : plugincontext::snapshot() , system info() , top processes() ahora aplican readsysteminfo y devuelven result plugin-api/src/context.rs samurai llama a ctx.snapshot() en 5 sitios (resumen del sistema, busqueda/top/info de procesos, analyze) y lee los valores directamente; ademas process.kill /threshold.set /config.set ya mapean err samurai m4.1: manejar result en cada punto de llamada a ctx.snapshot() /lectura (informe §9); el kernel m2 ejercita las mismas rutas a traves de samurai + engine superficie docker eliminada: se borran el struct dockerinfo , el campo systemsnapshot::dockers , el metodo systemdataprovider::docker info y su re-export en la raiz plugin-api/src/model.rs (struct + campo), provider.rs ; el grep de docker en toda la api esta vacio el provider del kernel todavia asigna dockers: vec![] (xtop/src/providers/sysinfo/provider.rs ) y el composite sigue sobrescribiendo docker info() (xtop/src/providers/composite.rs ) — esas referencias no pueden compilar contra la nueva revision de api; nada mas consumia la superficie kernel m2.4: eliminar la asignacion siempre vacia dockers y la sobrescritura docker info del composite canonicalizacion de helpers de glifos: los packs deben importar to color /border for /marker for /ascii border de xtop widget api::glyph en lugar de reimplementarlos widget-api/src/glyph.rs (doc del modulo: los packs no deben reimplementar) el pack base widgets/src/util.rs implementa a mano los cuatro helpers; el pack de bloques (widgets/packs/xtop-widget-blocks/src/lib.rs ) implementa a mano to color /ascii border /border for (sin marker for — su grafico fija marker::block ) y los dos packs ya divergen en plain widgets m3.3 (el pack base borra sus copias; los helpers privados del pack format bytes /format uptime /gauge gradient se quedan), m3.4 (el pack de bloques borra sus copias + el hack const : chartcharset y respeta state.charset() ). detalle en los puntos de llamada: el to color canonico toma [u8; 3] por valor (las copias de los packs tomaban &[u8; 3] ); border for toma el enum, no (state, widget, native) ratatui 0.30.2 en api (dep del workspace) mientras los consumidores fijan 0.29 api/cargo.toml (ratatui = \"0.30.2\" ) los crates consumidores que tambien usan ratatui directamente compilan dos versiones de ratatui hasta que se alineen; las semanticas de border::plain ya cambiaron en 0.29 (ver d3) kernel m2.6 (layout::vertical/horizontal etc. + alineacion crossterm), widgets m3.1, samurai m4.2. api es la referencia: ya esta en 0.30.2 (m1.1). mcp (extensions) no usa ratatui — sin impacto politica de rust-version = \"1.87\" + edition 2021 workspace + todos los manifests de miembros (d6) los consumidores con toolchains antiguas deben subir de version (samurai ya necesita 1.87) kernel m2.8, widgets m3.1, samurai + mcp m4.8 alertthresholds se convierte en el tipo de contrato serde con claves simples cpu high /mem high /disk high ; sin derive default (d8) plugin-api/src/host.rs + test de ida y vuelta el kernel conserva una copia local de la misma forma (xtop/src/config/schema.rs ) con default 90/90/90 y convierte en plugins/host.rs + state/widget state.rs kernel m2.2: borrar config::alertthresholds ; persistir xtop plugin api::alertthresholds ; desaparece el marshalling manual en host.rs/widget state.rs; las claves json quedan identicas el doc de data dir() ya no sobre-especifica una ruta concreta plugin-api/src/context.rs cambio solo de documentacion; el comportamiento no cambia (la ruta la sigue proporcionando el host) ninguna los enums de glifos no cambian en la raiz (chartcharset , widgetborders re-exportados; claves serde snake case half block , plain , ...) widget-api/src/lib.rs , glyph.rs ninguno — el formato de persistencia de configuracion es estable ninguna punteros de referencia para las migraciones: hito m2 del kernel (m2.2–m2.6, m2.8), widgets m3 (m3.1–m3.4), plugins + extensions m4 (m4.1, m4.2, m4.8), cableado de efectos del kernel m5.3, y los gates de grep entre repos en m7.4, todos en el roadmap del workspace."},{"locale":"es","repo":"api","title":"modelo de datos — xtop plugin api::model","label":"docs · data-model","path":"docs/data-model.md","slug":["api","docs","data-model"],"text":"modelo de datos — xtop plugin api::model el modelo de datos compartido del sistema vive en xtop plugin api::model (api/crates/plugin-api/src/model.rs ). dr-1 convierte este modulo en la unica fuente de verdad: cada struct siguiente se define una sola vez, aqui, y lo importa cualquier otro repo — nunca se redefine. todos los structs del modelo derivan debug, clone ; solo systeminfo deriva default (para que los providers puedan entregar una \"informacion de sistema\" vacia sin inventar valores). ninguno de los tipos del modelo deriva serde: cruzan las fronteras entre repos como valores rust planos. los derives de serde estan reservados para los tipos que se persisten o se configuran: alertthresholds (config del kernel, ver mas abajo) y los enums de glifos de xtop-widget-api . los hechos de poblacion de este documento se fundamentan en el provider sysinfo del kernel (xtop/src/providers/sysinfo/provider.rs ) y en las sondas de plataforma bajo xtop/src/providers/sysinfo/platform/ , que implementan el systemdataprovider del contrato. los \"extras\" (baterias, gpus, ips por interfaz, recuentos de hilos, opciones de montaje, governors) provienen de los arboles de plataforma; el crate sysinfo aporta el resto. cobertura de plataformas: linux lee /sys y /proc ; macos implementa baterias (pmset ), ips de interfaz (getifaddrs ), opciones de montaje (mount(8) ), recuentos de hilos (proc pidinfo ) y usuarios de directory services (dscl ) bajo platform/macos/ ; windows implementa baterias (getsystempowerstatus ), ips de interfaz (getadaptersaddresses ), opciones de montaje (getlogicaldrives /getvolumeinformationw ), recuentos de hilos (instantaneas toolhelp) y usuarios de cuentas locales (get-localuser , rids numericos) bajo platform/windows/ . las plataformas de respaldo devuelven valores vacios. inventario de structs struct rol --- --- systemsnapshot una muestra de toda la maquina, ensamblada una vez por tick. contenedor de cada grupo de metricas siguiente. cpuinfo un nucleo de cpu logico. memoryinfo totales/uso de la memoria fisica, con un porcentaje. swapinfo totales/uso del swap, con un porcentaje. diskinfo un sistema de archivos montado. diskioinfo bytes acumulados por montaje y rendimiento actual. networkinfo una interfaz de red: bytes acumulados, rendimiento actual, ips. processinfo un proceso, con el conjunto completo de campos forenses (grupos p0/p1/p2 siguientes). loadavg medias de carga de 1/5/15 minutos. batteryinfo una bateria (sonda de plataforma). gpuinfo una gpu (sonda nvidia-smi, respaldo sysfs). systeminfo identidad de la maquina que cambia lentamente (hostname, so, kernel, escritorio, shell, modelo de cpu, potencia del paquete). campos de systemsnapshot : cpus , memory , swap , disks , networks , processes , load avg , uptime , cpu temp , disk io , batteries , gpus , sys info . que rellena el provider del kernel el sysinfoprovider del kernel rellena la instantanea como sigue (las referencias de linea remiten a xtop/src/providers/sysinfo/provider.rs del repo hermano del kernel, referencia de solo lectura): struct / campo fuente --- --- cpuinfo { name, usage, cpu id, frequency, governor, temp c } cpus() de sysinfo por nucleo; cpu id es el indice de enumeracion; governor desde la sonda de plataforma read cpu governor(i) ; temp c °c por nucleo desde la sonda de plataforma read core temps — coretemp de linux cuando los sensores se corresponden con los nucleos logicos, none en cualquier otro caso (macos y windows no tienen fuente de sensor por nucleo — las zonas termicas de windows son agregadas, se leen via wmi por los components de sysinfo para cpu temp — ni los hosts linux sin sensores por nucleo legibles) memoryinfo { total, used, available, free, percent } getters de memoria de sysinfo; percent = used/total 100 , 0.0 cuando total es 0 swapinfo { total, used, free, percent } getters de swap de sysinfo; la misma regla de porcentaje diskinfo { mount point, total space, available space, used space, percent, file system, mount options } disks de sysinfo; used = total - available ; mount options se consulta desde la sonda de plataforma read mount options() diskioinfo { name, read bytes, write bytes, read speed, write speed } diskusage de sysinfo; las velocidades son deltas de bytes desde el refresh anterior sobre el tiempo transcurrido networkinfo { name, received, transmitted, rx speed, tx speed, ip } networks de sysinfo; las velocidades se calculan a partir de deltas desde el refresh anterior; ip desde read interface ips() processinfo processes() de sysinfo (ver los grupos p0/p1/p2 siguientes) loadavg { one, five, fifteen } system::load average() batteryinfo read batteries() de plataforma — linux /sys/class/power supply , macos pmset , windows getsystempowerstatus (una unica bateria agregada); plataformas de respaldo vacias gpuinfo read gpu info() : primero la sonda compartida nvidia-smi y luego el respaldo sysfs de plataforma cuando la lista esta vacia systeminfo { hostname, os version, kernel, desktop env, shell, cpu model, package power w } host/so/kernel/escritorio/shell capturados una vez en la construccion del provider : system::host name() , system::long os version() , system::kernel version() , xdg current desktop /desktop session , shell /comspec ; almacenados en cache en el provider. cpu model = la cadena brand de cpu de sysinfo del primer nucleo logico en la construccion (system::cpus()[0].brand() , p. ej. \"intel(r) core(tm) i7-14650hx\"); sysinfo la rellena en todas las plataformas que soporta — una brand vacia produce none . package power w se muestrea en cada refresh desde la sonda rapl de linux (ver mas abajo); none cuando no existe ninguna fuente rapl legible systeminfo::package power w (sonda rapl de linux) potencia instantanea del paquete en vatios, calculada a partir de deltas de los contadores de energia rapl de intel a la cadencia del refresh. fuentes, por orden de prioridad: (1) /sys/class/powercap/intel-rapl: /energy uj para cada dominio cuyo archivo name lee package-0 (uno por socket; las lecturas se suman), con respaldo al dominio intel-rapl: de indice mas bajo cuando ningun name coincide; (2) energy input de hwmon bajo /sys/class/hwmon/hwmon /name == powercap , solo el primer sensor. las lecturas estan en microjulios; los vatios = delta energy uj / 1 000 000 / elapsed secs con deltas a prueba de wraparound (los contadores dan la vuelta en max energy range uj ). la primera muestra tras el arranque establece una linea base y produce none (sin contador previo); una fuente ilegible (driver ausente, permiso denegado, fallo de lectura transitorio) tambien produce none y reinicia la linea base — el valor nunca se fabrica. macos y windows mantienen none (ninguna fuente rapl/potencia de paquete publica en ninguna de las dos plataformas) y las plataformas de respaldo ponen la sonda a none systemsnapshot::cpu temp temperatura maxima sobre los components de sysinfo systemsnapshot::uptime system::uptime() (segundos) processinfo — grupos de campos el struct lleva 22 campos agrupados, todos rellenados por el kernel (los marcadores p0/p1/p2 siguientes siguen el codigo fuente del provider): - base: pid , name , cpu usage , memory , user id , state , cmd (primer elemento de argv). - p0 (identidad del proceso): exe path , parent pid , cmd full (argv completo). - p1 (forense): start time (segundos de epoca — el provider convierte el inicio relativo al arranque de sysinfo mediante boot epoch = now - uptime para que los consumidores comparen contra un unico reloj), run time (now - start time ), effective user id , group id , cwd , thread count (sonda de plataforma read thread count ). - p2 (recursos): open files , open files limit , disk total read bytes , disk total write bytes , environ , session id . recuento de campos: 22 (7 base + 3 p0 + 6 p1 + 6 p2). las necesidades de la fila de procesos ux9.1 (programa + comando + nombre de usuario) quedan totalmente cubiertas por este conjunto — no se anadio ningun campo: cmd /cmd full /exe path portan el programa y su linea de comandos, y user id (ademas de effective user id ) porta el uid numerico como cadena. el mapeo uid → nombre de login no forma parte del modelo de datos (es un mapeo de visualizacion): los widgets lo resuelven a traves de widgetstate::uid to name(uid) (widget-api; el kernel lee /etc/passwd en unix, ademas de los usuarios de directory services en macos y las cuentas locales en windows, indexados por el uid numerico — windows expone el rid numerico del sid como user id ) y se quedan con el uid numerico cuando no existe ningun nombre. las muestras de cpu por proceso recientes que dibuja un spark de braille tambien viven en la vista del widget, no en el modelo: widgetstate::process cpu history(pid) devuelve la serie por pid acotada que el kernel alimenta cada tick (ver widget-contract.md). dos reglas de orden se aplican a snapshot().processes : 1. el provider pre-ordena por cpu usage descendente y trunca hasta un tope (default max processes = 200 , sobrescribible mediante xtop max processes ) para que el trabajo por tick siga siendo acotado. 2. el plugincontext::top processes(n) del contrato re-ordena y trunca por si mismo, asi que la garantia orientada al plugin (\"top n por cpu\") nunca depende del orden del productor (ver plugin-contract.md). ciclo de vida de la instantanea se produce una systemsnapshot por tick de monitorizacion: 1. el kernel hace tick al intervalo configurado (por defecto update interval ms = 1000 en el config por defecto del kernel; appstate::on tick en xtop/src/state/app.rs ). 2. provider.refresh all() refresca sysinfo, los discos, las redes y los components, y registra los contadores de bytes como lineas base para el calculo de la tasa siguiente (mapas prev net , prev disk y last refresh ). 3. provider.snapshot() ensambla los structs anteriores en una systemsnapshot . 4. el kernel guarda en cache la muestra en appstate (last snapshot ) y cada widget/accion de ese frame lee la misma muestra cacheada (appstate::snapshot cache() ); la ruta de renderizado nunca vuelve a muestrear el sistema. los buffers de historial (cpu por nucleo, porcentaje de memoria, tasas rx/tx de red sumadas) se alimentan desde la misma muestra. 5. los handlers de tick de los plugins se ejecutan despues contra un plugincontext cuyos metodos de lectura devuelven la misma familia de instantaneas. antes del primer tick no hay muestra: la vista de widgets (widgetstate::snapshot() ) devuelve none , y la vista de host de plugins (hoststate::snapshot() ) recurre a forzar una muestra nueva (appstate::snapshot() clona la cache o muestrea una vez). eliminado deliberadamente — superficie docker m1.4 elimino del contrato la superficie muerta del modelo de docker: - el struct dockerinfo (estaba en model.rs ), - el campo systemsnapshot::dockers , - el metodo por defecto systemdataprovider::docker info y su re-export en la raiz. motivo: nada en el ecosistema los consumia. el provider sysinfo del kernel siempre asignaba un dockers: vec![] vacio, y ningun widget, plugin o extension leia el campo ni el metodo. un grep en todo el repo de \"docker\"/\"dockerinfo\" esta ahora vacio dentro de api . el arbol de trabajo del kernel todavia contiene las referencias obsoletas (dockers: vec![] en xtop/src/providers/sysinfo/provider.rs y la sobrescritura de docker info() en xtop/src/providers/composite.rs ); esas son eliminaciones del lado del kernel registradas como m2.4 y desapareceran cuando el kernel empiece a consumir esta revision del contrato (ver changes.md). alertthresholds — contrato serde alertthresholds vive en xtop plugin api::host (no en model ) y se re-exporta en la raiz del crate. deriva debug, clone, serialize, deserialize (m1.2) — y deliberadamente nada mas: no se anadieron default , copy ni partialeq al tipo de contrato (decision d8); el kernel conserva sus propios valores por defecto hasta que m2 adopte el tipo de api. contrato serde: nombres de campo simples, sin atributos rename . las claves json son exactamente cpu high , mem high , disk high — las mismas claves que el kernel persiste hoy en su config json (config.alerts en xtop/src/config/schema.rs usa un layout de struct identico; m2.2 sustituye esa copia del kernel por este tipo). el test de ida y vuelta dentro del crate fija la forma serializada exacta:"},{"locale":"es","repo":"api","title":"contrato de efectos — xtop-effect-api","label":"docs · effect-contract","path":"docs/effect-contract.md","slug":["api","docs","effect-contract"],"text":"contrato de efectos — xtop-effect-api xtop-effect-api (api/crates/effect-api/src/lib.rs ) define el contrato de efectos compartido (dr-5). los efectos son transiciones visuales de buffer completo y con estado que el kernel aplica a cada frame renderizado — animaciones de intro, fundidos de entrada, cortinillas, post-procesado sobre toda la imagen del terminal. el crate es minimo a proposito (decision d4): exactamente dos elementos publicos — effect y effectmanifest . todavia no existe ningun tipo de pegamento de registro; se anadira uno cuando el cableado del kernel (m5) lo necesite. el crate depende solo de ratatui (por el tipo buffer ) y nunca del kernel. el contrato effectmanifest son datos 'static planos (los ids/nombres son constantes de tiempo de compilacion), asi que los manifests se comparan iguales entre copias del mismo efecto — los tests dentro del crate afirman partialeq sobre ellos. effect exige send + sync (el kernel puede alojarlo tras un handle compartido) y otorga al efecto la propiedad completa de su propio estado: todo lo que un efecto necesita recordar vive en self . contrato del host (quien llama a que, y cuando) los docs del crate definen con precision el lado del host, y se espera que el kernel (m5.3, tras una feature) implemente exactamente esto: 1. on frame se ejecuta en cada frame renderizado , despues de que los widgets y el layout se hayan dibujado en el buffer de ratatui y antes del flush del terminal. el efecto puede reescribir cualquier celda de buffer . 2. elapsed es el tiempo desde que el efecto empezo — no el delta de frame. un efecto que deba converger despues de p. ej. 800 ms se rige por elapsed = 800ms , sin importar cuantos frames haya hecho falta. 3. los efectos tienen estado entre frames : conservan en self todo el progreso que necesiten entre llamadas; el host no persiste ningun estado por ellos. 4. el host no marca el ritmo de los frames : hace tick a una cadencia de 1 s mas repintados ad-hoc (pulsaciones de tecla, cambios de layout). las transiciones visuales deben por tanto avanzar segun el tiempo de pared (elapsed ), nunca solo por el numero de frames — un fundido basado en el recuento de frames se arrastraria o saltaria segun la suerte de los repintados. no hay contrato de renderizado mas alla del buffer: el efecto ve la imagen final compuesta (superposiciones incluidas, una vez dibujadas) y la reescribe en el sitio. implementar un efecto un efecto es un struct que implementa el trait, conserva su progreso, y un test que impulsa frames falsos. el doble de test dentro del crate (framecounter ) es el patron de referencia: un struct con estado simple, un manifest const , y un cuerpo que muta self por llamada. impulsalo con frames sinteticos en los tests — pasale al efecto un buffer::empty(rect::new(0, 0, w, h)) y duration s crecientes, y luego afirma sobre la convergencia del buffer o sobre tus propios contadores (el test framecounter dentro del crate es el patron). nunca dependas del ritmo real de frames en los tests ni en el propio efecto. estado - contrato : hecho en este crate (m1.6), con los docs del crate estableciendo el contrato del host anterior. - implementaciones : ninguna todavia. el repo effects es un repo vivo sin crates; el primer efecto real (xtop-effect-fade ) y el effecthost del kernel detras de una feature son el hito m5. - consumidores : ninguno en la naturaleza todavia — el crate se valida con sus propios tests hasta m5. ningun efecto puede asumir una garantia de ritmo mas alla del contrato del host anterior."},{"locale":"es","repo":"api","title":"contrato de extensiones — xtop-extension-api","label":"docs · extension-contract","path":"docs/extension-contract.md","slug":["api","docs","extension-contract"],"text":"contrato de extensiones — xtop-extension-api xtop-extension-api (api/crates/extension-api/ ) es el protocolo de extensiones compartido (dr-4). las extensiones son comportamientos opcionales que el kernel puede alojar — hoy eso significa servidores : integraciones de larga duracion (el servidor mcp en xtop-extension-mcp ) que el kernel arranca bajo demanda y que dirigen la aplicacion a traves de una vista extensionhost . el crate es deliberadamente minimo e independiente: declara cero dependencias y nunca depende del kernel. su doc de crate preve que las extensiones puedan usar tambien xtop-plugin-api para el modelo de datos compartido, pero ninguna extension lo necesita todavia, asi que no existe esa dependencia en el manifest. superficie publica (toda re-exportada en la raiz): extension , extensionhost , extensioncontext , extensionmanifest , extensionerror . el modelo tal como existe hoy — estilo servidor solo manifest es obligatorio; run server tiene por defecto un error unknown , asi que una extension que declara un servidor debe sobrescribirlo. el kernel cablea extensiones concretas: para mcp despacha el subcomando xtop mcp a run server(\"mcp\", ...) (kernel commands/mcp.rs ), que bloquea durante toda la vida del bucle del servidor (en el caso de mcp: un bucle json-rpc 2.0 de leer-evaluar-imprimir sobre stdio). extensionmanifest : el vector servers es lo que consultaria un host para descubrir que valores de server id acepta la extension; run server es el punto de entrada para exactamente esos ids. que puede dirigir una extension — extensionhost / extensioncontext el kernel implementa extensionhost para su estado de aplicacion vivo (impl extensionhost for appstate en el xtop/src/plugins/extension host.rs del kernel) y entrega a las extensiones un extensioncontext sobre el. las extensiones actuan a traves del contexto — nunca sobre tipos del kernel: intencion de diseno, enunciada en los docs de extensionhost : los plugins siguen siendo la unidad del comportamiento de dominio ; las extensiones actuan a traves de ellos con execute plugin en lugar de reimplementar la logica de dominio. la extension mcp sigue esto: cada llamada a una tool de mcp se corresponde con una tupla (plugin id, action, params) que se ejecuta contra el plugin samurai alojado, tras un ctx.tick() opcional para refrescar los datos (extensions/extensions/xtop-extension-mcp/src/lib.rs ). el mapeo del kernel de errores de plugin a errores de extension (map plugin error en xtop/src/plugins/extension host.rs ) es la referencia del comportamiento de error entre contratos: recoverable /fatal se mapean uno a uno, unknownaction se convierte en un mensaje recoverable . extensionerror display : mensaje verbatim para recoverable , fatal: para fatal , unknown: para unknown . implementa std::error::error . el servidor mcp mapea sus propios fallos a estas variantes (fatal para los fallos de lectura/escritura de stdio, recoverable para json-rpc malformado). nota honesta de alcance — los hooks son trabajo futuro el doc del crate describe dos sabores de extension: hooks que tocan la config, el tema, el layout o el renderizado (\"disenados segun se necesiten\") y servidores . hoy solo existe el sabor de servidor: no hay ningun trait de hook, ningun registro de hooks, y extensionmanifest no porta metadatos de hook. la superficie de estilo hook se anadira a este crate cuando un consumidor real (feature del kernel) la necesite — hasta entonces se queda fuera del contrato a proposito (sin superficie especulativa, la misma regla que la decision d4 para los efectos). implementar una extension una extension implementa extension y ejecuta su bucle de servidor contra el contexto. el patron, siguiendo a mcpextension : el lado del kernel ya lo demuestra el comando mcp: crear la extension, crear extensioncontext::new(&mut state) sobre el estado de la aplicacion vivo, llamar a run server(server id, &mut ctx) y propagar el error hacia arriba. las extensiones consumen xtop-extension-api como dependencia git desde el repo extensions (ver architecture.md para el patron temporal de path-dep)."},{"locale":"es","repo":"api","title":"contrato de plugins — xtop-plugin-api","label":"docs · plugin-contract","path":"docs/plugin-contract.md","slug":["api","docs","plugin-contract"],"text":"contrato de plugins — xtop-plugin-api xtop-plugin-api (api/crates/plugin-api/ ) es el protocolo de plugins compartido de xtop (dr-1). dos partes implementan contra el: - el host (el kernel xtop ) proporciona un hoststate y entrega a los plugins un plugincontext sobre el; - los plugins (el repo xtop-cli/plugins , p. ej. xtop-plugin-samurai ) implementan plugin y actuan a traves de ese contexto. el crate nunca depende del kernel, asi que cualquier repo puede consumirlo de forma independiente. solo depende de ratatui (por el tipo frame en la closure de render del widget) y de serde (por alertthresholds ). superficie publica elemento modulo notas --- --- --- trait plugin plugin.rs trait central del plugin pluginmanifest manifest.rs metadatos estaticos incl. capacidades declaradas plugincontext context.rs acceso comprobado por capacidades al estado del host + dir de datos del plugin trait hoststate host.rs vista de solo-lectura-mas-acciones del kernel, implementada por el host runtimeconfig host.rs vista de tema/layout/intervalo/hostname alertthresholds host.rs tipo de contrato serde, ver data-model.md trait systemdataprovider provider.rs contrato de la fuente de datos (provider sysinfo del kernel, extras de plugins) plugincapability capability.rs capacidades declaradas + aplicadas pluginerror error.rs recoverable / fatal / unknownaction pluginwidget widget.rs registro de widget de plugin (renderiza sobre &dyn hoststate ) hex to rgb color.rs rrggbb → [u8; 3] ; las entradas invalidas/cortas recurren al negro por canal modulo model model.rs el modelo de datos compartido, ver data-model.md todo excepto model tambien se re-exporta en la raiz del crate (xtop plugin api::plugin , xtop plugin api::plugincapability , ...), que es como lo importan el plugin de ejemplo de este archivo y xtop-plugin-samurai . pluginmanifest el kernel lee el manifest cuando el plugin se registra para construir el contexto (capacidades) y para crear el directorio de datos del plugin bajo su base de datos de plugins. los consumidores deberian tomar la version de env!(\"cargo pkg version\") en lugar de fijarla a mano (m4.1 alinea a samurai). trait plugin semanticas del ciclo de vida (de los docs de los metodos): - manifest — metadatos estaticos; el unico metodo sin implementacion por defecto. - on enable / on disable — se llaman una vez cada uno, al cargar/habilitar y al deshabilitar/apagar. - on tick — se llama en cada tick (el kernel hace tick al intervalo configurado; 1 s por defecto, y los plugins como samurai dosifican su propio trabajo — cada 5.º tick — ademas de eso). - on key — eventos de tecla como cadenas; devuelve ok(true) cuando se consume. - data provider — datos extra de sistema opcionales; el kernel fusiona el provider devuelto en el flujo de datos principal a traves de su provider composite (solo en el lado del host para plugins que declararon readsysteminfo ). - widget — widget tui personalizado opcional; renderiza contra la vista hoststate (ver pluginwidget mas abajo). - execute — comandos con nombre y parametros de cadena, usados por agentes externos (ia/cli/ipc); devuelve una cadena tipo json. la implementacion por defecto responde unknownaction . debug + send es toda la carga de supertraits: el manager del host encapsula un plugin en una caja (box ) y el host registra los fallos sin tumbar la aplicacion (aislamiento de errores por plugin en el pluginmanager del kernel). plugincontext y aplicacion de capacidades plugincontext envuelve un &'a mut dyn hoststate mas el directorio de datos del plugin y sus capacidades declaradas. el estado vivo del kernel solo es alcanzable a traves de este contexto. cada metodo que toca el estado comprueba primero las capacidades declaradas del plugin; una capacidad ausente produce pluginerror::recoverable con el mensaje plugin does not have required capability: (asi los plugins denegados pueden recuperarse — el error no es fatal). metodo firma (real) capacidad requerida denegado → --- --- --- --- snapshot fn snapshot(&self) - result readsysteminfo recoverable system info fn system info(&self) - result readsysteminfo recoverable top processes fn top processes(&self, n: usize) - result , pluginerror readsysteminfo recoverable kill process fn kill process(&mut self, pid: u32) - result killprocesses recoverable set alert thresholds fn set alert thresholds(&mut self, cpu: f64, mem: f64, disk: f64) - result modifyconfig recoverable set theme by name fn set theme by name(&mut self, name: &str) - result modifyconfig recoverable set layout by name fn set layout by name(&mut self, name: &str) - result modifyconfig recoverable set update interval fn set update interval(&mut self, ms: u64) - result modifyconfig recoverable alerts fn alerts(&self) - alertthresholds ninguna — config fn config(&self) - runtimeconfig ninguna — data dir fn data dir(&self) - &path ninguna — detalles del contrato en los que confiar: - las lecturas devuelven result (decision d1). snapshot , system info y top processes aplican readsysteminfo exactamente igual que los metodos mutantes aplican las suyas, asi que los llamadores deben manejar err . - top processes(n) ordena por uso de cpu descendente antes de tomar n (sort by sobre cpu usage via total cmp , y despues truncate(n) — decision d2). es una ordenacion estable: los procesos con igual cpu conservan el orden de la instantanea. la garantia no depende del orden que produce la fuente de datos. los tests dentro del crate fijan este comportamiento. - data dir() lo proporciona el host. la ruta la decide el kernel y se pasa a plugincontext::new ; el contrato no prescribe deliberadamente una ubicacion concreta (m1.2 arreglo un doc sobre-especificado). los plugins pueden persistir estado por plugin dentro de el; el kernel lo crea en el registro (pluginmanager::register une el directorio base con el id del plugin). - alerts , config y data dir son lecturas sin gate disponibles para todos los plugins. - plugincontext::new(host, plugin data dir: pathbuf, capabilities: vec ) es publico; hoy el kernel es su unico llamador. variantes de plugincapability ( [non exhaustive] , deriva clone, debug, partialeq ): readsysteminfo , killprocesses , modifyconfig , renderwidgets , y custom(string) para lo no cubierto. existen dos puntos de aplicacion: las llamadas check capability anteriores (en este crate) para los metodos del contexto, y filtros del lado del host en el pluginmanager del kernel, que solo recoge data provider() s de plugins que declararon readsysteminfo y solo recoge widget() s de plugins que declararon renderwidgets . custom(string) no tiene todavia ningun check integrado en ningun sitio — es la via de escape para permisos especificos del ecosistema que el host pueda empezar a honrar mas adelante. hoststate la superficie del lado del kernel que un plugin puede tocar; el kernel la implementa para su estado de aplicacion vivo (impl hoststate for appstate en el xtop/src/plugins/host.rs del kernel). los plugins nunca dependen de tipos del kernel — solo de este trait. el trait tiene 9 metodos : metodo firma significado --- --- --- snapshot fn snapshot(&self) - systemsnapshot muestra completa (el host puede servir la cacheada) system info fn system info(&self) - systeminfo hostname/os/kernel/escritorio/shell kill process fn kill process(&mut self, pid: u32) - bool enviar terminacion; true si se envio la senal set alert thresholds fn set alert thresholds(&mut self, cpu: f64, mem: f64, disk: f64) sobrescribir los tres umbrales alerts fn alerts(&self) - alertthresholds umbrales actuales config fn config(&self) - runtimeconfig nombre de tema, nombre de layout, interval ms , hostname set theme by name fn set theme by name(&mut self, name: &str) - bool true si el tema existe set layout by name fn set layout by name(&mut self, name: &str) - bool true si el layout existe set update interval ms fn set update interval ms(&mut self, ms: u64) intervalo de tick los tests dentro del crate implementan un fakehost minimo con los 9 metodos sobre una systemsnapshot construida a mano — el patron a copiar al escribir dobles del host. pluginerror display : recoverable imprime el mensaje verbatim, fatal imprime fatal: , unknownaction imprime unknown action: . pluginerror implementa std::error::error . el extension host del kernel lo mapea a extensionerror al reenviar acciones de plugins a extensiones (kernel xtop/src/plugins/extension host.rs ). systemdataprovider fuente de datos de sistema en tiempo real (provider.rs ). lo implementan el provider sysinfo del kernel y los plugins que aportan metricas extra; el kernel fusiona los providers de los plugins en el flujo principal a traves de su provider composite. metodo por defecto --- --- refresh all(&mut self) requerido snapshot(&self) - systemsnapshot requerido disk io(&self) - vec vec![] batteries(&self) - vec vec![] gpu info(&self) - vec vec![] system info(&self) - systeminfo systeminfo::default() kill process(&self, pid: u32) - bool false as any(&self) - &dyn any / as any mut requerido (downcast para la composicion de providers) add extras(&mut self, vec ) no-op (los providers composite lo sobrescriben) pluginwidget un widget que un plugin registra para el renderizado tui. su closure de render dibuja contra la vista hoststate del plugin. este tipo es deliberadamente distinto del widgetregistration de xtop-widget-api (que dibuja sobre widgetstate ); son dos contratos diferentes y no deben compartir nombre (dr-2, m1.3 — este tipo se renombro desde un widgetregistration duplicado). el motor de renderizado del kernel da precedencia a los widgets de los plugins sobre cualquier pack (ver widget-contract.md). implementar un plugin — paso a paso los pasos reflejan lo que xtop-plugin-samurai (plugins/plugins/xtop-plugin-samurai/src/lib.rs ) hace hoy y lo que espera el host. 1. declarar la dependencia. los crates de plugins viven en el repo plugins y consumen el crate como dependencia git (flotante durante este ciclo; ver architecture.md para el patron temporal de path-dep): 2. implementar plugin . importar desde la raiz del crate (el estilo de import real que usa samurai): observa el flujo de capacidades: como manifest() solo declara readsysteminfo , ctx.top processes(5) tiene exito, mientras que ctx.set alert thresholds(...) responderia pluginerror::recoverable(\"plugin does not have required capability: modifyconfig\") . anade plugincapability::modifyconfig a las capacidades declaradas para permitirlo. 3. opcional: aportar un widget o datos. devuelve some(pluginwidget) desde widget() — el kernel solo lo recoge cuando el manifest declara renderwidgets — o some(box::new(my provider)) desde data provider() — solo se recoge con readsysteminfo declarado — con el provider fusionado a traves del provider composite del kernel. 4. cableado del host. el kernel registra el plugin como box a traves de su pluginmanager (crea el dir de datos desde el id del manifest, construye plugincontext::new(state, dir, capabilities) , llama a on enable ), y despues impulsa on tick por tick, on key por tecla, execute por comando con nombre y on disable al apagar. los autores de plugins no tocan hoststate ; solo ven el contexto. escribir un doble de test que implemente hoststate se hace copiando el patron fakehost de los tests dentro del crate en context.rs ."},{"locale":"es","repo":"api","title":"contrato de render de widgets — xtop-widget-api","label":"docs · widget-contract","path":"docs/widget-contract.md","slug":["api","docs","widget-contract"],"text":"contrato de render de widgets — xtop-widget-api xtop-widget-api (api/crates/widget-api/ ) es el contrato de renderizador para packs de widgets (dr-2). los widgets son las partes visuales que se dibujan dentro de las areas del layout: el kernel incluye un pack base, y cualquier otro pack (pack de bloques, comunidad, plugin) proporciona renderizadores para los mismos nombres de widget o para otros nuevos. tres piezas componen el crate: - state::widgetstate — la vista de solo lectura que recibe un renderizador; - glyph — los enums de estilo de glifos mas los mapeos canonicos a los primitivos de dibujo de ratatui; - renderer::{widgetregistration, widgetrenderer} — como registran los packs renderizadores por nombre de widget. la raiz del crate re-exporta widgetstate , widgetregistration , widgetrenderer y los enums de glifos (chartcharset , widgetborders ); los helpers de mapeo de glifos solo son alcanzables bajo el modulo glyph (xtop widget api::glyph::to color etc. — decision d5). como todo crate de contrato, nunca depende del kernel. depende de ratatui (por frame , rect , marker , los conjuntos de bordes), de serde (por los enums de glifos) y de xtop-plugin-api (por el modelo de datos que nombran sus metodos de widgetstate ). widgetstate — la vista del renderizador (28 metodos) cada renderizador dibuja contra este trait — nunca contra tipos del kernel. el kernel lo implementa sobre su appstate vivo (xtop/src/state/widget state.rs ) y el motor de renderizado se lo entrega a los renderizadores de los packs. de solo lectura por diseno: los widgets nunca mutan el estado de la aplicacion. los 28 metodos del trait, agrupados como en el codigo fuente: grupo metodo firma significado --- --- --- --- muestra snapshot fn snapshot(&self) - option muestra actual por tick; none antes del primer tick tema theme name fn theme name(&self) - &str nombre del tema activo tema theme fg fn theme fg(&self) - &[u8; 3] primer plano del tema como tripleta rgb tema theme bg fn theme bg(&self) - &[u8; 3] fondo del tema como tripleta rgb tema theme palette fn theme palette(&self) - &[[u8; 3]; 16] paleta de 16 entradas tema alerts fn alerts(&self) - alertthresholds umbrales de alerta actuales estilo de glifos charset fn charset(&self, widget: &str) - chartcharset charset resuelto para un widget estilo de glifos borders fn borders(&self, widget: &str) - widgetborders estilo de borde resuelto para un widget historial cpu history fn cpu history(&self) - &[vecdeque ] historial por nucleo (x, y) historial mem history fn mem history(&self) - &vecdeque historial del porcentaje de memoria historial net rx history fn net rx history(&self) - &vecdeque historial de la tasa de recepcion sumada (bytes/s) historial net tx history fn net tx history(&self) - &vecdeque historial de la tasa de transmision sumada (bytes/s) historial disk read history fn disk read history(&self) - &vecdeque historial de la tasa de lectura de disco sumada (bytes/s, agregada entre discos); por defecto vacio historial disk write history fn disk write history(&self) - &vecdeque historial de la tasa de escritura de disco sumada (bytes/s, agregada entre discos); por defecto vacio historial load history fn load history(&self) - &vecdeque historial de la media de carga de 1 minuto (load avg.one ); por defecto vacio vista/control search query fn search query(&self) - &str texto activo de busqueda de procesos vista/control process selected pid fn process selected pid(&self) - option fila seleccionada, anclada por pid vista/control process sort label fn process sort label(&self) - &str etiqueta de la columna de ordenacion activa vista/control process sort desc fn process sort desc(&self) - bool si la ordenacion activa de procesos es descendente; por defecto false (ascendente) reproduce el comportamiento previo a la direccion para los implementadores que no registran una direccion vista/control layout name fn layout name(&self) - &str nombre del layout activo vista/control is searching fn is searching(&self) - bool superposicion de busqueda activa vista/control fullscreen label fn fullscreen label(&self) - option etiqueta del widget a pantalla completa, none cuando no esta a pantalla completa vista/control sys info fn sys info(&self) - systeminfo copia propiedad de la identidad de la maquina vista/control process view fn process view(&self) - vec filas de procesos que dibuja el widget de procesos mapeo de procesos uid to name fn uid to name(&self, uid: u32) - option nombre de login para un uid numerico (el kernel lo resuelve desde /etc/passwd en unix, mas los usuarios de directory services via dscl en macos y las cuentas locales via get-localuser en windows, indexados por uid numerico); por defecto none — los renderizadores recurren al uid numerico (ux9.1) mapeo de procesos process cpu history fn process cpu history(&self, pid: u32) - vec muestras recientes de uso de cpu por proceso (porcentaje de un nucleo logico), de mas antigua a mas nueva, 30 muestras por pid; por defecto vacio — los renderizadores no dibujan nada para una serie vacia (ux9.1) opciones de layout logical core count fn logical core count(&self) - usize procesadores logicos que reporta el host; por defecto 1 reproduce el comportamiento previo a dr-ux1 para los renderizadores que lo ignoran opciones de layout widget options fn widget options(&self) - option objeto options del widget que se esta renderizando (none cuando el nodo de layout no porta ninguno); por defecto none ; los renderizadores deben tratar none como comportamiento por defecto los cinco metodos bajo \"opciones de layout\" y \"mapeo de procesos\" (adiciones dr-ux1/ux3/ux5/ux9.1) vienen con implementaciones por defecto , asi que los implementadores existentes compilan sin cambios y la salida sin options es byte-identica al comportamiento previo a las opciones. logical core count permite a un renderizador normalizar un uso de cpu por proceso (o por nucleo) — una fraccion de un nucleo logico — hasta una proporcion de la cpu de toda la maquina (visualizacion cpubasis::total ); el kernel lo implementa desde available parallelism . widget options porta las opciones de visualizacion por instancia desde el nodo de layout mientras ese widget se dibuja; el valor nunca es null , solo es valido durante la llamada de renderizado, y las claves desconocidas deben ignorarse. process sort desc alimenta el marcador de direccion del widget de procesos (▼ descendente / ▲ ascendente) en el encabezado de la columna ordenada; el kernel devuelve su flag vivo cycle sort . los tres metodos de historial anadidos para la superficie de datos ux8 (disk read history , disk write history , load history ) tambien se entregan con implementaciones vacias por defecto: son historiales acotados que el kernel alimenta por tick (tasas de disco agregadas en bytes/s y la media de carga de 1 minuto), y los implementadores que todavia no los registran siguen compilando sin cambios. los dos helpers de proceso de ux9.1 tienen por defecto none / una serie vacia: uid to name resuelve un uid numerico a un nombre de login (el mapeo uid→nombre es una preocupacion de visualizacion y vive deliberadamente en la vista de estado, no en el modelo de datos — los renderizadores recurren al uid numerico), y process cpu history devuelve las muestras de cpu por proceso acotadas (de mas antigua a mas nueva) para un pequeno spark de braille, vacio para pids no registrados. semanticas dignas de mencion: - el estilo de glifos esta resuelto : el kernel resuelve las sobrescrituras por widget contra el estilo global (uistyle::charset for/borders for en la config del kernel), asi que el renderizador no debe resolver nada por si mismo — el contrato ya devolvio la eleccion efectiva para el widget nombrado. - el historial son pares (f64, f64) — x (coordenada de tick/tiempo transcurrido) e y (valor), la forma que consumen los datasets chart de ratatui. el historial de red registra tasas, no contadores acumulados, asi que los graficos muestran rendimiento; lo mismo vale para los historiales de tasas de lectura/escritura de disco (bytes/s agregados entre discos). load history registra la media de carga de 1 minuto. todos los historiales estan acotados al history points del kernel. - process view es la unica muestra por tick filtrada por la consulta de busqueda activa y ordenada por la columna elegida por el usuario; la seleccion se ancla por pid para que el resaltado y la accion de matar coincidan siempre en la misma fila. - snapshot devuelve una referencia (option ) — toma prestada la muestra; nunca la clones por frame. sys info es la unica copia propiedad (barata, cambia lentamente). widgetrenderer y widgetregistration un pack registra un renderizador por nombre de widget. widgetregistration es el tipo de registro canonico (dr-2; nada fuera de widget-api lo define — gate de grep del workspace m7.4). hoy los packs devuelven su registro directamente como hashmap (registry() en el repo widgets); el engine resuelve (pack, name) en tiempo de renderizado. enums de glifos y sus valores serde ambos enums viven en glyph.rs , se re-exportan en la raiz del crate y derivan clone, copy, debug, partialeq, eq, serialize, deserialize con [serde(rename all = \"snake case\")] — estos son los valores que el kernel persiste en la config del usuario (uistyle /widgetstyle en el esquema de config del kernel). enum variants por defecto json (serde) --- --- --- --- chartcharset braille , dot , block , halfblock , bar braille \"braille\" , \"dot\" , \"block\" , \"half block\" , \"bar\" widgetborders native , rounded , double , plain , ascii native \"native\" , \"rounded\" , \"double\" , \"plain\" , \"ascii\" helpers canonicos de glifos (xtop widget api::glyph ) los packs no deben reimplementar estos mapeos — los importan de aqui, para que la misma configuracion se dibuje de forma identica en todos los packs. el kernel solo almacena los enums del contrato; convertirlos en glifos es trabajo de este modulo. los tests dentro del crate fijan cada fila de las tablas de mapeo siguientes. to color devuelve color::rgb(r, g, b) verbatim para la tripleta — las entradas de la paleta ya son rgb de 24 bits, asi que no se aplica ninguna cuantizacion. observa el parametro [u8; 3] por valor (las copias privadas de los packs toman &[u8; 3] ; los puntos de llamada se adaptan soltando la referencia cuando migran, m3). border for y ascii border el modulo symbols::border de ratatui ya no incluye un conjunto ascii (su plain es el marco de dibujo de caja de una linea desde ratatui 0.29), asi que el contrato proporciona el canonico. tanto plain como ascii se mapean a el; los packs no deben implementarse a mano su propia copia. decisiones de mapeo (un aspecto de borde por conjunto de ratatui, sin divergencia especifica de pack): widgetborders border for devuelve aspecto --- --- --- native ratatui::symbols::border::plain dibujo de caja estandar de una linea (┌─┐│└┘ ), el conjunto por defecto del propio ratatui — el aspecto clasico rounded border::rounded dibujo de caja con esquinas redondeadas double border::double dibujo de caja de linea doble plain ascii border marco ascii puro + - \\ ascii ascii border el mismo marco (ascii es una grafia de config explicita de la misma intencion) glifos de ascii border (codificados exactamente asi, y afirmados por test): glifo valor --- --- top left \"+\" top right \"+\" bottom left \"+\" bottom right \"+\" vertical left \"\\ \" vertical right \"\\ \" horizontal top \"-\" horizontal bottom \"-\" marker for el mapeo refleja el marker de ratatui del mismo nombre: chartcharset marker for devuelve --- --- braille marker::braille dot marker::dot block marker::block halfblock marker::halfblock bar marker::bar un pack que necesite un glifo diferente para la misma config no debe reimplementar la tabla; diverge deliberadamente y documenta por que. la regla dr-2 en la practica: los packs importan, nunca reimplementan antes de que existiera este contrato, cada pack reimplementaba a mano to color , border for , marker for y un conjunto ascii — y las copias ya discrepaban (el pack base mapeaba plain al plain de dibujo de caja, el pack de bloques lo mapeaba a su propio marco ascii). la migracion (repo widgets, m3.3/m3.4): - el util.rs del pack base borra sus copias de to color /border for /marker for /ascii border e importa las canonicas; los helpers de formato privados del pack (format bytes , format uptime , gauge gradient ) siguen siendo privados del pack donde todavia se usan; - el pack de bloques borra su to color /ascii border /border for implementados a mano, elimina su hack de lint const : chartcharset y honra state.charset(widget) en lugar de fijar marker::block . la divergencia de plain /ascii desaparece por construccion: ambas grafias son ascii aqui. widgets de plugins vs registros de packs el widgetregistration de xtop-widget-api dibuja sobre &dyn widgetstate — la vista de pack. el pluginwidget de xtop-plugin-api dibuja sobre &dyn hoststate — la vista de plugin (ver plugin-contract.md). son dos contratos distintos y deliberadamente no comparten nombre (m1.3). en el motor de renderizado del kernel el orden de resolucion es: primero los widgets de plugins, despues el pack elegido por el usuario para el nombre, y despues el pack por defecto (kernel ui/layout/engine.rs + ui/screen.rs ): los widgets de plugins pueden sustituir cualquier nombre. implementar un pack un pack es un crate que devuelve un registro nombre → renderizador. los renderizadores de widgets son funciones planas con la firma exacta fn(&mut frame, &dyn widgetstate, rect) : reglas del camino para los autores de packs: - renderiza solo desde widgetstate ; nunca nombres tipos del kernel. el pack base (xtop-widgets ) proporciona los nombres clasicos que usan los layouts por defecto — header , cpu , memory , storage , network , processes , disk io , battery , gpu — y los packs alternativos pueden sustituir cualquiera de ellos por nombre. - protegete de snapshot() == none (primer tick) y de areas pequenas/vacias; los renderizadores nunca deben entrar en panico con estado pequeno/vacio. - usa los helpers canonicos de glifos; cualquier divergencia deliberada debe documentarse (p. ej. una interpretacion visual alternativa de un charset). - las elecciones de estilo del widget llegan resueltas del contrato (state.charset(name) , state.borders(name) ); no resuelvas la config tu mismo."},{"locale":"es","repo":"effects","title":"xtop effects","label":"effects","path":"README.md","slug":["effects"],"text":"xtop effects efectos de frame y transiciones para xtop. cada efecto es un anadido visual que se enchufa al pipeline de renderizado del kernel a traves del crate de contrato compartido xtop-effect-api (en el repo api ): recibe el buffer de ratatui totalmente renderizado de un frame mas el tiempo transcurrido desde que el efecto empezo, y reescribe el buffer en el sitio antes del flush del terminal. estructura del workspace cada efecto vive en su propio crate xtop-effect- / . los helpers de efecto compartidos vivirian en un crate effects-lib/ — ese crate solo aparece cuando un segundo efecto comparte realmente codigo con uno existente, asi que no se crea vacio. efectos crate efecto id del manifest comportamiento ------- -------- ------------- ---------- xtop-effect-fade fade fade funde el frame renderizado desde negro en 500 ms; determinista, cero config documentacion - docs/write-effect.md — como implementar un efecto contra xtop-effect-api (recapitulacion del trait, contrato del host, patron de testing). - docs/effects.md — catalogo de los efectos integrados y la nota de integracion con el kernel. desarrollo desde la raiz de este repo: durante el desarrollo activo todos los repos viven lado a lado. la forma confirmada de xtop-effect-api es una dependencia git flotante ({ git = \"https://github.com/xtop-cli/api\" } , [workspace.dependencies] compartido). para compilar contra el checkout local de api en su lugar — necesario hasta que se haga push del repo api — apunta temporalmente esa dependencia a ../api/crates/effect-api (una ruta relativa a este repo), ejecuta los comandos anteriores y restaura despues la forma git y borra el cargo.lock generado (el repo lo ignora por diseno). ci local etapas: fmt clippy check test . licencia mit"},{"locale":"es","repo":"effects","title":"catalogo de efectos","label":"docs · effects","path":"docs/effects.md","slug":["effects","docs","effects"],"text":"catalogo de efectos efectos de frame integrados que distribuye este repositorio. todos ellos implementan el contrato de xtop-effect-api (ver docs/write-effect.md para la guia de autoria y el contrato del host). el catalogo es aditivo: un crate por efecto, una fila por efecto. fade (xtop-effect-fade , id del manifest fade ) el efecto de fundido de entrada. tipo: xtop effect fade::fadeeffect — un struct unit sin estado y sin configuracion de ningun tipo. como se ve fade toma el frame totalmente renderizado de un tick y funde su contenido rgb desde negro. durante la ventana, cada primer plano y fondo color::rgb se escala hacia negro; los colores que no son rgb (color::reset , indexados, escala de grises) y todos los modificadores de celda nunca se tocan. - elapsed == 0 : todos los canales rgb estan a cero — el frame es negro donde los widgets pintaron colores rgb. - elapsed estrictamente entre 0 y la ventana: cada canal rgb se situa en round(channel alpha) con alpha = elapsed / 500 ms . - elapsed = 500 ms : intensidad completa. en un terminal con fondo oscuro por defecto esto se lee como el frame entero fundiendose desde negro durante medio segundo. con un fondo claro por defecto, el fondo color::reset (por ejemplo el espacio vacio entre el texto) es visible de inmediato, mientras que el texto y los fondos rgb todavia suben desde negro. semantica exacta de la ventana - ventana: exactamente fade duration = 500 ms (una const del crate), medida desde el momento en que el efecto empezo, es decir, desde el valor elapsed que el host pasa en cada frame. - el efecto no conserva estado de progreso: es una funcion pura del buffer del frame y de elapsed , asi que nunca se desvia con el ritmo de frames ni con el jitter del tick. - garantia de determinismo: una vez que elapsed = fade duration , effect::on frame vuelve sin escribir una sola celda. cada frame posterior es byte-identico al buffer renderizado por el host, mientras el efecto siga activo — las llamadas repetidas son estables, y el buffer puede compararse celda por celda (el conjunto de tests lo demuestra). testing el conjunto de pruebas de xtop-effect-fade cubre: identidad de bytes al final de la ventana, atenuacion con elapsed cero (las celdas rgb atenuadas, las celdas color::reset /indexadas intactas), monotonia estricta en el punto medio del fundido, metadatos del manifest estables, llamadas repetidas despues de la ventana y buffers vacios. nota de integracion el kernel (repo xtop ) cablea los efectos integrados en su pipeline de renderizado detras de una feature opcional effects (roadmap del ecosistema m5.3, entregado con el hito del kernel): la closure de dibujo aplica el on frame del efecto activo al buffer despues del layout y antes del flush del terminal, pasando el tiempo transcurrido desde que el efecto empezo. hasta que ese cableado aterrice, este repositorio compila y hace test de forma independiente contra el crate de contrato xtop-effect-api ; nada de aqui depende del kernel."},{"locale":"es","repo":"effects","title":"escribir un efecto para xtop","label":"docs · write-effect","path":"docs/write-effect.md","slug":["effects","docs","write-effect"],"text":"escribir un efecto para xtop como implementar un efecto nuevo contra el crate de contrato compartido xtop-effect-api , que vive en el repositorio api y es la unica fuente de verdad del contrato de efectos (dr-5 en el roadmap del ecosistema). la implementacion de referencia en este repositorio es xtop-effect-fade . el contrato dos elementos publicos, y deliberadamente nada mas: no hay registro ni tipo de pegamento: un efecto es solo un tipo send + sync que implementa effect . el kernel mantiene los efectos concretos detras de una feature opcional y llama al trait. contrato de invocacion del host el kernel (xtop ) es el host. sus obligaciones, tal como documentan los docs del crate xtop-effect-api , son: - effect::on frame se llama en cada frame renderizado , despues de que los widgets y el layout se hayan dibujado en el buffer de ratatui y antes del flush del terminal. - elapsed es el tiempo desde que el efecto empezo — una edad absoluta, no un delta de frame. - los efectos tienen estado entre frames : cualquier progreso que un efecto necesite se conserva en self entre llamadas. - el host no marca el ritmo de los frames (hace tick a una cadencia de aproximadamente un segundo mas repintados ad-hoc). una transicion debe por tanto avanzar segun el tiempo de pared (elapsed ), nunca contando frames. un efecto nunca depende del kernel; solo usa xtop-effect-api y el tipo de buffer de ratatui. que debe garantizar el lado del efecto 1. manifest() devuelve metadatos estaticos y estables: un id corto (usado para seleccionar el efecto), un name legible por humanos y una description de una linea. 2. on frame puede reescribir cualquier celda del frame totalmente renderizado en el sitio. 3. una vez transcurrida la ventana de un efecto debe convertirse en un no-op: los frames posteriores siguen siendo byte-identicos al buffer renderizado por el host. 4. el mismo par (buffer, elapsed) debe producir la misma salida. el determinismo es lo que hace que los efectos sean comprobables sin una fuente de tiempo. el patron de funcion pura manten el progreso fuera del efecto cuando la semantica lo permita. elapsed es absoluto, asi que un efecto puede ser una funcion pura de (buffer, elapsed) — la implementacion siguiente es exactamente lo que hace xtop-effect-fade : solo acude al estado de self cuando lo visual necesite genuinamente un historial que elapsed no pueda expresar. con estado o sin el, el tipo debe seguir siendo send + sync . patron de testing como el efecto es una funcion pura de (buffer, elapsed) , los tests no necesitan terminal, ni fuente de tiempo, ni host: - construye un buffer con ratatui::buffer::buffer::empty(rect::new(0, 0, w, h)) y pinta unas pocas celdas de interes a traves del campo publico content . - llama a effect.on frame(&mut buffer, some duration) . - afirma sobre buffer.content() — los campos de celda (fg , bg , modifier , symbol ) implementan todos partialeq , asi que los buffers enteros se comparan con assert eq!(a.content(), b.content()) . el conjunto de tests de xtop-effect-fade cubre los casos limite que todo efecto deberia comprobar: elapsed == 0 (estado inicial), un valor a mitad de ventana, exactamente la ventana, llamadas repetidas despues de la ventana (estabilidad) y un buffer vacio. buffer: clone reduce las comprobaciones de identidad de bytes a una linea: aterrizar un efecto nuevo en este repositorio 1. crea xtop-effect- / con un manifest que herede los campos del workspace (version.workspace , edition.workspace , ...), dependiendo de ratatui y de xtop-effect-api a traves de [workspace.dependencies] . 2. anade el crate a members en el cargo.toml raiz. 3. implementa effect , refleja el patron de testing anterior y documenta la semantica visual exacta en los docs del crate. 4. anade una entrada de catalogo en docs/effects.md y una fila en la tabla del readme. el codigo de helper compartido pertenece a un crate effects-lib/ — pero solo cuando un segundo efecto comparta realmente codigo con uno existente. no lo crees vacio."},{"locale":"es","repo":"extensions","title":"extensiones de xtop","label":"extensions","path":"README.md","slug":["extensions"],"text":"extensiones de xtop repositorio oficial de las extensiones de xtop: comportamientos opcionales que el kernel puede alojar. hoy esto significa extensiones de tipo servidor : integraciones de larga duracion que el kernel arranca bajo demanda y que dirigen la aplicacion a traves del contrato de host en xtop-extension-api . el crate del contrato ofrece hoy exactamente una forma: una extension implementa el trait extension (manifest() + run server(server id, ctx) ) y actua a traves de una vista extensionhost que ofrece tick() y execute plugin() : las extensiones acceden a las funciones del kernel a traves de plugins, no mediante hooks directos. el kernel funciona por completo sin ninguna extension. trabajo futuro : las extensiones de tipo hook que afectan a la configuracion, el tema, el layout o el renderizado (hooks de pre/post renderizado, transformaciones de configuracion, nuevos comandos) se disenan a medida que se necesiten — nada en xtop-extension-api las implementa todavia. workspace cada extension vive en su propia carpeta bajo extensions/ : extension actual: xtop-extension-mcp - servidor mcp (model context protocol) sobre json-rpc 2.0 por stdio, lanzado por el kernel como xtop mcp . - expone el plugin samurai (repositorio de plugins) como 12 herramientas mcp; la tabla de herramientas se genera a partir de las constantes exportadas plugin id + actions:: del plugin, por lo que la extension depende del repositorio de plugins en tiempo de compilacion. - readme del crate · protocolo y mapeo de herramientas en docs/mcp-protocol.md · arquitectura de la extension en docs/architecture.md. para empezar (desarrollo) desde la raiz de este repositorio: durante el desarrollo activo, todos los repositorios conviven lado a lado y usan dependencias por ruta local: licencia mit"},{"locale":"es","repo":"extensions","title":"arquitectura de xtop-extension-mcp","label":"docs · architecture","path":"docs/architecture.md","slug":["extensions","docs","architecture"],"text":"arquitectura de xtop-extension-mcp xtop-extension-mcp es una extension de tipo servidor construida sobre el crate de contrato xtop-extension-api . nunca se comunica directamente con el kernel: el kernel lo aloja tras su feature mcp-extension y lo lanza desde el comando xtop mcp . uso del trait extension - mcpextension implementa el trait [extension ]: - manifest() declara el id mcp , el nombre xtop mcp server , la version a partir de la version del crate (env!(\"cargo pkg version\") ) y el unico id de servidor mcp . - run server(server id, ctx) rechaza todo id de servidor distinto de \"mcp\" con extensionerror::unknown y, en caso contrario, ejecuta el bucle del servidor stdio. - mientras se ejecuta, la extension maneja el kernel exclusivamente a traves de los dos metodos de [extensioncontext ]: - tick() — avanzar el tick de monitorizacion (tambien hace tick en los plugins alojados); el servidor lo invoca antes de cada ejecucion de tools/call . - execute plugin(plugin id, action, params) — ejecutar una accion de plugin; todas las llamadas a herramientas pasan por el. estructura - src/lib.rs : - run mcp server(ctx) — el bucle stdio: lee una linea cada vez, omite las lineas vacias, serializa y escribe la respuesta, y hace flush. - handle line(line, ctx) — analiza una linea json-rpc y la despacha a los handlers; el json malformado se convierte en extensionerror::recoverable (el servidor se detiene); todo lo demas devuelve el objeto de respuesta. se extrajo del bucle para que la capa json-rpc sea testeable sin terminal. - handle initialize / handle tools list / handle tools call — los tres handlers del protocolo. - tool actions , tool name() , action for tool() — la tabla de herramientas derivada de las constantes de samurai. - src/tests.rs — tests unitarios de la capa json-rpc contra un [extensionhost ] mock que registra las llamadas a execute plugin y devuelve resultados prefabricados (tick() no hace nada). por que depende del repositorio de plugins las herramientas que anuncia este servidor son las acciones del plugin samurai. antes de dr-6, el id de plugin \"samurai\" y las 12 cadenas de acciones se duplicaban a mano en este crate, por lo que renombrar una accion (o el plugin) rompia las herramientas mcp silenciosamente en tiempo de ejecucion . depender de xtop-plugin-samurai hace que el acoplamiento sea consistente en tiempo de compilacion: - plugin id dirige cada llamada a execute plugin (no hay ningun otro id de plugin codificado en el crate); - actions:: dirige tanto la lista de herramientas (tools/list , orden y nombres) como la traduccion de argumentos (tools/call ): cada nombre de herramienta se resuelve de nuevo a su constante de accion antes del despacho; - ahora, un renombrado en cualquiera de los dos lados hace fallar la compilacion de la extension en lugar de romper a un cliente de ia mas tarde. compensacion: la compilacion de la extension arrastra el crate del plugin (y con el ratatui y xtop-plugin-api ). esto es aceptable para una extension de servidor alojada por el kernel; a cambio, ambos repositorios se mantienen sincronizados. relacion con la documentacion de samurai - el protocolo a nivel de transporte (wire) y el mapeo de parametros por herramienta estan en mcp-protocol.md. - las acciones de samurai, su ciclo de vida y la mini-sintaxis de parametros estan documentadas en el repositorio de plugins (plugins/docs/architecture.md ); las heuristicas detras de process.alerts / alerts.status , en plugins/docs/rules.md . [extension ]: https://docs.rs/xtop-extension-api/latest/xtop extension api/trait.extension.html [extensioncontext ]: https://docs.rs/xtop-extension-api/latest/xtop extension api/struct.extensioncontext.html [extensionhost ]: https://docs.rs/xtop-extension-api/latest/xtop extension api/trait.extensionhost.html"},{"locale":"es","repo":"extensions","title":"protocolo mcp de xtop","label":"docs · mcp-protocol","path":"docs/mcp-protocol.md","slug":["extensions","docs","mcp-protocol"],"text":"protocolo mcp de xtop xtop-extension-mcp implementa un servidor model context protocol minimo sobre json-rpc 2.0 , delimitado por lineas en stdin/stdout: un objeto json por linea, una respuesta por peticion, sin http. el kernel lo lanza como xtop mcp (mcpextension::new().run server(\"mcp\", ctx) ). delimitacion del transporte - cada linea de entrada se recorta; las lineas vacias se omiten. - el json malformado termina el servidor con un error recuperable (el kernel lo reporta) — no se emite ninguna respuesta json-rpc, igual que en el bucle stdio. - toda peticion bien formada produce exactamente un objeto de respuesta con \"jsonrpc\": \"2.0\" y el id de la peticion repetido (echo). metodos gestionados metodo proposito --- --- initialize anuncia protocolversion 2024-11-05 , capabilities.tools , serverinfo (xtop , version = version del crate). tools/list las 12 herramientas generadas a partir de las constantes actions:: del plugin samurai. tools/call ejecuta una herramienta contra el plugin alojado. cualquier otra cosa json-rpc -32601 method not found: . notifications/initialized , ping , resources/ , logging , instructions y el procesamiento por lotes no estan implementados — quedan fuera del alcance de este servidor minimo. tabla de herramientas la tabla de herramientas se genera a partir de las constantes exportadas del plugin samurai (dr-6): este crate depende de xtop-plugin-samurai en tiempo de compilacion y mapea cada una de sus 12 constantes actions:: a una herramienta mcp cuyo nombre es la accion con . reemplazada por (system.summary → system summary , process.alerts → process alerts , …). no existe una segunda lista de cadenas mantenida a mano. herramienta accion de samurai argumentos mcp → cadena de params --- --- --- system summary system.summary — processes top processes.top count (por defecto 10), filter → count[,filter= ] processes search processes.search pattern , fields → pattern[,fields= ] process info process.info pid → \" \" (obligatorio) process kill process.kill pid → \" \" (obligatorio) threshold set threshold.set cpu , mem , disk → \" , , \" (los tres obligatorios) threshold get threshold.get — config get config.get — config set config.set uno de interval ms / theme / layout → \"interval ms= \" / \"theme= \" / \"layout= \" process alerts process.alerts — alerts status alerts.status — plugin status plugin.status — las descripciones y los objetos json inputschema que anuncia tools/list se mantienen aqui, por herramienta; las cadenas de acciones, el id del plugin y el mapeo de nombres de herramientas provienen del crate del plugin. comportamiento de tools/call para cada llamada de herramienta, el servidor: 1. resuelve el nombre de la herramienta de vuelta a su constante de accion (nombre desconocido → -32601 tool not found: ), 2. traduce el objeto arguments tipado de mcp a la sintaxis de parametros de cadena del plugin (argumentos obligatorios ausentes → -32602 con el nombre del argumento en el mensaje), 3. llama a ctx.tick() (refresca el estado del kernel y hace tick en los plugins alojados), 4. llama a ctx.execute plugin(plugin id, , ) — siempre contra el id del plugin samurai importado del crate del plugin, 5. envuelve una respuesta json exitosa del plugin como {\"content\": [{\"type\": \"text\", \"text\": \" \"}]} . mapeo de errores codigo json-rpc significado origen --- --- --- -32601 metodo no encontrado / herramienta no encontrada method desconocido o nombre de herramienta desconocido -32602 parametros no validos falta un argumento obligatorio de la herramienta (pid, cpu/mem/disk o clave de configuracion) -32000 error de servidor/ejecucion extensionerror de execute plugin (plugin ausente, capacidad denegada, accion fallida) ejemplo de sesion"},{"locale":"es","repo":"extensions","title":"xtop-extension-mcp","label":"xtop-extension-mcp","path":"xtop-extension-mcp/README.md","slug":["extensions","xtop-extension-mcp"],"text":"xtop-extension-mcp extension de servidor mcp (model context protocol) para xtop. expone el plugin samurai de xtop (monitorizacion del sistema, consultas y control de procesos, alertas heuristicas) como herramientas mcp a traves de un servidor stdio json-rpc 2.0. cualquier ia compatible con mcp — claude desktop, cline, etc. — puede conectarse: resumen del protocolo - json-rpc 2.0 sobre stdin/stdout, un objeto json por linea. - gestiona initialize , tools/list y tools/call ; cualquier otra cosa se responde con -32601 . los fallos de ejecucion se mapean a -32000 , y los argumentos ausentes, a -32602 . - el json malformado detiene el servidor con un error recuperable (no se emite ninguna respuesta). - detalles del protocolo (wire) y un ejemplo de sesion: docs/mcp-protocol.md. herramientas el servidor expone las 12 acciones de samurai como 12 herramientas. la tabla de herramientas se genera a partir de las constantes exportadas por xtop-plugin-samurai (dr-6): nombre de herramienta = accion con . reemplazada por : herramienta accion de samurai argumentos --- --- --- system summary system.summary — processes top processes.top count (por defecto 10), filter (regex) processes search processes.search pattern (regex), fields (name,cmd,user,state,exe,cwd ) process info process.info pid process kill process.kill pid threshold set threshold.set cpu , mem , disk threshold get threshold.get — config get config.get — config set config.set interval ms / theme / layout process alerts process.alerts — alerts status alerts.status — plugin status plugin.status — los argumentos tipados se traducen a la sintaxis de parametros de cadena del plugin antes de cada llamada a execute plugin (consulta docs/mcp-protocol.md). acoplamiento con samurai este crate depende de xtop-plugin-samurai en tiempo de compilacion: - cada llamada de herramienta apunta a plugin id ; no existe ningun otro id de plugin; - actions:: dirige la tabla de herramientas y la traduccion de argumentos. un renombrado del id del plugin o de una accion ahora rompe esta compilacion en lugar de romper a los clientes mcp en tiempo de ejecucion. arquitectura y justificacion: docs/architecture.md. como lo lanza el kernel xtop mcp (kernel, feature mcp-extension ) construye mcpextension::new() y llama a run server(\"mcp\", ctx) con un contexto de host que resuelve execute plugin contra los plugins alojados por el kernel. desarrollo licencia mit"},{"locale":"es","repo":"layouts","title":"xtop-layouts","label":"layouts","path":"README.md","slug":["layouts"],"text":"xtop-layouts definiciones de diseno (layout) dirigidas por datos para el monitor de sistema tui xtop. este crate (xtop-layout ) es dueno de todo lo relacionado con los disenos para que el kernel se mantenga fino: - model — arbol de diseno (layoutdef → divisiones de zonas → hojas de widget), serializable desde json/jsonc, sin dependencias de ui. - default/ — 10 disenos por defecto embebidos que se envian con el crate: los siete disenos ligados a modos (dashboard , vertical , horizontal , cpu focus , memory focus , network focus , process focus ) mas tres extras de preajuste — detail dashboard , detail network , detail processes (detail .jsonc ) — que se situan tras los modos en el orden de archivos y muestran options por widget (dr-ux6). tabla de preajustes (ux8.5: paginas densas de monitor completo sin filas huerfanas en tamanos comunes — cada division es exacta en porcentaje o absorbida por fill, ver docs/authoring.md \"guia de densidad\"): archivo \"name\" composicion a 100x34 (31 filas de cuerpo) widgets --- --- --- --- detail dashboard.jsonc detail dashboard header → banda de monitor de 18 filas (cpu a la izquierda, columna lateral del 36% de summary/sensors) → procesos de ancho completo de 13 filas header, cpu, summary, sensors, processes detail network.jsonc detail network header → banda de 22 filas (caja de network por interfaz + columna de summary/disk io/memory) → procesos de 9 filas header, network, disk io, summary, memory, processes detail processes.jsonc detail processes header → tira de estadisticas de 8 filas (summary/cpu/memory/storage/network) → procesos de altura completa de 23 filas header, summary, cpu, memory, storage, network, processes summary (panel de carga/uptime/recuento de procesos) y sensors (vista de calor de temperatura por nucleo) son las llegadas de la ola de widgets ux8.4 colocadas por los preajustes ux8.5; los nombres se resuelven en tiempo de render como cualquier otro id de widget. al iniciarse, el kernel los escribe como plantillas editables en el directorio de disenos de la configuracion del usuario cuando inicializa sus recursos (nunca sobrescribiendo archivos que el usuario ya haya editado). - custom/ — disenos de la comunidad : extras instalables, compartidos mediante prs. nunca viajan en el binario; el xtop layout install del kernel obtiene esta carpeta y copia el diseno al directorio de disenos de la configuracion del usuario (o copia el archivo tu mismo, p. ej. /.config/xtop/layouts/ en linux). - loader — carga/analisis de archivos jsonc y fusion con anulaciones del usuario. - mode — modos de diseno integrados y reglas de degradacion por tamano de terminal. estructura del diseno uso los archivos de diseno son json/jsonc: size acepta un numero fijo de filas/columnas, un porcentaje (\"45%\" ) o \" \" /omision para el espacio restante. los widgets se referencian por nombre; el kernel decide a que renderizador se mapea cada nombre. documentacion - docs/layout-schema.md — esquema formal de diseno jsonc (estructura del documento, gramatica de zonas, sintaxis de restricciones, dialecto de comentarios, valores por defecto embebidos). - docs/authoring.md — guia paso a paso: escribir, validar e instalar disenos, ids de widget, modos y umbrales de tamano de terminal. - docs/decisions.md — registro de decisiones de diseno (dr-3, ids de widget no validados, fusion por nombre). modelo de personalizacion 1. valores por defecto se compilan en el binario y se copian como plantillas a /.config/xtop/layouts/ en el primer arranque (primero los siete disenos ligados a modos, luego los extras de preajuste detail — el orden es parte del contrato de ranuras de modo, ver docs/authoring.md §6). 2. anulaciones : edita un archivo cuyo \"name\" coincide con un valor por defecto → sustituye a ese valor por defecto en su lugar (misma posicion de paleta, sin duplicados). 3. disenos nuevos : cualquier archivo extra aparece como un diseno adicional. 4. comunidad : comparte disenos mediante pr a layouts/custom/ ; los usuarios los copian o los instalan en su directorio de configuracion."},{"locale":"es","repo":"layouts","title":"disenos de la comunidad (instalables)","label":"custom","path":"custom/README.md","slug":["layouts","custom"],"text":"disenos de la comunidad (instalables) los disenos de esta carpeta no se compilan en el binario : son disenos extra que cualquiera puede instalar o compartir. esta carpeta es exactamente lo que el kernel obtiene para xtop layout install (hace sparse-clone de layouts/custom/ de este repositorio), asi que cada archivo aqui es un diseno de la comunidad instalable. - suelta un archivo de diseno aqui y abre un pr para compartirlo con todos. - cada archivo debe seguir el formato de diseno (ver el readme.md raiz y el esquema formal en docs/layout-schema.md ) y usar un \"name\" unico (no colisiones con los nombres de default/ ni con otros archivos de aqui). - convencion de nombres de archivo: my layout.jsonc (el nombre dentro del archivo es lo que muestra la tui; xtop layout install acepta cualquiera como clave de busqueda). - valida antes de compartir: xtop layout check . usar un diseno de la comunidad el kernel instala los disenos de la comunidad en el directorio de configuracion del usuario. de cualquier manera el archivo aterriza en la carpeta de disenos del usuario y se carga en el siguiente arranque de la tui (y pasa a formar parte de la paleta de disenos l ): xtop layout install hace coincidir un archivo por su nombre de archivo o por el \"name\" del diseno dentro (sin distincion de mayusculas), conserva el nombre de archivo original y se niega a sobrescribir un archivo existente en el directorio de disenos del usuario — edita ese archivo en su lugar. los archivos personalizados anulan un diseno integrado reutilizando su \"name\" (misma posicion de paleta) o aparecen como disenos extra con un \"name\" nuevo; ver docs/authoring.md para el flujo completo."},{"locale":"es","repo":"layouts","title":"guia de autoria: disenos personalizados","label":"docs · authoring","path":"docs/authoring.md","slug":["layouts","docs","authoring"],"text":"guia de autoria: disenos personalizados guia paso a paso para escribir un diseno personalizado para xtop, validarlo y ponerlo a disposicion — localmente o como diseno de la comunidad. la descripcion autoritativa del formato es docs/layout-schema.md ; esta guia se centra en el flujo de trabajo y en los ids, modos y el flujo de instalacion/validacion tal como se implementan en el kernel (xtop/src/commands/layout.rs , xtop/src/ui/layout/engine.rs , xtop/src/ui/screen.rs ). 1. donde viven los disenos el crate embebe diez disenos por defecto (layouts/default/ .jsonc ): primero los siete valores por defecto ligados a modos (ranuras de paleta 0–6), luego tres extras de preajuste (detail dashboard , detail network , detail processes — ranuras 7–9, dr-ux6). desde ux8.5 los extras son paginas densas de cobertura completa: detail dashboard es la pagina de monitor completo (cpu alto con columna lateral de summary /sensors + processes de ancho completo), detail network enfoca la caja de network por interfaz con una columna lateral de summary /disk io /memory y una tira inferior de processes , y detail processes es una tira compacta de estadisticas (summary + cpu /memory /storage /network ) sobre una tabla de processes de altura completa. los disenos personalizados se cargan desde el directorio de disenos del usuario al iniciarse (xtop/src/commands/share/bootstrap.rs fusiona los valores por defecto con todo lo que load layouts from dir encuentra alli): plataforma directorio de configuracion del usuario directorio de disenos del usuario --- --- --- linux $xdg config home/xtop , si no, /.config/xtop /layouts macos /library/application support/xtop /layouts windows %appdata%\\xtop \\layouts el crate xtop-layout en si nunca toca los directorios de diseno del sistema de archivos de los usuarios; el kernel es dueno de las rutas de arriba (xtop/src/config/platform/ ). 2. escribir un diseno crea un archivo .jsonc que contenga exactamente un diseno: reglas practicas (gramatica completa: docs/layout-schema.md ): - \"name\" debe ser unico. la fusion es por nombre exacto y sensible a mayusculas: un archivo cuyo nombre iguala a un valor por defecto sustituye a ese valor por defecto en su lugar (misma ranura de paleta); cualquier otro archivo se anade despues de los valores por defecto. los valores por defecto embebidos estan ordenados de modo que los primeros siete nombres son los disenos ligados a modos y los tres extras de preajuste (detail dashboard , detail network , detail processes ) vienen justo despues — nunca renumeres los primeros siete, y los nuevos extras embebidos deben anadirse al final (ver docs/layout-schema.md , \"valores por defecto embebidos\"). - usa un widget header de 3 filas arriba, como todos los valores por defecto. - en una division \"vertical\" los hijos se apilan de arriba a abajo; en \"horizontal\" se sientan lado a lado. los porcentajes se refieren a la division contenedora. - comenta libremente con // o / / ; las comas finales no se aceptan . - cada instancia de widget puede tomar un objeto options opcional que ajusta como se dibuja esa instancia — p. ej. { \"widget\": \"processes\", \"options\": { \"cpu\": \"total\" } } . las opciones son de paso directo (passthrough): el crate de diseno solo las almacena y las reenvia, los renderizadores las interpretan (gramatica y ejemplos: docs/layout-schema.md , seccion \"options de widget\"). sin options significa el comportamiento por defecto del widget. - valida el archivo antes de publicarlo (siguiente seccion). guia de densidad: sin bandas huerfanas, sin cajas famelicas los preajustes ux8.5 son densos: cada fila de diseno esta cubierta por una caja y cada caja es bastante alta para que su contenido crezca (los graficos/listas llenan su caja — la ola de widgets ux8.4). el motor de renderizado (xtop/src/ui/layout/engine.rs ) alimenta las restricciones a ratatui, donde los porcentajes miden la division completa y fill toma el sobrante, asi que escribe divisiones que embaldosan por construccion: - pilas de porcentaje puro : hijos con tamano \"p1%\" , \"p2%\" , ... con p1 + p2 + ... = 100 embaldosan la division exactamente en cada tamano de terminal (no se necesita fill). - cabecera fija + fill : pon una cabecera fija size: 3 (o una tira size: 8 ) junto a un hermano \" \" /fill, nunca junto a un porcentaje — los porcentajes medirian la division completa (incluidas las filas de la cabecera) y se pasarian de reclamar. por eso cada raiz por defecto es header (3) + cuerpo \" \" , con los porcentajes de embaldosado viviendo dentro del cuerpo. - fill junto a porcentajes : bien cuando los porcentajes suman menos de 100 — el fill absorbe el resto (p. ej. una columna \" \" de cpu junto a una columna lateral \"36%\" ). manten la demanda de porcentajes bajo 100 o el fill muere de hambre. - una division sin hermano fill ni porcentajes que sumen 100 deja una banda huerfana; las pruebas del loader (test detail presets split coverage full tiling ) hacen cumplir la regla en cada division de cada preajuste de detalle. presupuesto de filas en los tamanos comunes (cuerpo = filas de terminal − 3 filas de cabecera): widget contenido que absorbe filas con caja comoda (filas) mantener bajo --- --- --- --- cpu rejilla de nucleos + freq/temp por nucleo + historial de varias filas 12–22 — processes lista de procesos 9–30 — summary lineas de carga/uptime/recuento de proc + sparklines 6–9 10 sensors vista de calor de temperatura por nucleo (fallback de una linea sin temps) 7–12 14 memory filas ram/swp + secciones + grafico de historial 6–10 12 network filas por interfaz + grafico dual agregado 6–26 — disk io filas por disco + grafico r/w 6–12 — storage barras por disco 5–9 10 numeros aproximados a 100x34 (31 filas de cuerpo): detail dashboard = banda de monitor de 18 filas (cpu 64 cols × 18 filas, summary 8 filas, sensors 10 filas) sobre 13 filas de procesos; detail network = caja de network de 22 filas + columna lateral de summary/disk io/memory de 7/8/7 sobre 9 filas de procesos; detail processes = tira de 8 filas sobre una tabla de procesos de 23 filas. los mismos arboles dan a 80x24 (21 filas de cuerpo) divisiones 12/9, 15/6 y 8/13 y a 120x40 (37) 21/16, 26/11 y 8/29 — siempre cobertura completa, porque cada division es exacta en porcentaje o absorbida por fill. 3. validacion el kernel expone xtop layout check (xtop/src/commands/layout.rs ), que ejecuta el analizador exacto de este crate (parse layout err ): las funciones subyacentes del crate tambien son publicas: parse layout(source) - option y parse layout err(source) - result (la ultima es la que evalua check ). mientras desarrollas, prefiere un directorio que el kernel ya lee, o llama al crate desde una prueba de borrador — pero recuerda como se manejan los archivos invalidos : load layouts from dir se salta cualquier archivo que falle al analizarse e imprime [xtop-layout] skipping invalid layout file: en stderr, asi que un archivo roto nunca mata a la aplicacion — simplemente no aparece en la paleta. es facil pasarlo por alto, por eso existe xtop layout check . 4. ponerlo en activo - diseno personal : pon el archivo directamente en el directorio de disenos del usuario (tabla superior). el directorio se lee una vez al iniciarse, asi que reinicia xtop (o inicialo) para recoger los cambios; recorre la paleta con l , o selecciona el diseno por nombre desde la paleta de comandos. el nombre del diseno activo se persiste en la configuracion y se restaura en la siguiente ejecucion. - valores por defecto sembrados : al iniciarse el kernel siembra copias de las diez plantillas .jsonc por defecto en el directorio de disenos del usuario (archivo marcador .xtop initialized , version 4 desde que los extras de preajuste se renombraron a detail y el conjunto sembrado se actualizo; los archivos que ya existen nunca se sobrescriben — xtop/src/commands/share/assets.rs ). editar una copia sembrada es la forma soportada de ajustar un valor por defecto integrado; como las anulaciones coinciden por \"name\" , el archivo editado sustituye al valor por defecto en su ranura habitual de paleta. las copias sembradas de plantillas renombradas tampoco se eliminan nunca: tras una actualizacion que renombra una plantilla, los archivos nuevos se siembran junto a las copias antiguas, y cualquier copia antigua cuyo \"name\" ya no coincida con un valor integrado se comporta como un diseno de usuario (un extra al final de la paleta). elimina tu mismo las copias antiguas cuando no las hayas editado. - anulaciones frente a disenos nuevos : mismo nombre → sustitucion en su lugar (ranura preservada, sin duplicado). nombre nuevo → anadido como un diseno extra. 5. ids de widget los archivos de diseno referencian widgets por ids de cadena simples; no hay lista blanca en tiempo de compilacion, la resolucion ocurre en tiempo de render (xtop/src/ui/layout/engine.rs ). lo que referencian los preajustes hoy: id donde notas --- --- --- header , cpu , memory , storage , network , processes , disk io pack base (registro de xtop-widgets ) usados por cada valor por defecto (disenos de modo y preajustes de detalle); siempre disponibles summary , sensors registro del pack base (adiciones ux8.4) referenciados por los preajustes de detalle desde ux8.5: summary es el panel de carga/uptime/recuento de procesos con sparklines, sensors la vista de calor de temperatura por nucleo (fallback honesto de una linea cuando no hay datos de temp). se resuelven en tiempo de render como cualquier otro nombre — un archivo de diseno es valido antes de que llegue el pack, solo que no renderiza nada para un nombre que ningun renderizador proporciona battery , gpu registro del pack base renderizadores registrados; seleccionables en modo de pantalla completa, no usados por ningun valor por defecto. sus proveedores de datos dependen de la plataforma, asi que espera valores vacios donde no exista fuente samurai (widgets de plugin) plugins con la capacidad renderwidgets (kernel plugins/manager.rs ) referenciable cuando el kernel se compila con el plugin habilitado (p. ej. plugin-samurai ). los renderizadores de plugin tienen precedencia sobre los renderizadores de pack para el mismo nombre orden de referencia en tiempo de render: primero los widgets de plugin, luego el pack elegido por la configuracion de estilo, cayendo al pack base. un id con errores tipograficos o no disponible no renderiza nada en esa zona (la zona queda en blanco); el kernel imprime una advertencia unica en stderr por nombre desconocido (xtop: layout ' ' references unknown widget ' ' — xtop/src/ui/layout/engine.rs ), y la vista de pantalla completa muestra no widget registered for ' ' en su lugar. por eso, xtop layout check es solo validacion estructural: no puede saber si un id se resolvera. 6. modos y umbrales el crate calcula como degrada el modo solicitado con el tamano de terminal (src/mode.rs , detect effective layout ). umbrales exactos: modo solicitado tamano de terminal diseno efectivo --- --- --- cualquiera ancho lo que hace el kernel ( xtop/src/commands/layout.rs, cmd install): 1. ejecuta git clone --depth 1 --filter=blob:none --sparse https://github.com/xtop-cli/layouts en un directorio temporal (git debe estar en path), luego git sparse-checkout set layouts/custom. 2. escanea layouts/custom/ en busca de archivos .json/ .jsonc y elige el primero cuyo nombre de archivo o \"name\" analizado sea igual a (sin distincion de mayusculas). 3. copia ese archivo al directorio de disenos del usuario bajo su nombre de archivo original ; se niega a sobrescribir un destino existente (edita en su lugar) y da error cuando nada coincide o git falla. asi que para compartir un diseno: pon my layout.jsonc en layouts/custom/ de este repositorio ( \"name\" unico, validado con xtop layout check), abre un pr, y despues de que aterrice cualquiera puede xtop layout install `. igualmente puedes saltarte el repositorio: copia el archivo tu mismo al directorio de disenos del usuario — la aplicacion lo carga en el siguiente arranque de cualquier manera. el kernel no lee los archivos de la comunidad de este repositorio en tiempo de ejecucion; solo se vuelven vivos despues de la instalacion (o de una copia manual)."},{"locale":"es","repo":"layouts","title":"decisiones de diseno","label":"docs · decisions","path":"docs/decisions.md","slug":["layouts","docs","decisions"],"text":"decisiones de diseno registro breve de las decisiones que dan forma a este crate y al formato de los archivos de diseno. basado en el codigo de este repositorio y en las fuentes del kernel referenciadas abajo. d-1 — los disenos viven en un crate independiente y libre de ui (dr-3) decision (roadmap dr-3). el modelo de diseno, el loader y los modos viven solo en xtop-layout ; el kernel no tiene modelo de diseno ni loader internos. el crate no tiene dependencia de ratatui ni dependencia de ningun crate de contrato xtop- : es puro dato + serde (docs del modulo en src/lib.rs ). el kernel lo consume como una dependencia de git y traduce el arbol a restricciones concretas de ui en tiempo de render (xtop/src/ui/layout/engine.rs mapea los valores de layoutconstraint a restricciones de ratatui). por que. el kernel se mantiene como un host fino; la autoria de disenos, los valores por defecto y las reglas de degradacion son probables sin terminal ni el ecosistema de packs de widgets; el formato de diseno es una superficie de intercambio estable entre repositorios que nunca necesita el registro de widgets en tiempo de compilacion. consecuencias aceptadas: - el esquema del formato es implicito en el modelo serde + el visitor manual de src/model.rs — documentado formalmente en docs/layout-schema.md en lugar de un .schema.json generado (nada consume hoy un archivo de json schema). - los ids de widget son cadenas opacas (siguiente entrada), y nada en este repositorio puede verificarlos contra los repositorios de widgets o plugins. - el contrato de orden de la paleta (valores por defecto ligados a modos en las ranuras 0–6, orden fijo, extras de preajuste/usuario anadidos despues) se hace cumplir por el orden de default layout sources + mode from layout index + la semantica de merge layouts y se fija con pruebas unitarias (ver docs/authoring.md §6). d-2 — los ids de widget son cadenas no validadas; la resolucion es en tiempo de ejecucion decision. los archivos de diseno nombran los widgets como cadenas simples (layoutnode::widget { name } en src/model.rs ). el crate no lleva una lista blanca: no tiene dependencia de los repositorios de widgets/ plugins, asi que no puede saber que ids son validos para una compilacion dada del kernel (que packs estan compilados, que plugins estan habilitados). consecuencia, tal como se implementa en el kernel actual. los ids de widget se resuelven en tiempo de render contra, en orden: los widgets de plugin (p. ej. samurai ), luego el pack elegido por el estilo, luego el pack base (xtop/src/ui/layout/engine.rs , render named ). si nada coincide, el id no renderiza nada — la zona de diseno queda en blanco; el kernel informa de los nombres desconocidos una vez por proceso en stderr (warn unknown widgets , engine.rs ) y la ruta de pantalla completa muestra no widget registered for ' ' (xtop/src/ui/screen.rs ). el propio informe de este repositorio se limita a saltarse los archivos estructuralmente invalidos en stderr (load layouts from dir ); un archivo con forma valida pero un id de widget desconocido se analiza sin problema. por que se mantiene asi. la validacion en tiempo de compilacion acoplaria este repositorio al registro de widgets y a cada combinacion de flags de feature del kernel; la resolucion en tiempo de ejecucion mantiene los disenos portables entre compilaciones del kernel. la red de seguridad estructural que si existe es xtop layout check (kernel commands/layout.rs ), que valida los archivos con parse layout err de este crate. d-3 — fusion por nombre, preservando la ranura decision. la fusion es por el campo \"name\" del diseno con igualdad exacta y sensible a mayusculas (merge layouts en src/loader.rs ): un archivo de usuario que sustituye a un valor por defecto conserva la ranura de paleta del valor por defecto; los nombres nuevos se anaden despues de los valores por defecto. mode from layout index mapea solo las ranuras 0–6 a modos; todo lo que las supere (los tres extras de preajuste detail embebidos en las ranuras 7–9 y los disenos de usuario) se aborda por nombre, y el kernel persiste/restaura los disenos por nombre (config.layout name ). por que. el orden de paleta y el acoplamiento modo ↔ diseno deben permanecer estables mientras los usuarios personalizan libremente; ver docs/authoring.md §6 para el contrato que esto impone a los colaboradores (nombres unicos, orden de valores por defecto nunca renumerado). estado - crate: xtop-layout v0.1.0, rust-version = \"1.87\" , edicion 2021, mit, sin dependencias de terceros mas alla de serde/serde json. - decisiones d-1..d-3 registradas el 2026-09-04 como parte del hito m6 (documentacion del esquema implicito)."},{"locale":"es","repo":"layouts","title":"esquema del archivo de diseno (jsonc)","label":"docs · layout-schema","path":"docs/layout-schema.md","slug":["layouts","docs","layout-schema"],"text":"esquema del archivo de diseno (jsonc) descripcion formal, basada en codigo, del formato de diseno que consume xtop-layout (crate xtop-layout v0.1.0). el esquema no se hace cumplir por un .schema.json separado: lo implementan el modelo serde y el visitor manual en src/model.rs , mas el eliminador de comentarios jsonc en src/loader.rs . este documento refleja ese codigo exactamente, asi que es autoritativo para los colaboradores y para el comando xtop layout check del kernel (que ejecuta la misma funcion parse layout err de src/loader.rs ). convenciones de archivo - un diseno por archivo. el nombre de visualizacion del diseno es el campo \"name\" dentro del archivo, no el nombre del archivo. - los archivos pueden usar .jsonc o .json ; jsonc (comentarios permitidos) es la convencion en todas partes de este repositorio. - los valores por defecto integrados viven en layouts/default/ y se embeben en el binario a traves de default layout sources en src/loader.rs (10 archivos: siete valores por defecto ligados a modos + tres extras de preajuste detail ). - los disenos de usuario viven en el directorio de configuracion de la plataforma bajo layouts/ — ver docs/authoring.md para las rutas concretas por plataforma. - un directorio se carga con load layouts from dir : cada entrada .json / .jsonc se analiza; los archivos que fallan al analizarse se saltan y se informan en stderr (ver docs/authoring.md ). - los nombres de diseno deben ser unicos entre valores por defecto y archivos de usuario: merge layouts hace coincidir los nombres con igualdad de cadena exacta y sensible a mayusculas. estructura del documento un documento de diseno es un unico objeto json con exactamente dos claves: clave tipo requerida significado --- --- --- --- name string si nombre de visualizacion mostrado en la paleta de la tui; usado para la coincidencia de modos y anulaciones. ausente → error missing field \\ name\\ . root objeto de zona si el arbol de diseno. ausente → error missing field \\ root\\ . cualquier otra clave de nivel superior se rechaza (el deserializador personalizado solo acepta name /root ; error unknown field ... ). dentro de root y dentro de cada objeto de zona el analizador es permisivo: las claves extra se ignoran silenciosamente, solo se leen las claves de la tabla inferior. la raiz usa la misma gramatica de objeto que cada zona (abajo). su clave size se analiza y valida como la de cualquier otra zona, pero el valor se descarta : un layoutdef conserva solo el nodo raiz, asi que la raiz siempre abarca toda la zona de render del terminal que el kernel le da. objetos de zona cada zona de una division (y la raiz) es un objeto json: clave tipo significado --- --- --- size numero, string u omitido restriccion que esta zona obtiene de su division padre (ver abajo). omitido ≡ \" \" (fill). widget string id de widget; ver docs/authoring.md para los ids que existen. options objeto json opciones de visualizacion opcionales por widget (dr-ux1); solo significativas junto con widget . passthrough: este crate almacena y serializa el objeto tal cual y nunca lo interpreta — el kernel se lo entrega al renderizador del widget, que es dueno de la semantica (ver \"options de widget\" abajo). direction string direccion de la division: \"vertical\" o \"horizontal\" , sin distincion de mayusculas (se compara direction.to lowercase() , asi que se aceptan \"vertical\" , \"vertical\" , etc.; cualquier otra cosa → error invalid direction: ). areas array de objetos de zona hijos de una division. opcional; ausente/vacio → una division vacia que se analiza pero no renderiza nada. debe estar presente exactamente una de widget y direction ; si no, el archivo se rechaza con layout area must have 'widget' or 'direction' . si ambas estan presentes la zona se analiza, widget gana y las claves de division (direction , areas ) se ignoran. las claves extra de un objeto de zona se ignoran (el deserializador de struct no habilita deny unknown fields ). options de widget — opciones de visualizacion por widget cada instancia de widget puede llevar un objeto json options que refina como se dibuja esa instancia: semantica, exactamente como se implementa en src/model.rs (layoutarearaw ): - options es opcional; una clave ausente y un null explicito ambos deserializan a none (el widget se renderiza con su comportamiento por defecto). - el valor debe ser un objeto json; es un passthrough : el modelo lo almacena como serde json::value y nunca interpreta su contenido. las claves desconocidas dentro del objeto se preservan tal cual a traves del analisis y la re-serializacion (el orden de claves dentro de options puede normalizarse al orden del mapa de serde json; el contenido no cambia). - los renderizadores son duenos de la semantica: cada widget documenta las claves que reconoce (ver los docs del repositorio de widgets). las opciones en nodos de division, o en una zona donde direction gana, se analizan pero se ignoran. - la igualdad de layoutdef /layoutnode y el serializador espejo incluyen options ; un widget sin opciones se serializa byte-identicamente al formato pre-dr-ux1 (la clave options solo se emite cuando esta presente), asi que los archivos de diseno existentes y sus viajes de ida y vuelta no cambian. - la zona raiz sigue la misma gramatica; las options de una hoja de widget raiz se conservan en el nodo como las de cualquier otra zona (el size de la raiz sigue descartandose). size — sintaxis de restricciones el valor crudo se deserializa como numero json o cadena json (sizeraw en src/model.rs ), y luego se convierte a una restriccion: grafia jsonc restriccion efecto en el kernel --- --- --- (omitido) fill tomar el espacio restante numero desnudo 3 length(3) 3 filas fijas (division vertical) o columnas (division horizontal) \"45%\" percentage(45) 45% del contenedor padre \" \" fill tomar el espacio restante detalles de analisis, exactamente como se implementa en tryfrom : - numero desnudo : deserializado como u16 — debe ser un entero entre 0 y 65535 . las fracciones (3.5 ), los negativos y los valores fuera de rango fallan la deserializacion json del enum sin etiquetar (data did not match any variant of untagged enum sizeraw ). - \" \" : la cadena exacta, comprobada antes de la rama de porcentaje. cualquier otra cadena que termine en % va a la rama de porcentaje. - \"nn%\" : todos los caracteres % finales se recortan (trim end matches('%') , asi que \"45%%\" se analiza como 45 ), luego el resto se analiza como u16 ; el fallo produce invalid percentage: . el numero analizado no se comprueba contra 100 por rango — cualquier cosa 0..=65535 se acepta sintacticamente; el kernel entrega el valor al motor de restricciones de la tui, asi que los porcentajes por encima de 100 son posibles pero no significan nada alli. \"%\" solo falla (invalid percentage: % ). - cualquier otra cadena (ni \" \" , ni termina en % , o vacia) → error invalid size constraint: . las zonas fill las resuelve el renderizador del kernel con igual peso (constraint::fill(1) en xtop/src/ui/layout/engine.rs ): cuando varios hermanos usan \" \" (u omiten size ), el espacio sobrante se comparte por igual. semantica de direccion el formato es agnostico al framework de ui; el motor de render del kernel mapea cada division a un layout de tui de la misma direccion: - \"direction\": \"vertical\" — los hijos se apilan de arriba a abajo ; cada zona abarca el ancho completo y recibe una altura segun su size . - \"direction\": \"horizontal\" — los hijos se colocan de izquierda a derecha ; cada zona abarca la altura completa y recibe una anchura segun su size . traduccion de restricciones (kernel ui/layout/engine.rs ): length(n) → constraint::length(n) , percentage(p) → constraint::percentage(p) , fill → constraint::fill(1) . los porcentajes son relativos a la division contenedora completa (las longitudes fijas no se restan primero — una cabecera length(3) junto a un hijo \"60%\" hace que el porcentaje mida el 60% de toda la division, no el 60% del sobrante). el motor de restricciones siempre embaldosa el padre completamente: las lengths se tallan exactamente y las zonas fill absorben cada fila/columna restante (varios fills lo comparten por igual). los porcentajes se satisfacen exactamente cuando ninguna length fija de la misma division se pasa de reclamar la zona (pilas de porcentaje puro que suman 100); en cuanto las lengths se sientan junto a los porcentajes, las demandas exceden la zona y el solucionador las relaja de formas no especificadas — por eso cada preajuste integrado mantiene las lengths solo junto a fills, y expresa las partes de filas con pilas de porcentaje puro (\"reglas de embaldosado\" abajo). reglas de embaldosado: construir divisiones sin bandas huerfanas como los porcentajes miden la division completa y los fills absorben el resto, una division deja una banda huerfana (filas/columnas que ninguna caja cubre) solo cuando no tiene un hermano fill ni hijos de porcentaje que sumen 100. los preajustes integrados siguen dos reglas, y los usuarios de xtop layout check deberian tambien: 1. pilas de porcentaje puro : hijos con tamano \"p1%\" , \"p2%\" , ... con p1 + p2 + ... = 100 embaldosan la division exactamente en cada tamano de terminal — no se necesita fill, no se mezcla ninguna length fija. 2. pilas de length + fill : una cabecera/tira fija (size: 3 , size: 8 ) se sienta junto a un hermano \" \" /fill, que absorbe el sobrante. los porcentajes no deben aparecer junto a lengths en la misma division (medirian la division completa y se pasarian de reclamar). 3. fill junto a porcentajes esta bien cuando los porcentajes suman menos de 100 — el fill absorbe el resto (p. ej. \" \" + \"36%\" → la caja fill recibe el otro 64%). manten la demanda de porcentajes bajo 100 o el fill muere de hambre hasta cero. ux8.5 aplica estas reglas a cada preajuste detail para que ninguna pila deje una banda vacia en la parte inferior del terminal en los tamanos comunes (100x34, 80x24, 120x40); la prueba unitaria test detail presets split coverage full tiling recorre cada division de cada preajuste de detalle y las hace cumplir, y docs/authoring.md (\"guia de densidad\") muestra las filas trabajadas. dialecto jsonc los comentarios se eliminan antes del analisis json con strip jsonc comments en src/loader.rs (el archivo se analiza luego con serde json , que solo acepta json estricto): - comentarios de linea // — hasta el siguiente salto de linea. - comentarios de bloque / ... / — no anidables; un comentario de bloque sin terminar consume silenciosamente el resto del archivo. - el eliminador es consciente de cadenas: los marcadores de comentario dentro de un literal de cadena no inician un comentario. tanto \" como ' se registran como posibles delimitadores de cadena (se respetan los escapes), pero solo \"...\" es json valido, asi que usa comillas dobles para cada valor. - las comas finales no se soportan (el eliminador no las quita; serde json las rechaza). manten la sintaxis json estricta por lo demas. forma canonica (re)serializada layoutdef implementa un serializador espejo, asi que los disenos re-serializados salen con la misma gramatica y estas grafias exactas: numeros size para longitudes fijas, cadenas \"nn%\" para porcentajes, \" \" para fill (el fill siempre se escribe explicitamente), y luego o bien widget (seguido de options cuando el nodo lleva una) o direction + areas . valores por defecto embebidos default layout sources en src/loader.rs embebe diez archivos. los primeros siete son los valores por defecto ligados a modos en orden de paleta (indice 0–6); despues tres extras de preajuste (indice 7–9) llevan las variantes detail (dr-ux6). los extras se anaden despues de los valores por defecto ligados a modos para que los indices de modo nunca se muevan; son disenos ordinarios con un \"name\" que no se mapea a ningun layoutmode (abordados por nombre desde la paleta y alcanzables desde la tecla de ciclo de diseno del kernel despues de los modos). el orden de ranuras y los nombres estan cubiertos por pruebas unitarias (test default layouts count and order , test preset extras parse with widget options , test detail presets split coverage full tiling , test detail presets reference registry widget names ): ranura archivo \"name\" estructura raiz ids de widget usados --- --- --- --- --- 0 dashboard.jsonc dashboard vertical: header (3) → division horizontal (45%, cpu 50% + division vertical de memory/storage/network) → processes (52%) header, cpu, memory, storage, network, processes 1 vertical.jsonc vertical vertical: header (3), cpu (8), memory (8), storage (6), network (5), processes ( ) header, cpu, memory, storage, network, processes 2 horizontal.jsonc horizontal vertical: header (3) → division horizontal ( ) de cpu/memory/storage/network (25% cada uno) header, cpu, memory, storage, network 3 cpu focus.jsonc cpu focus vertical: header (3), cpu (60%), processes ( ) header, cpu, processes 4 memory focus.jsonc memory focus vertical: header (3), memory (60%), processes ( ) header, memory, processes 5 network focus.jsonc network focus vertical: header (3) → division horizontal (50%) de network + disk io (50% cada uno) → processes ( ) header, network, disk io, processes 6 process focus.jsonc process focus vertical: header (3) → division horizontal (8) de cpu/memory/storage/network (25% cada uno) → processes ( ) header, cpu, memory, storage, network, processes 7 detail dashboard.jsonc detail dashboard vertical: header (3) → cuerpo ( ) dividido en una banda de monitor (58%) de una division horizontal (cpu con options {cores/show freq} + columna lateral 36% de summary 42% / sensors 58%) y processes de ancho completo (42%, options {cpu: total}) header, cpu, summary, sensors, processes 8 detail network.jsonc detail network vertical: header (3) → cuerpo ( ) dividido en una banda de network (70%) de una division horizontal (network 60% options {ifaces: all} + columna lateral 40% de summary 30% / disk io 40% / memory 30%) y processes (30%, options {cpu: total}) header, network, disk io, summary, memory, processes 9 detail processes.jsonc detail processes vertical: header (3) → cuerpo ( ) dividido en una tira de estadisticas (division horizontal , 8 filas: summary 20% / cpu 28% / memory 18% / storage 17% / network 17%) y processes ( , options {cpu: both}) header, summary, cpu, memory, storage, network, processes se referencian nueve ids de widget distintos entre los valores por defecto: header , cpu , memory , storage , network , processes , disk io mas las llegadas ux8.4 summary y sensors (usadas por los preajustes de detalle; son nombres simples para este crate — la resolucion ocurre en tiempo de render). ninguno de los valores por defecto pone un size en la raiz. los disenos de modo comparten la misma forma: un header de 3 filas, el widget enfocado (mas una columna companera para network focus ) tomando la mayor parte del medio, y la lista processes llenando el resto (ver docs/authoring.md ). los tres extras de preajuste muestran options por widget (seccion \"options de widget\") y siguen las reglas de embaldosado de arriba (seccion \"reglas de embaldosado\"): el widget processes ejercita las claves de base cpu (\"total\" /\"both\" ), detail dashboard le da a cpu las claves cores y show freq , detail network le da a network la clave ifaces , y los nuevos widgets densos summary y sensors (adiciones al registro ux8.4) llenan las columnas cargadas de contenido de los preajustes redisenados (ux8.5). esas claves son solo refinamientos de visualizacion — los archivos son disenos validos sin ellas, y los renderizadores que aun no reconocen una clave la ignoran."},{"locale":"es","repo":"plugins","title":"plugins de xtop","label":"plugins","path":"README.md","slug":["plugins"],"text":"plugins de xtop repositorio oficial de los plugins nativos y comunitarios de xtop. workspace cada plugin vive en su propia carpeta bajo plugins/ : como funcionan los plugins - cada plugin se publica/instala de forma independiente y se integra en el kernel a traves de xtop-plugin-api . - el kernel habilita los plugins de forma opcional (feature flags para los integrados, descubrimiento en tiempo de ejecucion para los externos). una compilacion normal de xtop nunca requiere este repositorio. - instala un plugin desde el kernel con: xtop plugin install (fuente por defecto: https://github.com/xtop-cli/plugins) para empezar (desarrollo) desde la raiz de este repositorio: durante el desarrollo activo, todos los repositorios conviven lado a lado y usan dependencias por ruta local: licencia mit"},{"locale":"es","repo":"plugins","title":"arquitectura del plugin samurai","label":"docs · architecture","path":"docs/architecture.md","slug":["plugins","docs","architecture"],"text":"arquitectura del plugin samurai xtop-plugin-samurai es el plugin de monitorizacion de seguridad del ecosistema: implementa el contrato [plugin ] de xtop-plugin-api , ejecuta diez reglas heuristicas sobre la tabla de procesos, expone todo a traves de una api json de acciones y renderiza un widget tui. nunca depende del kernel, solo del crate del contrato. el id canonico y los nombres de accion son constantes exportadas (plugin id , actions:: ) y son la unica fuente de la tabla de herramientas de xtop-extension-mcp (dr-6). ciclo de vida el kernel aloja el plugin a traves de xtop-plugin-api : hook comportamiento --- --- manifest() id samurai (= plugin id ), nombre samurai , version a partir de la version del crate (env!(\"cargo pkg version\") ), descripcion, capacidades: readsysteminfo , killprocesses , modifyconfig , renderwidgets . on enable() marca el plugin como habilitado. on disable() marca el plugin como deshabilitado. on tick() cuenta los ticks; cada quinto tick reemplaza la lista de alertas con una ejecucion de analisis nueva (tick count % 5 == 0 ). los errores de analisis se propagan como pluginerror en lugar de ser ignorados. execute(action, params) api json de acciones (mas abajo); siempre registra last action /last action result . el plugin arranca habilitado y reporta su propio estado a traves de plugin.status . api de acciones execute(ctx, action, params) devuelve una cadena json (o pluginerror::{recoverable, unknownaction} ). los parametros usan la mini-sintaxis que se muestra a continuacion: cadenas simples, analizadas a mano para cada accion. accion parametros notas --- --- --- system.summary — promedio de cpu, memoria en gb/porcentaje, recuentos de procesos/disco/interfaces, tiempo de actividad (uptime), hostname, recuento actual de alertas. processes.top count o count,filter= el recuento se establece en 10 por defecto cuando no se puede analizar; 0 es un error. la regex del filtro opcional se compara con is match contra cualquiera de name, cmd, exe. processes.search pattern o pattern,fields=a,b pattern es una unica regex compilada; se compara con is match contra cualquier campo listado. campos: name , cmd , user , state , exe , cwd (por defecto name ; los campos desconocidos se ignoran). se ordena por cpu desc, con un tope de 100 resultados. process.info pid json completo de un proceso; error cuando el pid no existe. process.kill pid requiere killprocesses . process.alerts — el array completo de alertas de la ultima ejecucion de analisis. threshold.set cpu,mem,disk tres porcentajes separados por comas; requiere modifyconfig . threshold.get — umbrales actuales cpu high /mem high /disk high . config.get — theme, layout, interval ms, hostname. config.set interval ms= o theme= o layout= requiere modifyconfig ; cualquier otra cosa es un error de analisis. alerts.status — totales y recuentos por severidad, mas las 5 alertas principales. plugin.status — habilitado, recuento de ticks, ultima accion/resultado, recuentos de alertas activas y criticas. las entradas json de procesos incluyen: pid , name , cpu , mem bytes , state , user , cmd , exe , ppid , threads , run time , cwd . analisis heuristico - se ejecuta cada quinto tick (los ticks del kernel duran 1 s por defecto, asi que aproximadamente cada 5 segundos de tiempo real; la frecuencia de ticks la decide el kernel). - diez reglas con los umbrales exactos y las listas de patrones documentados en rules.md; como maximo 50 alertas por ejecucion. - lee la tabla de procesos a traves de plugincontext::snapshot() , con acceso controlado por capacidad (readsysteminfo ). - el historial de spawn storms (spawn history ) persiste entre ejecuciones dentro de la instancia del plugin. widget el plugin registra un widget de plugin llamado samurai (el mismo plugin id ). renderiza un panel con borde con el titulo samurai y dos lineas de estado del agente. para que el widget aparezca, el plugin debe estar habilitado (feature del host + capacidad renderwidgets ) y un layout debe hacer referencia a un widget llamado samurai ; el kernel renderiza los widgets de los plugins antes que los widgets integrados del pack. relacion con xtop-extension-mcp el servidor mcp ya no vive en este repositorio : es xtop-extension-mcp , en el repositorio de extensiones, que el kernel lanza como xtop mcp . la extension: - depende de este crate en tiempo de compilacion y usa plugin id para execute plugin(plugin id, …) ; - construye su tabla de 12 herramientas a partir de las constantes actions:: (nombre de herramienta = accion con . → ); - traduce los argumentos mcp tipados a la mini-sintaxis de parametros anterior y mapea los fallos de ejecucion a errores json-rpc. consulta el mcp-protocol.md de la extension para el mapeo a nivel de protocolo (wire). [plugin ]: https://docs.rs/xtop-plugin-api/latest/xtop plugin api/trait.plugin.html"},{"locale":"es","repo":"plugins","title":"reglas heuristicas de samurai","label":"docs · rules","path":"docs/rules.md","slug":["plugins","docs","rules"],"text":"reglas heuristicas de samurai el analizador de samurai ejecuta las diez reglas siguientes sobre la tabla de procesos en vivo y conserva hasta 50 alertas por ejecucion (max alerts , truncadas al final del pase). cada regla devuelve como maximo una alerta por proceso y por ejecucion; las reglas se evaluan para cada proceso en orden de prioridad, por lo que un mismo proceso puede producir varias alertas. severidades: critica , advertencia , informativa . las alertas persisten entre ejecuciones: un nuevo analisis reemplaza toda la lista de alertas, por lo que process.alerts / alerts.status reflejan la ultima ejecucion completada (las ejecuciones ocurren cada quinto tick; consulta architecture.md). todo el emparejamiento de patrones es una simple contencion de subcadena / regex sobre el campo buscado: no hay envoltura con barras ni anclaje a la cadena completa. los umbrales listados son las constantes exactas de src/lib.rs . --- regla 1 — ruta de ejecutable sospechosa (suspicious exe path ) - disparador : exe path comienza con uno de estos prefijos: /tmp/ , /dev/shm/ , /var/tmp/ , /proc/ , /private/tmp/ , /private/var/tmp/ . - severidad : critica. - ajustes : lista blanca de prefijos suspicious path prefixes . regla 2 — proceso huerfano (orphan process ) - disparador : parent pid == 1 y el nombre del proceso en minusculas no contiene ninguno de los nombres de demonio permitidos: systemd , init , launchd , sshd , login , getty , nginx , apache2 , httpd , bash , sh , zsh , tmux , screen . - severidad : critica cuando run time = 500 y el nombre no esta en la lista blanca: chrome , firefox , code , code , idea , java , dotnet , python , node , mysqld , postgres , xorg , dockerd . - severidad : critica cuando thread count 1000 o cpu usage 200.0 ; si no, advertencia. regla 9 — recuento alto de descriptores de archivo (suspicious fd anomaly ) - disparador : open files = 1000 y el nombre no esta en la lista blanca: mysql , postgres , nginx , httpd , apache , chrome , firefox , code , java , dotnet , dockerd . - severidad : informativa. regla 10 — spawn storm (recent spawn storm ) - disparador : mas de 5 pids nuevos distintos del mismo nombre de proceso dentro de una ventana movil de 120 segundos . nuevo significa run time 5) y la comprobacion de novedad son literales en rule spawn storm`; el historial vive por plugin entre ejecuciones."},{"locale":"es","repo":"plugins","title":"xtop-plugin-samurai","label":"xtop-plugin-samurai","path":"xtop-plugin-samurai/README.md","slug":["plugins","xtop-plugin-samurai"],"text":"xtop-plugin-samurai samurai es un plugin de monitorizacion y gestion del sistema preparado para ia para xtop. expone metricas del sistema, informacion de procesos y configuracion en tiempo de ejecucion a traves de una api json de acciones, ejecuta diez reglas heuristicas de deteccion de amenazas sobre la tabla de procesos y renderiza un pequeno widget tui. el plugin es una libreria que implementa el trait plugin de xtop-plugin-api ; el kernel lo aloja a traves de un feature flag y no distribuye un binario propio. funcionalidades - api json de acciones sobre un unico punto de entrada execute() : resumen del sistema, consultas de procesos (top por cpu, busqueda por regex, informacion por pid), eliminacion de procesos, umbrales de alerta (get/set), configuracion en tiempo de ejecucion (get/set), estado de las alertas y estado del plugin. - analisis heuristico de procesos con 10 reglas (rutas sospechosas, procesos huerfanos, suplantacion, escalada euid/uid, hijos de navegadores, nombres de mineros/rootkits conocidos, lineas de comandos con pipe/descarga, recuentos altos de hilos/fd, spawn storms) — se ejecuta cada quinto tick, con un tope de 50 alertas por ejecucion. - datos de alertas y estado expuestos como json para scripts, agentes y la extension mcp. - un widget de plugin llamado samurai para layouts tui personalizados. constantes del ecosistema este crate es la unica fuente de verdad (dr-6) para: - plugin id — el id del plugin alojado (\"samurai\" ), usado por el kernel y por la extension mcp para execute plugin . - actions:: — los 12 nombres de accion que entiende execute() : system summary , processes top , processes search , process info , process kill , process alerts , threshold set , threshold get , config get , config set , alerts status , plugin status . api de acciones toda interaccion pasa por: la respuesta siempre es una cadena json. los nombres de accion, los parametros y las formas de las respuestas estan documentados en docs/architecture.md; referencia rapida: accion parametros --- --- system.summary (ninguno) processes.top count o count,filter= processes.search pattern o pattern,fields=name,cmd,user,state,exe,cwd process.info pid process.kill pid process.alerts (ninguno) threshold.set cpu,mem,disk (porcentajes) threshold.get (ninguno) config.get (ninguno) config.set interval ms= , theme= o layout= alerts.status (ninguno) plugin.status (ninguno) semantica de las regex. los patrones de busqueda y filtrado se pasan directamente a regex::regex::new — sin envoltura /…/ . cada patron es una unica regex compilada, que se compara con is match() contra cualquiera de los campos que selecciones (fields= en processes.search ; name/cmd/exe en el filtro de processes.top ). ejemplos: analisis heuristico el analizador se ejecuta cada quinto tick (el kernel emite un tick aproximadamente una vez por segundo, asi que mas o menos cada 5 segundos) y conserva hasta 50 alertas por ejecucion repartidas en 10 reglas . las reglas y sus umbrales exactos estan documentadas en docs/rules.md; resumen: 1. ejecutable desde una ruta sospechosa (/tmp , /dev/shm , /var/tmp , …) 2. proceso huerfano (ppid=1) que no es un demonio conocido 3. suplantacion (nombre de sistema fuera de la ruta canonica, o discordancia entre nombre y exe) 4. escalada de privilegios euid != uid 5. hijo desconocido de un proceso de navegador 6. nombres o patrones de comando de mineros/rootkits conocidos (xmrig , minerd , pool.monero , …) 7. lineas de comandos con pipe/descarga (curl … sh , base64 -d , …) 8. recuento de hilos = 500 (que no sea un servidor/navegador en lista blanca) 9. descriptores de archivo abiertos = 1000 (que no sea un servidor/navegador en lista blanca) 10. spawn storm: mas de 5 instancias nuevas de un nombre dentro de 120 s widget tui el plugin registra un widget llamado samurai (igual a plugin id ). para verlo, habilita el plugin (necesita la capacidad renderwidgets ) y anade \"samurai\" a un layout: integracion con mcp el propio samurai no incluye un servidor mcp integrado . el acceso a las herramientas para ia lo proporciona el crate xtop-extension-mcp del repositorio de extensiones, que el kernel lanza con xtop mcp . depende de este crate en tiempo de compilacion, dirige el plugin con execute plugin(plugin id, action, params) y deriva sus 12 herramientas de las constantes actions:: ; consulta docs/mcp-protocol.md en ese repositorio para el mapeo del protocolo (wire). documentacion - docs/architecture.md — ciclo de vida, api de acciones y sintaxis de parametros, capacidades, widget, relacion con mcp. - docs/rules.md — las 10 reglas heuristicas con umbrales exactos y patrones de disparo. desarrollo licencia mit"},{"locale":"es","repo":"widgets","title":"xtop-widgets","label":"widgets","path":"README.md","slug":["widgets"],"text":"xtop-widgets paquete de widgets base para la tui xtop. los widgets son renderizadores puros: dibujan dentro de un rect recibiendo unicamente el contrato de solo lectura xtop-widget-api widgetstate — nunca tipos del kernel. el kernel resuelve (pack, name) en tiempo de render, de modo que cualquier pack puede sustituir a un widget integrado por nombre. estructura (ux9.3: un crate por widget) nombres de widget (disenos por defecto) header , cpu , memory , storage , network , processes , disk io , summary , sensors , battery , gpu . los disenos referencian los widgets por estos nombres; el agregador registra los crates de widget bajo los mismos nombres. battery y gpu no forman parte de los disenos por defecto — el kernel llega a ellos mediante el modo de pantalla completa. summary (cargas/medidores/recuentos de procesos agregados) y sensors (temperaturas por nucleo; estado vacio honesto sin datos de sensores) son las adiciones de ux8.4 para los disenos densos. seleccion de un pack en el kernel - el style.pack global se aplica a todo nombre sin una anulacion por widget. - el style.widgets. .pack por widget gana sobre la eleccion global. - los nombres ausentes en un pack elegido vuelven al pack base. - los widgets de plugins conservan precedencia sobre cualquier pack. los packs se integran en el binario como features de cargo (como los plugins). la feature widget-blocks habilita el pack de bloques ascii (xtop-widget-blocks , un hermano de los crates por widget para que los consumidores de git resuelvan su dependencia de xtop-widget-core ). disenar un widget 1. copia un crate de widget (xtop-widget- / ) o usa el widget scaffold del kernel — el crate es la unidad disenable/instalable. 2. implementa pub fn render(f, state, area) en src/lib.rs contra xtop-widget-core (marco, roles, graficos) y el contrato. 3. registra el crate bajo un nombre de widget (el agregador para los integrados; la tabla de packs para los crates de la comunidad) — el comando widget install del kernel gestiona el cableado. 4. escribe pruebas junto al widget con el doble testkit . el mapeo de glifos (colores, bordes, marcadores de grafico) proviene de los helpers canonicos en xtop widget api::glyph — ver docs/authoring.md para el recorrido completo del contrato. documentacion - docs/authoring.md — como se construye y registra un crate de widget: render , registry() , widgetrenderer /widgetregistration , la vista widgetstate , el motor compartido, los helpers canonicos de glifos, la semantica de seleccion de pack y el flujo de instalacion del kernel. - docs/widgets.md — referencia de cada nombre de widget registrado (pack base + extras solo de pantalla completa), el esquema completo de opciones de diseno por widget (claves, valores por defecto, ejemplos, reglas de fallback) y en que difiere el pack de bloques."},{"locale":"es","repo":"widgets","title":"packs de widgets de la comunidad","label":"custom","path":"custom/README.md","slug":["widgets","custom"],"text":"packs de widgets de la comunidad este directorio es el hogar reservado para los packs de la comunidad. actualmente es un andamiaje vacio: todavia no se ha contribuido ningun pack. los packs de la comunidad siguen la misma forma que los integrados (registry() sobre xtop-widget-api ), y el motor del kernel los integra en tiempo de compilacion — no hay carga de packs en tiempo de ejecucion: 1. escribe el crate del pack con un pub fn registry() - hashmap ; los packs que viven aqui se anaden como miembros de este workspace (bajo custom/ ). 2. el kernel depende del crate detras de una feature opcional de cargo y anade una entrada pack { name, renderers } a su lista de precedencia (xtop/src/ui/layout/engine.rs::packs() ). 3. los usuarios seleccionan el pack con style.pack (global) o style.widgets. .pack (por widget); los nombres ausentes vuelven al pack base. ver docs/authoring.md para el contrato completo de autoria. el descubrimiento dinamico de packs en tiempo de ejecucion se aplaza deliberadamente (roadmap del workspace, §7)."},{"locale":"es","repo":"widgets","title":"como crear (autoria) un crate de widget","label":"docs · authoring","path":"docs/authoring.md","slug":["widgets","docs","authoring"],"text":"como crear (autoria) un crate de widget un widget es un crate : se renderiza contra el contrato de solo lectura xtop-widget-api y expone un unico punto de entrada render . el kernel muestra un widget cuando un pack lo registra por nombre; este repositorio incluye dos packs — el pack base xtop-widgets (la raiz de este workspace, un agregador de los crates por widget) y el pack ascii xtop-widget-blocks (un hermano de los crates por widget). los packs de la comunidad viven en custom/ (ver su readme). estructura del repositorio la unidad que el usuario disena e instala: un crate la unidad instalable y disenable de un widget es su carpeta de crate xtop-widget- / . para disenar tu propio widget: 1. copia un crate de widget existente (cp -r xtop-widget-processes xtop-widget-mycpu ) o genera un andamiaje (el comando widget scaffold del kernel emite la misma forma). 2. renombra el paquete en cargo.toml y conserva las dependencias de contrato: xtop-widget-api + xtop-plugin-api (tipos de modelo) + xtop-widget-core (motor compartido) + ratatui ; la version sigue en 0.1.0 (politica de versiones: todo temprano, sin incrementos). 3. implementa pub fn render(f: &mut frame, state: &dyn widgetstate, area: rect) en src/lib.rs — dibuja tu vista dentro de area , protegida para rects diminutos, usando xtop-widget-core para el marco, los colores y los graficos. 4. registra el crate bajo un nombre de widget. los crates integrados los registra el agregador (xtop-widgets::registry , src/lib.rs ); los crates de la comunidad se anaden como miembros del workspace y se cablean en el kernel del mismo modo que los packs (ver \"semantica de seleccion de pack\" mas abajo) — el kernel resuelve (pack, name) en tiempo de render, de modo que un crate disenado sustituye a un widget integrado por nombre sin cambiar el diseno. las pruebas propias del widget viven en su [cfg(test)] mod tests (junto al codigo) y usan el doble de prueba compartido de la feature testkit de xtop-widget-core — declarada en los [dev-dependencies] del crate como xtop-widget-core = { features = [\"testkit\"] } . contrato del renderizador un renderizador es una funcion simple dibujada sobre un [frame ] de ratatui dentro de un [rect ], que recibe el estado de la aplicacion como &dyn widgetstate : widgetregistration es el tipo de registro canonico y vive unicamente en xtop-widget-api . los renderizadores nunca ven tipos del kernel — cada valor que necesitan llega a traves de widgetstate . el punto de entrada registry() un pack expone una funcion que devuelve sus renderizadores por nombre de widget (los nombres que usan los disenos y el modo de pantalla completa). el agregador construye un hashmap a partir de sus crates de widget: un pack registra solo los nombres que dibuja. los nombres que no proporciona vuelven al pack base (ver seleccion de pack mas abajo), asi que xtop-widget-blocks registra cpu , memory , processes , network , storage , disk io , summary y sensors en su apariencia ascii mientras que cualquier otro nombre conserva el renderizado base. que ofrece widgetstate widgetstate (xtop-widget-api, state.rs ) es la vista muestreada y de solo lectura de la aplicacion en ejecucion. sus metodos se agrupan como: - la muestra — snapshot() - option : una instantanea por tick; none antes del primer tick, asi que los renderizadores comienzan con un retorno temprano. - tema — theme name() , theme fg() , theme bg() , theme palette() (16 entradas rgb), alerts() (umbrales de cpu/mem/disco). - estilo de glifos, ya resuelto — charset(name) , borders(name) respetan el estilo global mas cualquier anulacion por widget; los renderizadores nunca resuelven la configuracion por si mismos. - historial para graficos — cpu history() (por nucleo vecdeque ), mem history() , net rx history() , net tx history() . cada entrada es (x, y) ; el eje x es el indice/tiempo de la muestra. - mapeo de procesos (ux9.1) — uid to name(uid) resuelve un uid numerico al nombre de inicio de sesion que el kernel leyo de /etc/passwd (none = mostrar el uid numerico) y process cpu history(pid) devuelve las muestras recientes de cpu por proceso (de la mas antigua a la mas nueva; vacio = no se dibuja nada). - estado de vista/control — search query() , process selected pid() , process sort label() , process sort desc() (direccion para el marcador de ordenacion: true = descendente), layout name() , is searching() , fullscreen label() , sys info() (incl. los lectores ux9.1 cpu model y package power w ), y process view() (las filas de procesos, ya filtradas por la consulta de busqueda y ordenadas por la columna del usuario; la seleccion esta anclada por pid). - opciones de visualizacion (dr-ux1) — widget options() devuelve el objeto options del nodo de diseno que se esta renderizando actualmente (none = comportamiento por defecto) y logical core count() el recuento de procesadores logicos del host (usado para normalizar los valores de cpu por proceso a un porcentaje de toda la maquina). las claves reconocidas por cada widget estan documentadas en docs/widgets.md (\"opciones de diseno por widget\"). el motor compartido (xtop-widget-core ) los crates de widget no reimplementan roles de paleta, analisis de opciones, resolucion de glifos, el motor de graficos o la rampa de temperatura — eso vive en xtop-widget-core : - util — formato (bytes/velocidades/uptime/usado-libre), constantes de roles de paleta, gauge gradient , la rampa de temperatura (temp color ), resolved charset /resolved borders , draw frame (el prologo de marco estandar de widget) y el lienzo de bufer directo painter . - options — helpers de analisis para el json options del diseno mas los tipos de seleccion de nucleo/grafico de cpu. - chart — el motor de graficos coloreado por celda (historiales) y los helpers de chispa de una fila (spark cells , etc.) para braille por fila. - testkit (feature de cargo, solo de desarrollo) — el doble de widgetstate + los helpers de terminal fuera de pantalla para pruebas. el mapeo canonico de glifos (colores, bordes, marcadores de grafico) vive en el crate de contrato — los packs no deben reimplementarlo : (to color /border for /marker for se omiten deliberadamente de la reexportacion en la raiz del crate; la ruta de importacion glyph de arriba es la unica canonica.) - to color([u8; 3]) - color — convierte una entrada de la paleta del tema a un color de ratatui (color::rgb tal cual). - border for(widgetborders) - set — el marco de bordes para la eleccion de bordes por widget resuelta. native → el marco estandar de una linea con box-drawing (border::plain , la apariencia por defecto de ratatui); rounded → border::rounded ; double → border::double ; plain y ascii → ascii border (el conjunto puro + - ). - marker for(chartcharset) - marker — el marcador de grafico de ratatui del mismo nombre (braille , dot , block , halfblock , bar ). - ascii border — el marco ascii canonico, exportado para los casos que necesitan el conjunto en si. como el mapeo es canonico, la misma configuracion dibuja de forma identica en cada pack. un pack que quiera un glifo genuinamente distinto para la misma configuracion diverge deliberadamente y documenta por que; no debe copiar la tabla de mapeo. el pack de bloques conserva su propia apariencia en las etiquetas del medidor de cpu (un relleno ascii ) mientras que los marcadores de grafico respetan state.charset(name) igual que el pack base. patron de acceso a los colores del tema los widgets pintan desde los arrays del tema, convirtiendo las entradas a traves de to color : los colores de los medidores eligen indices de paleta a partir de los umbrales de state.alerts() mediante xtop widget core::util::gauge gradient . los indices son roles semanticos (dr-ux3): la tabla de roles vive en xtop-widget-core/src/util.rs (0 bg, 1 alert, 2 good, 3 warn, 4 lectura/descarga, 5 escritura/subida, 6 acento, 7 fg, 8 dim, 9–15 rampa de series multiples) y docs/widgets.md la repite. los packs pueden usar los mismos numeros pero no deben inventar ranuras no documentadas. semantica de seleccion de pack (contrato del kernel) el motor del kernel resuelve (pack, name) en tiempo de render, tal como se implementa en xtop/src/ui/layout/engine.rs : - los packs se compilan en el binario del kernel en una lista ordenada por precedencia que comienza con el pack integrado default (xtop widgets::registry ); los packs adicionales (p. ej. blocks ) se compilan detras de features de cargo del kernel. - para cada nombre de widget el motor pregunta a la configuracion de estilo que pack lo proporciona: una anulacion por widget (style.widgets. .pack ) gana sobre el style.pack global; cuando no se elige ningun pack, el nombre se resuelve contra el pack default . - si el pack elegido no registra ese nombre (o el nombre del pack es desconocido), el motor vuelve al pack default . - los widgets de plugins (renderizados sobre la vista hoststate del plugin) se comprueban primero y tienen precedencia sobre cualquier pack. asi, un pack que quiera sustituir al cpu integrado registra un renderizador bajo \"cpu\" , el usuario selecciona el pack, y cualquier nombre que el pack no proporcione conserva su renderizado base. instalar un crate disenado (flujo del kernel ux9.2) el comando widget del kernel refleja los comandos de plugins: - widget scaffold emite una plantilla de crate de un solo widget (la forma de los crates xtop-widget- de este repositorio: manifiesto con las cuatro dependencias de contrato + src/lib.rs con render + una prueba que usa el testkit). - widget list muestra los crates integrados y sus nombres de registro. - widget install anade el crate al manifiesto y a la tabla de packs, de modo que xtop renderiza el widget bajo su nombre registrado desde el siguiente lanzamiento — los disenos lo seleccionan exactamente igual que un widget integrado."},{"locale":"es","repo":"widgets","title":"referencia de widgets","label":"docs · widgets","path":"docs/widgets.md","slug":["widgets","docs","widgets"],"text":"referencia de widgets cada widget del pack base es su propio crate (xtop-widget- ), que comparte los modulos del motor en xtop-widget-core (ver readme.md para la estructura). ambos packs dibujan dentro de un marco estandar: titulo, borders::all , el conjunto de bordes resuelto desde la configuracion de bordes por widget a traves del mapeo canonico xtop widget api::glyph::border for , y los colores fg/bg del tema. con la configuracion por defecto (widgetborders::native ) cada widget usa el marco estandar de una linea con box-drawing de ratatui (border::plain ) — la apariencia de bordes por widget es una eleccion de configuracion, no una eleccion de pack. opciones de glifos (ux7.4) cada nodo de diseno que nombra un widget puede llevar un objeto json options . dos claves son reconocidas por todos los widgets de ambos packs, con una precedencia fija — opcion de nodo de diseno valor de la configuracion de estilo valor por defecto del contrato : clave valores efecto --- --- --- charset \"braille\" \\ \"dot\" \\ \"block\" \\ \"half block\" \\ \"bar\" glifos de grafico del widget (nombres serde del enum del contrato). cuando el nodo no establece nada, gana la configuracion de estilo (state.charset(widget) , global o por widget style.widgets. ); el valor por defecto de la configuracion es braille . borders \"native\" \\ \"rounded\" \\ \"double\" \\ \"plain\" \\ \"ascii\" el conjunto de bordes del marco del widget. cuando el nodo no establece nada, gana la configuracion de estilo (state.borders(widget) ); el valor por defecto de la configuracion es native . la resolucion se ejecuta una vez por render de widget en el helper compartido (xtop-widget-core/src/util.rs resolved charset /resolved borders , consumido por cada crate de widget y por el pack de bloques). un valor de opcion malformado o desconocido se ignora — entonces se aplica el valor de la configuracion. opciones de diseno por widget (dr-ux1 + ux7) las tablas siguientes son la lista completa de claves reconocidas. reglas: - las claves desconocidas y los valores malformados se ignoran (se aplica el valor por defecto documentado); un widget nunca rompe el renderizado por una opcion erronea. - cuando una opcion filtra elementos (nucleos, interfaces, montajes) y nada coincide, el widget vuelve a todos los elementos para que nunca quede en blanco. - todos los colores provienen de la paleta del tema a traves del canonico xtop widget api::glyph::to color ; los indices de paleta son roles semanticos (dr-ux3). la tabla de roles vive al principio de xtop-widget-core/src/util.rs y refleja la tabla de roles de docs/customization.md del kernel: 0 bg, 1 alert, 2 good, 3 warn, 4 lectura/descarga (rx), 5 escritura/subida (tx), 6 acento, 7 fg, 8 dim/separadores, 9–15 la rampa de series multiples. cpu clave valores por defecto efecto --- --- --- --- chart \"average\" \\ \"per-core\" \"average\" zona de historial: el promedio de toda la maquina, o la vista por nucleo — cada columna toma el color del nucleo mostrado que alcanza su pico alli, desde la rampa de series del tema (ranuras 9..15 en ciclo). cores \"all\" \\ especificacion de subconjunto \"all\" restringir los nucleos mostrados: \"0,2,4-7\" (ids/intervalos, ascendentes). se aplica a las filas de nucleos y al grafico por nucleo. show freq bool false anade la frecuencia por nucleo (dim, alineada a la derecha, 2.40ghz ) siempre que un nucleo mostrado informe de una. el modelo lleva cpuinfo.frequency (mhz); las filas degradan con elegancia cuando todas las frecuencias son 0. show temp bool \\ \"auto\" \"auto\" celda de temperatura por nucleo (47° , a la derecha del todo, en negrita) y la marca braille de calor por nucleo: \"auto\" las muestra siempre que la instantanea lleve una temperatura por nucleo (cpuinfo.temp c , linux); true las fuerza a aparecer, false las oculta. las temperaturas nunca se fabrican : sin ninguna temperatura some en ningun sitio las celdas permanecen ocultas incluso bajo true . renderizado (ux9.5): una fila normalizada por nucleo — celda label cell (anchura fija), celda percent (alineada a la derecha, coloreada por rol), celda opcional frequency (dim), luego la barra de gradiente que rellena el resto de la columna, y a la derecha la marca de calor por nucleo (un glifo braille/bloque cuya altura es la parte de la temperatura del anclaje de 80 °c, coloreado por la rampa de calor) mas la celda opcional temperature (47° , coloreada por la rampa, a la derecha del todo) cuando la fila informa de una temperatura. solo se usan dos columnas cuando cada columna conserva su anchura minima de fila (etiqueta + porcentaje + barra ≥ 4 celdas, mas los extras cuando estan habilitados); las zonas mas estrechas vuelven a una columna. cuando ambos extras dejarian la barra famelica, el bloque de temperatura cede primero, luego la frecuencia. las filas que no caben en la altura se recortan (ganan los nucleos superiores, en orden de columnas). titulo (ux9.5): el nombre del modelo de cpu (saneado, truncado) se anade entre parentesis cuando el kernel informa de uno — cpu (amd ryzen 7 5800x …) — max 48°c — junto con el sufijo existente de temperatura maxima; sin modelo, el clasico cpu (max: 47.5°c) permanece byte-identico. los titulos largos se cortan a la anchura de la zona con … . fila unificada de uso+temp+potencia (ux9.5): cuando la rejilla deja al menos dos filas, la primera fila sobrante es un medidor compuesto de una linea: usage 42% ▂▄▆▆ temp 47° ▅▆▅▆ power 38.4w ▂▃▄▅ — tokens de palabra con valores reales intercalados con porciones coloreadas (la fila es su propia leyenda, asi que no se necesita una clave separada). cada porcion es una parte honesta: el uso de su escala de 100% (gradiente de medidor), la temp del anclaje caliente de 80 °c (rampa de calor), la potencia de un techo de visualizacion documentado de 200 w (gradiente de medidor, aviso al 50 %, alerta al 90 % del techo). los segmentos aparecen solo para datos que son some : el segmento de temp necesita al menos una temperatura por nucleo (y la preferencia de visualizacion show temp ), el segmento de potencia el lector de paquete del kernel (systeminfo.package power w — rapl en linux). con solo datos de uso la fila es la barra promedio clasica (avg: nn% + barra de gradiente). cuando no existe historial dibujable, la linea resumen numerica arrastra la potencia del paquete (avg: 42% pkg 38.4w ) para que la potencia nunca quede oculta. la fila nunca dibuja basura vacia: no se fabrican segmentos. linea de especificacion de historial (ux9.5): la fila braille de historial anonima inferior ahora se autodescribe — su divisor lleva la etiqueta dim history: cpu % cuando la anchura lo permite. debajo de las filas la zona de historial usa el motor de graficos (ver \"motor de graficos\" mas abajo). cuando el motor no puede dibujar (zona mas estrecha que 12 columnas, o menos de dos muestras de historial) la fila sobrante muestra un resumen numerico compacto en lugar de basura: avg: nn% (con los recuentos de nucleos mostrados/total cuando un subconjunto cores oculta nucleos, y el lector pkg de arriba). processes clave valores por defecto efecto --- --- --- --- cpu \"core\" \\ \"total\" \\ \"both\" \"core\" base de la columna cpu. core = fraccion de un nucleo logico, celdas como 12.5% . total = cpu usage / logical core count() (la parte de la cpu de toda la maquina), estilo parte-de-maquina: un decimal por debajo de 10 (0.7 ), un entero en/por encima de 10 (34 ). both muestra las dos columnas lado a lado (cpu por nucleo + cpu% total). el cpu usage por proceso subyacente nunca cambia. columns.memory bool true mostrar la columna mem (la columna tambien se elimina automaticamente cuando la zona es demasiado estrecha). columns.user bool true mostrar la columna user (misma regla de eliminacion automatica). columns.cmd bool true mostrar la columna command (misma regla de eliminacion automatica). zebra bool true fondos dim alternados en las filas impares. diseno fijo (ux7.3 + ux9.4): pid (alineado a la derecha, 7), name (alineado a la izquierda, flexible, truncado con … ), la chispa de cpu (ver mas abajo), cpu% (derecha, 6; dos columnas bajo both ), mem (derecha, 10), user (izquierda, 9) y command (izquierda, flexible) — separados por un dim │ . las columnas name y command comparten la anchura flexible de la fila (name conserva como maximo 24 caracteres, command el resto); las columnas numericas estan alineadas a la derecha; la fila de cabecera es de acento-negrita; el marcador de ordenacion (▼/▲ de process sort desc ) se renderiza solo en la celda de cabecera de la columna ordenada. las columnas se eliminan de derecha a izquierda a medida que la zona se estrecha: command, luego user, luego mem, luego la columna cpu de base total, luego la chispa; por debajo del minimo solo queda pid cpu% . las filas son lineas logicas unicas — nada se envuelve o colisiona. nombres de usuario (ux9.4): la columna user muestra el nombre de inicio de sesion resuelto mediante state.uid to name(uid) (el kernel lee /etc/passwd ); cuando el kernel no tiene un mapeo (cuenta desconocida o no local) se muestra el uid numerico — los nombres son un mapeo de visualizacion, nunca fabricados. command (ux9.4): cada fila muestra la linea de comando completa — el vector de argumentos del kernel (cmd full , unido) cuando esta poblado, cayendo al unico cmd , luego la ruta del ejecutable, luego ? — junto al name corto del programa. chispa de cpu (ux9.4): una pequena chispa braille por fila (4 celdas, degradando a 2 en filas estrechas) de las muestras recientes de cpu del proceso (state.process cpu history(pid) , de la mas antigua a la mas nueva, un glifo por cubo, picos de cubo preservados). cada celda se colorea por uso a traves de la rampa de calor — los procesos inactivos pintan glifos pequenos y bajos, los que martillean celdas completas ⣿ en el color de alerta. el charset braille pinta glifos braille (⣀⣰⣶⣿ ), los charsets de bloque la rampa de bloques de 8 niveles (la opcion charset se aplica). un historial vacio (pid visto recientemente) dibuja un marcador de posicion dim · — las muestras nunca se fabrican. desplazamiento de viewport. el process view() del kernel devuelve la lista completa ordenada (filtrada por busqueda); la seleccion se ancla por pid. el widget deriva el indice seleccionado de la posicion del pid en esa lista y renderiza una ventana alrededor suyo — la seleccion siempre esta visible, la ventana comienza en la fila 0 mientras la seleccion este cerca de la parte superior, y los saltos conservan aproximadamente media pantalla de contexto. cada proceso de la lista es alcanzable con las teclas de arriba/abajo existentes; una barra de desplazamiento dim (pulgar de acento) aparece en el borde derecho cuando la lista es mas larga que la zona. no se necesito ningun cambio en el kernel (el kernel ya mueve la seleccion sobre la lista completa ordenada). busqueda (pre-filtro del kernel): la subcadena de la consulta en la columna name se resalta (fondo de acento, negrita). la seleccion conserva el estilo de fila de acento; el resaltado de confirmacion de kill necesitaria una bandera \"kill pendiente\" en el contrato del widget, que no existe — senalada como una dependencia del lado del kernel. network clave valores por defecto efecto --- --- --- --- ifaces \"all\" \\ [\"eth0\", ...] \"all\" que interfaces cubren las filas y las lineas agregadas. las filas son lineas logicas unicas (nunca envueltas). niveles de anchura (anchura interna): = 60 filas por interfaz con nombre, barra de actividad (escalada a la interfaz visible mas rapida, coloreada por su direccion dominante), velocidades rx y tx en sus roles de direccion, y totales acumulados dim; = 41 elimina los totales; = 26 elimina tambien la barra; por debajo de 26 el widget muestra dos lineas agregadas (rx rate tot bytes / tx … ) sobre la seleccion, truncadas con … . una pista dim +n more sustituye a la cola cuando la lista de interfaces desborda la caja. el grafico de historial rx/tx de toda la maquina (el contrato solo registra un historial de red por direccion, nunca por interfaz) se dibuja debajo de las filas cuando la caja tiene al menos 16 columnas de ancho y ambos historiales llevan ≥ 2 muestras; usa el motor de graficos con el coloreado fijo de rol rx (4) / tx (5) y consume cada fila sobrante. mientras el historial esta vacio el widget expande las filas de interfaces a toda la caja y — cuando la lista es corta — anade lineas rx/tx agregadas en vivo para que no queden filas muertas entre el contenido y el marco. storage clave valores por defecto efecto --- --- --- --- disks \"all\" \\ [\"/\", \"/boot\"] \"all\" que montajes mostrar (coincidencia por mount point exacto). una fila por montaje, nunca envuelta. las filas anchas (ux9.6) son mount …bar… nn% used 50 gb · free 200 gb — las cantidades usadas y libres (free = total space − used space , honesto: el kernel mapea used space exactamente asi); la cantidad libre se colorea por la rampa de medidor de lo usado (mucho espacio libre se lee como good, un disco casi lleno se lee como alert) y, en filas muy anchas, una barra braille de parte libre sigue al detalle (altura = la parte libre actual, color = la misma rampa de margen — diskinfo no lleva historial de capacidad, asi que no hay serie temporal que chispear; la barra es una lectura braille instantanea honesta). por debajo de la anchura amplia los niveles degradan: mount nn% mas la barra (la etiqueta cede espacio a la barra), por debajo de 11 columnas texto plano mount nn% . cuando la caja da a cada montaje al menos dos filas (height/n = 2, height = 4, width = 18 ) cada montaje se renderiza como un bloque de medidor — linea de montaje (etiqueta + porciento alineado a la derecha + cantidades usada/libre), luego la barra de usada u , mas la barra de disponible a (diskinfo.available space , reflejada a traves de la rampa de usada invertida) cuando existe una tercera fila — asi las barras por disco escalan con la altura de la caja. solo viven aqui las metricas de capacidad — las velocidades de e/s por dispositivo estan en disk io (que no tiene campos de capacidad en el modelo, asi que los indicadores libres pertenecen solo a este widget). memory clave valores por defecto efecto --- --- --- --- sections [\"memory\", \"available\", \"swap\"] las tres que filas de medidor dibujar (ram, luego la fila available, luego swap). una lista vacia/desconocida conserva las tres. una fila de medidor por seccion, nunca envuelta: label (negrita), una celda de porcentaje alineada a la derecha (coloreada por rol), luego la barra de gradiente. las filas anchas muestran las cantidades usada y libre (ux9.6): ram 50% ██████ used 8.0 gb · free 7.0 gb ⣿⣶⣰ — la cantidad libre (memoryinfo.free del kernel para ram, swap.free para swap) se colorea a traves de la rampa de margen (good mientras haya margen de sobra, warn/alert a medida que la maquina se aprieta), y la fila ram arrastra una chispa braille de la parte libre a lo largo del tiempo derivada honestamente del historial de porcentaje usado del kernel (free = 100 − used por muestra; el kernel solo registra el porcentaje usado). cada celda de la chispa se colorea por la escasez de la parte libre, asi que una maquina que se ahoga pinta glifos rojos bajos. la fila available (avl ) se renderiza solo cuando la instantanea puede derivarla (memoryinfo.available , total no nulo) — su barra se rellena con la parte available , coloreada por la misma rampa de medidor invertida (gauge(100 − avail%) ), asi que se vuelve roja de alerta cuando la maquina se queda sin margen; swap se vuelve rojo de alerta pasando el umbral de memoria. por debajo de 10 columnas internas todo el widget colapsa a una linea resumen (ram 50% avl 50% swp 13% ). el grafico de historial de ram se dibuja debajo de las filas cuando la zona tiene al menos 14 columnas de ancho y el historial tiene ≥ 2 muestras; el grafico consume cada fila sobrante (ver \"motor de graficos\") — el grafico es el historial de ram, nunca por seccion. disk io una fila de una sola linea por dispositivo, nunca envuelta: las filas anchas muestran name , las velocidades de lectura y escritura (roles de direccion 4/5) mas pequenas barras de velocidad escaladas al dispositivo mas rapido de la vista; por debajo de la anchura compacta las filas vuelven a name r rate w rate con unidades sin espacios en cajas muy estrechas, truncadas con … como ultimo recurso. no disk i/o data cuando la instantanea no lleva ningun dispositivo. cuando la caja tiene al menos 16 columnas de ancho y el contrato registra los historiales de disco agregados de toda la maquina (disk read history() / disk write history() , ambos ≥ 2 muestras), las filas reservan dos filas de texto y las filas sobrantes alojan el grafico dual de lectura/escritura — lecturas rol 4, escrituras rol 5, eje y el pico visible de ambas series (misma geometria que el grafico de red; las filas son por dispositivo mientras que los historiales son agregados de toda la maquina, exactamente como las filas de red frente a su historial agregado). una pista dim +n more sustituye a la cola de dispositivos que no cabe. sin historiales las filas simplemente se expanden a toda la caja (fallback solo de texto). diskioinfo expone solo velocidades y contadores de bytes — sin capacidad — asi que los indicadores usado/libre de ux9.6 viven en storage (diskinfo por montaje). summary panel compacto siempre lleno de numeros agregados; sin opciones especificas de widget (solo claves de glifos). filas de contenido (cada una una unica linea logica, truncada con … , nunca envuelta): 1. load 2.81 2.30 2.42 — valores coloreados por su parte de los nucleos logicos (logical core count() ), misma regla de medidor que el header. 2. fila de medidor cpu — uso promedio de la maquina: celda de porcentaje, barra de gradiente coloreada por rol, recuento de nucleos dim en filas anchas. 3. fila de medidor mem — porcentaje usado + barra; detalle used/total en filas anchas. 4. procs 264 run 2 sleep 211 … — el recuento de procesos de la instantanea mas los recuentos por estado (cubos por subcadena sin distincion de mayusculas de processinfo.state : run/sleep/zombie/idle/ stop, cualquier otra cosa se pliega en \"other\"); cuando cada cadena de estado esta vacia solo se muestra el total. los recuentos nunca fabrican un cubo. 5. uptime 0d 7h 27m 9s . las cajas mas altas que las cinco filas de contenido dibujan el grafico de historial de promedios de carga (load history() ) en las filas sobrantes — autoescalado al pico de la ventana visible (una vista de tendencia, coloreada con el rol good, la misma escala y color que la chispa en linea); con muy poca altura para el grafico (o sin historial) la fila de carga arrastra una chispa en linea de rampa de bloques cuando la anchura lo permite. con altura 4 el widget muestra las filas de contenido superiores y aun asi llena la caja. sensors panel de temperatura por nucleo; sin opciones especificas de widget (solo claves de glifos). cuando cualquier nucleo expone cpuinfo.temp c (linux), el widget renderiza una rejilla en orden de columnas de celdas cpu0 47° — cada valor coloreado por la rampa de temperatura (interpolada entre los roles good/warn/alert del tema, ver \"rampa de temperatura\") con una barra /de gradiente escalada al anclaje de 80 °c en filas de una sola columna; el titulo lleva la temperatura maxima (snapshot().cpu temp , cayendo al valor maximo por nucleo). las filas que desbordan la altura se recortan en silencio (como la rejilla de cpu). cuando no existen datos de temperatura en ningun sitio (temp c es none en cada nucleo — macos, windows, hosts sin sensores), la caja renderiza la linea honesta no temperature data mas los promedios de carga (coloreados por rol como en el header) — nunca vacia, nunca fabricada. las filas sobrantes bajo la rejilla (o las lineas de estado vacio) alojan el grafico de historial de promedios de carga cuando el kernel lo registra y la anchura lo permite. header , battery , gpu no se reconocen opciones especificas de widget (las claves desconocidas se ignoran; se aplican los valores por defecto). las claves de glifos charset /borders de la tabla superior se aplican a cada widget, incluidos estos. motor de graficos (ux7.1 + ux8.4) xtop-widget-core/src/chart.rs implementa el trazador coloreado por celda usado por las zonas de historial de cpu/memory/network/disk io/summary/ sensors y los helpers de chispa de una fila (spark cells /spark glyph /spark levels ) usados por las chispas braille por fila (chispas de cpu de procesos, chispas de memoria libre, barras braille de parte libre de storage). modelo: - una serie es una lista de muestras (x, y) (ordenadas por x, espaciadas uniformemente — los historiales del contrato lo estan). las columnas muestrean un interpolante lineal por tramos en su indice central; las muestras que comparten columna tambien contribuyen con su maximo (preservacion de picos). - las columnas se rellenan desde la linea base cero a la resolucion vertical: braille = 4 sub-filas por fila de texto (puntos ⣀ ⣰ ⣶ ⣿ para 1–4 sub-filas encendidas), block /half block = 8 sub-filas por fila de texto via la rampa de bloques ▁▂▃▄▅▆▇█ . una zona de grafico de altura h renderiza h filas de texto — el motor nunca colapsa: los widgets le entregan todo el rect sobrante, asi que una caja estilo 100×34 de memory/network/disk io con cinco filas sobrantes dibuja un trazado braille de cinco filas (un valor de 25% enciende la(s) fila(s) inferior(es) por completo mas una celda superior parcial). un trazado de altura 1 es una sparkline y siempre usa la rampa de bloques de 8 niveles — una fila de texto no puede alojar mas de 4 niveles braille, que es exactamente el braille de una linea apretada que este motor sustituye. - las mini chispas de una fila son diferentes: las muestras discretas se mapean a celdas discretas, asi que los charsets braille pintan glifos braille (⣀⣰⣶⣿ — una celda braille ya lleva 4 sub-niveles) y los charsets de bloque la rampa de 8 niveles. los colores por celda provienen de un mapeo de roles proporcionado por el llamador (reglas de calor para el uso, reglas de escasez para las partes libres). las series mas cortas que el recuento de celdas pintan solo sus muestras (las celdas restantes quedan vacias). - los charsets dot /bar conservan la ruta clasica chart de ratatui (marcador via el canonico marker for ), incluidas sus etiquetas de eje — el motor nunca las dibuja. regla de color (determinista, por celda): - series de rol fijo (cpu por nucleo, network rx/tx, disk io r/w): la celda toma el color de rol de la serie cuya sub-fila encendida mas alta este mas arriba; los empates se resuelven a la serie listada primero (network pasa rx antes que tx, asi que los empates se leen como rx; disk io pasa las lecturas antes que las escrituras). - calor (serie unica promedio/ram/carga): la sub-fila encendida mas alta se mapea a gauge gradient(level/total subrows 100, alert at) en las mismas ranuras de rol que usan los medidores — las celdas bajo el 50% del eje son good , del 50% al umbral de alerta warn , en/sobre el alert . cpu usa alerts().cpu high , ram alerts().mem high ; los graficos de carga de summary/sensors usan el recuento de nucleos logicos como eje y alerts().cpu high como umbral (la misma semantica que el coloreado de carga del header); los graficos de network/disco siempre estan coloreados por rol. las zonas de historial muestran un divisor dim ─ cuando hay al menos tres filas disponibles y el trazado inferior tiene al menos dos filas de alto (el divisor de cpu lleva la etiqueta dim history: cpu % cuando la anchura lo permite — ux9.5). anchuras minimas de grafico: cpu 12, memory 14, network 16, disk io 16, carga de summary/sensors 12 columnas — las zonas mas estrechas vuelven al resumen numerico descrito por widget. rampa de temperatura (ux8.4) la ui de temperatura (la celda show temp de cpu + las marcas de calor por nucleo, la rejilla del widget sensors, el segmento de temp de la barra unificada de cpu) se colorea por una rampa derivada de los propios roles de medidor del tema — no se inventa ninguna ranura nueva de paleta , asi que la tabla de roles se mantiene consistente y los colores de rol de bajo contraste que el kernel eleva al cargar el tema se propagan a la rampa. puntos finales (documentados en xtop-widget-core/src/util.rs ): - en/bajo 45 °c la rampa es el color de rol good (ranura 2), - a 60 °c pasa el color de rol warn (ranura 3), - en/sobre 80 °c es el color de rol alert (ranura 1), con los colores intermedios interpolados por canal entre los colores de rol (util::temp color ). pack base (xtop-widgets ) registry() registra 11 nombres: header , cpu , memory , storage , network , processes , disk io , battery , gpu , summary , sensors . header , cpu , memory , storage , network , processes , disk io , summary y sensors son los nombres que referencian los disenos por defecto del kernel; battery y gpu no forman parte de ningun diseno por defecto y son alcanzables a traves del modo de pantalla completa del kernel (fullscreenwidget::battery/gpu mapean a los nombres \"battery\" /\"gpu\" en ui/screen.rs del kernel). todos los renderizadores devuelven pronto cuando la instantanea es none (pre-primer-tick), asi que cada widget es seguro con un estado vacio. nombre dibuja datos de opciones --- --- --- --- header una linea resumen (area.width = 80 ) o dos: segmentos con codigo de color — host (fg en negrita) \\ tema (acento) \\ diseno (ranura de rampa 9) \\ uptime \\ promedios de carga coloreados por su parte de los nucleos logicos; anade [full: …] y marcadores [/] search cuando estan activos. el bloque pertenece a un paragraph . sys info().hostname , layout name() , snapshot().uptime /load avg , fullscreen label() , is searching() , logical core count() solo claves de glifos cpu una fila por nucleo (etiqueta/porcentaje/freq/marca-de-calor/temp/barra, ver arriba), 2 columnas cuando es bastante ancho; la fila unificada de uso+temp+potencia y el grafico de historial etiquetado debajo cuando la zona lo permite. titulo: modelo + temperatura maxima. snapshot().cpus (incl. temp c ), sys info().cpu model /package power w , cpu history() , alerts().cpu high chart , cores , show freq , show temp , claves de glifos memory filas de medidor ram/avl/swp (cantidades usada/libre + chispa de parte libre de ram en filas anchas) mas el grafico de historial de ram cuando la zona es bastante ancha y alta; el titulo gana un marcador ⚠ sobre el umbral de memoria. snapshot().memory /swap , mem history() , alerts().mem high sections , claves de glifos storage una fila de una sola linea por disco montado (etiqueta, porcentaje, barra de gradiente; cantidades usada/libre en filas anchas + barra braille libre cuando es muy ancha); las cajas altas renderizan bloques de medidor de tres lineas por disco (mount + barra u usada + barra a available) para que las barras por disco escalen con la altura de la caja. snapshot().disks , alerts().disk high disks , claves de glifos network filas de velocidad de una sola linea por interfaz (o lineas rx/tx agregadas cuando es estrecho); grafico de historial rx/tx debajo cuando es bastante ancho y alto (lineas en vivo agregadas llenan el hueco mientras el historial esta vacio). snapshot().networks , net rx history() , net tx history() ifaces , claves de glifos processes tabla de columnas fijas con pid / name / chispa-de-cpu / cpu% / mem / user / command, separadores dim │ , cabecera de acento con el marcador de ordenacion solo en la columna ordenada, filas zebra, fila de seleccion, ventana de desplazamiento de viewport con barra de desplazamiento en el borde derecho; nombres de usuario resueltos a traves del mapa de uids del kernel, comandos desde el argv completo, chispas braille de cpu por proceso. process view() , uid to name() , process cpu history() , process sort label() , process sort desc() , search query() cpu , columns , zebra , claves de glifos disk io una fila de una sola linea por dispositivo: nombre, velocidades r/w en los roles de direccion (las filas anchas anaden barras de velocidad); grafico de historial dual lectura/escritura en las filas sobrantes cuando el contrato registra los historiales de disco agregados; \"no disk i/o data\" cuando esta vacio. snapshot().disk io , disk read history() , disk write history() solo claves de glifos battery un medidor por bateria: nombre, %, estado, minutos para lleno/vacio cuando corresponda; \"no battery data available\" cuando esta vacio. snapshot().batteries solo claves de glifos gpu un medidor por gpu: nombre, %, memoria usada/total, temperatura; \"no gpu data available\" cuando esta vacio. snapshot().gpus solo claves de glifos summary promedios de carga (coloreados por rol segun la parte de nucleos) + medidores cpu/mem + recuentos de procesos + uptime; grafico de historial de promedios de carga en las filas sobrantes (chispa en linea en la fila de carga con alturas pequenas). snapshot().load avg /uptime /processes /memory /cpus , load history() , logical core count() solo claves de glifos sensors rejilla de temperatura por nucleo coloreada por la rampa de temperatura (el titulo lleva la maxima); \"no temperature data\" + promedios de carga cuando no existe ningun temp c en ningun sitio; el grafico de carga llena las filas sobrantes. snapshot().cpus (temp c ) /load avg , cpu temp , load history() solo claves de glifos los historiales los dibuja el motor de graficos descrito arriba (charsets braille/block/half-block, colores por celda), conservandose la ruta clasica chart de ratatui para los charsets dot /bar . pack de bloques (xtop-widget-blocks ) registra cpu , memory , processes , network , storage , disk io , summary y sensors ; cualquier otro nombre vuelve al pack base (contrato del kernel, ver docs/authoring.md ). activalo con la feature widget-blocks del kernel y selecciona el pack por widget o globalmente. el pack de bloques consume el mismo motor compartido que los crates de widget (xtop-widget-core ): los roles de paleta, los analizadores de opciones, la resolucion de glifos, el motor de graficos y los helpers de chispa son canonicos — el pack conserva su identidad ascii (rellenos ) y su composicion de filas/tablas en su propio codigo. en que difiere del pack base: - identidad ascii: las barras y los rellenos son caracteres en lugar de glifos de bloque; el marco y los glifos de grafico siguen el charset y los bordes resueltos exactamente igual que el pack base. las chispas braille por fila y las marcas de calor usan los mismos helpers de glifos que el pack base (resueltos por charset). - cpu — titulado \"cpu blocks\" (+ el modelo y la temp maxima): una fila de una sola linea por nucleo (etiqueta, porcentaje, frecuencia opcional, marca de calor por nucleo, temperatura y barra ), respetando cores , show freq , show temp ; cuando la rejilla deja una fila, la linea unificada de uso+temp+potencia (porciones , paridad ux9.5) se dibuja debajo. - memory — titulado \"memory (blocks)\": filas de medidor ram/avl/swp con barras , cantidades usada/libre en filas anchas y la chispa braille de parte libre de ram, mas el grafico de historial de ram a traves del motor (braille por defecto; los conjuntos de glifos block /half block se alcanzan por widget), respetando sections . - processes — titulado \"processes (blocks)\": una tabla ascii separada por (pid / name / chispa-de-cpu / cpu… / mem / user / command) con la politica de eliminacion de columnas del pack base, nombres de usuario resueltos, lineas de comando completas, chispas braille de cpu por proceso, marcador de ordenacion en la columna ordenada, filas zebra y la misma ventana de desplazamiento de viewport; respetando cpu , columns , zebra . - network — titulado \"network (blocks)\": filas de barra por interfaz (o lineas agregadas cuando es estrecho) con los mismos niveles de anchura y el grafico dual rx/tx del motor en las filas sobrantes, respetando ifaces . - storage — titulado \"storage (blocks)\": una fila de relleno por montaje con cantidades usada/libre en filas anchas (+ la barra braille de parte libre cuando es muy ancha), respetando disks y el umbral de alerta de disco; las cajas altas renderizan los bloques de medidor de tres lineas por disco (mount + barras u + a ). - disk io — titulado \"disk i/o (blocks)\": lineas r/w por dispositivo con barras y unidades compactas en cajas estrechas mas el grafico dual lectura/escritura del motor en las filas sobrantes cuando existen los historiales agregados. - summary — titulado \"summary (blocks)\": las mismas filas de contenido con barras de medidor y el grafico de promedios de carga del motor en las filas sobrantes. - sensors — titulado \"sensors (blocks)\": rejilla de temperatura por nucleo con barras de calor (mismos colores de rampa) y el mismo estado vacio honesto. notas de comportamiento - todos los widgets son defensivos con zonas pequenas: las secciones de grafico solo se renderizan cuando la zona es bastante ancha/alta, las filas de texto son lineas logicas unicas (truncadas con … donde un nivel no puede alojarlas), y los widgets de listas dejan de anadir filas cuando la zona se agota. las pruebas de humo renderizan cada widget registrado de ambos packs a 100x34, 100x30, 80x24, 60x20, 40x15 y 20x10 con estado vacio y muestreado, verifican que cada fila permanece dentro del marco (sin deteccion de envoltura) y que los graficos producen glifos de varias filas cuando el trazado tiene al menos dos filas de alto (ver src/lib.rs de xtop-widgets , mod tests , y xtop-widget-blocks/src/lib.rs , mod tests ; ambas suites de pruebas comparten el testkit de xtop-widget-core — el doble de widgetstate detras de la feature de cargo testkit ). - los widgets nunca mutan estado y nunca tocan tipos del kernel; las entradas de renderizado son la vista widgetstate mas el systemsnapshot por tick. - los datos de historial de cpu (cpu history() ) son por nucleo y estan alineados por indice con snapshot().cpus ; el grafico por nucleo lo lee a traves del cpu id de los nucleos mostrados. los historiales de red son velocidades de toda la maquina (bytes/s), asi que el eje y del grafico de red es el maximo visible de ambas series. la misma forma se aplica a los historiales de disco agregados (disk read history() / disk write history() , bytes/s) y a load history() (promedio de carga de 1 minuto) — los tres son la superficie aditiva ux8.3 de widgetstate con valores por defecto vacios, consumida solo cuando no esta vacia. ux9.4 anade el mapa uid→nombre (uid to name , por defecto none ) y el historial de cpu por proceso acotado (process cpu history , por defecto vacio) a esa superficie aditiva. - cada color que pinta una cantidad, segmento o chispa esta dirigido por el tema a traves de las reglas documentadas de rol/rampa; nada nuevo se inventa cuando los datos son none — los widgets muestran el fallback honesto (uid numerico, ? , · , segmento oculto, barra clasica) en su lugar."},{"locale":"es","repo":"xtop","title":"registro de cambios","label":"changelog","path":"CHANGELOG.md","slug":["xtop","CHANGELOG"],"text":"registro de cambios [0.1.0] - 2026-09-04 nota de politica de versiones: el ecosistema sigue en una fase temprana — todo esta en 0.1.0. la entrada de abajo consolida en una unica lista acumulativa el trabajo del kernel posterior al refactor de este ciclo (formato de tema v2 + motor de contraste, extras de preset, direccion de ordenacion, chrome con colores de rol, externalizacion del ecosistema, superficie de datos, densidad y legibilidad). superficie de datos del kernel - formato de tema v2: los 12 ficheros de tema incluidos llevan claves explicitas background /foreground de las paletas propietarias canonicas (los ficheros heredados caen a slot0/slot7); semilla actualizada 4 → 5. - motor de contraste (luminancia wcag): al cargar, los roles de texto (fg, dim, accent, cebra, marcas de series) se normalizan contra el fondo del tema con elevaciones que preservan el tono cuando estan por debajo del minimo; regresion fijada (el fg de paris nunca es palette[7] cuando iguala el fondo). - superficie de datos: cpuinfo.temp c (la sonda coretemp de linux rellena las temperaturas por nucleo donde no hay ambiguedad; los stubs de macos/windows/respaldo devuelven none) e historiales acotados de widgetstate para las tasas agregadas de lectura/escritura de disco y la carga media. - cpuinfo.frequency , .governor , .name (provider.rs) y otras metricas recopiladas (state, run time, threads, open files, disk bytes, exe path, cmd, cmd full, parent pid) siguen fluyendo por el snapshot para los widgets y los plugins. - widgets densos: las cajas escalan los graficos a toda la altura; vista de temperatura por nucleo de cpu (show temp ); fila de memoria disponible; graficos braille duales de varias filas para network/disk io; nuevos widgets summary y sensors (ambos packs); presets detail reconstruidos como paginas de monitor completo sin filas huerfanas en los tamanos habituales (reglas de teselado documentadas). presets, chrome y extras de layout - los tres extras de preset se llaman detail (nombres de fichero detail dashboard , detail network , detail processes ; nombres visibles detail dashboard , detail network , detail processes ). superposicion de ayuda, docs, tests y comentarios actualizados; version de semilla subida de 3 → 4 para que las instalaciones existentes vuelvan a sembrar las plantillas de layout renombradas (los ficheros sembrados antiguos se dejan intactos, segun la politica de nunca-sobrescribir). - presets de layout como extras (dr-ux6): el kernel incrusta los tres layouts de preset de referencia de xtop-cli/layouts despues de los siete layouts ligados a modos; la tecla de ciclo de layout los alcanza en orden de fichero y vuelve al inicio, la paleta de comandos los lista por nombre, y la siembra de assets del primer arranque los copia como plantillas editables (version de semilla 2 → 3). - el documento de diseno del ciclo de ux se reestructura en docs/design.md : una declaracion neutral de las propias reglas de diseno de xtop (roles de tema, conjuntos de glifos, opciones de visualizacion por widget, respaldos de ancho minimo, reglas de color de graficos, funcionalidades diferidas), con las claves de opcion reconocidas por widget referenciadas de forma cruzada a los docs del repo de widgets. todos los enlaces actualizados. - pulido del chrome del kernel (arrastre de ux5, huecos cerrados): la superposicion de busqueda dibuja ahora su borde con el conjunto de glifos configurado style.borders , igual que los popups de ayuda y paleta; los titulos conservan el rol accent, los separadores el rol dim, y no quedan bordes de doble linea fijos en el chrome del kernel. - el ciclo de direccion de ordenacion y las opciones de visualizacion por widget se arrastran sin cambios desde el mismo ciclo; los cambios de renderizado del lado de los widgets (lenguaje visual v2) aterrizan en las notas del changelog del repo de widgets. ordenacion de procesos, paleta de comandos, colores de rol - direccion de ordenacion de procesos: la tecla de ordenacion (s , configurable) alterna entre invertir el orden en la columna actual (arranque por defecto: cpu% descendente, sin cambios) y avanzar a la siguiente columna (cpu% → mem → pid → name), que empieza descendente — una pulsacion invierte ▲ /▼ , la siguiente pulsacion avanza. el orden realmente renderizado respeta la direccion, y process sort desc() (nuevo metodo aditivo de widgetstate , por defecto false ) permite al widget de procesos dibujar el marcador de columna. - la entrada \"sort\" de la paleta muestra la columna actual con su marcador de direccion. - chrome del kernel con colores de rol (dr-ux3): nuevos accesores de rol de tema (accent() = ranura 6, dim() = ranura 8, documentados en docs/colors.md ); la superposicion de ayuda y los popups de busqueda/paleta usan titulos/bordes accent, separadores dim y el conjunto de glifos style.borders configurado (la ayuda ya no fija bordes de doble linea); los medidores de la vista minima conservan sus colores de rol con los roles documentados. - texto de la superposicion de ayuda actualizado: se listan el comportamiento de direccion de ordenacion y los layouts de preset; la seccion \"layouts\" refleja modos → presets → layouts de usuario. externalizacion del ecosistema (ciclo monocrate completado) - el kernel es ahora un host fino sobre el ecosistema externalizado. consume los crates de contrato de xtop-cli/api (xtop-plugin-api , xtop-widget-api , xtop-extension-api ), los packs de widgets de xtop-cli/widgets , los layouts de xtop-cli/layouts , el plugin samurai de xtop-cli/plugins y la extension mcp de xtop-cli/extensions como dependencias git flotantes con feature flags opcionales. - eliminado el resto muerto y sin compilar src/commands/plugins dir tmp/ del layout pre-monocrate. (ruta escrita en scripts/audit.sh como guarda de ausencia.) - rust-version = \"1.87\" declarado. ci solo local - el flujo de ci de github actions (.github/workflows/ci.yml ) se ha eliminado: el bloqueo es solo local (scripts/ci.sh , scripts/audit.sh ) y no se volvera a habilitar hasta que el pipeline de releases este 100% listo. consolidacion de contratos - eliminado config::alertthresholds : la configuracion persistida usa ahora xtop plugin api::alertthresholds directamente (claves json identicas cpu high /mem high /disk high ; valores por defecto 90/90/90 construidos en la capa de configuracion). codigo puente de marshalling manual eliminado de plugins/host.rs y state/widget state.rs . - los registros de widgets de plugins usan xtop plugin api::pluginwidget ; el alias duplicado pluginwidgetfn en ui/layout/engine.rs ha desaparecido. - restos de docker eliminados (asignacion dockers: vec![] , forwarding compuesto de docker info ) — el modelo de api ya no lleva datos de docker. - las lecturas del contexto de plugin estan restringidas por capacidad y son de tipo result en el upstream; consumidores adaptados. temas - miami ya esta incrustado y sembrado como los otros 11 temas (se distribuyen 12 ficheros de tema jsonc, todos en default themes ); version de semilla subida a \"2\" para que las instalaciones existentes reciban la nueva plantilla. - documentacion y recuentos de temas corregidos en todas partes (12 temas; x compilado como respaldo de arranque). - theme::themes dir() enruta por el config::config dir() consciente de la plataforma, asi que los temas viven junto a config.json y los layouts tambien en macos/windows. actualizacion de dependencias - ratatui 0.29 - 0.30.2 (api builder layout::vertical /layout::horizontal ); crossterm alineado con el backend de ratatui (un unico crossterm 0.29 en el grafo via ratatui-crossterm ). funcionalidades nuevas - los nombres de widget desconocidos en el layout activo producen una advertencia unica en stderr por nombre (xtop: layout ' ' references unknown widget ' ' ). - la feature opcional effects (apagada por defecto) cablea el xtop-effect-fade integrado a traves de la clave de config effect (\"fade\" activa el fundido de entrada de 500 ms; ausente/desconocido lo desactiva). las compilaciones sin la feature no arrastran dependencias extra. docs y tuberia de releases - docs/ refrescado: claves de configuracion, arquitectura de plugins (plugins como crates separados, comportamiento real de plugin list install scaffold , extension mcp), estado del rfc multi-repo, recuentos de features/temas; colors.md movido bajo docs/ . - tabla de roles de la paleta de docs/colors.md corregida segun el uso real del codigo (las ranuras 4/5 son el par lectura/escritura rx/tx, no \"storage/tx\"); opciones de visualizacion por widget documentadas en docs/customization.md ; recuentos/ciclos de layouts actualizados en los docs. - version de install.sh sincronizada con la version del crate; eliminadas las afirmaciones de build-dep de openssl (el crate no tiene dependencia openssl); comentario placeholder de install.ps1 eliminado. - roadmap.md del kernel sincronizado con el estado implementado (fases 4-6 y los elementos de refactor r2/r3); scripts/audit.sh ampliado con las guardas de directorio muerto y de semillas de temas. - version del crate fijada a 0.1.0 (version del instalador sincronizada) segun la politica de versiones del ecosistema: mantenerse temprano, sin mas subidas. historial anterior [0.0.1] - 2026-06-18 config: persistencia de layouts personalizados - config ahora tiene campo layout name que almacena el nombre del layout seleccionado (soporta layouts mas alla de los 7 built-in layoutmode ) - en configuraciones existentes, layout name es opcional ( [serde(default)] ) y se usa como respaldo layout mode - save config guarda el nombre del layout actual por su indice en layout defs , permitiendo restaurar layouts personalizados al reiniciar - appstate::new prioriza config.layout name si no es vacio, con fallback a layout index from mode - config se guarda automaticamente al seleccionar tema o layout desde la paleta de comandos paleta de comandos: soporte macos - agregada ruta directa para ctrl+p en el bucle de eventos, independiente del sistema de keybindings (soluciona problemas donde crossterm reporta la tecla con distinto casing o el keybinding no se resuelve) - key event to str aplica to ascii lowercase() al caracter cuando ctrl esta activo, normalizando \"ctrl+p\" a \"ctrl+p\" - agregado \"ctrl+p\" como variante alternativa en el keybinding por defecto - debug output de teclas en compilaciones debug (cargo build sin --release ) para diagnosticar problemas: [key] 'ctrl+p' plugins: seguridad y arquitectura - plugincontext ahora verifica capabilities antes de ejecutar acciones sensibles: kill process , set alert thresholds , set theme by name , set layout by name , set update interval requieren killprocesses o modifyconfig segun corresponda - plugincapability::custom migrado de &'static str a string para flexibilidad - pluginmanifest usa string en vez de &'static str , permitiendo plugins que generen metadata dinamicamente - [non exhaustive] agregado a plugincapability para evolucion segura del enum pluginmanager: robustez - with plugin manager mut() reemplaza el patron inseguro take() + some() que podia perder el manager si una ruta de error no lo restauraba. todos los callers migrados: on tick , manejo de teclas, shutdown, y mcp server - register() ahora devuelve result en vez de result , consistente con el resto del sistema - metodo build context() elimina la duplicacion de construccion de plugincontext en 5 metodos distintos - data dir del plugin ahora apunta al directorio del plugin (plugins/ / ), no a plugins/ /config.json capabilities: validacion real - collect widgets() solo acepta widgets si el plugin declara renderwidgets - collect data providers() solo acepta providers si el plugin declara readsysteminfo - plugincontext inyecta las capabilities declaradas y verifica en cada metodo sensible mcp server - migrado de take() /some() a with plugin manager mut() para seguridad - eliminado doble tick all() innecesario en el handler de herramientas sistema de providers - systemdataprovider ahora tiene metodo add extras() con default no-op, eliminando la necesidad de downcast mut:: () - kill process() verifica que el uid del proceso coincida con el usuario actual antes de enviar sigterm - limite de procesos configurable via sysinfoprovider::max processes (default 200) - eliminados noopbatteryprovider , noopgpuprovider , noopdockerprovider — codigo muerto no utilizado sentinel: calidad de datos - migracion completa de construccion manual de json (format! con strings escapados) a serde json::json!() — elimina riesgo de inyeccion json y corrupcion por caracteres especiales tui - layoutconstraint::fill mapeado correctamente a ratatui::constraint::fill(1) en vez de min(0) infraestructura - config dir() centralizada en xtop core::infrastructure::config::config dir() — eliminada duplicacion en 5 modulos - ci workflow creado en .github/workflows/ci.yml : cargo fmt --check , cargo clippy -- -d warnings , cargo test [0.2.0] - 2026-06-03 refactorizacion total del proyecto - migrado a workspace multi-crate: xtop-core , xtop-tui , xtop-cli - eliminado el monolito src/ — ahora cada capa vive en su propio crate - eliminadas 5 dependencias muertas: serde , serde json , clap , chrono , tokio (se redujo de 84 a 55 crates) - eliminado codigo muerto: inputmode , show help , swap history , process table state , graph colors() - eliminados todos los [allow(dead code)] nueva arquitectura hexagonal - capa de dominio (xtop-core/domain/ ): modelos de datos puros + trait systemdataprovider - capa de aplicacion (xtop-core/application/ ): appstate , metricshistory (con vecdeque ), layoutmode , effectivelayout - capa de infraestructura (xtop-core/infrastructure/ ): sysinfoprovider , theme loader , config , providers stub - capa de presentacion (xtop-tui/ ): terminal, render widgets separados, format helpers - binary (xtop-cli/ ): entry point con inyeccion de dependencias layout responsive - detect effective layout(width, height, mode) adapta el layout automaticamente: - dashboard ( 100x30): layout completo 2-columnas - compact ( 80x24): mas compacto - vertical ( 90% : color cambia a rojo - memoria 90% : color rojo + icono de advertencia en el titulo - thresholds configurables en alertthresholds (cpu high, mem high, disk high) mejoras de codigo - vec + remove(0) reemplazado por vecdeque con pop front() (o(1)) - helper format bytes() elimina repeticion de 1024.0 / 1024.0 / 1024.0 - helper format uptime() para formato legible de tiempo activo - metricshistory::set max points() para configurar puntos del historico configuracion persistente - /.config/xtop/config.json : guarda tema, layout, intervalo, history points, alerts - /.config/xtop/themes/ .json : temas personalizados por el usuario - guardado automatico al salir con q - temas built-in (13) se fusionan con temas personalizados tests - 39 tests unitarios (de 0): layout detection, history, themes, format helpers, config - ci workflow .github/workflows/ci.yml : check, fmt, clippy, test, build keybindings completos tecla accion ------- -------- q salir (guarda config) ? ayuda t / t siguiente/anterior tema l siguiente layout f / f toggle fullscreen / ciclar widget / buscar procesos esc cancelar busqueda / cerrar ayuda"},{"locale":"es","repo":"xtop","title":"contribuir a xtop","label":"contributing","path":"CONTRIBUTING.md","slug":["xtop","CONTRIBUTING"],"text":"contribuir a xtop en primer lugar, gracias por considerar contribuir a xtop . personas como tu hacen que esta herramienta sea mejor. ¿como puedo contribuir? informar de errores - comprueba que el error no se haya informado ya buscandolo en github en issues. - si no encuentras una issue abierta que aborde el problema, abre una nueva. asegurate de incluir un titulo y una descripcion clara, tanta informacion relevante como sea posible y un ejemplo de codigo o un caso de prueba ejecutable que demuestre el comportamiento esperado que no se produce. sugerir mejoras - abre una issue nueva con la etiqueta enhancement . - proporciona una explicacion clara y detallada de la funcionalidad que quieres y de por que es importante. pull requests 1. haz un fork del repositorio y crea tu rama desde main . 2. asegurate de tener instaladas las dependencias necesarias (rust y cargo). 3. haz tus cambios y pruebalos localmente con cargo run . 4. ejecuta cargo fmt y cargo clippy para garantizar la calidad y el formato del codigo. 5. crea una pull request. asegurate de que la descripcion de la pr describa con claridad el problema y la solucion. incluye el numero de issue relevante si procede. configuracion de desarrollo comunidad si tienes preguntas o necesitas ayuda, no dudes en ponerte en contacto a traves de las issues o discusiones de github (si estan habilitadas)."},{"locale":"es","repo":"xtop","title":"xtop","label":"xtop","path":"README.md","slug":["xtop"],"text":"xtop rust license platform ratatui monitor de sistema tui multiplataforma escrito en rust. usa ratatui para la interfaz de terminal y sysinfo para obtener metricas del sistema en tiempo real. contenidos caracteristicas capturas instalacion rapida inicio rapido documentacion contribucion licencia x caracteristicas uso de cpu por nucleo con deteccion de temperatura monitorizacion de ram y swap con grafico historico seguimiento de red rx/tx por interfaz visualizacion de almacenamiento y e/s de disco lista de procesos con busqueda en vivo y ordenacion por cpu/memoria/pid/nombre con conmutacion de direccion ▲/▼ monitorizacion de la bateria (sondas reales en linux, macos y windows) monitorizacion de la gpu (nvidia mediante nvidia-smi en cualquier plataforma; amd/intel mediante sondas sysfs de linux) 12 temas de color con soporte de temas personalizados mediante jsonc 7 modos de layout integrados + 3 layouts de preset detail, con soporte de layouts personalizados mediante jsonc modo de pantalla completa para cualquier widget umbrales de alerta configurables configuracion persistente consulta docs/features.md para un desglose detallado de las funcionalidades. capturas ver mas capturas instalacion rapida linux curl -fssl https://raw.githubusercontent.com/xtop-cli/xtop/main/install.sh bash windows (powershell) irm https://raw.githubusercontent.com/xtop-cli/xtop/main/install.ps1 iex macos y otras plataformas install.sh esta pensado para los gestores de paquetes de linux. en macos (o cualquier otra plataforma) instala con cargo: cargo install --git https://github.com/xtop-cli/xtop --all-features o compila desde el codigo fuente (ver mas abajo). compilar desde el codigo fuente git clone https://github.com/xtop-cli/xtop.git cd xtop cargo run --release para instrucciones de instalacion detalladas, consulta docs/installation.md . inicio rapido ejecuta xtop despues de la instalacion. controles principales: tecla accion q salir (guarda la configuracion) ? alternar la superposicion de ayuda t / t siguiente / tema anterior l siguiente modo de layout f / f alternar / recorrer la pantalla completa / buscar procesos para todos los detalles de uso, consulta docs/usage.md . documentacion caracteristicas — desglose detallado de las funcionalidades instalacion — guia completa de instalacion y desinstalacion uso — keybindings, modulos, superposicion de ayuda configuracion — referencia del fichero de configuracion y sus ajustes personalizacion — temas y layouts personalizados colores — referencia de paletas de los 12 temas incluidos plugins — arquitectura de plugins y su creacion arquitectura multi-repo — rfc y estructura del ecosistema roadmap registro de cambios contribucion licencia x dev & perfil de github & xscriptor"},{"locale":"es","repo":"xtop","title":"roadmap de xtop","label":"roadmap","path":"ROADMAP.md","slug":["xtop","ROADMAP"],"text":"roadmap de xtop este documento describe el roadmap de desarrollo de xtop , un monitor de sistema tui moderno y multiplataforma. se sincroniza automaticamente con las issues de github. fase 1: funcionalidades principales - [x] estructura tui basica con ratatui y crossterm ( 1) - [x] recopilacion de informacion del sistema con sysinfo ( 2) - [x] uso de cpu por nucleo y deteccion de la temperatura maxima ( 3) - [x] monitorizacion de memoria y swap con graficos historicos ( 4) - [x] seguimiento de subida y descarga de red ( 5) - [x] visualizacion del uso de almacenamiento ( 6) - [x] lista de los 50 procesos principales ordenados por uso de cpu ( 7) - [x] compatibilidad multiplataforma (macos, linux, windows) ( 8) fase 2: interfaz y temas - [x] gestor de layouts dinamico que soporta varios modos (dashboard, vertical, process focus) ( 9) - [x] implementar 13 esquemas de color integrados (x , madrid , tokio , etc.) ( 10) (hoy se distribuyen 12 ficheros de tema, todos incrustados y sembrados; los docs cuentan 12. ver docs/colors.md para la referencia de paletas.) - [x] cambio instantaneo de tema y layout en tiempo de ejecucion ( 11) - [x] diseno adaptativo para terminales estrechas ( 12) fase 3: despliegue y distribucion - [x] scripts automatizados de instalacion/desinstalacion para linux y macos (install.sh , uninstall.sh ) ( 13) - [x] scripts automatizados de instalacion/desinstalacion para windows (install.ps1 , uninstall.ps1 ) ( 14) - [x] comandos rapidos de instalacion con curl/wget ( 15) - [ ] pipeline ci/cd para releases binarias multiplataforma automatizadas ( 16) - [ ] paquetes de distribucion (aur, homebrew, winget, apt) ( 17) fase 4: configuracion y personalizacion - [x] soporte de fichero de configuracion persistente (guarda las preferencias de tema y layout) ( 18) - [x] creacion de temas de usuario personalizados mediante configuracion ( 19) - [x] intervalos de actualizacion configurables para las metricas del sistema ( 20) - [x] keybindings personalizables ( 21) fase 5: funcionalidades avanzadas de monitorizacion - [x] seguimiento de la velocidad de lectura/escritura de e/s de disco ( 22) - [ ] seleccion granular de interfaces de red ( 23) — los datos rx/tx por interfaz se ( 39) muestran, pero elegir que interfaz reporta/dibuja el kernel aun no es configurable. - [ ] monitorizacion de uso, temperatura y vram de la gpu (nvidia/amd) ( 24) — ( 40) nvidia mediante nvidia-smi en cualquier plataforma (linux/macos/windows); amd/intel sigue siendo parcial solo en linux (/sys/class/drm); macos/windows conservan el estado vacio honesto para amd/intel (sin api publica de utilizacion). - [ ] monitorizacion del estado de la bateria ( 25) — linux sondea real (/sys/class/ ( 41) power supply); macos real (pmset); windows real con bateria agregada (getsystempowerstatus; por bateria via setupapi pendiente); las plataformas sin soporte se quedan vacias. - [ ] integracion del uso de recursos de contenedores docker ( 26) — el soporte docker se ( 42) elimino del modelo de datos compartido (api m1.4: nada lo consumia), asi que este elemento queda aparcado hasta que aparezca un consumidor real. fase 6: gestion interactiva de procesos - [x] terminacion interactiva de procesos (envio de senales de kill) ( 27) - [ ] buscar, filtrar y resaltar procesos por nombre ( 28) — la busqueda y el ( 43) filtrado existen; el resaltado de coincidencias dentro de la lista de procesos esta pendiente. - [ ] vista de arbol para la jerarquia de procesos ( 29) - [ ] ordenar procesos por memoria, pid o usuario ( 30) — la ordenacion por ( 44) cpu/memoria/pid/nombre esta implementada; aun no hay columna de usuario. fase 7: integracion con x - [ ] integracion con x ( 31) - [ ] crear el paquete xp ( 32) - [ ] anadir a los repositorios de x ( 33) fase 8: refactor del kernel linea base hecha: kernel de crate unico (src/ por areas: config, theme, state, plugins, providers, ui, commands); hosts de plugins y extensiones sobre los contratos de api; samurai y la extension mcp viven en sus propios repos. aqui solo se lista el trabajo de refactor pendiente (la logistica de git se gestiona fuera de este roadmap). r2 - pasada de calidad de codigo - [x] auditoria de comentarios doc de modulos en todo src/ ( 40) — los siete modulos ( 45) de nivel superior declarados en src/main.rs (commands, config, plugins, providers, state, theme, ui) llevan todos docs //! concisos que describen su area. - [x] cfg(target os) solo dentro de los arboles platform/ ( 41) — aplicado por ( 46) scripts/audit.sh (0 ocurrencias fuera de platform/). - [ ] revision de re-exportaciones comodin y de higiene de pub ( 42) — quedan 22 ( 47) re-exportaciones pub use ...:: comodin (umbral de audit.sh: 30); la revision sigue abierta. - [x] dividir commands/plugins.rs en modulos list/install/scaffold ( 43) - [x] key event to str a un modulo de entrada compartido si se reutiliza en otro sitio ( 44) — ( 48) un unico punto de uso (commands/run.rs), asi que no se necesita modulo compartido. - [x] ui/share/error.rs solo cuando aparezca manejo real de errores de ui (sin modulos ( 49) vacios) ( 45) — no se creo a proposito; documentado en la lista diferida del roadmap raiz. - [x] los widgets se subdividen internamente cuando superan un modulo ( 46) — ( 50) los renderers de widgets del kernel se externalizaron al repo widgets (m3); el kernel ya no es dueno de los widgets de los packs. r3 - herramientas de auditoria estructural - [x] scripts/audit.sh con umbrales de fallo ( 47) — el script bloquea: ( 51) cfg(target os) fuera de los arboles platform/ = 0, ficheros de mas de 600 lineas = 0, marcadores todo/fixme/xxx/hack = 0, pub use ...:: comodin <= 30, loc por area de nivel superior <= 2400, arbol de plugins muerto pre-monocrate ausente (su ruta vive solo dentro del script de auditoria como la propia guarda), miami incrustado en las semillas de temas (12 temas en total). el grafo de dependencias/ciclos de modulos y la deteccion de pub sin usar no estan implementados por el script (ver la nota diferida abajo). seguimientos diferidos (rastreados con la lista del roadmap raiz §7): - grafo de dependencias/ciclos de modulos de audit.sh y deteccion de pub sin usar, ademas de umbrales por fichero por encima de 300/600 lineas (la cifra de 200 lineas de la issue original es anterior a los umbrales actuales de 300/600)."},{"locale":"es","repo":"xtop","title":"colores","label":"docs · colors","path":"docs/colors.md","slug":["xtop","docs","colors"],"text":"colores cada tema (formato v2 de ux8.1) lleva un par explicito de background / foreground mas una palette de 16 colores hexadecimales. el fondo (background) es el fondo de pantalla/marco contra el que se mide cualquier otro rol; el primer plano (foreground) es el color de texto principal. los ficheros sin el par explicito (temas heredados de terceros) caen a la ranura 0 / ranura 7 al cargar. al cargar, el kernel normaliza el tema (ux8.2): los roles de texto y marcas que no alcanzan su minimo de contraste wcag contra el fondo se elevan automaticamente, de forma determinista y preservando el tono (ver customization.md — normalizacion de contraste ). los ficheros incluidos abajo permanecen canonicos: son las paletas propietarias exactas; el tema en memoria es la vista de ejecucion con legibilidad garantizada. las ranuras no son arbitrarias: cada una es un rol fijo (los packs de widgets las leen mediante las constantes role y el chrome del kernel mediante theme::bg()/fg()/accent()/dim() ). esta leyenda es el resumen; la tabla completa de fuente unica con el uso por rol vive en customization.md — referencia de la paleta . todas las paletas de abajo mantienen los mismos roles de ranura (alias de fondo heredado en 0, accent en 6, alias de primer plano heredado en 7, dim en 8, rampa brillante de series desde 9); solo varian los tonos de cada tema: los temas claros como helsinki o madrid simplemente eligen tonos mas oscuros para los mismos roles de modo que el contraste funcione sobre un fondo claro. ranura rol background fondo explicito — fondo de pantalla/marco; theme::bg() foreground primer plano explicito — texto principal; theme::fg() 0 alias de fondo heredado ( role bg ); coincide con background en todos los temas incluidos excepto x , donde la paleta propietaria conserva un negro de terminal ligeramente mas claro 1 alert (rellenos de nivel alto, linea media de cpu) 2 good (rellenos normales, linea de ram, bateria) 3 warn (parada media del degradado) 4 read / rx (rx de red, lecturas de disco) 5 write / tx / gpu (tx de red, escrituras de disco) 6 accent (titulos, cabeceras, seleccion) 7 alias de primer plano heredado ( role fg ) — los renderers de texto usan el foreground explicito; la ranura 7 es el ancla casi blanca de la familia de tono base y puede legitimamente igualar el fondo (paris lo hace) 8 dim (filas cebra, separadores, divisores) 9 - 15 rampa brillante de series (graficos de varias series) x madrid lahabana miami paris tokio oslo helsinki berlin london praha bogota"},{"locale":"es","repo":"xtop","title":"configuracion","label":"docs · configuration","path":"docs/configuration.md","slug":["xtop","docs","configuration"],"text":"configuracion xtop guarda su configuracion automaticamente al salir. el fichero es config.json en el directorio de configuracion de la plataforma, junto a las carpetas themes/ y layouts/ : /.config/xtop/config.json (linux) /library/application support/xtop/ (macos) %appdata%\\xtop\\ (windows) en linux puedes cambiar el directorio base con $xdg config home . claves clave tipo valor por defecto descripcion theme string \"x\" nombre del tema de color seleccionado actualmente (uno de los 12 incluidos o un tema de usuario). layout mode string \"dashboard\" modo de layout integrado. uno de dashboard , vertical , horizontal , cpufocus , memoryfocus , networkfocus , processfocus . se ignora mientras layout name nombre un layout personalizado valido. layout name string \"\" nombre del layout activo cuando es personalizado (no integrado). cuando no esta vacio y se encuentra, tiene prioridad sobre layout mode . update interval ms integer 1000 intervalo de muestreo en milisegundos. se limita a 100&ndash;3.600.000 al cargar. history points integer 100 puntos de datos conservados para los graficos historicos. alerts object ver abajo umbrales de alerta; consulta umbrales de alerta . keybindings object ver abajo keybindings por accion; consulta keybindings . style object ver abajo estilo de glifos de los widgets (charset de graficos, bordes, packs); consulta estilo . effect string (opcional) ausente efecto de frame aplicado a cada frame renderizado: \"fade\" activa el fundido de entrada integrado (solo en compilaciones hechas con la feature effects ). cualquier otro valor desactiva los efectos. ejemplo { \"theme\": \"miami\", \"layout mode\": \"dashboard\", \"layout name\": \"\", \"update interval ms\": 1000, \"history points\": 100, \"alerts\": { \"cpu high\": 90.0, \"mem high\": 90.0, \"disk high\": 90.0 }, \"keybindings\": { \"quit\": [\"q\"], \"help\": [\"?\"], \"next theme\": [\"t\"], \"prev theme\": [\"t\"], \"next layout\": [\"l\"], \"toggle fullscreen\": [\"f\"], \"cycle fullscreen\": [\"f\"], \"search\": [\"/\"], \"command palette\": [\"ctrl+p\", \"ctrl+p\"], \"cancel\": [\"escape\"], \"kill process\": [\"k\"], \"process up\": [\"up\"], \"process down\": [\"down\"], \"cycle sort\": [\"s\"] }, \"style\": { \"charset\": \"braille\", \"borders\": \"native\", \"pack\": null, \"widgets\": {} } } las claves desconocidas y las claves opcionales ausentes se ignoran; un fichero que no se pueda analizar cae a los valores por defecto de arriba. umbrales de alerta cuando una metrica supera su umbral configurado, el widget correspondiente cambia a color rojo y muestra un indicador de advertencia en su titulo. clave descripcion valor por defecto cpu high porcentaje de uso de cpu que dispara una advertencia 90.0 mem high porcentaje de uso de memoria que dispara una advertencia 90.0 disk high porcentaje de uso de disco que dispara una advertencia 90.0 keybindings cada accion acepta una lista de cadenas de tecla; gana la primera coincidencia. las teclas se escriben como: caracteres simples ( \"q\" , \"/\" , \"?\" ), caracteres en mayuscula ( \"t\" , \"f\" ), teclas con modificador ( \"ctrl+p\" , \"alt+x\" ) y teclas con nombre ( \"escape\" , \"enter\" , \"backspace\" , \"tab\" , \"up\" , \"down\" , \"left\" , \"right\" , \"delete\" , \"home\" , \"end\" , \"pageup\" , \"pagedown\" ). clave (accion) vinculacion por defecto accion quit [\"q\"] guardar la configuracion y salir help [\"?\"] alternar la superposicion de ayuda next theme [\"t\"] siguiente tema prev theme [\"t\"] tema anterior next layout [\"l\"] siguiente layout toggle fullscreen [\"f\"] alternar la vista de pantalla completa cycle fullscreen [\"f\"] recorrer el widget en pantalla completa search [\"/\"] iniciar la busqueda de procesos command palette [\"ctrl+p\", \"ctrl+p\"] abrir la paleta de comandos (ctrl+p tambien funciona como respaldo fijo en el codigo) cancel [\"escape\"] cancelar la busqueda / cerrar superposiciones kill process [\"k\"] terminar el proceso seleccionado (comprobacion de seguridad de mismo usuario) process up [\"up\"] mover la seleccion de procesos hacia arriba process down [\"down\"] mover la seleccion de procesos hacia abajo cycle sort [\"s\"] recorrer la columna de ordenacion de procesos (cpu% → memory → pid → name) estilo el objeto style controla los glifos de los widgets. los valores son los enums comunes del ecosistema de xtop-widget-api : charset : braille (por defecto), dot , block , half block , bar &mdash; marcadores de grafico usados por los graficos historicos. borders : native (por defecto, marco clasico de una linea), rounded , double , plain , ascii ( plain / ascii dibujan un marco ascii puro +- ). pack : pack de widgets usado para cada widget sin anulacion por widget ( \"default\" o \"blocks\" cuando se compila con la feature widget-blocks ). widgets : mapa de anulaciones por nombre de widget, cada una acepta charset , borders y pack . consulta customization.md para un ejemplo y la semantica completa por widget. temas y layouts personalizados para temas y layouts personalizados, consulta la guia de personalizacion . volver al readme"},{"locale":"es","repo":"xtop","title":"guia de personalizacion","label":"docs · customization","path":"docs/customization.md","slug":["xtop","docs","customization"],"text":"guia de personalizacion xtop permite personalizar en tiempo de ejecucion los temas de color y los modos de layout mediante ficheros jsonc externos. esta guia explica como crear y gestionar tus propios temas y layouts. indice temas ubicacion formato referencia de la paleta temas iniciales orden de carga consejos layouts ubicacion formato restricciones de tamano widgets disponibles ejemplos layouts iniciales orden de ciclo notas packs de widgets temas ubicacion los ficheros de tema viven en la subcarpeta themes/ del directorio de configuracion de la plataforma (el mismo arbol que config.json y los layouts): /.config/xtop/themes/ .jsonc (linux) /library/application support/xtop/themes/ (macos) %appdata%\\xtop\\themes\\ (windows) el directorio y los ficheros de tema incluidos se crean automaticamente en el primer arranque; no hace falta copiar nada a mano. formato cada fichero de tema define un name , un par explicito de background / foreground y una palette de 16 entradas (formato v2, ux8.1). los colores son cadenas hexadecimales con un prefijo opcional. los comentarios ( // y / / ) se admiten en ficheros jsonc. las claves background / foreground son opcionales para ficheros de terceros escritos contra el antiguo formato de 16 ranuras: las claves ausentes caen a palette[0] / palette[7] . las entradas de la paleta no son colores arbitrarios: cada ranura tiene un rol fijo (ver la referencia de la paleta ), de modo que los temas siguen siendo intercambiables y cada renderer — los packs de widgets y el chrome del kernel por igual — elige los colores por rol, nunca por gusto. { // my-custom-theme -- dark background, warm accents \"name\": \"my-custom-theme\", \"background\": \" 1a1b1c\", // screen/frame background (role bg) \"foreground\": \" abb2bf\", // primary text (role fg) \"palette\": [ \" 1a1b1c\", // 0: legacy background alias (role bg) \" e06c75\", // 1: alert red (high fills, avg cpu line) \" 98c379\", // 2: good green (normal fills, ram line) \" e5c07b\", // 3: warn yellow (gradient mid stop) \" d19a66\", // 4: read / rx (network rx, disk reads) \" c678dd\", // 5: write / tx / gpu (network tx, disk writes) \" 56b6c2\", // 6: accent (titles, headers, selection) \" abb2bf\", // 7: legacy foreground alias (role fg) \" 3e4451\", // 8: dim (zebra rows, separators, dividers) \" e06c75\", // 9..15: bright series ramp (multi-series charts) \" 98c379\", \" e5c07b\", \" d19a66\", \" c678dd\", \" 56b6c2\", \" abb2bf\" ] } normalizacion de contraste (ux8.2) cada tema se normaliza una vez, justo despues de analizarse, contra su background explicito. el motor mide la razon de contraste wcag de cada rol y eleva automaticamente los colores que no alcanzan su minimo, de forma determinista y preservando el tono (los colores se mueven hacia el blanco sobre fondos oscuros y hacia el negro sobre los claros, en pasos pequenos hasta superar el minimo): rol minimo fuente texto de primer plano 4.5:1 foreground explicito (ficheros heredados: ranura 7) accent 3.0:1 ranura 6 dim 3.0:1 ranura 8 texto de filas cebra 3.0:1 primer plano pintado sobre la banda dim (las ranuras comparten la 8) rampa de series / acentos 2.0:1 ranuras 1–5, 7, 9–15 (marcas de color sobre el fondo) el minimo de dim y el del texto de filas cebra comparten la ranura 8, asi que no pueden cumplirse ambos en paletas cuyo primer plano esta cerca del fondo: el texto de filas cebra siempre gana, y dim conserva el valor mas alto que sigue superandolo (para las paletas incluidas helsinki / oslo no es posible elevarlo en absoluto y dim conserva su valor canonico). los valores elevados sustituyen a las entradas de la paleta en memoria, de modo que los renderers siguen leyendo theme palette() y los accesores de rol sin cambios — los ficheros incluidos/de usuario nunca se reescriben : la normalizacion ocurre solo al cargar. referencia de la paleta esta tabla es la unica referencia fiable de roles: los accesores de tema del kernel ( bg() / fg() / accent() / dim() en src/theme/model.rs ) y las constantes role de los packs de widgets (repo widgets, src/util.rs ) se corresponden exactamente con estas ranuras, y la columna de uso refleja lo que el codigo pinta hoy realmente. si un renderer necesita un color, lo toma de aqui — no hay indice de paleta sin documentar. el texto se dibuja con el par explicito de foreground / background ( theme fg() / theme bg() en el contrato de widgets); las ranuras de la paleta alimentan las marcas de color. indice rol uso real (kernel + packs de widgets) 0 alias de fondo heredado ( role bg ) fondo de terminal/bloque de los marcos de los packs; la propia pantalla pinta la clave background explicita ( theme::bg() ) 1 alert ( role alert ) rellenos rojos/de nivel alto: medidores de cpu/mem/swap por encima de su umbral de alerta, la linea media del grafico de cpu, el medidor de cpu de la vista minima 2 good ( role good ) rellenos verdes/normales: parada baja del degradado, linea del historico de ram, relleno de la bateria, medidor de memoria de la vista minima 3 warn ( role warn ) rellenos amarillos: parada media del degradado para cpu/mem/storage/swap al 50% o mas pero por debajo del umbral de alerta 4 read/rx ( role rx ) metricas de descarga/lectura: totales/lineas rx de red, medidores de lectura de disk io 5 write/tx ( role tx ) metricas de subida/escritura: totales/lineas tx de red, medidores de escritura de disk io, relleno de gpu 6 accent ( role accent ) acentos: cabecera/seleccion de la tabla de procesos, spans de teclas de la ayuda, titulos/bordes de superposiciones; theme::accent() 7 alias de primer plano heredado ( role fg ) ancla casi blanca de la familia de tono base; puede legitimamente igualar el fondo (paris: ranura 7 == su fondo — los renderers dibujan el texto con el foreground explicito, theme::fg() ) 8 dim ( role dim ) atenuado/secundario: fondos de filas cebra, separadores de columna, divisores de graficos, notas apagadas; theme::dim() 9 – 15 rampa brillante de series ( role series start .. role series end ) siete variantes brillantes para graficos de varias series (lineas de historico por nucleo, ciclan por indice de serie) los temas incluidos repiten la familia de tono base en las ranuras 9–15 (variantes brillantes de las ranuras 1–7 en el mismo orden), que es lo que hace que la rampa de series se vea coherente. manten esa convencion al escribir un tema: las ranuras 9–15 deben distinguirse entre si y de las ranuras 1–8. temas iniciales xtop incluye 12 temas . la paleta x (fondo casi negro, acentos purpura-rosa) esta compilada en el binario como respaldo de arranque; las 12 definiciones — incluida x y miami — estan incrustadas en el binario como plantillas de siembra. el primer arranque las escribe en el directorio de temas de arriba, de modo que cada tema incluido esta disponible sin copiar nada. si quieres restaurarlos mas tarde, copialos desde el repositorio: cp -r assets/themes/ /.config/xtop/themes/ linux macos: /library/application support/xtop/themes/ temas disponibles: x , berlin , bogota , helsinki , lahabana , london , madrid , miami , oslo , paris , praha , tokio . todas las paletas de los temas estan documentadas en colors.md . orden de carga la paleta x compilada (respaldo de arranque, indice 0). los temas del directorio de temas (sembrados en el primer arranque) cargan encima; un fichero que reutilice el nombre x anula la paleta compilada en su sitio. si un tema personalizado tiene el mismo nombre que uno incluido, lo reemplaza ; los nombres nuevos se anaden despues del conjunto incluido. consejos prueba los temas en escala de grises ( london , berlin ) como base y anade tus propios colores de acento. prefiere escribir las claves explicitas background / foreground : el respaldo (ranura 0/ranura 7) es solo para ficheros heredados, y un primer plano en la ranura 7 puede igualar el fondo (la paleta paris incluida conserva esa rareza a proposito — el par explicito es el anclaje de los roles). no luches contra el normalizador de contraste: los roles por debajo de su minimo se elevan al cargar. escribe la paleta que quieras; el motor garantiza los minimos en memoria mientras el fichero permanece canonico. las ranuras 9–15 son la rampa brillante de series: haz que cada entrada sea una variante mas brillante de las ranuras 1–7 en orden para que los graficos de varias series sigan siendo distinguibles. nunca reaproveches una ranura: los renderers eligen los colores segun la tabla de roles de arriba, asi que un tema que reordene los roles rompe todos los widgets que los leen. layouts ubicacion coloca los ficheros de layout en el directorio de layouts de la plataforma (directorio de configuracion + layouts ): /.config/xtop/layouts/ .jsonc (linux) /library/application support/xtop/layouts/ .jsonc (macos) %appdata%\\xtop\\layouts\\ .jsonc (windows) se aceptan las extensiones .jsonc y .json . formato un layout es un arbol recursivo de divisiones (splits) y widgets : layoutdef ├── name: string └── root: area ├── direction: \"horizontal\" \"vertical\" ├── size: constraint (optional, defaults to \" \") └── areas: [area, ...] ├── area with \"widget\" → leaf node (renders a widget) └── area with \"direction\" → nested split restricciones de tamano sintaxis significado \" \" u omitida rellenar el espacio restante 3 (numero) n filas/columnas fijas \"45%\" porcentaje del padre widgets disponibles widget descripcion header barra de informacion del sistema (uptime, carga, teclas) cpu medidores de uso de cpu por nucleo memory medidores de ram + swap + grafico del historico de ram storage medidores de uso de disco por punto de montaje network totales de red rx/tx y velocidades processes tabla de procesos con filtro de busqueda disk io velocidades de lectura/escritura de disco battery medidores de carga de la bateria gpu medidores de uso de gpu ejemplos layout personalizado simple un layout minimo de tres filas: cabecera, cpu y procesos. { // \"monitor\" — cpu top-half, processes bottom-half \"name\": \"monitor\", \"root\": { \"direction\": \"vertical\", \"areas\": [ { \"widget\": \"header\", \"size\": 3 }, { \"widget\": \"cpu\", \"size\": \"55%\" }, { \"widget\": \"processes\", \"size\": \" \" } ] } } layout anidado complejo un dashboard completo con una division horizontal en la seccion central: { \"name\": \"my-dashboard\", \"root\": { \"direction\": \"vertical\", \"areas\": [ { \"widget\": \"header\", \"size\": 3 }, { \"direction\": \"horizontal\", \"size\": \"50%\", \"areas\": [ { \"widget\": \"cpu\", \"size\": \"60%\" }, { \"direction\": \"vertical\", \"size\": \"40%\", \"areas\": [ { \"widget\": \"network\", \"size\": \"50%\" }, { \"widget\": \"disk io\", \"size\": \"50%\" } ] } ] }, { \"widget\": \"processes\", \"size\": \" \" } ] } } layouts iniciales los 10 layouts integrados se distribuyen en el crate xtop-layout ( github.com/xtop-cli/layouts , carpeta layouts/default/ ) y quedan incrustados en el binario. al arrancar, sus fuentes jsonc se copian al directorio de layouts de la plataforma (ver ubicacion ) como plantillas editables. los layouts de la comunidad viven en layouts/custom/ del mismo repo; instala uno con xtop layout install &lt;name&gt; (o copia el fichero al directorio de layouts de tu plataforma). valida un fichero local con xtop layout check &lt;file&gt; . un fichero de layout cuyo name coincida con un layout integrado lo anula (p. ej. edita dashboard.jsonc para personalizar el dashboard). los ficheros con nombres nuevos aparecen como layouts extra. layouts de modo: dashboard , vertical , horizontal , cpu focus , memory focus , network focus , process focus — estos siete se corresponden con los modos de layout. extras de preset: detail dashboard , detail network , detail processes — layouts centrados en el detalle anadidos despues de los modos (no son modos en si; se seleccionan por nombre). ejercitan las opciones de visualizacion por widget (base de cpu en processes , cores / show freq en cpu , ifaces en network ); consulta la seccion de opciones por widget mas abajo. orden de ciclo layouts de modo (dashboard → vertical → horizontal → cpu focus → memory focus → network focus → process focus) extras de preset ( detail dashboard → detail network → detail processes ) cualquier layout personalizado del directorio de layouts de la plataforma con un nombre nuevo (orden del sistema de ficheros) los ficheros personalizados que reutilizan un name integrado lo anulan en su sitio (sin duplicados) vuelve al dashboard pulsa l para avanzar por todos los layouts disponibles. notas si un nombre de widget de tu layout no coincide con ningun widget disponible, esa area se omite y xtop imprime una advertencia unica en stderr ( xtop: layout '&lt;layout&gt;' references unknown widget '&lt;name&gt;' ). las divisiones anidadas pueden ser arbitrariamente profundas, pero el anidamiento muy profundo puede desbordar terminales pequenas. la terminal debe tener al menos 40×8 para que cualquier layout se renderice; las terminales mas pequenas muestran una advertencia. las terminales muy pequenas (menos de 60×14) caen a un layout minimo fijo en el codigo (medidores de cpu + memoria + lista de procesos). estilo de glifos de los widgets los graficos (cpu/memoria/red) y los bordes de los widgets se dibujan con estilos de glifos que puedes cambiar en config.json bajo la clave style (ver configuration.md ): { \"theme\": \"x\", \"style\": { \"charset\": \"block\", \"borders\": \"ascii\", \"widgets\": { \"cpu\": { \"charset\": \"bar\" }, \"network\": { \"borders\": \"double\" } } } } charset : braille (por defecto), dot , block , half block , bar . borders : native (por defecto; el marco clasico de una linea con box-drawing), rounded , double , plain y ascii (tanto plain como ascii dibujan un marco ascii puro +- ). widgets : anulaciones por widget. las claves son los nombres de widget que usan los layouts: header , cpu , memory , storage , network , processes , disk io , battery , gpu . cada entrada acepta charset , borders y un pack opcional (pack de widgets con el que renderizar ese nombre, p. ej. \"blocks\" ). un style.pack global fija el pack de cada widget sin anulacion por widget. los estilos de glifos solo cambian la apariencia: los datos detras de cada widget los dibujan los packs de widgets (ver plugin.md para saber como un plugin anade renderers completamente nuevos, que tienen prioridad sobre los packs). opciones de visualizacion por widget mas alla del estilo de glifos, cada instancia de widget en un fichero de layout puede llevar un objeto json options que refina como dibuja esa instancia sus datos. el formato de layout lo acepta en los nodos de widget como paso opaco (ver el repo de layouts, docs/layout-schema.md , seccion \"widget options \"): { \"name\": \"my layout\", \"root\": { \"direction\": \"vertical\", \"areas\": [ { \"widget\": \"header\", \"size\": 3 }, { \"widget\": \"cpu\", \"size\": \"60%\", \"options\": { \"cores\": \"all\" } }, { \"widget\": \"processes\", \"size\": \" \" } ] } } el kernel reenvia el objeto options de cada nodo al renderer del widget mientras esa instancia se dibuja (via widgetstate::widget options en el contrato de widget-api). varias instancias del mismo widget en un layout pueden llevar opciones distintas. sin clave options (o con null ) el widget se renderiza exactamente igual que antes de esta funcionalidad: los valores por defecto conservan el comportamiento actual byte a byte. solo las claves documentadas refinan un widget; las claves desconocidas se ignoran. las claves reconocidas se documentan por widget en el repo de widgets ( docs/widgets.md ) segun llegan los hitos de ux. los presets detail incluidos muestran la primera oleada: la base de cpu de processes ( cpu : \"total\" / \"both\" ), las claves de nucleo/frecuencia de cpu ( cores , show freq ) y la lista de interfaces de network ( ifaces ). hasta que un widget documente una clave, esa clave es inerte (valores por defecto dr-ux2). las vistas de pantalla completa y minima buscan las opciones por nombre de widget en el layout actual (primer nodo que coincide); cuando el layout no tiene ese nodo usan los valores por defecto. los renderers de widgets de plugins ven el hoststate del plugin, no widgetstate : las options del layout no se reenvian a los plugins. packs de widgets los packs de widgets son la unidad instalable de codigo de widgets: cada pack es un crate separado ( xtop-widget-&lt;name&gt; ) que registra renderers por nombre de widget contra el contrato xtop-widget-api . el kernel incluye dos packs de serie: el pack base ( default , siempre compilado) y el pack blocks (detras de la feature cargo widget-blocks ) — y los lista en un unico catalogo en tiempo de compilacion ( src/ui/layout/pack table.rs , una fila (feature, label) por pack), que comparten el motor de render y xtop widget list . la gestion de packs de widgets replica el flujo de trabajo de los plugins: comando descripcion xtop widget list enumera los packs de widgets cableados en el kernel (filas de la tabla de packs cuya feature cargo esta declarada en el cargo.toml raiz) xtop widget scaffold &lt;name&gt; crea una plantilla compilable de pack de un solo widget en widgets-dev/xtop-widget-&lt;name&gt;/ (ignorada por git) xtop widget install &lt;name&gt; instala un pack por nombre desde github.com/xtop-cli/widgets xtop widget install &lt;url path&gt; instala un pack desde cualquier url git o un directorio local de crate xtop widget install es un flujo de trabajo que automodifica el fuente (en el mismo espiritu que xtop plugin install ): anade una dependencia opcional + una feature flag widget-&lt;name&gt; al cargo.toml raiz, anade una fila (feature, label) y su brazo de vinculacion al registro en el catalogo de packs de src/ui/layout/pack table.rs , y ejecuta cargo check . el pack no se habilita por defecto : anade widget-&lt;name&gt; a la lista por defecto de [features] (o compila con --features widget-&lt;name&gt; ) y recompila. una vez habilitado, selecciona el pack por widget con style.pack (todos los widgets) o style.widgets.&lt;name&gt;.pack (un widget), y coloca sus nombres de widget en un fichero de layout. la guia de autoria (el contrato del pack, como registran los packs sus renderers, las opciones de los renderers) vive en la documentacion del repo de widgets ( docs/authoring.md , docs/widgets.md ). ← volver al readme"},{"locale":"es","repo":"xtop","title":"lenguaje de diseno de xtop","label":"docs · design","path":"docs/design.md","slug":["xtop","docs","design"],"text":"lenguaje de diseno de xtop nota de alcance para quien lea: este documento expone las reglas de diseno visual con las que renderiza xtop (el kernel 0.1.0 y los packs de widgets en su estado de trabajo de ux, 2026-09-04): roles de tema, conjuntos de glifos, opciones de visualizacion por widget, respaldos de ancho minimo y reglas de color de los graficos. las afirmaciones del lado de los widgets se contrastan con el arbol de trabajo del repo de widgets a la misma fecha; el informe final del agente de widgets (tmp/ux2-4-widgets-report.md , si esta presente) es la autoridad para los detalles por widget y prevalece sobre las afirmaciones resumidas de aqui. el lenguaje se inspira en los monitores de recursos de la clase de btop; las notas de referencia recogidas durante la investigacion ux0 viven bajo tmp/ en la raiz del workspace. reglas regla donde esta implementada --- --- el chrome viene solo de los roles del tema (dr-ux3): una unica tabla de roles en docs/customization.md / docs/colors.md ; el chrome del kernel (popups de ayuda/paleta/busqueda) usa titulos/bordes de accent y separadores de dim, y respeta el conjunto de bordes configurado (style.borders , incluido el ascii puro +- ); los marcos de los widgets dibujan los roles accent/dim (widgets/src , role ) kernel src/ui/overlay/ , src/ui/screen.rs ; packs de widgets el estado de ordenacion siempre es visible: la columna de procesos que ordena lleva un marcador de color, una pulsacion invierte la direccion (▼ ↔ ▲ ), la siguiente pulsacion avanza cpu% → mem → pid → name (arranque por defecto = cpu% descendente, orden clasico conservado) kernel appstate::cycle sort + process sort desc() en widgetstate ; el widget de procesos pinta el marcador desde ese estado la discriminacion visual por widget se guia por datos desde el fichero de layout (options en los nodos de widget, dr-ux1), no solo desde la configuracion global: dos instancias del mismo widget en una misma pantalla pueden renderizarse distinto repo layouts (docs/layout-schema.md , \"widget options \"); kernel widgetstate::widget options ; claves reconocidas por widget en los docs del repo de widgets la seleccion de fila sobrevive a los reordenamientos: la fila de proceso seleccionada esta anclada por pid y resaltada (fondo accent / fila invertida); las filas cebra y los separadores de columna usan el rol dim widget de procesos (widgets/ ) + estado de seleccion del kernel los marcadores de los graficos y los bordes respetan el conjunto de glifos elegido por widget (charset: braille/dot/block/half block/bar; borders: native/rounded/double/plain/ascii), con valores globales por defecto y anulaciones por widget en config.json bajo style ; las options por nodo de layout pueden refinarlos por instancia chartcharset /border for en el contrato de widget-api; packs de widgets; el kernel pasa style.widgets. las series de datos reciben colores emparejados por rol: las filas por nucleo usan un relleno degradado, los roles alert/warn/good dirigen el color de estado, y las lineas de varias series (rx/tx, lectura/escritura) usan el par de ranuras documentado (rx = ranura 4, tx = ranura 5) packs de widgets; leyenda de roles en docs/colors.md las terminales pequenas degradan limpiamente en lugar de envolverse: los modos de layout caen segun el tamano (por debajo de 60×14 → medidores minimos + lista de procesos; dashboard se compacta por debajo de 100×28 y se vuelve vertical por debajo de 80 columnas), con un suelo duro de 40×8 xtop-layout mode.rs (detect effective layout ), kernel ui/screen.rs las opciones de visualizacion nunca inventan datos: cada opcion refina como se dibujan las metricas existentes (bases, columnas, nucleos, interfaces, discos, unidades); las metricas subyacentes son siempre reales packs de widgets + data providers los presets de layout son extras con nombre, no modos: siete layouts ligados a modos conservan las ranuras de modo fijas; los presets detail dashboard , detail network y detail processes (detail .jsonc en el repo layouts) los siguen en el orden de ciclo, y luego van los layouts de usuario/comunidad (dr-ux6) repo layouts src/loader.rs ; ciclo de layouts del kernel + xtop layout install claves de opcion reconocidas por widget el paso opaco options del layout lo interpretan los renderers de widgets; cada widget documenta las claves que reconoce en el repo de widgets (docs/widgets.md ). los presets detail incluidos ejercitan la primera oleada en los widgets processes , cpu y network (consulta el doc de esquema del repo layouts para los objetos exactos que llevan). diferido (deliberadamente no modelado) estas caracteristicas de los monitores de referencia no tienen aun equivalente en xtop; cada linea indica la razon de datos o de arquitectura, de modo que nada de esto esta simulado con stubs: - panel de detalle por proceso (graficos, estado, e/s, padre, cmd, …): el historico de cpu por proceso existe desde ux9.1 (widgetstate::process cpu history , un ring acotado por pid alimentado desde la lista de procesos visible), asi que una pequena chispa de cpu por fila es dibujable; aun no existen buffers de historico de estado/e/s/padre. los metadatos del proceso (estado, padre, cmd, nº de hilos, ficheros abiertos, bytes de disco) ya estan en el modelo y disponibles para vistas futuras. - vista de arbol de procesos con prefijos expandir/contraer: sin semantica de arbol en el contrato de la vista de procesos; el anclaje por pid asume una lista plana y ordenable. necesita una decision de modelo antes del trabajo de ui. - modo de seguimiento + banners de pausa/seguimiento: no existe el concepto de follow en la maquina de estados del kernel (inputmode es normal/searching/commandpalette). los banners requeririan anadidos al estado de la vista. - temperaturas por nucleo: desde ux8.3 el provider recoge temperaturas por nucleo en linux (cpuinfo::temp c , sensores coretemp) cuando los sensores se corresponden con los nucleos logicos; las maquinas sin correspondencia legible conservan solo el maximo agregado (cpu temp ), asi que alli no se dibuja ninguna fila de temperatura por nucleo. - rampas de degradado de 101 pasos por familia de metricas: la paleta de 16 ranuras no puede alojar rampas de 101 pasos por familia. xtop usa un degradado documentado de 3 paradas (roles alert/warn/good) y la rampa brillante para las lineas de varias series. - leyendas superpuestas en los graficos (uptime en el grafico de cpu, texto de escala de red): el uptime/la carga viven en el widget header; las escalas del grafico se dibujan en el eje del grafico. las leyendas superpuestas necesitarian un rol de estilo de texto-sobre-grafico que la paleta no define. - fondo transparente y downconvert a 16 colores: el rol de fondo de la paleta siempre se pinta; no existe una ruta sgr de 16 colores. ambos son caracteristicas del modo de color del kernel, diferidas. - chips manejados con raton / botones de atajo inline: la entrada es primero-teclado (superposicion de ayuda con ? ); el soporte de raton se limita al scroll con rueda de la lista de procesos. - conmutadores de mostrar/ocultar por caja en ejecucion: el cambio de layout cubre la seleccion de cajas (l , paleta, config), pero los conmutadores de mostrar/ocultar por caja en ejecucion no estan modelados en el motor de layouts. - techos fijos de los graficos de red: los graficos se autoescalan al maximo muestreado; los techos fijos configurables no estan implementados. - dialogo de confirmacion de kill: k mata directamente la seleccion anclada por pid; no existe dialogo/banner (el feedback visual es que la fila desaparece). donde va mas lejos xtop - los packs de widgets son intercambiables por nombre de widget y por instancia de layout (style.widgets. .pack ), asi que un unico binario puede incluir varios lenguajes visuales para la misma metrica. - la discriminacion visual se guia por datos desde el fichero de layout (options en los nodos de widget), no solo desde la configuracion global: dos instancias del mismo widget en una misma pantalla pueden renderizarse distinto. - el kernel se guia por providers; los datos vienen del provider de sysinfo y de los plugins, asi que las vistas de detalle solo se construyeron donde los datos existen de verdad. documentos relacionados - docs/colors.md — paletas de temas + leyenda de roles; docs/customization.md — la unica tabla de roles, options por widget, presets de layout. - repo de widgets — docs/widgets.md : claves de opcion reconocidas por widget. - repo de layouts — docs/layout-schema.md : los presets detail y la gramatica del paso opaco options ."},{"locale":"es","repo":"xtop","title":"caracteristicas","label":"docs · features","path":"docs/features.md","slug":["xtop","docs","features"],"text":"caracteristicas desglose detallado de las capacidades de monitorizacion e interfaz de xtop. monitorizacion del sistema cpu porcentaje de uso por nucleo y por hilo, mostrado como medidores horizontales. lectura de la temperatura maxima de la cpu cuando hay sensores de hardware disponibles. barras codificadas por color que indican visualmente los niveles de carga. memoria medidor de uso de ram que muestra la usada, la total y el porcentaje. medidor de uso de swap. grafico de lineas historico que sigue el uso de ram a lo largo del tiempo. numero de puntos de datos historicos configurable. red seguimiento en tiempo real de la subida (tx) y la descarga (rx) por interfaz de red. datos totales transferidos mostrados junto a las velocidades de transferencia actuales. almacenamiento medidores de uso de disco por punto de montaje que muestran el espacio usado, el disponible y el total. barras de porcentaje visuales para cada sistema de ficheros montado. e/s de disco seguimiento de la velocidad de lectura y escritura por dispositivo de disco. mostrado en bytes por segundo con escalado automatico de unidades. procesos lista desplazable de los procesos en ejecucion ordenados por uso de cpu. busqueda en vivo que filtra por nombre de proceso. muestra el nombre del proceso, el uso de cpu y el uso de memoria. gpu medidores de uso de gpu: datos reales en linux (nvidia mediante nvidia-smi , amd/intel mediante /sys/class/drm ); nvidia tambien se detecta mediante nvidia-smi en macos y windows. las gpu amd/intel y las gpu de apple no exponen una api publica de utilizacion, asi que el widget permanece vacio para ellas (sin lecturas inventadas). bateria medidores de nivel de carga de la bateria: datos reales en linux ( /sys/class/power supply ), macos ( pmset ) y windows ( getsystempowerstatus , bateria agregada). los equipos sin bateria muestran el estado vacio honesto. temas 12 esquemas de color: 12 ficheros de tema jsonc se distribuyen en assets/themes/ y quedan incrustados en el binario como plantillas de siembra del primer arranque. temas personalizados definidos como ficheros jsonc con un par explicito de fondo/primer plano y una paleta de 16 colores hexadecimales (los ficheros heredados de solo 16 ranuras siguen cargando). temas siempre legibles: cada tema se normaliza en contraste al cargarse (minimos wcag para los roles de texto y marcas, ux8.2); los ficheros distribuidos se mantienen canonicos. cambio instantaneo de tema con t (siguiente) y t (anterior). referencia de paletas en colors.md . layouts 7 modos de layout integrados: dashboard, vertical, horizontal, cpu focus, memory focus, network focus y process focus, mas 3 layouts de preset detail ( detail dashboard , detail network , detail processes ) que se suceden despues de los modos y muestran las opciones de visualizacion por widget. layouts personalizados definidos como ficheros jsonc con un arbol recursivo de divisiones/widgets; cada instancia de widget puede llevar un objeto options (base de cpu, nucleos, interfaces, ...) controlado desde el fichero de layout. modo de pantalla completa para cualquier widget, activado con f . diseno adaptativo que se ajusta automaticamente al ancho y al alto de la terminal. layout minimo de respaldo para terminales muy pequenas. umbrales de alerta avisos visuales cuando el uso de cpu, memoria o disco supera los limites configurables. cambios de color a rojo e indicadores de advertencia en los titulos de los widgets. persistencia el tema, el layout, el intervalo de actualizacion, los puntos de historico, los umbrales de alerta y el estilo de glifos actuales se guardan automaticamente al salir. la configuracion se almacena como config.json en el directorio de configuracion de la plataforma: /.config/xtop/ en linux, /library/application support/xtop/ en macos, %appdata%\\xtop en windows. volver al readme"},{"locale":"es","repo":"xtop","title":"guia de instalacion","label":"docs · installation","path":"docs/installation.md","slug":["xtop","docs","installation"],"text":"guia de instalacion indice instalacion rapida (linux) instalacion rapida (windows) macos opciones del instalador compilar desde el codigo fuente desinstalacion distribuciones compatibles instalacion rapida (linux) el script de instalacion detecta la distribucion y su gestor de paquetes, instala los prerrequisitos de compilacion (git y, si falta, el toolchain de rust via rustup), clona el repositorio, lo compila en modo release e instala el binario en /usr/local/bin . con curl curl -fssl https://raw.githubusercontent.com/xtop-cli/xtop/main/install.sh bash con wget wget -qo- https://raw.githubusercontent.com/xtop-cli/xtop/main/install.sh bash instalacion rapida (windows) requiere rust (cargo) instalado. ejecuta en powershell: irm https://raw.githubusercontent.com/xtop-cli/xtop/main/install.ps1 iex macos install.sh esta pensado para los gestores de paquetes de linux y no tiene rama para macos. instala con cargo (necesita el toolchain de rust de rustup): cargo install --git https://github.com/xtop-cli/xtop --all-features el binario acaba en /.cargo/bin ; asegurate de que este en tu path. --all-features habilita el plugin samurai, la extension mcp, el pack de widgets blocks y el modulo de efectos. opciones del instalador puedes ejecutar el script de instalacion con banderas adicionales para tener mas control: check dependencies without installing ./install.sh --check-deps install only dependencies (rust, build tools) ./install.sh --install-deps show help ./install.sh --help distribuciones compatibles arch linux y derivados debian / ubuntu y derivados fedora / rhel y derivados opensuse y derivados alpine linux y derivados compilar desde el codigo fuente clona el repositorio: git clone https://github.com/xtop-cli/xtop.git cd xtop compila y ejecuta con optimizaciones de release: cargo run --release desinstalacion los desinstaladores eliminan solo el binario; la configuracion de usuario bajo el directorio de configuracion se conserva. linux curl -fssl https://raw.githubusercontent.com/xtop-cli/xtop/main/install.sh bash -s -- --uninstall windows irm https://raw.githubusercontent.com/xtop-cli/xtop/main/uninstall.ps1 iex volver al readme"},{"locale":"es","repo":"xtop","title":"arquitectura multi-repo (org xtop-cli)","label":"docs · multi-repo","path":"docs/multi-repo.md","slug":["xtop","docs","multi-repo"],"text":"arquitectura multi-repo (org xtop-cli) estado: en vivo (2026-09-04). este documento es la localizacion del rfc de arquitectura del ecosistema: describe como se divide la organizacion xtop-cli en repos, como dependen las piezas entre si y hacia donde se dirige el diseno. la documentacion detallada por area vive en la carpeta docs/ de cada repo; el orden de push y el estado de los hitos viven en el roadmap.md raiz del workspace. organizacion repo (xtop-cli) rol contenido --- --- --- api contratos workspace con los cuatro crates de contrato: xtop-plugin-api (modelo de datos, traits de plugin/host, alertthresholds , pluginwidget ), xtop-widget-api (registro de packs + helpers de glifos), xtop-extension-api (host de extensiones), xtop-effect-api (efectos de frame) xtop kernel la app: binario de crate unico, src/ por areas (commands, config, plugins, providers, state, theme, ui). consume todos los demas repos widgets renderers packs de renderers de widgets contra xtop-widget-api : pack base xtop-widgets + pack alternativo xtop-widget-blocks (crate hermano de los crates por widget para que los consumidores git resuelvan su dependencia xtop-widget-core ) + comunidad en custom/ layouts disposicion crate xtop-layout : modelo de layout guiado por datos + cargador jsonc + modos de layout, mas layouts/default/ (7 layouts ligados a modos + 3 extras de preset detail ) y layouts/custom/ (comunidad, instalables) plugins funcionalidad implementaciones de plugins contra xtop-plugin-api (primer miembro: xtop-plugin-samurai ) extensions hooks del kernel extensiones estilo servidor contra xtop-extension-api (xtop-extension-mcp ) effects animacion efectos de frame contra xtop-effect-api (xtop-effect-fade : fundido de entrada de 500 ms desde negro) disposicion de desarrollo local (checkouts hermanos en una carpeta, como en este workspace): principio de dependencia: contratos en api , los consumidores apuntan hacia arriba cada repo consumidor depende solo de los crates de contrato de api — nunca del kernel — asi que cada repo compila de forma independiente: - api : tipos puros + protocolos (manifests, capacidades, errores, modelo de snapshot, contratos de provider/widget/extension/efecto). solo depende de ratatui /serde . aun no publicado en crates.io (ver los seguimientos del roadmap §6). - kernel xtop : aloja el ecosistema — implementa hoststate / widgetstate / extensionhost para su estado en vivo, ejecuta pluginmanager y el provider compuesto, renderiza los layouts resolviendo los nombres de widget en los packs, y maneja los efectos (tras feature). compila bien sin ninguna feature opcional: cargo build --no-default-features es el nucleo puro. - widgets / layouts / plugins / extensions / effects : consumen solo los tipos de api ; cada repo compila de forma independiente y nunca depende del kernel. modos de integracion nivel mecanismo uso --- --- --- en compilacion (hoy) dependencias git de cargo + feature flags opcionales cada integracion: plugin samurai, extension mcp, pack blocks, efecto fade en desarrollo xtop plugin install (clona, automodifica el cargo.toml del kernel, ejecuta cargo check ) primeros pasos con un repo de plugins en ejecucion (futuro, rfc) descubrimiento binario/abi de xtop-plugin- / xtop-effect- / xtop-extension- en los directorios de configuracion + xtop dev dir terceros sin recompilar — ver la lista diferida del roadmap raiz §7 el kernel nunca requiere ningun repo externo en tiempo de ejecucion: las piezas opcionales del ecosistema son cargo features (plugin-samurai , mcp-extension , widget-blocks , effects ); los crates de contrato (los cuatro crates xtop- -api mas xtop-widgets y xtop-layout ) son incondicionales porque el chrome y las vistas de estado del kernel estan escritos contra ellos. flujo de desarrollo (deps de path temporales) los repos del ecosistema se editan antes de subirlos. para compilar un consumidor contra el estado hermano aun no subido, su cargo.toml reemplaza temporalmente la dependencia git por una dependencia de path (path = \"../api/crates/plugin-api\" , path = \"../widgets\" , ...), o — cuando el manifest del propio consumidor debe permanecer intacto — una seccion [patch.\"https://github.com/xtop-cli/ \"] temporal redirige las fuentes git a los checkouts hermanos. todas las anulaciones temporales se eliminan antes de que el propietario haga push; los manifests finales llevan las deps git flotantes mostradas arriba. por que existe la division el kernel fue originalmente un workspace de crates propiedad del kernel (xtop-core /xtop-tui /xtop-cli ), y luego un monocrate con plugins dentro. cada eje de personalizacion crecio hasta tener su propio repo para que: - un contribuidor pueda publicar un pack de widgets, un layout, un plugin, una extension o un efecto sin tocar la base de codigo del kernel; - cada repo compile contra los contratos de api por si solo (comprobable de forma aislada, sin importar el kernel); - el kernel siga siendo un host fino: el modelo de metricas, los layouts y los renderers de widgets vienen todos de los crates del ecosistema. reglas de fuente unica que mantienen honesta la division (ver la seccion de \"decisions\" del roadmap raiz): - el modelo de metricas y el protocolo de plugins existen solo en xtop-plugin-api ; - el registro de widgets y el mapeo de glifos/estilo existen solo en xtop-widget-api (el widget del lado del plugin es xtop plugin api::pluginwidget ); - las constantes del ecosistema viven en el productor (xtop-plugin-samurai exporta plugin id y sus 12 nombres de accion; xtop-extension-mcp construye su tabla de herramientas a partir de ellos). fases 1. f0 (hecho) : org xtop-cli , repos creados, clones locales en una carpeta. 2. f1 (hecho) : crates de contrato de api extraidos del modelo de dominio del kernel; el kernel y sus hermanos los consumen. 3. f2 (hecho) : samurai movido al repo plugins ; mcp movido a extensions ; las features del kernel apuntan a los repos hermanos via deps git. 4. f3 (hecho) : workspace effects con el efecto fade; el kernel lo cablea detras de la feature opcional effects . 5. f4 (hecho) : contrato del host de extensiones + xtop-extension-mcp como primera extension estilo servidor. 6. f5 (pendiente) : publicar los crates de api en crates.io; etiquetar y fijar las deps git; ci por repo y releases del kernel (ver el roadmap raiz §6/§7). temas abiertos de rfc - descubrimiento dinamico en ejecucion (carga por abi o por directorio) — diferido explicitamente en el roadmap raiz §7 hasta que el modelo de features en compilacion deje de ser coherente. - publicacion: esquema de versionado para los crates de contrato y la estrategia de fijacion de cada consumidor."},{"locale":"es","repo":"xtop","title":"sistema de plugins","label":"docs · plugin","path":"docs/plugin.md","slug":["xtop","docs","plugin"],"text":"sistema de plugins xtop aloja plugins : funcionalidad adicional distribuida como crates separados que implementan el contrato de xtop-plugin-api (un crate del repo xtop-cli/api ). los plugins se integran en tiempo de compilacion: el kernel los cablea mediante dependencias git de cargo y feature flags, nunca mediante descubrimiento en tiempo de ejecucion. el plugin integrado es xtop-plugin-samurai (consulta multi-repo.md para ver la estructura del ecosistema). puedes compilar xtop sin ningun plugin ni extension: cargo build --release --no-default-features o con el conjunto por defecto (plugin samurai + extension mcp): cargo build --release arquitectura el sistema de plugins tiene cuatro componentes del kernel: componente ubicacion en el kernel finalidad trait plugin + tipos del contrato xtop plugin api (crate, externo) interfaz que implementa cada plugin; manifest, capacidades, errores, modelo de datos pluginmanager src/plugins/manager.rs registra plugins, ejecuta sus ticks y les despacha eventos impl de hoststate src/plugins/host.rs vista del lado del kernel del estado en vivo que los plugins pueden tocar compositeprovider src/providers/composite.rs fusiona el provider del kernel con los data providers de los plugins los plugins nunca dependen del kernel: solo ven el estado a traves de plugincontext , que se construye sobre el trait hoststate . los widgets de plugins se renderizan sobre &amp;dyn hoststate mediante xtop plugin api::pluginwidget (distinto del registro de packs de widgets en xtop-widget-api , que dibuja sobre widgetstate ). el trait plugin pub trait plugin: debug + send { fn manifest(&amp;self) -&gt; pluginmanifest; fn on enable(&amp;mut self, ctx: &amp;mut plugincontext) -&gt; result&lt;(), pluginerror&gt;; fn on disable(&amp;mut self, ctx: &amp;mut plugincontext) -&gt; result&lt;(), pluginerror&gt;; fn on tick(&amp;mut self, ctx: &amp;mut plugincontext) -&gt; result&lt;(), pluginerror&gt;; fn on key(&amp;mut self, ctx: &amp;mut plugincontext, key: &amp;str) -&gt; result&lt;bool, pluginerror&gt;; fn data provider(&amp;self) -&gt; option&lt;box&lt;dyn systemdataprovider&gt;&gt;; fn widget(&amp;self) -&gt; option&lt;pluginwidget&gt;; fn execute(&amp;mut self, ctx: &amp;mut plugincontext, action: &amp;str, params: &amp;str) -&gt; result&lt;string, pluginerror&gt;; } todos los metodos excepto manifest() tienen implementaciones por defecto, asi que un plugin minimo solo declara su manifest. metodo por defecto cuando se invoca manifest() obligatorio siempre que se necesiten metadatos on enable() sin operacion el plugin se registra al arrancar on disable() sin operacion cuando xtop se apaga on tick() sin operacion cada ciclo de actualizacion ( 1 s por defecto) on key() false pulsacion de tecla en modo normal (devuelve true para consumirla) data provider() none al arrancar (se fusiona en el compositeprovider) widget() none despues de cada tick (refresca el mapa de widgets del plugin) execute() unknownaction un agente externo (ia, cli, mcp) invoca un comando pluginmanifest cada plugin declara su identidad y sus necesidades en manifest().capabilities : readsysteminfo -- leer metricas del sistema killprocesses -- terminar procesos modifyconfig -- cambiar temas, layouts, umbrales e intervalos renderwidgets -- registrar widgets tui personalizados custom(string) -- cualquier cosa no cubierta arriba plugincontext acceso seguro y limitado al estado de la aplicacion. las lecturas restringidas por capacidad devuelven result ; las escrituras requieren la capacidad correspondiente y, si no la tienen, fallan con pluginerror::recoverable : ctx.snapshot()? // full systemsnapshot (needs readsysteminfo) ctx.top processes(n)? // top n processes by cpu, sorted desc ctx.system info()? // hostname, os, kernel (needs readsysteminfo) ctx.kill process(pid)? // kill process by pid (needs killprocesses) ctx.set alert thresholds(cpu, mem, disk)? // (needs modifyconfig) ctx.set theme by name(\"tokio\")? // (needs modifyconfig) ctx.set layout by name(\"cpu focus\")? // (needs modifyconfig) ctx.set update interval(500)? // (needs modifyconfig) ctx.alerts() // current alert thresholds ctx.config() // theme, layout, interval, hostname ctx.data dir() // host-provided plugin data dir data providers el kernel muestrea el sistema con su propio provider ( sysinfoprovider en src/providers/sysinfo/ ) y compone los providers de los plugins mediante compositeprovider : refresh all() refresca el provider principal y cada uno de los extra snapshot() delega en el principal disk io() , batteries() , gpu info() y system info() usan el resultado del principal cuando no esta vacio; en caso contrario, el primer resultado no vacio de un provider extra kill process() intenta primero con el principal y luego con los extra widgets de plugins un plugin puede registrar un widget personalizado mediante widget() . la closure de render recibe la vista de plugin del estado ( &amp;dyn hoststate ) y dibuja con ratatui plano: fn widget(&amp;self) -&gt; option&lt;pluginwidget&gt; { some(pluginwidget { name: \"samurai\".to string(), render: arc::new( f, state, area { // draw using ratatui; state is &amp;dyn hoststate. }), }) } los widgets personalizados se colocan en los layouts por nombre en ficheros de layout jsonc: { \"name\": \"my-layout\", \"root\": { \"direction\": \"vertical\", \"areas\": [ { \"widget\": \"header\", \"size\": 3 }, { \"widget\": \"samurai\", \"size\": \"30%\" }, { \"widget\": \"processes\", \"size\": \" \" } ] } } los widgets de plugins tienen prioridad sobre los widgets de packs con el mismo nombre (el kernel resuelve primero los widgets de plugins al renderizar). un layout que referencia un nombre que ni un pack ni un plugin proporcionan imprime una advertencia unica en stderr. comandos cli comando descripcion xtop plugin list enumera los plugins cableados en el cargo.toml del kernel (entradas de feature con dep:&lt;name&gt; ) xtop plugin install &lt;name&gt; instala un plugin por nombre desde github.com/xtop-cli/plugins xtop plugin install &lt;url&gt; instala un plugin desde cualquier url git xtop plugin scaffold &lt;name&gt; crea una plantilla de crate de plugin en plugins-dev/ (ignorada por git) flujo de instalacion (comportamiento real) xtop plugin install no descarga un binario y no registra nada en tiempo de ejecucion. edita el propio cargo.toml del kernel (un flujo de trabajo que automodifica el fuente): resuelve el repo fuente: github.com/xtop-cli/plugins para un nombre, o la url dada lo clona (shallow, sparse) en un directorio temporal localiza el crate xtop-plugin-&lt;name&gt; dentro del clon (la raiz del repo, o las subcarpetas plugins/ / crates/ ) anade una dependencia git opcional + una feature flag al cargo.toml raiz del kernel (el mismo patron que usa el xtop-plugin-samurai integrado) ejecuta cargo check para verificar que el manifest se resuelve, y luego limpia el plugin queda registrado pero no habilitado por defecto . para habilitarlo, anade su feature a la lista default de [features] del cargo.toml del kernel (o compila con --features &lt;name&gt; ) y recompila. cada plugin instalado necesita ademas una linea de registro en src/commands/share/bootstrap.rs bajo su feature flag. build xtop with samurai plugin enabled (default already includes it) cargo build --release --features plugin-samurai build xtop with samurai + another plugin cargo build --release --features \"plugin-samurai,plugin-mything\" extensiones: el servidor mcp las extensiones son los hooks estilo servidor del kernel (contrato: xtop-extension-api ). la extension incluida es el servidor mcp (model context protocol) del repo xtop-cli/extensions ( xtop-extension-mcp ), que expone las acciones de los plugins alojados como herramientas mcp sobre stdio. se compila cuando la feature mcp-extension esta activa (forma parte de las features por defecto). xtop mcp las herramientas mcp se ejecutan contra el plugin samurai, asi que la feature plugin-samurai tambien es necesaria. entre los clientes compatibles estan claude desktop, cline, cursor y continue.dev. configuracion de claude desktop { \"mcpservers\": { \"xtop\": { \"command\": \"xtop\", \"args\": [\"mcp\"] } } } pruebas interactivas single command echo '{\"jsonrpc\":\"2.0\",\"id\":1,\"method\":\"tools/call\",\"params\":{\"name\":\"system summary\",\"arguments\":{}}}' xtop mcp interactive session xtop mcp la extension mcp depende de xtop-plugin-samurai en tiempo de compilacion: el id del plugin y los 12 nombres de accion son constantes de fuente unica ( plugin id , actions:: ), de modo que la tabla de herramientas nunca puede desviarse de la implementacion del plugin. anadir un plugin manualmente crea el andamiaje del crate (o escribelo a mano en tu propio repo): xtop plugin scaffold mything creates plugins-dev/xtop-plugin-mything/ conviertelo en un repo git y subelo (el crate debe vivir en la raiz del repo o bajo plugins/ / crates/ , p. ej. github.com/you/xtop-plugin-mything ). instalalo en el kernel (anade una dependencia git opcional + feature flag en el cargo.toml raiz): xtop plugin install https://github.com/you/xtop-plugin-mything edicion manual equivalente: [dependencies] xtop-plugin-mything = { git = \"https://github.com/you/xtop-plugin-mything\", optional = true } [features] plugin-mything = [\"dep:xtop-plugin-mything\"] registralo bajo la feature flag en src/commands/share/bootstrap.rs : [cfg(feature = \"plugin-mything\")] use xtop plugin mything::mythingplugin; // in register plugins(): [cfg(feature = \"plugin-mything\")] { let plugin = box::new(mythingplugin::new()); if let err(e) = mgr.register(plugin, state) { eprintln!(\"[xtop] failed to load mything plugin: {e}\"); } } los detalles del contrato para autores de plugins (manifest, capacidades, tipos de error, widgets) viven en la documentacion del repo api; el plugin samurai de xtop-cli/plugins es la implementacion de referencia. volver al readme"},{"locale":"es","repo":"xtop","title":"uso","label":"docs · usage","path":"docs/usage.md","slug":["xtop","docs","usage"],"text":"uso indice keybindings modulos superposicion de ayuda paleta de comandos modo de pantalla completa layouts adaptativos comandos de terminal keybindings tecla accion q salir de la aplicacion (guarda la configuracion) ? alternar la superposicion de ayuda t siguiente tema de color t tema de color anterior l siguiente layout (recorre los modos de layout, los presets detail y despues los layouts personalizados) f alternar el modo de pantalla completa del widget actual f recorrer el foco de pantalla completa por todos los widgets disponibles / abrir la busqueda / el filtro de procesos enter confirmar el filtro de busqueda backspace borrar un caracter en el campo de busqueda esc cancelar la busqueda / cerrar la superposicion de ayuda modulos header -- muestra el tiempo de actividad del sistema, la carga media, el nombre del tema actual y el modo de layout activo. cpu -- barras de uso horizontales para cada nucleo de la cpu. si hay sensores de hardware disponibles, muestra la temperatura maxima de la cpu. memory -- medidores de uso de ram y swap, mas un grafico de lineas que muestra el uso de ram a lo largo de una ventana de historico configurable. storage -- medidores de uso de disco por punto de montaje que muestran la capacidad y el espacio usado. network -- datos totales descargados (rx) y subidos (tx) por interfaz, con las tasas de transferencia actuales. disk i/o -- velocidades de lectura y escritura por dispositivo de disco en bytes por segundo. processes -- lista desplazable de procesos ordenados por uso de cpu (los 200 primeros por defecto; se puede cambiar con xtop max processes ), con busqueda en vivo que filtra por nombre de proceso. gpu -- medidores de uso de gpu (disponible en hardware compatible). battery -- medidores de nivel de carga de la bateria (disponible en hardware compatible). superposicion de ayuda pulsa ? en cualquier momento para ver en pantalla la lista completa de keybindings disponibles. vuelve a pulsar ? o pulsa esc para cerrar la superposicion. busqueda de procesos la busqueda filtra la lista de procesos en tiempo real: pulsa / para abrir la barra de busqueda en la parte superior de la lista de procesos. escribe cualquier consulta: los resultados se filtran al instante por nombre de proceso. pulsa enter para confirmar el filtro, esc para cancelarlo y backspace para borrar caracteres. una superposicion centrada con un indicador /query muestra la entrada de busqueda actual. paleta de comandos pulsa ctrl+p para abrir la paleta de comandos, una lista de acciones buscable (tambien se puede abrir con el keybinding command palette ). la paleta tiene tres paginas: main (ir a temas/layouts, alternar o recorrer la pantalla completa, buscar, ayuda, recorrer la ordenacion de los procesos, tema aleatorio, salir), themes (saltar a cualquier tema cargado) y layouts (saltar a cualquier layout). escribe para filtrar, usa up / down para moverte, enter para ejecutar la accion seleccionada y esc para cerrar. modo de pantalla completa pulsa f para alternar el modo de pantalla completa del widget con foco actual. el widget se expande hasta ocupar toda el area de la terminal (menos la barra de cabecera). pulsa f para recorrer el foco de pantalla completa por los widgets en secuencia: cpu, memory, storage, network, processes, disk i/o, gpu, battery, y despues salir. layouts adaptativos la interfaz se adapta automaticamente al tamano de la terminal: tamano de la terminal comportamiento modo dashboard, 100+ columnas y 28+ filas layout de dashboard completo con 2 columnas modo dashboard, 80&ndash;99 columnas o menos de 28 filas layout compacto modo dashboard, con menos de 80 columnas layout apilado en vertical otros modos el layout del modo solicitado (vertical, horizontal, cpu/memory/network/process focus) menos de 60 columnas o menos de 14 filas layout minimo: solo cpu, memoria y procesos menos de 40 x 8 se muestra un mensaje de advertencia (terminal demasiado pequena) comandos de terminal ademas de la tui, xtop incluye subcomandos de gestion (ejecuta xtop --help para ver la misma lista): comando descripcion xtop mcp inicia el servidor mcp (transporte stdio) para agentes de ia xtop plugin list enumera los plugins cableados en el cargo.toml del kernel xtop plugin install &lt;name url&gt; instala un plugin (automodifica el manifest del kernel) xtop plugin scaffold &lt;name&gt; crea una plantilla de crate de plugin en plugins-dev/ xtop widget list enumera los packs de widgets cableados en el kernel (features de cargo + tabla de packs) xtop widget install &lt;name url path&gt; instala un pack de widgets (automodifica el manifest + la tabla de packs) xtop widget scaffold &lt;name&gt; crea una plantilla de crate de pack de un solo widget en widgets-dev/ xtop layout check &lt;file&gt; valida un fichero de layout jsonc xtop layout install &lt;name&gt; instala un layout de la comunidad desde github.com/xtop-cli/layouts los plugins y los packs de widgets se integran en tiempo de compilacion: los comandos registran crates y feature flags en los fuentes del kernel; habilita una feature en la lista por defecto de [features] (o compila con --features &lt;name&gt; ) y recompila para incluirla. los detalles de los packs de widgets viven en customization.md (&ldquo;widget packs&rdquo;) , los de los plugins en plugin.md . volver al readme"}];
